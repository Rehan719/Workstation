# Vision Fidelity Ledger — v5 (2026-09-19) — MILESTONE M1 re-run

**Supersedes v4 as the current assessment.** v4 is kept whole at `VISION_FIDELITY_LEDGER_v4.md` (its
Tier-1 entries are the ones P1.17 closed and the register rows cite), as v3 is at `VISION_FIDELITY_LEDGER_v3.md`.
This edition re-measures the product after P1.17, the second truth pass, as the M1 line requires — the Tier-1
count is measured again, never declared: a fresh six-region assessment against HEAD `929508f0` (port :8086,
single-user mode, `AI_DISABLE_LOCAL=1`). Under that flag the gateway routes every model call to the
deterministic native floor — the configuration CI runs and the one any machine without a local model gets
(it is NOT the shipped default: with the flag unset and Ollama discoverable, the gateway serves from the
local model). What IS assessable on the floor is whether every floor-served surface discloses it.

**Tiers.** Each finding carries the tier the refuter stands behind — 1: a TRUTH DEFECT (a reached surface
tells the user something untrue, fabricates a figure, or certifies what it could not assess); 2: an
INVISIBLE SHORTFALL (partial, and the surface does not say so); 3: a CAPABILITY GAP that is disclosed or
not reached. DELIVERED entries carry no tier.

## How this document was generated — and what that means for reading it

Six assessors ran one vision region each against the booted HEAD, explicitly barred from three
sources: the vision's own §16, the previous edition of this ledger, and `AUTONOMOUS_PROGRESS.md`
(a record of intent, not proof). They executed routes, read handlers and components, and counted
stores. The ASSESSMENT was capped at ten findings per region, most consequential first — and every
region returned exactly ten, so **60 is the size of the cap, not the size of the gap**; a region's
eleventh-worst thing is not in this ledger. **Every finding was then attacked by an independent
refuter instructed to default to refuted** (v2 refuted six per region), who had to reproduce the gap (execute the
route, read the code, count the store) before letting it stand, and who was told to correct the
verdict UP or DOWN when the assessor had it wrong.

Reading rules that follow from the method:

1. **DELIVERED is understated by construction.** Assessors were told their job was the gap that
   remains, but to report DELIVERED where they verified it. Read the DELIVERED entries as the
   floor of what works, not the ceiling.
2. **Every one of the 60 findings was individually refuted.** The refuters reproduced
   53 as stated and overturned 7 — 0 moved to a
   HARSHER verdict (a DELIVERED claim that was not), 4 to a MILDER one (a STUB that was real
   machinery with an undisclosed shortfall). The verdict in each heading is the one the REFUTER
   stands behind; the assessor's original is shown where it differs.
3. **The floor is the environment.** A finding that says 'floor scaffold reached the user' is
   not a complaint that no model ran — it is a finding that the surface did not SAY so, or
   certified what it could not assess. That is the §15 principle-6 line, and it binds.
4. **Refuters reproduced with their OWN inputs.** Several verdicts below carry evidence from a
   second journey, VSB, change-record or transfer the refuter created on the live backend —
   a finding that reproduces under a second, independently chosen input is stronger evidence
   than one observation.

## Summary

| standing verdict (after refutation) | count | as assessed |
|---|---|---|
| STUB | 12 | 13 |
| MISSING | 1 | 2 |
| DOC_OVERCLAIM | 2 | 4 |
| API_ONLY | 2 | 2 |
| PARTIAL | 36 | 32 |
| DELIVERED | 7 | 7 |
| **total** | **60** | **60** |

Standing tiers (non-DELIVERED entries, the tier the refuter stands behind):

| tier | count | meaning |
|---|---|---|
| **1** | **27** | truth defect on a reached surface — the M1 measure (target 0) |
| 2 | 17 | invisible shortfall (Phase P2's tier) |
| 3 | 9 | disclosed or unreached capability gap (Phase P3/P4) |

Per region:

| region | sections | findings | STUB | MISSING | DOC_OVERCLAIM | API_ONLY | PARTIAL | DELIVERED |
|---|---|---|---|---|---|---|---|---|
| R1 | §10 + §11 | 10 | 2 | 0 | 1 | 0 | 6 | 1 |
| R2 | §4 + §13 | 10 | 1 | 0 | 0 | 0 | 8 | 1 |
| R3 | §5 + §17.3 + §17.4 | 10 | 3 | 0 | 0 | 1 | 4 | 2 |
| R4 | §6 + §7 + §17.2 | 10 | 2 | 0 | 1 | 0 | 6 | 1 |
| R5 | §1–§3, §3A, §9, §14, §15, §17.1 | 10 | 1 | 1 | 0 | 1 | 6 | 1 |
| R6 | §8 + §12 + §17.5 | 10 | 3 | 0 | 0 | 0 | 6 | 1 |

The distilled, actionable form of the surviving gaps is **prompt v11 rev 2's `<ledger>` and
`<delivery_plan>`** (`docs/FABLE_DELIVERY_PROMPT.md`). This document is the evidence base behind
them: every `<ledger>` item cites the entries here it rests on by region.index, and every plan
workstream carries the region.index entries it closes (or says it rests on another instrument —
the reach audit for the scatter, the Owner's hand for P4).

---

## R1 — §10 + §11 — the solution-quality bar, continuous compliance, and the faith-content constitution

**Assessor's region summary:** R1 (§10 and §11) against HEAD 929508f0 on the live :8086 backend (floor only). Tier-1 truth defects remain, so M1 is not met for this region. I found four. (1) The ethical 'engine' is a bare-word regex. 'suicide' or 'kill' in a benign subject yields a FAIL: a suicide-prevention helpline, pest control and a varroa-mite beekeeping service all fail. The FAIL is then sealed as §10 'safe: measured false', routed to Change Control (cca-7d10b0532a) and stamped 'NOT cleared for use' on the export. (2) In the opposite direction, keyword-screen passes are recorded as §10 'compliant' and 'safe' MEASURED by the gate, and Deliverables shows an emerald 'COMPLIANCE: PASS'. Examples: a lithium-soldering kit for children sold without age checks, and a riba savings product that passes the Sharia row because it says 'halal'. assure_delivery drops coverage_gaps. (3) The Qur'an text path passes through alquran.cloud's Basmala-prepended ayah 1 for 112 surahs (and a BOM on 1:1) and labels it 'authentic… exact'. The hifz written-recall check then scores a character-perfect 112:1 at 35%. (4) Genesis stage 5 selects a candidate it has vetoed, contradicting its own 'cannot be selected', and reports the journey 'complete'. Tier 2 shortfalls: Genesis-shipped floor deliverables wear an emerald 'in-house · verbatim-ingest' badge, and floor exports carry no floor label; the tafsir route silently clamps a nonexistent surah (200 → 114) despite §11.2's refusal claim; written recall is orthography-bound with no normalisation (a correct undiacritised recall scores 49%); typed-number QMS gates feed the 'real' non-conformance rate sealed on deliverables. DELIVERED, verified by execution: translation refuses with 503 on the floor; tafsir injects the sourced Arabic, withholds translation and transliteration on the floor and renders floor_note and disclaimer; recitation is never scored; ayah and hifz bounds are validated (422/400); SM-2 hifz works; the QEP compliance audit returns null and does not assert; QEP analytics reports measured:false; Genesis honestly withholds modelled/simulated/ranked on identical candidates; the Deliverables §10 chip discloses '2 measured · 14 not measured'. DOC_OVERCLAIM: §10/§11 describe AI-mediated compliance engines in every workflow, but the system has regex screens, UK-Legal covers only employment terms, and Constitutional is absent from the delivery gate. The UI claims above come from reading the source; no browser was driven. State I created on the live backend by executing as instructed: deliverables deliv-62102889 and deliv-04b8d3e5, two Genesis journeys (ship_output false), QMS gate runs plus defect DEF-d6028ec4 (opened then closed; platform gates_run is now 2 and the rate 0.5), hifz/gamification uid 'w476probe', and several tafsir, compliance and recall calls. Scratch files are in C:/tmp/w476_r1/. Nothing under the repository was edited.

### R1.0 · §11 live compliance (ethical engine) → §10 'safe' / deliverable export / Genesis veto — **STUB** · tier 1

- **severity (assessor):** critical
- **claim:** §11: solutions are ethically evaluated for human and environmental well-being and safety, with compliance engines built into every workflow. §15.6: honesty over polish, never fabricate.
- **observed:** The 'ethical engine' is a bare-word regex (agentic_core/compliance/ethical_engine.py:25-27 _HARM_SEVERE = kill|murder|...|suicide). Any subject that contains one of those words gets 'human: fail — Severe human-harm indicator', which makes the overall verdict FAIL, even when the subject is a helpful service. The FAIL is then stamped as a real safety judgement: §10 'safe' is recorded met=False, measured=True, source=gate. On material labels the delivery is routed to Change Control (Change Control Agency, CCA) as a compliance violation, and the export's page one reads 'NOT cleared for use'.
- **evidence:** POST /api/v1/compliance/check {subject:'Suicide prevention helpline for young men in Leeds'} → overall 'fail', ethical 'fail' ('human: fail'). The same happens for 'A pest control service that kills rats…' and for the varroa-mite beekeeping brief. The live store holds deliverable deliv-e112d50f (a Genesis output for 'varroa mites are killing a third of my colonies… organic treatment service'): compliance 'fail', compliance_routed_to_cca=true, compliance_cca_id 'cca-7d10b0532a', bar criteria.safe {met:false, measured:true, source:'gate'}. GET /api/v1/deliverables/deliv-e112d50f/export?format=md line 3: '> **COMPLIANCE VERDICT: FAIL — NOT cleared for use — routed to Change Control (cca-7d10b0532a) — ethical: human: fail…**'. POST /api/v1/genesis/journey {problem:'A free suicide prevention helpline and peer-support service for young men in Leeds'} → stage_5 vetoed ['pragmatic','innovative','lean'], safety 0.5, compliance overall 'fail'. Deliverables.tsx:254 renders the red 'compliance: fail' chip, and the Download button reads 'Download (carries FAIL verdict)'.
- **disclosed to the user:** No. The row's coverage is 'engine' and its reason reads '(engine-backed)'. Nothing tells the user that the 'fail' comes from one word matching.
- **refutation: SURVIVED** (reproduced by the refuter). I reproduced this. The ethical 'engine' is one bare-word regex. Any subject containing a single severe-harm word, 'suicide' included, fails the 'human' dimension, so the overall verdict is FAIL. That FAIL is labelled '(engine-backed)', coverage 'engine', and is recorded as a measured §10 'safe' failure. The Deliverables page and the export then show a helpful service as 'NOT cleared for use'. This is a reached surface stating a false safety judgement.
- **refuter's evidence:** I sent POST /api/v1/compliance/check {subject:'Suicide prevention helpline for young men in Leeds'}. It returned overall 'fail' and the ethical row {status:'fail', reason:'human: fail; environment: pass; quality: not_assessed; value: review (engine-backed)', coverage:'engine'}. In agentic_core/compliance/ethical_engine.py, lines 25-27 hold _HARM_SEVERE (kill|murder|…|suicide) and line 58 is a plain .search(). GET /api/v1/deliverables/deliv-e112d50f has quality_assurance.quality.compliance.overall 'fail', compliance_cca_id 'cca-7d10b0532a' and bar_measured.criteria.safe {met:false, measured:true, source:'gate'}. The md export's line 3 reads '> **COMPLIANCE VERDICT: FAIL — NOT cleared for use — routed to Change Control (cca-7d10b0532a) — ethical: human: fail…'. Deliverables.tsx:307 shows 'Download (carries FAIL verdict)'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** A single harm-vocabulary hit should give 'review' with the matched term named, never 'fail'. Only an ethical row whose coverage is 'engine' should set §10 'safe' as measured. Keyword-only safety evidence should be recorded as source 'screen' with met=None.

### R1.1 · §10 bar 'compliant' + 'safe' recorded as MEASURED from keyword screens; Deliverables 'COMPLIANCE: PASS' chip — **STUB** · tier 1

- **severity (assessor):** critical
- **claim:** §10: every solution is safe, compliant, verified, tested and validated. §11: live compliance to legal, regulatory and industry standards, AI-mediated, with Halal/Sharia, UK-Legal, Regulatory, EHS and Ethical engines.
- **observed:** quality.py:142-146 records 'compliant' as met = (overall != 'fail') and 'safe' as met = no fail among ethical/ehs/sharia_halal, both with measured=True and source 'gate'. The inputs are keyword screens that describe themselves as 'not an EHS assessment' and 'not a certification'. The Sharia row passes whenever the word 'halal', 'waqf' or similar appears in the text. assure_delivery (quality.py:228) copies only overall/compliant/verdicts and drops the screen's coverage_gaps and basis. The Deliverables page therefore shows an emerald 'COMPLIANCE: PASS' chip with no 'screen' qualifier (Deliverables.tsx:254-258), and its tooltip says 'MEASURED by this gate: compliant · safe' (Deliverables.tsx:217).
- **evidence:** POST /api/v1/deliverables/produce {type:'brief', brief:'Halal kit for under-12s: build a home e-bike battery pack from salvaged lithium cells soldered at the kitchen table; sold without age checks'} → deliv-04b8d3e5: compliance overall 'pass', compliant true, no coverage_gaps field; bar_measured.criteria.safe {met:true, measured:true, source:'gate'}; criteria.compliant {met:true, measured:true}; measured_criteria ['compliant','safe']. POST /api/v1/compliance/check {subject:'A halal savings account that pays depositors a guaranteed fixed 6% return on money lent to borrowers'} → sharia_halal 'pass', overall 'pass', compliant true (riba without the word 'riba'). The Genesis journey (solar coop) likewise records safe/compliant as measured and gives each candidate 'safety 1.0'.
- **disclosed to the user:** Partly. The standalone ComplianceChecker header says 'It flags; it does not certify'. The Deliverables and Genesis chips and the §10 tooltip show 'pass' and 'MEASURED … safe' with no such qualifier.
- **refutation: SURVIVED** (reproduced by the refuter). I reproduced this. The Deliverables page shows an emerald 'compliance: pass' chip for a dangerous under-12s lithium-battery kit. Its tooltip says 'MEASURED by this gate: compliant · safe', and bar criteria.safe is recorded as met with measured:true. The screens missed 'lithium', 'soldered' and 'without age checks'. A riba product also passes Sharia because of the word 'halal'. One small correction to the assessor: the per-row 'reason' strings ('a keyword screen, not a certification', 'not an EHS assessment') do survive in the stored record. The chip does not render them, though: its title shows only framework:status. coverage_gaps and basis are dropped (the compliance keys are only overall, compliant and verdicts). A reached surface therefore certifies 'safe' and 'pass' for something it could not assess.
- **refuter's evidence:** deliv-04b8d3e5 has brief 'Halal kit for under-12s: build a home e-bike battery pack from salvaged lithium cells soldered at the kitchen table; sold without age checks'. Its compliance keys are ['overall','compliant','verdicts'] with overall 'pass', ehs 'pass — keyword screen found no hazard term', ethical 'pass'. Its bar_measured.criteria.safe is {met:true, measured:true, source:'gate'}. POST /compliance/check with the fixed-6%-return 'halal savings account' returned sharia_halal 'pass' and overall 'pass'. quality.py:142-148 marks compliant and safe as measured, and :228 copies only overall/compliant/verdicts. In Deliverables.tsx, :217 is the 'MEASURED by this gate' tooltip, :254-257 is the chip with title `framework:status` only, and complianceCls is emerald for 'pass'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Record compliant/safe as met=None (basis 'keyword screen only') unless a row with coverage 'engine' actually read the subject. Carry coverage_gaps and basis into quality['compliance'] and label the chip 'screen: pass'. The halal row should not pass on the subject's own use of the word 'halal'.

### R1.2 · §11.2 faith-content constitution — sourced-only, exactly (QEP ayah/surah/tafsir/hifz) — **PARTIAL** · tier 1

- **severity (assessor):** critical
- **claim:** §11.2: 'Sourced-only, exactly'. Qur'an text only from the authoritative source, labelled at source. Written recall is the learner's typed Arabic compared with the authoritative text.
- **observed:** The quran-uthmani edition prepends the Basmala to ayah 1 of every surah except 1 and 9. The platform passes this through unmodified (religious_domain/api.py:249-262 fetch_ayah_arabic, :318-324 get_surah, :366-373 get_ayah) and labels it 'Authentic Arabic text… not AI-generated'. As a result, 112 surahs show a first ayah that is not the text of that ayah under Hafs numbering. It also breaks written recall. Ayah 1:1 carries a leading U+FEFF (BOM) as well.
- **evidence:** GET /api/v1/qep/ayah/112/1 → text_arabic 'بِسْمِ ٱللَّهِ ٱلرَّحْمَٰنِ ٱلرَّحِيمِ قُلْ هُوَ ٱللَّهُ أَحَدٌ'. GET /api/v1/qep/surah/2 → ayaat[0] 'بِسْمِ ٱللَّهِ ٱلرَّحْمَٰنِ ٱلرَّحِيمِ الٓمٓ' (surah 9 ayaat[0] is correct). POST /api/v1/religion/quran-tafsir {surah:112, ayah_start:1, ayah_end:4} → arabic_text '(1) بِسْمِ ٱللَّهِ … قُلْ هُوَ ٱللَّهُ أَحَدٌ', arabic_source 'alquran.cloud — authentic sourced text'. POST /api/v1/qep/tajweed/analyse with the 112:1 reference and the learner's character-perfect 'قُلْ هُوَ ٱللَّهُ أَحَدٌ' → text_similarity 0.3492. QEPStudio.tsx:98 and :117 use this reference for the hifz review card and 'Compare with the authentic text'. GET /api/v1/qep/ayah/1/1 → the text begins with '﻿'.
- **disclosed to the user:** No. The surface asserts that the text is exact and authentic.
- **refutation: SURVIVED** (reproduced by the refuter). I reproduced this. Under Hafs numbering, ayah 1 of surah 112 does not include the Basmala. The platform serves the Basmala-prefixed text as ayah 112:1 and labels it authentic, exact text. That breaks §11.2 'exactly' on the QEPStudio reader and hifz card, and in the tafsir arabic_text. It also sinks written recall: a character-perfect recall of 112:1 scores 35%. The 1:1 text starts with a BOM. These are reached surfaces presenting sacred text inexactly under an 'authentic' label, so tier 1 stands.
- **refuter's evidence:** GET /api/v1/qep/ayah/112/1 returned text_arabic 'بِسْمِ ٱللَّهِ ٱلرَّحْمَٰنِ ٱلرَّحِيمِ قُلْ هُوَ ٱللَّهُ أَحَدٌ'. An od dump of /qep/ayah/1/1 shows the text_arabic begins with bytes 357 273 277 (U+FEFF). POST /religion/quran-tafsir {surah:200} returned arabic_text '(1) بِسْمِ ٱللَّهِ … قُلْ أَعُوذُ بِرَبِّ ٱلنَّاسِ' labelled 'authentic sourced text'. POST /qep/tajweed/analyse with the 112:1 reference and 'قُلْ هُوَ ٱللَّهُ أَحَدٌ' gave comparison.text_similarity 0.3492. religious_domain/api.py:246-264 fetch_ayah_arabic returns the quran-uthmani text unmodified. QEPStudio.tsx:98 feeds /qep/ayah into the review card, :117 feeds it into the recall compare, and :186-187 is the 'Qur'an — authentic text' header.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** For surahs other than 1 and 9, strip the leading Basmala from ayah 1 (or request an edition that does not prepend it) and strip U+FEFF. Show the Basmala separately as a header labelled 'not part of ayah 1'. Add a test for 112:1, 2:1 and 1:1.

### R1.3 · §10 'modelled · simulated · optimised · ranked' — Genesis stage 5 selects a vetoed candidate — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** §10/§15.5: solutions are modelled, simulated, optimised and ranked before delivery. Stage 5's own method states 'A candidate the screen FAILS is vetoed and cannot be selected.'
- **observed:** genesis.py:394 `winner = (_eligible or candidates)[0]`: when every candidate is vetoed, a vetoed candidate still wins. The journey carries it into Design and Commercialisation and reports status 'complete'. The UI puts a 'selected' chip on it (GenesisJourney.tsx:719-726) and prints '…vetoed for §11 failure: pragmatic, innovative, lean — the winner is carried into Design.' (GenesisJourney.tsx:742). The page shows no veto indicator.
- **evidence:** POST /api/v1/genesis/journey {problem:'A free suicide prevention helpline and peer-support service for young men in Leeds', realm:'enterprise', domain:'health', ship_output:false} → status 'complete', stage_5.selected 'pragmatic', vetoed ['pragmatic','innovative','lean'], selection_basis 'selected pragmatic on 0.40 form 0.754 · 0.35 compliance 1.0 · 0.25 safety 0.5; vetoed for §11 failure: pragmatic, innovative, lean'. By contrast, the solar-coop journey honestly withholds modelled/simulated/ranked/optimised ('TIE… resolved by list order, NOT by evidence'; candidates_distinct 1). That part is DELIVERED.
- **disclosed to the user:** Contradictory. The same line says the candidate was both selected and vetoed, while the method text promises that a vetoed candidate cannot be selected.
- **refutation: SURVIVED** (reproduced by the refuter). I reproduced this. genesis.py:394 `winner = (_eligible or candidates)[0]` selects a vetoed candidate when every candidate fails, and carries it into Design, and the journey reports 'complete'. The rendered method text says a vetoed candidate 'cannot be selected', which is untrue here. One correction: on the floor the three candidates are identical (candidates_are_alternatives false), so GenesisJourney.tsx renders the amber comparison note instead of the cards. The 'selected' chip at :719-726 therefore does not appear in this environment. The contradictory selection_basis line at :742 ('selected pragmatic … vetoed for §11 failure: pragmatic, innovative, lean — the winner is carried into Design') and the method text at :704 still render. The upstream cause is the false veto from finding 0.
- **refuter's evidence:** I sent POST /api/v1/genesis/journey {problem:'A free suicide prevention helpline and peer-support service for young men in Leeds', realm:'enterprise', domain:'health', ship_output:false}. It returned status 'complete', stage_5.selected 'pragmatic', vetoed ['pragmatic','innovative','lean'], candidates_are_alternatives false, and selection_basis 'selected pragmatic on 0.40 form 0.754 · 0.35 compliance 1.0 · 0.25 safety 0.5; vetoed for §11 failure: pragmatic, innovative, lean'. The method string at genesis.py:405-406 reads '…is vetoed and cannot be selected'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When every candidate is vetoed, stop the journey after stage 5 with status 'blocked_by_screen' and name the vetoes. Never carry a vetoed candidate forward.

### R1.4 · §11.4 floor provenance — Genesis-shipped deliverables and exports — **PARTIAL** · tier 2

- **severity (assessor):** high
- **claim:** §11.4: every AI-served surface renders serving provenance, using the shared amber 'structured floor — not model analysis' badge.
- **observed:** A Genesis journey ships its output to Deliverables with ai_provenance.served_by 'verbatim-ingest' and source_served_by {native: 11}. Deliverables.tsx:208 calls provenanceBadge(served_by) on that string, which returns an EMERALD 'in-house · verbatim-ingest' badge. The fact that every one of the 11 producing calls was the floor never reaches that badge. Exports for any floor-served deliverable, native included, carry the subtitle 'produced on Workstation IDBO's own AI fabric (in-house · native|verbatim-ingest)' (deliverables.py:345, :437) with no 'not model analysis' label.
- **evidence:** GET /api/v1/deliverables/deliv-e112d50f → ai_provenance {served_by:'verbatim-ingest', source_served_by:{native:11}}; content is floor scaffold ('## SAMAJH / - affordable organic / - organic treatment…' repeated). GET /api/v1/deliverables → the list row shows served_by 'verbatim-ingest'. The export md subtitle for deliv-04b8d3e5 reads '_brief · produced on Workstation's own AI fabric (in-house · native)_'. The QMS chip does show 'QMS —' (not assessable), so a partial signal exists.
- **disclosed to the user:** Only indirectly, through the grey 'QMS —' tooltip. The provenance badge is green, and the export says nothing about the floor.
- **refutation: SURVIVED** (reproduced by the refuter). I reproduced this. provenanceBadge only goes amber for the literal 'native'. For served_by 'verbatim-ingest' it returns the emerald 'in-house · verbatim-ingest' badge, even though ai_provenance.source_served_by is {native:11}. The Deliverables badge therefore breaks the §11.4 rule that the amber floor badge be shown. Exports say 'own AI fabric (in-house · native|verbatim-ingest)' with no floor label. The badge text is literally true and the grey QMS '—' chip gives an indirect signal, so tier 2 (an omission) is right.
- **refuter's evidence:** GET /api/v1/deliverables/deliv-e112d50f returned ai_provenance {served_by:'verbatim-ingest', source_served_by:{native:11}}. In lib/api.ts, lines 65-72 make only sb==='native' amber and return emerald otherwise. Deliverables.tsx:208 calls provenanceBadge(served_by) and ignores source_served_by. The e112 export subtitle is '_report · produced on Workstation's own AI fabric (in-house · verbatim-ingest)_'. The deliv-04b8d3e5 export is '(in-house · native)'. These come from deliverables.py:345 and :437.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In Deliverables use provenanceMapBadge(ai_provenance.source_served_by) when it is present. In _to_markdown/_doc_subtitle, add 'structured floor — not model analysis' whenever served_by or source_served_by is all-floor.

### R1.5 · §11.2 'nonexistent ayaat are refused' — tafsir surah bounds — **PARTIAL** *(assessed DOC_OVERCLAIM)* · tier 2

- **severity (assessor):** medium
- **claim:** §11.2: nonexistent ayaat are refused against the real 114-surah ayah-count table; a truncated range is disclosed as the covered range.
- **observed:** religion.py:159 `surah = max(1, min(req.surah, 114))` silently clamps a nonexistent surah and then produces a tafsir of a different surah with HTTP 200. An ayah_end beyond the surah is also clamped silently (range_note is emitted only for the 10-ayah cap). /qep/ayah refuses correctly, which shows the two paths are inconsistent. The ReligionHub tafsir field is free text labelled 'Surah (1–114)' (ReligionHub.tsx:160).
- **evidence:** POST /api/v1/religion/quran-tafsir {surah:200, ayah_start:1} → 200, reference 'Surah 114:1', arabic_text of An-Nas, no note. {surah:0} → 'Surah 1:1'. {surah:1, ayah_start:3, ayah_end:99} → 'Surah 1:3-7', range_note null. {surah:2, ayah_start:1, ayah_end:30} → range_note 'range capped at 10 ayaat…' (correct). GET /api/v1/qep/ayah/115/1 → 400; /qep/ayah/1/8 → 422 'surah 1 has 7 ayaat'.
- **disclosed to the user:** Only through the returned reference string. The substitution itself is never stated.
- **refutation: REFUTED — corrected to PARTIAL.** The gap is real, but DOC_OVERCLAIM is the wrong verdict. This is a system shortfall against §11.2, not a canon overclaim: the tafsir route clamps silently where the vision requires refusal and a disclosed range. /qep/ayah refuses correctly, as the vision says. The only disclosure is indirect, through the returned reference string, so tier 2 stands.
- **refuter's evidence:** POST /religion/quran-tafsir {surah:200, ayah_start:1} returned reference 'Surah 114:1', An-Nas Arabic and a floor tafsir, with no note. {surah:1, ayah_start:3, ayah_end:99} returned reference 'Surah 1:3-7' with range_note None. religion.py:159 is `surah = max(1, min(req.surah, 114))`. GET /qep/ayah/1/8 returned 422 'surah 1 has 7 ayaat'. ReligionHub.tsx:160 is a free-text 'Surah (1–114)' field.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Return 422 for a surah outside 1..114, and for an ayah_end greater than the ayah count, or emit range_note for the clamp, matching get_ayah.

### R1.6 · §11.3 written-recall assessment (QEPStudio 'Compare with the authentic text') — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §11.3: assessment is written recall only, comparing the learner's typed Arabic with the authoritative text, with its scope stated.
- **observed:** The scope is stated honestly ('says NOTHING about recitation'), and non-Arabic input is refused, so recitation is never scored. However, the instrument is raw character Levenshtein against Uthmani orthography (tajwid/coach.py:87-103) with no normalisation of harakat, alif-wasla, superscript alif or BOM. The score therefore mostly measures keyboard orthography rather than recall, and a correct recollection cannot reach 100%. QEPStudio.tsx:270 shows 'written similarity N% (normalised Levenshtein)', where 'normalised' means length-normalised and not orthographic normalisation.
- **evidence:** POST /api/v1/qep/tajweed/analyse against the 1:1 reference: exact copy 1.0; the same text without the BOM 0.9744; correct with full standard tashkeel 0.7179; correct without diacritics 0.4872 (plus a 'madd (kasra+ya) … absent' marker); wrong words 'بسم الله الملك القدوس' 0.3077; a different ayah 0.1538.
- **disclosed to the user:** No. The user is not told that diacritics and Uthmani-specific glyphs count as errors.
- **refutation: SURVIVED** (reproduced by the refuter). I reproduced this. The comparison is raw character Levenshtein with no orthographic normalisation. A correct recall typed in standard script is scored as largely wrong: 112:2 in standard alif without diacritics scored 47%, and 112:1 without diacritics scored 19%. Only 'WRITTEN-recall measure only' and 'normalised Levenshtein' are shown, and the user is not told that diacritics and Uthmani glyphs count as errors. The scope ('says nothing about recitation') is disclosed, so this is not tier 1.
- **refuter's evidence:** POST /qep/tajweed/analyse with the 112:2 reference 'ٱللَّهُ ٱلصَّمَدُ' gave: Uthmani copy with alif-wasla 0.7647, 'اللَّهُ الصَّمَدُ' 0.6471, 'الله الصمد' 0.4706. With the 112:1 reference, 'قل هو الله احد' gave 0.1905 with madd markers 'absent'. tajwid/coach.py:87-103 applies strip() only. QEPStudio.tsx:270 renders 'written similarity N% (normalised Levenshtein)'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Report two figures: skeletal-letter similarity (harakat, wasla and BOM stripped, alif forms unified) and full-orthography similarity, each labelled. Say on screen that the reference uses Uthmani script.

### R1.7 · §10 living QMS — non-conformance rate fed by typed numbers — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §10: verified, tested and validated. The QMS non-conformance rate is 'a real rate' (VBSSystemsPanel chip title) and is sealed into every deliverable's quality record.
- **observed:** POST /api/v1/vbs/qms/gate takes caller-typed coverage and stubs_found, and its docstring says it runs 'a real ISO-9001-aligned quality gate'. Each call counts as a real gate run in the platform non-conformance rate. The VBSSystemsPanel 'run a gate' control (VBSSystemsPanel.tsx:46-55, default 0.97) produces a 'PASSED' that lowers the rate. /reverify with typed metrics closes a defect as 'caller_attested' and also counts as a gate run. Floor-served deliverables never run the gate (correct), so in this environment the qms_non_conformance_rate stamped on every new deliverable comes entirely from typed numbers.
- **evidence:** Before: /api/v1/vbs/qms/defects → gates_run 0. POST /qms/gate {coverage:0.5, stubs_found:true} → passed false, non_conformance_rate 1.0, opened DEF-d6028ec4. POST …/correct, then …/reverify {coverage:1.0, stubs_found:false} → status 'closed', reverify_basis 'caller_attested', passed true. Summary → gates_run 2, non_conformance_rate 0.5. The UI reverify path does send content (measured_from_content) and colours caller_attested amber (VBSSystemsPanel.tsx:190). The raw gate button has no such label.
- **disclosed to the user:** Partly: an attested reverify is coloured amber. The typed-number gate and its effect on the platform rate are not disclosed.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed from the code and the live store. /vbs/qms/gate takes caller-typed coverage and stubs_found and counts each call as a real gate run. The UI 'run a gate' control (default 0.97) prints PASSED or FAILED with no 'typed input' label. The chip calls the result 'a real rate'. quality.py:256 stamps qms.get_non_conformance_rate() on deliverables. The live summary is currently made entirely of typed probes. A caller-attested reverify is coloured amber, so the disclosure is partial. Tier 2 stands. I did not POST new gates, to avoid shifting the shared rate further.
- **refuter's evidence:** GET /api/v1/vbs/qms/defects returned summary {gates_run:2, gate_failures:1, non_conformance_rate:0.5}, and its one defect has meta {coverage:0.5, stubs_found:true} and reverify_basis 'caller_attested'. vbs_systems.py:40 has the docstring 'Run a real ISO-9001-aligned quality gate'. In VBSSystemsPanel.tsx, :47-52 posts the typed gateCov/gateStubs, :157 is the chip 'a real rate', and :174 is the PASSED/FAILED chip.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Record typed-number gates as 'what-if' runs excluded from gates_run and the non-conformance rate, or label the button 'what-if (typed metrics — not a measurement)'. Count only measured gates in the sealed rate.

### R1.8 · §11 faith-content constitution — rules 1, 3, 4, 5 and the QEP routes — **DELIVERED**

- **severity (assessor):** none
- **claim:** §11.1/.3/.4/.5: Qur'an Arabic is never AI-generated; recitation is never scored; floor output is never presented as translation or scholarship; AI content is labelled AI-assisted with provenance, and learners are directed to teachers.
- **observed:** Verified by execution. The /qep/translation/status route (translation_available false) and a translate POST with Arabic text → 503 'the deterministic native floor cannot translate'. The tafsir route fetches the Arabic from source and injects it as given material (religion.py:190-213). On the floor it withholds Transliteration and Translation (sections_withheld), sends a floor_note and disclaimer, and ReligionHub.tsx:143-157 renders them. The tajweed lesson on the floor → floor_served true, the 'outline, not a lesson' chip, and a teacher disclaimer. The status route says 'Recitation is never scored'. The hifz routes (SM-2): schedule {surah 1, [5,50]} → 422; review '1:99' → 422; quality 9 → 400; the valid 112:1-4 flow works, with the review count labelled 'not a hifz certification'. The QEP compliance/audit route → compliant null with not_checked controls (not asserted). The QEP analytics/overview route → measured false. XAI attributions are labelled display weights. Remaining gaps: the floor lesson calls meaningless keyword output a 'study checklist', and the tafsir tab's description 'drawing on the classical mufassirun' is static copy shown above floor output (the floor_note sits beside it).
- **evidence:** Routes and payloads as listed. GET /api/v1/qep/status → constraints include 'Recitation is never scored — no phonetic model exists to score it'. POST /api/v1/religion/halal-review with pork gelatin, served by the floor → no status is fabricated, with the amber floor badge through DomainTool.tsx:239-242.
- **disclosed to the user:** Yes
- **refutation: SURVIVED** (reproduced by the refuter). I spot-checked this by execution and it holds for rules 1, 3, 4 and 5 on the QEP routes. Translation refuses with 503, the status route states recitation is never scored, hifz refuses nonexistent refs with 422, and /qep/ayah refuses nonexistent ayaat. It is DELIVERED only within that scope: rule 2 ('exactly') fails (findings 2 and 5), and rule 4's amber-badge requirement fails on Deliverables (finding 4).
- **refuter's evidence:** GET /qep/translation/status returned translation_available false with 'the deterministic native floor cannot translate'. POST /qep/translation/translate returned 503. POST /qep/hifz/review {ayah_ref:'1:99'} returned 422 "ayah_ref '1:99' does not name an ayah". GET /qep/status contains 'Recitation is never scored — no phonetic model exists to score it'. GET /qep/ayah/1/8 returned 422.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Change the floor lesson's floor_note from 'study checklist' to 'no rule content — headings only', and make the tafsir description conditional on provenance.

### R1.9 · §10 Solution-Quality Bar + §11 'compliance engines, AI-mediated, in every workflow' — canon vs system — **DOC_OVERCLAIM** · tier 3

- **severity (assessor):** medium
- **claim:** §10: every solution is specifically designed, modelled, simulated, optimised, categorised and ranked; best-in-class, innovative, effective, efficient, commercially viable; verified, tested and validated. §11: engines for Halal/Sharia, UK-Legal, Regulatory, EHS, Ethical and Constitutional/gaas are integrated into every synthesis, generative and operational workflow, AI-mediated.
- **observed:** On the floor, which is all this environment has, a produced deliverable has 14 of 16 bar criteria not measured and 2 'measured' by keyword screens (see finding 2). The deliverable surface discloses this ('§10 bar: 2 measured · 0 attested · 14 not measured', Deliverables.tsx:225-229), and the gate honestly reports 'not assessable'. The §11 'engines' are regexes: Halal is a vocabulary check plus HalalComplianceOfficer; UK-Legal matches only employment-statute terms and returns 'no engine covers this area' for everything else; Regulatory and EHS are word lists; Ethical is a regex. The Constitutional gate is not part of assure_delivery at all (only /compliance/check runs it, and for content it returns 'not_checked'). None of this is AI-mediated. The floor content itself nests every requested heading inside each section (a template echo).
- **evidence:** POST /api/v1/deliverables/produce {type:'report', brief:'A halal community-owned solar microgrid cooperative…'} → deliv-62102889: qms_gate_passed null, qms_basis 'not assessable — floor-served…', bar_measured.summary '2 measured · 0 attested · 14 not measured'; content repeats '## Executive Summary … ## Next Steps' inside every section. GET /api/v1/compliance/frameworks describes each row honestly (for example 'keyword screen … not an EHS assessment'). compliance.py:73-81 holds the regexes.
- **disclosed to the user:** Yes on the Deliverables §10 chip and the ComplianceChecker page. The vision text itself states the capability as present.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. §10 and §11 in the vision state AI-mediated compliance engines and a complete quality bar in every workflow. The system has keyword and regex screens: GET /compliance/frameworks describes itself honestly as 'haram-term screen … not a certification', and the ethical dimension is a regex. The Constitutional row is absent from delivery assurance: the stored deliverable compliance has 5 rows and no 'constitutional'. The deliverable §10 chip discloses '2 measured · 14 not measured'. The overclaim sits in the canon, and the surface discloses it (apart from the misleading pieces already in findings 0 and 1), so tier 3.
- **refuter's evidence:** docs/WORKSTATION_IDBO_WHOLE_VISION.md:376-389 holds the §10 and §11 text. The compliance verdicts on deliv-04b8d3e5 and deliv-e112d50f are sharia_halal, uk_legal, regulatory, ehs and ethical, with no constitutional row. /compliance/check shows the constitutional row as 'not_checked … not applicable to content'. qms_basis reads 'not assessable — floor-served…'. The §10 chip is at Deliverables.tsx:222-229.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Mark the vision §10/§11 capability sentences with measured status, for example '(screens today; engines: Halal only)'. Close the capability gap separately.

---

## R2 — §4 + §13 — the end-to-end lifecycle (Describe → … → Run forever) and what the output IS

**Assessor's region summary:** R2 (§4 lifecycle + §13 output). I assessed this against the live backend at http://127.0.0.1:8086 and did not modify the repository. I ran four things: 2 one-call Genesis journeys with establish (vsb-24267609d1 'Varroa Guard R2', a Yorkshire beekeeper; vsb-6cbffb7c1d 'Fell Physio R2', with review_gates [research, design] set at birth), 1 two-step /genesis/establish/stream, and on vsb-24267609d1 the /evolve, /concept, board-pack, repo, ship, mobile and review-gates routes plus a deliverables produce+export. I read the shipped repo on disk at C:/tmp/w476b/vsb_repos/vsb-24267609d1. What a user sees is inferred from GenesisJourney.tsx and VSBCockpit.tsx; I did not drive a browser.  STAGE COUNT. The journey returns 8 text stages. None contains model reasoning, because all 13 calls were served by the floor. 6 stages (concept, research, candidates, design, operations, commercialisation) carry the user's problem only as echoed word pairs plus a truncated 'Subject:' line inside engine scaffold ('Component for want affordable'). Each has 13–14 of the 17 content words from the problem. 2 stages (the Cognitive Cascade and the MJM Assessment) contain 0 of the 17. Their text is another interaction's recalled memory ('intermittent fasting', 'insulin sensitivity') plus the engine's own prompt words. - All 3 candidates came back byte-identical. The payload says this, and the UI shows the note instead of ranking cards. - The §10 bar withholds modelled, simulated, ranked and optimised, and all 5 stage verifications read 'not assessable'. Both are honest.  WHAT AN ESTABLISHED VSB CONTAINS. The §13 corollary holds on the ship path. The body (concept, design, commercialisation, operations) is 4 of 4 'content pending the owned model' in every file I checked: the genome, the business plan, the website, the web-app and PWA data.json, and the board pack. The shipped surfaces are real, not stubs: - a git repo with 4 commits - a 3-page static site - a tabbed web app - an installable PWA with manifest and service worker, served at /mobile/page/index (HTTP 200) - a board pack §4.9 export is honest: 8 live formats, and pdf/docx/pptx return 400 against a disclosed catalogue.  ONE GATED LIFECYCLE? Partly. Review gates are enforced on the later steps that move the lifecycle forward (evolve and ship both return 409 and name the gate). But they do not pause the stages they name, and three different stage lists co-exist: 8 gate stages, the 10 §4 steps and a 6-tile UI rail.  WHAT REMAINS. Five Tier-1 truth defects stand on reached surfaces, so M1 is not met for R2: 1. Recalled memory from another interaction is presented as the beekeeper's own cognitive and MJM analysis, and the provenance card says it was composed 'from your inputs'. 2. A keyword false positive ('killing' in 'mites are killing') produces an engine-backed ethical/human FAIL. It vetoes every candidate, holds the first economy cycle and stamps every surface with a compliance fail. 3. Status 'operational', a completed 'Operate' milestone and all 6 stage tiles lit are hard-coded or derived from the wrong facts. 4. The board pack certifies compliant and safe by screening its own 'pending' placeholder sentence. 5. 'Shipped body is current' is shown after the founder records a concept (the trigger is API-only, so I graded it tier 2). There are further tier-2 gaps in the export, in EVIDENCE.md's handling of the veto, and in evolve. Outside my region, the establish stream labels the Chief 'the owner's digital-twin Chief' while the board record says 'no twin model is trained'. That belongs to the §5 auditor.

### R2.0 · §4.2 Understand & Map / §4.1 (Genesis Phase 1) — **STUB** · tier 1

- **severity (assessor):** critical
- **claim:** §4.2: AI-mediated problem mapping — understand the person, the context and the real underlying problem. §15.6: never fabricate.
- **observed:** POST /api/v1/genesis/journey {problem:'I keep bees in Yorkshire and varroa mites are killing a third of my colonies…', domain:'agriculture', establish:true}. phase_1_conceptualisation.cognitive_cascade contains 0 of the 17 content words in the problem. It repeats six times the subject 'Analyse the following thro (domain: general)' with bullets 'architecture specialised · specialised intelligence · cognitive architecture…'. mjm_assessment repeats nine times 'Process this through the three phases of Mus (domain: general)' with bullets 'intermittent fasting · insulin sensitivity · sensitivity adults'. Those words come from ANOTHER interaction: memory.json holds 3 hits for 'intermittent fasting improve insulin sensitivity in adults'. Domain 'agriculture' became 'general'. The UI (GenesisJourney.tsx:428-429) renders these as 'Cognitive Cascade (6 engines)' and 'MJM Assessment' under Phase 1. The provenance card above them (GenesisJourney.tsx:629-634) says the floor served 11 calls and that its output is 'honest structured composition from your inputs'. In fact 13 calls ran, and these 2 are neither counted nor composed from the user's inputs.
- **evidence:** agentic_core/api/intelligence.py:315 and :344 call gateway.query(prompt, agent=…) with no augment=False. gateway.py:117/148 default augment=True, so _augment() prepends recalled memory and relabels the prompt 'User: You are the Cognitive Architecture…', and the floor takes its subject from that line. agentic_core/api/genesis.py (Phase 1): cognitive/mjm are captured with _public_prose but bypass the provenance-recording _q closure, so served_by.native=11 and served_by_agent has no cognitive or MJM entry. Scratch record: C:/tmp/w476_r2/journey.json.
- **disclosed to the user:** No. The card claims the opposite ('from your inputs'); no per-section floor or pending marker.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced from the code and the recorded journey. intelligence.py:315 and :344 call gateway.query without augment=False. gateway.py:148 therefore runs _augment, which prepends recalled memory and appends 'User: {prompt}'. The floor takes its subject from the persona line, so the stored cognitive_cascade reads 'Analyse the following thro (domain: general)' with bullets 'architecture specialised / specialised intelligence…'. mjm_assessment carries 'intermittent fasting / insulin sensitivity', which is recall from another interaction. genesis.py:287/291 capture both outside the provenance-recording _q closure (:249-260). ai_provenance is {'native': 11}, and served_by_agent has 11 genesis_* entries with no cognitive or MJM entry. GenesisJourney.tsx:428-429 renders both sections as Phase 1 analysis. :629-634 says the floor served '11 of these calls — honest structured composition from your inputs'. That statement is untrue for the two sections shown directly beneath it. This is a reached surface that misleads, so tier 1 stands.
- **refuter's evidence:** C:/tmp/w476_r2/journey.json: phase_1_conceptualisation.cognitive_cascade and mjm_assessment content, and ai_provenance.served_by={'native':11} with no cognitive_cascade_ai or mjm_orchestrator_ai in served_by_agent. agentic_core/api/intelligence.py:315,:344; agentic_core/ai/gateway.py:92-113,:148; agentic_core/api/genesis.py:249-260,:287-293; apps/workstation-superapp/src/pages/synthesis/GenesisJourney.tsx:428-429,:629-634.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Pass augment=False and route the two calls through the journey's provenance-recording _q (which carries the Problem field). When floor-served, render these two sections as 'pending the owned model' as the VSB body does, and count them in served_by.

### R2.1 · §4.5 / §4.8 / §11 as it drives the lifecycle — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** §4.5: candidates are ranked on evidence including compliance and safety. §4.8: establish a VSB that delivers and commercialises. §15.6: never certify what was not assessed.
- **observed:** The same beekeeper journey: every candidate gets screen.overall=fail with ethical 'human: fail; … (engine-backed)'. stage_5.vetoed=[pragmatic, innovative, lean]. established_vsb.birth_vitals.first_cycle.held='compliance_fail_hold'. Ship commit messages read 'compliance fail' on repo/website/webapp/mobile. /evolve proposes 'remediate the §11 screen posture (currently fail)'. The exported report is stamped 'COMPLIANCE VERDICT: FAIL — NOT cleared for use — routed to Change Control'. Control, POST /api/v1/compliance/check: {subject:'I keep bees … varroa mites are killing a third of my colonies…'} gives overall fail, but {subject:'An affordable organic varroa treatment service for small-scale beekeepers in Yorkshire'} gives overall review with ethical human: pass. A pest-control sentence is certified an engine-backed severe human-harm FAIL, and the matched word is dropped from every surfaced reason.
- **evidence:** agentic_core/compliance/ethical_engine.py:26: severe lexicon r'\b(kill|murder|…)\w*'. ethical_engine.py:60: 'Severe human-harm indicator'. agentic_core/api/compliance.py:267-269 condenses the result to 'human: fail; …' and loses the matched term. genesis.py _screen_candidate: 'disqualified': overall=='fail'.
- **disclosed to the user:** The fail is disclosed. That it rests on one keyword ('killing', about mites) is not; the reason shown is only 'human: fail'.
- **refutation: SURVIVED** (reproduced by the refuter). I reproduced this live, with a sharper control than the assessor used. POST /api/v1/compliance/check with 'My bees are dying because varroa mites are killing a third of my colonies' returns overall=fail. The ethical verdict reason is 'human: fail; environment: pass; quality: not_assessed; value: review (engine-backed)'. Changing the single word to 'harming' returns overall=review, compliant=true. ethical_engine.py:26 matches kill\w* as severe human harm. The dimension reason names the term ('Severe human-harm indicator: killing'), but compliance.py:267-269 condenses the result to 'human: fail' and labels it 'engine-backed', so the matched word never reaches the user. In the journey, all three candidates are screened ethical=fail and vetoed, and birth_vitals.first_cycle.held='compliance_fail_hold'. The system certifies, as an engine-backed severe human-harm FAIL, something it only keyword-matched. That verdict blocks the founder's distributions. Tier 1 stands.
- **refuter's evidence:** Live: POST http://127.0.0.1:8086/api/v1/compliance/check. 'killing' gives overall fail, ethical reason 'human: fail; …(engine-backed)'. 'harming' gives overall review, compliant true. agentic_core/compliance/ethical_engine.py:24-26,:58-60,:(reason join) 'dimension: status'. agentic_core/api/compliance.py:262-270. journey.json: every candidate has screen.verdicts.ethical='fail'; established_vsb.birth_vitals.first_cycle.held='compliance_fail_hold'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Carry the matched term into the surfaced reason. Downgrade a lone lexicon hit to 'review — keyword match, not an assessment' unless it has a human object. Never hold distributions or veto candidates on a keyword-only severe hit.

### R2.2 · §4.8 Establish / §4.10 Run forever (status, progress map, stage rail) — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** §4.10: the VSB self-operates and forever evolves. §15.6: honesty over polish.
- **observed:** Every establish path writes status:'operational', stage:'commercialise' as literals, whatever the state. For vsb-24267609d1 the real state is: body 4 of 4 pending, first §11 screen FAIL, first cycle held, and living.autonomous_cycles=false (Self-run OFF). For vsb-6cbffb7c1d: research and design gates pending, yet GET /api/v1/vsb/vsb-6cbffb7c1d returns status 'operational', stage 'commercialise'. What the user sees, from source: - GenesisJourney.tsx:893 prints the literal '{vsb_id} · operational · governance allowed' and an emerald 'Living Enterprise IDBO generated'. - VSBCockpit.tsx:453 shows the status as a green Badge. - The shipped README.md says 'Status: operational'. - The progress map (GenesisJourney.tsx ~466) lights 'Operate ✓ — Run · defend · improve · grow' when vsb && surfaces, though its own comment says milestones light 'from REAL state'. - The 6-tile stage rail (GenesisJourney.tsx:507-508) marks every stage done whenever a result exists, including 'Model · Simulate · Rank — candidate solutions scored on evidence → best' on a run whose ranking the payload withholds. The one-call path never renders birth_vitals (no match for birth_vitals, held or compliance in GenesisJourney.tsx or VSBCockpit.tsx). Only the SSE two-step path shows 'First Economy Cycle: held'.
- **evidence:** genesis.py:842, :978, :1048, :1152 and vsb.py:1782 set status 'operational' literally. GenesisJourney.tsx:461-467 and :507-508. VSBCockpit.tsx:453. Stream output: C:/tmp/w476_r2/stream.txt ends 'complete | Operational | … is alive.' right after 'held: compliance_fail_hold'.
- **disclosed to the user:** Partly. The body-pending line is shown. The compliance hold, the Self-run-off state and the pending gates are not shown beside the green 'operational' and 'Operate ✓'.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. GET /api/v1/vsb/vsb-24267609d1 and GET /api/v1/vsb/vsb-6cbffb7c1d both return status='operational', stage='commercialise' and living.autonomous_cycles=False. The status is a literal at genesis.py:842, :978, :1048 and :1152 and at vsb.py:1782. For vsb-24267609d1, the journey's birth_vitals show first_screen.overall='fail' and first_cycle held 'compliance_fail_hold'. What the user sees, from the source: - VSBCockpit.tsx:453 renders detail.status as an emerald Badge. - GenesisJourney.tsx:893 prints '· operational ·'. - The progress map (:461-467) lights 'Operate — Run · defend · improve · grow' when vsb && surfaces, despite its own 'from REAL state' comment. - The 6-tile rail (:507-508) sets done = !!result for every tile. - The shipped README.md:6 says 'Status: operational'. Neither component contains birth_vitals or held. An entity that is held for compliance failure, with self-run off, is shown in green as operating. Tier 1 stands.
- **refuter's evidence:** Live GET /api/v1/vsb/vsb-24267609d1 and /vsb-6cbffb7c1d: operational / commercialise / autonomous_cycles False. journey.json birth_vitals shows first_screen fail and first_cycle held compliance_fail_hold. apps/workstation-superapp/src/pages/enterprise/VSBCockpit.tsx:453. GenesisJourney.tsx:461-467,:507-508,:893. C:/tmp/w476b/vsb_repos/vsb-24267609d1/README.md:6. agentic_core/api/vsb.py:1782.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Derive status from facts (e.g. 'registered — held: §11 fail', 'registered — not operating: Self-run off', 'gated'). Light 'Operate' only when autonomous_cycles is true and the last cycle ran. Light rail tiles from the payload's per-stage attestations. Render birth_vitals on the one-call path.

### R2.3 · §13 / §17.3 Board pack (compliance certification) — **PARTIAL** · tier 1

- **severity (assessor):** medium
- **claim:** §13: the output is compliance-screened (§11) throughout. §15.6: never certify what was not assessed.
- **observed:** GET /api/v1/vsb/vsb-24267609d1/board-pack returns narrative 'narrative pending the owned model — this board pack has not been composed…'. Its quality_assurance shows compliance.overall='review', compliant=True, and bar_measured.criteria.compliant={met:true, measured:true, source:'gate'}, safe={met:true, measured:true, source:'gate'}. At the same moment the entity's own §11 screen is FAIL (ship record: repo/website/webapp/mobile compliance_overall 'fail'; board_pack 'review'). The screen read only the placeholder sentence and certified the enterprise compliant and safe. The UI (GenesisJourney.tsx ~1062) shows a 'compliance: review' chip on the pack.
- **evidence:** agentic_core/api/vsb.py:1172-1180: narrative = the pending string when served_by is 'native', then assure_delivery(narrative, …) screens that string. Stored pack: C:/tmp/w476b/vsb_board_packs/vsb-24267609d1/latest.json.
- **disclosed to the user:** No. The chip reads 'review' and nothing says the screen read only a placeholder or that the entity itself stands FAIL.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. vsb.py:1171-1180 swaps in the pending sentence when sb=='native', then screens that sentence with assure_delivery. In bp.json the narrative is the pending string and quality.not_assessable=true. Even so, quality.compliance is overall='review', compliant=true, and bar_measured.criteria.compliant and .safe are both {met:true, measured:true, source:'gate'}. That is a measured compliant/safe certification derived from one placeholder sentence, while the entity's own ship record shows compliance_overall 'fail' on every surface. The UI chip (GenesisJourney.tsx ~1062) shows 'compliance: review' on the pack, softer than the entity's FAIL, with no note that only the placeholder was screened. The strongest untruth sits in the API fields (measured:true, met:true). The chip itself contradicts the entity's standing FAIL. Tier 1 stands.
- **refuter's evidence:** C:/tmp/w476_r2/bp.json: quality_assurance.quality.compliance.overall='review', compliant=true; bar_measured.criteria.compliant/safe met true, measured true, source gate; not_assessable true. Live GET /api/v1/vsb/vsb-24267609d1/repo/ship: every surface compliance_overall 'fail'. agentic_core/api/vsb.py:1171-1180. GenesisJourney.tsx:1062-1065.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When the narrative is pending, report compliance as 'not assessable — nothing composed' (compliant/safe met:null). Otherwise screen the constitutional/mission layer the pack actually carries, and show the entity's latest §11 verdict on the pack.

### R2.4 · §13 Living repo (drift honesty after founder input) — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §13: the repo is the enterprise's living body and continues to evolve; W290/W309 say a shipped repo is never silently outdated.
- **observed:** POST /api/v1/vsb/vsb-24267609d1/concept {concept:'A county-wide mobile service that treats hives with oxalic acid vapour…'} returns concept_source 'founder'. Afterwards GET /repo/ship gives stale:false and /repo ship_status gives stale:false. The shipped BUSINESS_PLAN.md, web/index.html and data.json still say 'content pending the owned model — this enterprise has not yet composed its own concept'. VSBCockpit.tsx:782 would therefore show emerald 'Shipped body is current'. The /concept route has no UI caller (no match in apps/workstation-superapp/src). The only path is the board pack's concept_note, which tells the user to 'record a concept at POST /api/v1/vsb/{id}/concept'. I graded this tier 2 rather than 1 only because the trigger is API-only.
- **evidence:** agentic_core/api/vsb.py:1092-1106 (record_vsb_concept) saves without calling mark_repo_stale (vsb.py:1899). VSBCockpit.tsx:782-783.
- **disclosed to the user:** No. The cockpit asserts the opposite ('current').
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed by reading the code. record_vsb_concept (vsb.py:1092-1106) sets genesis_blueprint.concept and concept_source='founder', then calls _save_vsb with no mark_repo_stale. By contrast, rename (:1375) and evolution apply (:2245) both call it. The concept feeds the shipped body (vsb.py:107, :469), so a recorded concept makes the shipped body outdated while ship_status stays stale:false. VSBCockpit.tsx:782-783 would show 'Shipped body is current'. grep finds no UI caller of /concept, and the only pointer to it is the board pack's concept_note (vsb.py:1203). The trigger is API-only, so tier 2 is right. I did not re-POST, to avoid mutating state; the missing call is visible in the code.
- **refuter's evidence:** agentic_core/api/vsb.py:1092-1106 (no mark_repo_stale), :1375 and :2245 (the other paths do call it), :1900 def mark_repo_stale. apps/workstation-superapp/src/pages/enterprise/VSBCockpit.tsx:782-783. grep '/concept' in apps/workstation-superapp/src returns no vsb caller.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Call mark_repo_stale(vsb_id, 'founder recorded concept') in /concept (and wherever the body changes), and give the founder a UI field for concept, design and commercialisation so the pending body can actually be filled.

### R2.5 · §4.9 Output in selectable formats (journey export) — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §4.9: research and deliverables as reports and presentations. §13 corollary: a floor-served body ships as the founder's words plus an honest pending state, never as the enterprise's concept.
- **observed:** GenesisJourney 'Export → Report' (GenesisJourney.tsx:232-261) sends the floor text word for word to POST /api/v1/deliverables/produce. I replayed it as deliv-e112d50f and exported with GET /export?format=html. The exported artefact's sections 'Concept → Optimal Solution Concept', 'Design', 'Commercialisation' carry keyword-pair scaffold ('Component for want affordable'). The subtitle is 'produced on Workstation IDBO's own AI fabric (in-house · verbatim-ingest)'. The html contains no 'not assessable' or floor statement: the only mentions of 'deterministic' are two incidental 'deterministic scaffold' lines. The stored record does know: qms_gate_passed=None, not_assessable=True, source_served_by {native:11}. So the ship path applies the §13 pending rule and the export path does not.
- **evidence:** agentic_core/api/deliverables.py:433-438 (_doc_subtitle uses prov.served_by='verbatim-ingest' and ignores source_served_by and qms_gate_passed). deliverables.py:337-345 (_to_markdown, same). Scratch copy: C:/tmp/w476_r2/export.html.
- **disclosed to the user:** In-app via the QMS chip only. The exported file, which is what leaves the platform, does not say it.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. _doc_subtitle (deliverables.py:433-438) and _to_markdown (:337-345) build provenance only from ai_provenance.served_by and ignore source_served_by, not_assessable and qms_gate_passed. The exported file reads 'produced on Workstation IDBO's own AI fabric (in-house · verbatim-ingest)'. It contains no 'not assessable' and no 'floor' (grep count 0), while its body holds floor keyword scaffold such as 'Component for want affordable.' and 'Component for organic treatment.'. The in-app QMS chip does disclose the shortfall, so tier 2 is defensible. It sits on the edge of tier 1, because the artefact that leaves the platform presents floor scaffold as the fabric's output.
- **refuter's evidence:** C:/tmp/w476_r2/export.html: grep -c 'not assessable|floor' = 0; subtitle 'in-house · verbatim-ingest'; 'Component for want affordable.' agentic_core/api/deliverables.py:337-345,:433-438.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When source_served_by is floor or qms_gate_passed is null, put 'Floor-served — structured frame, not model reasoning; not assessable' in the export subtitle, or apply the same pending substitution the ship path uses.

### R2.6 · §4.5 Model · Simulate · Rank (veto outcome carried into shipped evidence) — **PARTIAL** · tier 2

- **severity (assessor):** low
- **claim:** §4.5: the best solution is selected on evidence, and a §11 failure vetoes a candidate.
- **observed:** stage_5.vetoed lists all three candidates, yet stage_5.selected='pragmatic'. The UI line reads '…vetoed for §11 failure: pragmatic, innovative, lean — the winner is carried into Design'. The shipped EVIDENCE.md says 'Selected Candidate (§4.5 evidence-ranked) — id: pragmatic · rank: 1 · score: 0.771'. It never says the selected candidate was disqualified, though it does note that the comparison did not happen.
- **evidence:** genesis.py: winner = (_eligible or candidates)[0], so when every candidate is vetoed the vetoed first one wins. GenesisJourney.tsx ~745 appends '— the winner is carried into Design'. C:/tmp/w476b/vsb_repos/vsb-24267609d1/EVIDENCE.md.
- **disclosed to the user:** The veto list is in the UI's selection_basis line but is contradicted by 'the winner'. It is absent from the shipped EVIDENCE.md.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. The comment at genesis.py:388 says 'a candidate the §11 screen FAILS cannot win'. Yet :394 sets winner = (_eligible or candidates)[0], so when every candidate is vetoed, a vetoed candidate wins. The journey shows vetoed=[pragmatic, innovative, lean] and selected='pragmatic'. GenesisJourney.tsx:742 appends '— the winner is carried into Design' after the veto list. EVIDENCE.md has no 'veto' or 'disqualif' anywhere; it presents 'Selected Candidate (§4.5 evidence-ranked)' and notes only that no comparison happened. The veto is visible in the UI's selection_basis, so tier 2 fits. One minor correction: the repo has 8 commits (two ships), not 4.
- **refuter's evidence:** journey.json stage_5_model_simulate_rank: selected 'pragmatic', vetoed all three. agentic_core/api/genesis.py:388-394. GenesisJourney.tsx:742. grep -i 'veto|disqualif' in C:/tmp/w476b/vsb_repos/vsb-24267609d1/EVIDENCE.md finds nothing. git log shows 8 commits.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When _eligible is empty, set selected=null and say 'no candidate cleared §11 — nothing selected', both in the payload and in EVIDENCE.md.

### R2.7 · §4.10 Learn · Improve · Grow (evolve) — **PARTIAL** · tier 3 *(assessed tier 2)*

- **severity (assessor):** low
- **claim:** §4.10: the VSB forever evolves on live intelligence, representing the founder's instructions ever better.
- **observed:** POST /api/v1/vsb/vsb-24267609d1/evolve {context:'beekeepers ask for a winter oxalic acid treatment round'} returns generation 1. The only proposal is 'compliance_posture: remediate the §11 screen posture (currently fail)', whose expected_impact 'the screen returns to pass; distributions are never held' is a predicted outcome with no instrument behind it. The founder's context was silently not acted on. The floor emitted no EVOLVE lines, so the deterministic fallback ran, and the response carries no served_by or floor note. generation still increments. The route does honour gates: on vsb-6cbffb7c1d it returned 409 naming the gate.
- **evidence:** agentic_core/api/vsb.py:2031-2080 (EVOLVE parse, fallback, generation += 1 unconditionally, return dict has no provenance field).
- **disclosed to the user:** No. Nothing says the model path produced nothing or that the founder's context was not used.
- **refutation: REFUTED — corrected to PARTIAL.** Overstated. The 'founder's context silently ignored' part is API-only. The cockpit's Evolve button (VSBCockpit.tsx:154-155) sends only {trigger:'cockpit'}, and no UI sends a context. The context is also placed in the prompt (vsb.py:2022). Only the floor fails to use it, and that is the environment. The fallback proposal is honest and evidence-based. It is drawn from the real latest §11 screen (fail), it carries basis 'latest §11 compliance screen (evidence-based, owned)', and it routes to Change Control, where it is never auto-applied. expected_impact states a goal, not a measured claim. Incrementing generation for a cycle that produced a proposal is not untrue. What remains is a tier-3 gap: the response carries no served_by, so nothing says the model path produced no EVOLVE lines.
- **refuter's evidence:** C:/tmp/w476_r2/evolve.json: proposals[0].basis='latest §11 compliance screen (evidence-based, owned)', evolution_pending_cca set. agentic_core/api/vsb.py:1821-1827 (context field), :2017-2027 (prompt includes context), :2043-2073 (evidence-only fallback). apps/workstation-superapp/src/pages/enterprise/VSBCockpit.tsx:154-155 (UI sends no context).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Return served_by and a note ('floor: no model proposals; N evidence-derived proposals; your context was not acted on'). Advance generation only when a proposal exists.

### R2.8 · §4 One gated lifecycle (Mode 3 review gates) — **PARTIAL** · tier 3

- **severity (assessor):** low
- **claim:** §4: one continuous, gated, verified workflow from Describe to Run forever.
- **observed:** A journey with review_gates:['research','design'] set at birth produced vsb-6cbffb7c1d. The research and design stages still ran and returned text (1404 and 2636 characters). The gates show 'pending' for stages already done. initial_ship was deferred ('review gate blocks progress (Mode 3): research pending, design pending'). /evolve and /repo/ship both returned 409 with the gate named and a clear_by route: verified. Three different stage lists co-exist: 8 gate stages (intake…launch), the 10 §4 steps, and the 6-tile UI rail. There is no Develop (§4.6) stage in the journey at all.
- **evidence:** GET /api/v1/vsb/vsb-6cbffb7c1d/review-gates. genesis.py JourneyRequest.review_gates. GenesisJourney.tsx:1083 (copy: gates block 'lifecycle movers — ship, evolve, cascades…').
- **disclosed to the user:** Yes. The gate panel says gates block movers, not stages.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. GET /api/v1/vsb/vsb-6cbffb7c1d/review-gates returns stages ['research','design'] and an 8-stage lifecycle (intake, research, design, build, validate, commercialise, …). Gating is by design at the movers. POST /evolve on the gated entity returned 409, which is a refusal with no mutation. The stage-list inconsistency and the absence of a distinct Develop stage are real but disclosed or structural, and the gate panel copy says gates block movers. Tier 3 stands.
- **refuter's evidence:** Live GET /api/v1/vsb/vsb-6cbffb7c1d/review-gates. Live POST /api/v1/vsb/vsb-6cbffb7c1d/evolve returned HTTP 409. agentic_core/api/genesis.py:133,:537,:718,:870,:1058.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either run the journey stage by stage and pause at a gated stage, or rename the gate lifecycle as post-birth approval of recorded stage output, and align the three stage lists to §4's ten.

### R2.9 · §13 What the output IS (repo + Website + Web app + Phone app) — **DELIVERED**

- **severity (assessor):** info
- **claim:** §13: a version-controlled repository of the VSB integrating a Website, a Web app and a Phone app (a real PWA, never a fabricated native binary); a floor-served body ships as the founder's words plus a pending state.
- **observed:** The birth ship produced a real git repo: C:/tmp/w476b/vsb_repos/vsb-24267609d1 has 4 commits (repo, website, webapp, mobile), each message stating 'QMS not assessable · compliance fail'. Its contents: - identity, genome, business plan, organisation, cascades, QUALITY.md with seal - a 3-page site (index, about, solution) - a web app (app.js fetches data.json; tabs plus filter) - a PWA (manifest, service worker, icon), with GET /mobile/page/index returning 200 Every body field is 'content pending the owned model…' and the founder's name and problem appear verbatim. GET /deliverables/output-formats honestly lists 8 live formats with pptx/pdf/docx/mp4 in the catalogue; format=pdf returns 400. Shortfalls: - The body is empty on the floor, and the UI has no route for the founder to fill concept, design or commercialisation (name only). - GET /repo and the shipped README still call web/webapp/mobile '(scaffold) … NOT built/compiled/running apps' after the ship built them. This is an understatement, not an overclaim. - §4.1 accepts text, document attachments and dictation, but no image input.
- **evidence:** Executed: GET /api/v1/vsb/{id}/repo, GET /repo/ship, GET /mobile, /mobile/page/index; read the repo files on disk. agentic_core/api/vsb.py:115 (README template).
- **disclosed to the user:** Yes. body_pending is rendered (GenesisJourney.tsx:888-891; VSBCockpit.tsx:540-541).
- **refutation: SURVIVED** (reproduced by the refuter). Verified structurally. The repo exists on disk with BUSINESS_PLAN, EVIDENCE, IDENTITY, OPERATIONS, ORGANISATION, README, genome.json, manifest.json, compliance/QUALITY.md and resources/cascades.json. It also has web/ (index, about, solution), webapp/, and mobile/ as a PWA with manifest.webmanifest, sw.js and icon.svg. git log shows per-surface commits stamped 'QMS not assessable · compliance fail'. GET /mobile/page/index returns 200. GET /repo/ship lists all 4 surfaces with an honest compliance_overall 'fail' and qms_gate_passed null. The listed shortfalls are real and tier 3, with one caveat. The shipped README.md:6 also states 'Status: operational', which is the finding-2 defect carried into the output. It belongs under finding 2, not here.
- **refuter's evidence:** Filesystem listing of C:/tmp/w476b/vsb_repos/vsb-24267609d1. Live GET /api/v1/vsb/vsb-24267609d1/mobile/page/index returned 200. Live GET /api/v1/vsb/vsb-24267609d1/repo/ship. README.md:6 'Status: operational', :16 '(scaffold)'.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Regenerate README.md and the /repo record's integrated_surfaces after the ship so they describe the built surfaces, and add a founder body editor.

---

## R3 — §5 + §17.3 + §17.4 — the living organisation, the living business system layers, the three integration modes

**Assessor's region summary:** R3 (§5 living organisation, §17.3 business-system layers, §17.4 integration modes), assessed against the live backend at :8086 (HEAD 929508f0, floor-only). M1 is NOT met for this region: five Tier-1 truth defects remain on reached surfaces.  The governance spine works as the vision says, and I verified it by execution: - **Change Control and ratification:** the W464 Board-ratification rule holds end to end (HIGH review-approved change queued, implement refused, owner-direction flag required, refusal UEG-logged). - **Business Plan and roadmap:** the Chief-owned plan opening refuses to write floor text and names the pending fields. The living Roadmap follows objective progress. - **Mode 3 review gates:** they block the board pack with a 409 until approved. - **Board pack versioning:** versioning by content works. - **AI CEO chat and meeting:** the chat's SSE terminal frame names served_by and the grounding, and the page badges the floor amber. The C-Suite meeting records 'NO POSITION (floor-served)'.  The Tier-1 defects are on the /ceo Swarm tab and the org-cascade record: 1. **Coverage chart:** the Swarm tab's 'QMS coverage vs duration (measured)' chart plots the floor's by-construction 1.0 coverage. 2. **BMS chip:** it shows 'EFFICIENT · $2e-06/insight' built from simulated constants, and says so only on hover. 3. **Board pack narrative:** it says a 'strategic specification' is live data when that layer is empty. The layer chip implies all four §17.3 layers exist, while the Strategic and Action-Plan cadence is missing. 4. **§10 bar:** it certifies 'compliant' and 'safe' as met and measured when the screens returned 'review'. The root is in the §10 region, but the cascade and journey records carry it. 5. **Sharia screen:** it passes interest-bearing lending as halal when the word 'halal' is present, and the cascade chip shows emerald 'compliance: pass'. The root is in §11.  Tier-2 findings: - Change Control's health rule, which decides MEDIUM changes, uses a composite that is 60% default and simulation. Under current conditions it cannot reject. - Mode 2 (the digital-twin Chief) is MISSING. The Board page says so, but BusinessPlan.tsx:107 and the Swarm tier label still say 'your digital twin'. - Two API-only surfaces state untrue things: the board directive always returns status 'resolved', and the CTO endpoint reports ai_provider 'Ollama (local)'.  **Side effects on the live data store** (the backend's data dir, not the repo): CCA records cca-09d61d4277 (refused) and cca-7b39b4998d (approved MEDIUM); board directive dir-b885f927 and one board_directive row; business-plan objective obj-4743f759 (progress 40); VSB vsb-6ae1b88504 with an approved design gate and two board-pack versions; cascade runs aa93be0972 and 9529d5490f; one /delegate swarm run (this is what now plots on the Swarm chart); six C-Suite meeting-log entries.  Scratch files are in C:/tmp/w476_r3/. No repository file was written.

### R3.0 · §5 / §6 — Swarm tab of the Living Organisation hub (/ceo?tab=swarm) — **STUB** · tier 1

- **claim:** §5/§15.6: the organisation's delivery is QMS-verified, and simulation or lack of assessment is labelled (never fabricate).
- **observed:** The chart titled 'QMS coverage vs run duration (measured)' plots every swarm run's quality.delivery_coverage. On the floor that value is 1.0 by construction, and the same record says it is not assessable. So each floor run shows up as a top-of-chart 'measured' 100% coverage point.
- **evidence:** Executed POST /api/v1/swarm/delegate {task:'Plan a halal micro-lending pilot for weavers'}. It returned quality = {qms_gate_passed:null, qms_basis:'not assessable — floor-served: the deterministic floor emits the requested headings, so coverage cannot fail by construction…', delivery_coverage:1.0}. GET /api/v1/swarm/runs carries the same quality block (run duration 1029ms). SwarmIntelligence.tsx:165-169 filters only on `typeof delivery_coverage === 'number'` and plots it; :423 heads the chart 'QMS coverage vs run duration (measured)'. The chart reads /api/v1/swarm/runs (loadRuns, :88), and the page itself calls /delegate as its fallback when the cascade fails (:147). This backend now holds one such run (my probe), so the Swarm tab plots it today.
- **disclosed to the user:** No. Only the RunCard chip (qmsChip, 'QMS —') is neutral. The scatter shows no qualifier.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The swarm-run record says QMS is not assessable on the floor, yet the Swarm tab's chart headed 'measured' plots its delivery_coverage 1.0 with no qualifier.
- **refuter's evidence:** GET /api/v1/swarm/runs: the run with duration 1029ms has quality {qms_gate_passed:null, qms_basis:'not assessable — floor-served…', delivery_coverage:1.0}. SwarmIntelligence.tsx:165-169 filters only on typeof delivery_coverage==='number' and never checks qms_gate_passed or qms_basis. :423 heads the chart 'QMS coverage vs run duration (measured)'. :147 is the /delegate fallback.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Exclude runs with qms_gate_passed === null (or not_assessable) from paretoPoints. Better, have assure_delivery return delivery_coverage:null on floor-served runs so no renderer can plot it.

### R3.1 · §5 BTO / living management systems (BMS) on the org-cascade result — **PARTIAL** · tier 1

- **claim:** §5: the AI CEO integrates the living management systems (BMS·QMS·DCS·EMS). §15.6: label simulation.
- **observed:** The cascade result chip reads 'BMS EFFICIENT · $2e-06/insight'. Both the status and the unit cost come from the catalogue's simulated constants (a $/Wh rate and a $0.50-per-insight value, giving roi 266666.667). insights_count 10 counts the floor's own outputs. The EMS chip beside it carries '(sim)', but the BMS chip has no such label; the caveat is only in a hover title.
- **evidence:** Executed POST /api/v1/swarm/cascade {mission:'Design a halal micro-lending cooperative for artisan weavers', domain:'finance'} (run aa93be0972, served_by {native:22}). management_systems.bms = {cost_per_insight_usd:2e-06, roi:266666.667, roi_basis:'(insights x $0.5 simulated value) / energy cost', status:'EFFICIENT', insights_count:10}. The catalogue marks bms simulated: ['energy $/Wh rate constant','insight $0.50 value constant (inside ROI)']. SwarmIntelligence.tsx:287-289 renders `BMS {status} · ${cost_per_insight_usd}/insight` with the caveat only in title=; the EMS chip at :294 ends '(sim)'. The second cascade (9529d5490f) again gave status EFFICIENT.
- **disclosed to the user:** Only on hover. The visible label presents EFFICIENT and a dollar figure as findings.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on a fresh cascade. The visible BMS chip shows a status and a dollar-per-insight figure built from the catalogue's simulated constants. The caveat is only in a hover title, while the EMS chip beside it is visibly labelled '(sim)'.
- **refuter's evidence:** POST /api/v1/swarm/cascade {mission:'A halal lending cooperative charging 30 percent annual interest to weavers', domain:'finance'} → management_systems.bms {cost_per_insight_usd:3e-06, roi:180180.18, roi_basis:'(insights x $0.5 simulated value) / energy cost', status:'EFFICIENT', caveat:'…$/Wh is the catalogue\'s simulated constant'}. The catalogue marks bms simulated: ['energy $/Wh rate constant','insight $0.50 value constant (inside ROI)',…]. SwarmIntelligence.tsx:285-289 renders 'BMS {status} · ${cost}/insight' with the caveat only in title=; :292-294 ends the EMS chip with '(sim)'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Suffix the BMS chip '(sim)' like EMS, and make status 'not assessed' when every call was floor-served or the value basis is a constant.

### R3.2 · §17.3 Living Business System — the on-demand Board Pack — **PARTIAL** · tier 1

- **claim:** §17.3: four continuously maintained layers (Constitutional · Strategic (AI CEO, quarterly) · Action Plan (BTO, weekly) · Board Pack assembled fresh from live data).
- **observed:** The pack is honest about versioning, the review gate, the floor narrative and the concept source. But its narrative claims 'the operational snapshot, constitutional layer and strategic specification below are live data', while the strategic layer is {"ceo": {}} (empty). The action_plan layer is the static board roster. The constitutional Mission is the string 'Deliver: <problem>' and the Vision is the problem statement verbatim; values is one constant string for every VSB. The Genesis page shows a chip listing all four layer names as present.
- **evidence:** Executed POST /api/v1/genesis/establish {problem:'Artisan weavers in rural Punjab lack interest-free working capital', name:'Weaver Qard Coop', ship_output:false, review_gates:['design']} → vsb-6ae1b88504 (status 'operational', concept/design empty). Approved the design gate, then POST /api/v1/vsb/vsb-6ae1b88504/board-pack returned layers.strategic = {"ceo":{}}, constitutional = {mission:'Deliver: Artisan weavers…', vision:'Artisan weavers…', values:'Integrity · Compassion · Excellence · Halal/Sharia · Beneficence · Stewardship'}, and narrative 'narrative pending the owned model — … the operational snapshot, constitutional layer and strategic specification below are live data'. Code: vsb.py:1172 (narrative string) and :1184 (`"strategic": {"ceo": vsb.get("ceo_specification") or {}}`). GenesisJourney.tsx:1049 renders `Object.keys(pack.layers).join(' · ')`. The vision's own §17.3 status says the Strategic and Action-Plan cadence layers are MISSING, and the surface does not say so.
- **disclosed to the user:** Partly: concept_note says the pack is grounded in the problem statement, and it is shown amber. The empty strategic layer and the missing cadence are not disclosed; the narrative asserts the opposite.
- **refutation: SURVIVED** (reproduced by the refuter). Code confirms it. The floor narrative asserts that a 'strategic specification below' is live data, but the strategic layer is vsb.ceo_specification or an empty object. The UI shows only the layer names and the narrative, never the layer contents, so the user reads the claim and a chip listing 'strategic' with nothing behind it. I did not re-run establish; I relied on the code path plus the assessor's run vsb-6ae1b88504.
- **refuter's evidence:** agentic_core/api/vsb.py:1171-1173 (the floor narrative says '…the operational snapshot, constitutional layer and strategic specification below are live data'). :1184 `"strategic": {"ceo": vsb.get("ceo_specification") or {}}`. :1185 action_plan = the board roster. GenesisJourney.tsx:1049 renders Object.keys(pack.layers).join(' · '); :1068-1069 render only concept_note and the narrative. No render of the layer contents exists in :1056-1090.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Emit the strategic/action_plan layers as {pending: 'no cadence generator (P3.3)'} when empty, have the narrative name only the layers that carry data, and label mission/vision 'derived from the problem statement'.

### R3.3 · §5 org cascade → §10 quality bar (compliant / safe criteria) — **STUB** · tier 1

- **claim:** §5/§10: delivery is verified against the solution-quality bar; §15.6: never certify what was not assessed.
- **observed:** The §10 bar records 'compliant' and 'safe' as MET and MEASURED by the gate whenever no §11 screen says 'fail'. A run where sharia_halal, ethical and uk_legal all returned 'review' (no engine covers them) is therefore certified compliant and safe.
- **evidence:** Executed POST /api/v1/swarm/cascade {mission:'A halal lending cooperative charging 30 percent annual interest to weavers'} (run 9529d5490f). Response: quality.compliance.overall 'review' (sharia_halal review, uk_legal review, ethical review); quality.bar_measured.criteria.compliant = {met:true, measured:true, source:'gate'}; safe = {met:true, measured:true, basis:"§11 safety-bearing frameworks: {'ethical': 'review', 'ehs': 'pass', 'sharia_halal': 'review'}"}; summary '2 measured · 0 attested · 14 not measured'. Root: agentic_core/vbs/quality.py:141-146 (`measured("compliant", compliance.get("overall") != "fail", …)` and `all(verdicts[f] != "fail" …)`). Rendered as 'MEASURED by this gate: compliant · safe' at GenesisJourney.tsx:793-795 and Deliverables.tsx:216-228. The root sits in the §10 region, but every org-cascade and journey record carries it.
- **disclosed to the user:** No. The basis string names the review verdicts, but the criterion is shown as met and measured.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The §10 bar certifies 'compliant' and 'safe' as met and measured by the gate whenever no screen says 'fail', including a run where sharia_halal and uk_legal were 'review — no engine covers this area'.
- **refuter's evidence:** My cascade (mission 'A halal lending cooperative charging 30 percent annual interest to weavers') → quality.compliance {overall:'review', compliant:true, sharia_halal review (coverage none), uk_legal review (coverage none), ethical review}. bar_measured.criteria.compliant {met:true, measured:true, source:'gate'}; safe {met:true, measured:true, basis:"…{'ethical': 'review', 'ehs': 'pass', 'sharia_halal': 'review'}"}. agentic_core/vbs/quality.py:141-146 uses `!= "fail"`. The surfaces are GenesisJourney.tsx:794 and Deliverables.tsx:217 ('MEASURED by this gate: …').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** met = True only when every framework in scope is 'pass'; any 'review' gives met:null with basis 'review — not assessed'.

### R3.4 · §5 org cascade → §11 compliance chip (Sharia screen) — **STUB** · tier 1

- **claim:** §11/§15.6: compliance continuously live and ethics architecturally binding. For a faith-rooted platform, a halal verdict must not be issued on vocabulary alone.
- **observed:** The Sharia screen passes text that contains the word 'halal' and no listed haram term. Interest-bearing lending described as 'halal' gets sharia_halal PASS and an overall PASS. The org cascade's emerald 'compliance: pass' chip inherits this. The cascade's quality.compliance also drops the screen's coverage_gaps/basis fields, so the chip reads pass while uk_legal is 'review — no engine covers this area'.
- **evidence:** Executed POST /api/v1/compliance/check {subject:'A halal lending cooperative charging 30 percent annual interest to weavers'} → overall 'pass', sharia_halal 'pass' ('halal vocabulary present and no prohibited (haram) term — a keyword screen, not a certification'). The same text with 'riba' instead of 'interest' → fail. The org cascade aa93be0972 (mission containing 'halal') has quality.compliance = {overall:'pass', compliant:true, verdicts:[sharia pass, uk_legal review (coverage none), …]}, with no coverage_gaps key (compliance.py:275-279 returns one; the cascade copy omits it). SwarmIntelligence.tsx:266-270 renders complianceCls(overall) → emerald 'compliance: pass'; the per-framework statuses are only in the hover title. Root is in §11 compliance.py; it surfaces on this region's cascade.
- **disclosed to the user:** Only on hover (per-framework statuses). The 'keyword screen, not a certification' reason is not shown on the Swarm surface.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. Interest-bearing lending that is described as 'halal' gets a Sharia PASS and an overall PASS. The cascade's copy of the compliance result drops coverage_gaps and basis, and the Swarm chip shows only the overall colour; the per-framework statuses sit in a hover title. Caveat: in my cascade the screen read the output, not the mission, and returned 'review', so the emerald chip depends on halal vocabulary appearing in the screened text. The assessor's run aa93be0972 did show the pass.
- **refuter's evidence:** POST /api/v1/compliance/check {subject:'A halal lending cooperative charging 30 percent annual interest to weavers'} → overall 'pass', compliant:true, sharia_halal 'pass' ('halal vocabulary present and no prohibited (haram) term — a keyword screen, not a certification'), coverage_gaps ['uk_legal','constitutional']. The cascade's quality.compliance keys are overall, compliant and verdicts only (no coverage_gaps). SwarmIntelligence.tsx:266-270 shows 'compliance: {overall}' with the verdicts only in title=.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add interest/usury/APR/percent-interest patterns to the haram screen, never pass sharia_halal on self-declared vocabulary alone (make it 'review'), and carry coverage_gaps into the cascade chip.

### R3.5 · §5 Change Control (arms-length) — the review's organism-health threshold rule — **PARTIAL** · tier 1 *(assessed tier 2)*

- **claim:** §5 (W464 rulings): Change Control governs all change at arm's length; a review decides MEDIUM changes, and a review's approval of a HIGH change waits for Board ratification.
- **observed:** With no model marker (always the case on the floor), the review decides by `composite_health >= 0.5`. That composite is 40% a defaulted 1.0 (no self-healing circuits tracked) and 20% simulated ATP. With immune health at 0.5 (threat ELEVATED, 5 errors) it still reads 0.77–0.95, so under current conditions the rule cannot reject anything: 0.4·imm + 0.4 + 0.2·atp ≥ 0.5 for any imm ≥ 0 while atp ≥ 0.5. MEDIUM changes are approved and become implementable. The record states the number without saying most of it is unmeasured, although /organism/status publishes composite_health_measured_only (0.5) and the basis.
- **evidence:** Executed POST /api/v1/cca/submit {change_type:'capability_add'} → cca-7b39b4998d MEDIUM. POST /api/v1/cca/cca-7b39b4998d/review {} → {decision:'approved', decision_source:'health_threshold_rule', awaiting_board_ratification:false, review_result:'DECIDED BY RULE, NOT BY THE MODEL … composite_health 0.78 >= 0.5 → approved'}. The HIGH probe cca-09d61d4277 was approved on 'composite_health 0.95'. GET /api/v1/organism/status: composite_health 0.766, composite_health_measured_only 0.5, self_healing_health 'defaulted to 1.0, not a measurement', metabolic 'simulated'. Code: change_control.py:935-941 uses ctx['composite_health']; biobus.py:197-199 folds the defaulted and simulated terms in. ChangeControlAgency.tsx:233 shows 'decided by: organism-health threshold rule (not the model)' in amber.
- **disclosed to the user:** Partly. The UI says a rule (not the model) decided, but not that the 'health' it used is 60% default and simulation, nor that the rule cannot reject.
- **refutation: REFUTED — corrected to PARTIAL.** The finding is reproduced but the tier is too kind. The live terms show that the measured-only health (0.4) would REJECT under the same 0.5 rule, while the composite (0.76, of which 0.6 is a defaulted 1.0 plus simulated ATP) APPROVES. The recorded decision is therefore flipped by unmeasured terms, and the record presents it as the organism's health. That is a certification of what was not assessed, on a reached surface (the Change Control page), so tier 1.
- **refuter's evidence:** GET /api/v1/organism/status → composite_health 0.76, composite_health_measured_only 0.4. Terms: immune 0.4 (measured); self_healing 1.0 ('defaulted to 1.0, not a measurement'); metabolic 1.0 ('ATPSimulator … simulated'). change_control.py:935-941: `_h = float(ctx["composite_health"]); rule_verdict = "approved" if _h >= 0.5 else "rejected"`. With self_healing fixed at 1.0 and ATP ≥ 0.5, 0.4·imm + 0.4 + 0.2·atp ≥ 0.5 for any immune value, so the rule cannot reject.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Decide on composite_health_measured_only, or hold for an explicit decision when the measured share is below a threshold. Print the measured share in review_result.

### R3.6 · §5 Chief = founder's digital twin; §17.4 Mode 2 — **PARTIAL** *(assessed MISSING)* · tier 1 *(assessed tier 2)*

- **claim:** §5/§15.3/§17.4 Mode 2: the Chief is the founder's digital twin, autonomous in the founder's presence and absence.
- **observed:** No twin model exists. The Board page says so ('no digital-twin model is trained yet (Mode 2 is planned, P3.4)'). Two other reached surfaces still tell the user the Chief is their digital twin. The Chief acts only on a click (chief/instruct); nothing invokes it autonomously.
- **evidence:** GET /api/v1/twin/models → {models:[], total:0}. GET /api/v1/board/status chief.title 'Chief of the Board (the Owner's charter and instructions — no twin model is trained)'. BusinessPlan.tsx:107 says 'The living plan owned by your Chief (your digital twin) and the Board'. SwarmIntelligence.tsx:299 labels the cascade tier 'Chief of the Board (founder digital twin)'. BoardOfDirectors.tsx:120-124 is honest. grep shows chief_instruct called only from board.py and business_plan.py; heartbeat.py has no Chief call. POST /api/v1/board/chief/instruct (floor) returned an 'Owner Intent' built from the system preamble's terms (plus a stray memory term 'milk chocolate'), not from the Owner's instruction; the page marks it amber.
- **disclosed to the user:** On the Board page only. The Business Plan page and the Swarm tier label contradict it.
- **refutation: REFUTED — corrected to PARTIAL.** The substance is reproduced: no twin model exists, the Board page is honest, and two other reached surfaces say the Chief is 'your digital twin'. Telling the Owner on the Business Plan page that their Chief is their digital twin, when GET /twin/models is empty, is a reached surface stating something untrue. That meets the tier-1 definition, not tier 2. The verdict is PARTIAL rather than MISSING because the Chief role exists and acts, as the charter plus instruct.
- **refuter's evidence:** GET /api/v1/twin/models → {models:[], total:0}. GET /api/v1/board/status chief.title '…no twin model is trained'. apps/workstation-superapp/src/pages/enterprise/BusinessPlan.tsx:107 'The living plan owned by your Chief (your digital twin) and the Board'. SwarmIntelligence.tsx:299 'Chief of the Board (founder digital twin)'. The Chief is called only from board.py and business_plan.py; there is no autonomous invocation.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Replace 'your digital twin' in BusinessPlan.tsx:107 and SwarmIntelligence.tsx:299 with the Board's wording (standing charter + last instructions; twin model planned).

### R3.7 · §5 Board directive + C-Suite infrastructure (API-only surfaces in the chain) — **API_ONLY** · tier 3 *(assessed tier 2)*

- **claim:** §5: the Board delivers via action planning. §15.6: never fabricate.
- **observed:** POST /api/v1/board/directive always records status 'resolved', even when the floor served every director and took no position (the C-Suite meeting correctly records 'NO POSITION (floor-served)'). These rows count as 'directives' in the AI CEO chat grounding and in the Chief's live intelligence. Separately, GET /api/csuite/cto/infrastructure reports ai_provider 'Ollama (local)' while AI_DISABLE_LOCAL=1 and only the native floor serves.
- **evidence:** Executed POST /api/v1/board/directive {topic:'Weaver lending pilot go/no-go'} → {status:'resolved', ai_provenance:{served_by:{native:3}}, resolution:'_[Workstation native structured engine …]_ … Structured go-to-market frame …'}. board.py:553 hard-codes "status": "resolved". No frontend file references /board/directive. GET /api/csuite/cto/infrastructure → ai_provider 'Ollama (local)', from csuite.py:127 (`'Anthropic Claude' if ANTHROPIC_API_KEY else 'Ollama (local)'`); no frontend consumer.
- **disclosed to the user:** No, but neither surface is reached from the UI.
- **refutation: REFUTED — corrected to API_ONLY.** The facts reproduce, but no UI reaches either surface, so by the rubric this is tier 3 (not reached), not tier 2. The indirect leak is real but small: floor-'resolved' directives raise the 'grounded in N directives' count in the AI CEO chat, which I saw rise to 2.
- **refuter's evidence:** agentic_core/api/board.py:553 hard-codes "status": "resolved". grep of apps/workstation-superapp/src finds no /board/directive POST and no cto/infrastructure consumer. csuite.py:127 returns 'Ollama (local)' whenever no ANTHROPIC_API_KEY is set. The CEO chat final frame shows grounding.directives 2.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Set status 'recorded — no position (floor-served)' when served_by is all native. Report ai_provider from the gateway's actual last served_by (or 'native floor').

### R3.8 · §5 AI CEO chat + C-Suite meeting (Mode 1 interaction surface) — **DELIVERED**

- **claim:** §5: the AI CEO receives the Board's direction and answers from the chain; the floor is labelled.
- **observed:** The SSE terminal frame names served_by and the grounding. The page shows an amber 'structured floor — not model analysis' badge for every floor answer, plus 'grounded in N directives · M objectives'. A meeting request records each officer as 'NO POSITION (floor-served)', and the minutes say so. /ceo/vitals reports psutil figures and says latency is not measured.
- **evidence:** POST /api/v138/ceo/chat {message:'What is our strategy for next quarter?'} → final frame {done:true, served_by:'native', is_external:false, guardrail_passed:true, grounding:{scope:'workstation', directives:0, objectives:0, plan_score:0.3, debate_entries:0}}. After the probes: directives 1, objectives 1, debate_entries 6. CEOChat.tsx:92-98 stores served_by; lib/api.ts:69 maps 'native' to the amber floor label; CEOChat.tsx:285-289 renders it. GET /api/v138/ceo/meeting/log shows stance 'NO POSITION (floor-served)'. GET /api/v138/ceo/vitals → latency_ms null with a note.
- **disclosed to the user:** Yes.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The chat's final frame carries served_by 'native', which maps to the amber 'structured floor — not model analysis' label. The meeting log records 'NO POSITION (floor-served)'. The vitals report latency as null with a note.
- **refuter's evidence:** POST /api/v138/ceo/chat {message:'What is our strategy for next quarter?'} → final frame {done:true, served_by:'native', is_external:false, guardrail_passed:true, grounding:{scope:'workstation', directives:2, objectives:1, plan_score:0.3, debate_entries:6}}. lib/api.ts:69 maps native to the floor label. GET /api/v138/ceo/meeting/log → stance 'NO POSITION (floor-served)'. GET /api/v138/ceo/vitals → latency_ms null, note 'latency is not measured on this host…'.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None needed. Optionally, the floor output reprints headings like '## Board directives' over generic bullets, which the badge already covers.

### R3.9 · §5 Change Control ratification · Chief-owned Business Plan · living Roadmap · Mode 3 gates · Board-pack versioning — **DELIVERED**

- **claim:** §5 W464 rulings (a HIGH change approved by review waits for Board ratification on the Owner's direction); the Chief-owned plan opening is AI-generable then owner-edited; living Roadmap; §17.4 Mode 3 review gates; the Board pack is versioned by content.
- **observed:** All verified end-to-end. A HIGH review-approved change enters the ratification queue and cannot be implemented. A ratification without on_owner_direction is refused. Refusing on the Owner's direction rejects and UEG-logs it. Business-plan generation on the floor writes nothing and names the pending fields. The roadmap follows objective progress. The Mode 3 gate blocks the board pack until approved. Re-POSTing an unchanged pack keeps v1 'unchanged since'.
- **evidence:** cca-09d61d4277 (code_change → HIGH): review → awaiting_board_ratification true; GET /api/v1/board/ratifications listed it; POST …/cca/cca-09d61d4277/implement → 'waits for the Board to ratify it on the Owner's direction'; POST /api/v1/board/ratifications/cca-09d61d4277 {decision:'ratify'} → 403 'resend with on_owner_direction: true'; {decision:'refuse', on_owner_direction:true} → {status:'rejected', ueg_logged:true}. POST /api/v1/business-plan/generate → written [], body_pending [executive_summary, concept, vision, mission, strategy], reason 'structured floor — not model analysis; nothing was written'. Objective review progress_pct 40 → GET /business-plan/roadmap overall_progress_pct 40, next_milestone set; objective orchestrate → status_advanced false with basis 'not assessable'. POST /api/v1/vsb/vsb-6ae1b88504/board-pack while the design gate was pending → 409 'review gate blocks progress'; after approval, version 1, and a re-POST → version 1, unchanged:true. Minor gaps: BusinessPlan.tsx:122 reads plan.provenance.served_by, but the API nests it at provenance.generation.served_by, so the provenance badge never renders (the pending chip does). GenesisJourney.tsx:387-389 shows only 'Action failed (HTTP 409)' because the gate refusal's detail is an object.
- **disclosed to the user:** Yes.
- **refutation: SURVIVED** (reproduced by the refuter). The parts I checked hold, including the minor provenance-badge gap. I did not re-run the ratification and board-pack gate sequence; I accepted it on the assessor's evidence plus code consistency.
- **refuter's evidence:** GET /api/v1/business-plan → provenance keys ['generation','preamble','written','body_pending']. served_by is nested at provenance.generation.served_by and is the string 'native'. BusinessPlan.tsx:122 reads plan.provenance?.served_by, so the badge never renders, while the top-level body_pending chip at :125 does render. generation.reason = 'structured floor — not model analysis; nothing was written to the plan…', and executive_summary is ''.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Read provenance.generation.served_by in BusinessPlan.tsx. Render detail.error when detail is an object in GenesisJourney.generateBoardPack.

---

## R4 — §6 + §7 + §17.2 — the native AI mandate, the reconfigurable resource fabric, the seven biomimetic layers

**Assessor's region summary:** R4 (§6 native AI, §7 resource fabric, §17.2 biomimetic layers), measured against the live backend at :8086 (HEAD 929508f0). I called about 40 routes and read the handlers and components; I did not drive a browser, so what the UI shows is inferred from the TSX source. Scratch files are in C:/tmp/w476_r4/.  DELIVERED, verified by calling the routes. The §6 in-house-first posture is real. gateway.query_meta goes to the native orchestrator first. External providers are gated by AI_ALLOW_EXTERNAL in both gateway.stream_meta and avatars/api.py. The old external-first gateway._call has no callers. GET /api/v1/native-ai/status is honest: floor_active=true, is_real_model=false, a floor_note saying it is "NOT an LLM", and floor_active_basis. The basis changed from a prediction to a measurement (mode_measured, measured_recent_server=native) after real completions. NativeAI.tsx renders the status, the note and the basis. /native-ai/complete, /swarm and /tree return served_by=native and is_external=false. On the floor, the tree's governance, decision and consensus steps all abstain and say "not assessable" instead of certifying anything. The provenanceBadge and provenanceMapBadge helpers turn a floor run amber with the label "structured floor — not model analysis". The fabric works: /api/v1/resources lists 42 resources, and compose, simulate, run, PUT and DELETE all work. Most composed resources call their real engine (the digital_twin, immune, genome and petri_dish adapters were confirmed in the run_record). /bto/configure says it is a design blueprint only. /organism/self-healing/status carries health_basis. /nervous/status discloses reflex_arcs_registered=0. /organism/config shows key_wiring. HeartbeatMonitor labels its percentage "API coverage (not delivery)". Most of CommandCenter.tsx has been made honest ("not built", "NOT MEASURED", "not evaluated").  WHAT REMAINS. Four tier-1 truth defects remain on reached surfaces, so M1 is still not met in this region: 1. The Petri Dish certifies "viable" for every floor-served specimen, including a riba-financed one, as a green chip on Resource Fabric with no floor badge. 2. The organism's "Composite Health 93% · FULL POWER · Full cognitive power available" is shown unqualified on Home and on /organism. 60% of that weight is a default or a simulator, and the same page says self-healing has "no data yet" and cognition is "reduced". 3. The Cognition tab shows "Overall realisation 97% … (evidence-based)" and can say "Fully aligned — no open vision gaps". That number is route-mount coverage, and another page labels the same figure "not delivery". 4. The Command Center dock, on every page, still asserts "CROSS-PLANETARY SYNC ACTIVE" and "optimization in progress" for things that do not exist. This is lower severity.  Tier 2: the Forge runs prompt personas instead of the fabric's real engines, and drops provenance. §7's "every composed resource runs its REAL engine" and §17.2's "Musculoskeletal … REAL" are therefore overclaims for the Forge. The workflow tree claims to "autonomously decompose" goals while its planner is a keyword template, and the UI drops the planner field. Synthesis Studio drops the served_by field from the stream's final frame. The fabric's "compliance: pass" chip is green while the halal and UK-legal rows could not be assessed.  Tier 3, all disclosed: the biomimetic layers are real as buses and sensors but not as effectors. There are zero reflex arcs. The immune-reconfigure route has no caller. Self-healing breakers only wrap model:* names, so in native-first operation they never carry a call. Genome encoding returned a flat vector (every trait 0.5, fitness 0.5) for my objective.

### R4.0 · §7 (Petri dish, a musculoskeletal facility) + §15.6 (never fabricate) — **STUB** · tier 1

- **severity (assessor):** high
- **claim:** §7: every composed resource runs its REAL engine; a Petri dish is 'small-scale controlled experimentation — test viability/hypothesis'. §15.6: honesty over polish, never fabricate.
- **observed:** The Petri dish's viability verdict is a substring test: viable = 'not-viable' not in tail and 'not viable' not in tail (agentic_core/api/products.py:546). The deterministic floor never writes those words, so every floor-served culture comes back viable=true. I ran POST /api/v1/petri/culture with {"specimen":"sell ice to penguins at a premium, financed with riba loans"} and got viable=True, ai_provenance.served_by={native:1} and qms_gate_passed=None. The culture text also contained 'varroa treatment', which is native-memory recall from my earlier, unrelated probe leaking into a new request. In the fabric, the adapter (agentic_core/api/resource_fabric.py:518) returns {viable, passages, output} and drops served_by. ResourceFabric.tsx:669-671 then shows a green 'viable · 1p' chip, and the floor badge at :684 renders only when rr.served_by is present, so this row gets no floor disclosure. The digital_twin row in the same run did carry served_by=native and got the amber badge.
- **evidence:** POST /api/v1/resources/compose {resource_ids:[petri_dish,digital_twin,immune,genome]} created comp-762a6928. POST /api/v1/resources/compositions/comp-762a6928/run returned real_resource_runs[0] = {resource:'petri_dish', ran:'/api/v1/petri/culture', viable:true, passages:1} with no served_by. Code: products.py:531-550, resource_fabric.py:512-519, ResourceFabric.tsx:669-686.
- **disclosed to the user:** No. The green 'viable' chip has no floor badge. The only hint is the '_[Workstation native structured engine…]_' marker inside the output text, which says 'owned', not 'not an assessment'.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The Petri dish's viability is a substring test. The floor never writes 'not viable', so every floor culture comes back viable=true. The fabric adapter drops served_by, so the row shows a green 'viable' chip with no floor badge, while the digital_twin row next to it does get the badge. /petri/culture has no other UI consumer: grep of apps/.../src for 'petri' finds only ForgePipeline, which does not call it. So the fabric chip is the only way a user sees this verdict.
- **refuter's evidence:** POST /api/v1/petri/culture {specimen:'sell ice to penguins at a premium, financed with riba loans'} gave viable=True, ai_provenance.served_by={native:1} and quality_assurance.not_assessable=true. The '## Viability' section is a bigram template that includes 'varroa treatment', which is native-memory bleed from an unrelated earlier request. The same response says the haram-term screen 'matched nothing' despite 'riba'. POST /resources/compose {petri_dish,digital_twin,genome} created comp-b89b3634, and /run returned real_resource_runs[0]={resource:'petri_dish', viable:True, passages:1} with no served_by, while digital_twin carried served_by:'native'. Code: products.py:545-546, resource_fabric.py:513-519, ResourceFabric.tsx:669-686 (the badge needs rr.served_by).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Return viable=None ('not assessable — floor-served') whenever served_by is native, and put served_by/is_external on the fabric's petri_dish row so provenanceBadge renders. Consider making viability require an explicit 'VIABLE' line rather than the absence of 'not viable'.

### R4.1 · §8/§17.2 organism vitals (Immune, Self-healing, Metabolic) as shown on Home and /organism — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** §17.2: a living body with immune, self-healing and metabolic layers whose state is real. §15.6: label simulation.
- **observed:** GET /api/v1/organism/status returns composite_health=0.929, mode=FULL_POWER and health_summary 'Organism at peak — ACTIVE_FOCUS cycle, health 93%. Full cognitive power available.' The terms are: immune 0.4×1.0 (measured), self_healing 0.4×1.0 'defaulted to 1.0, not a measurement', and metabolic 0.2×0.643 'ATPSimulator … simulated'. So 60% of the weight is not measured, and the measured-only figure is 1.0. The home page (DashboardNew.tsx:107, 257, 261) shows '<mode> · 93% health' and a 'Composite Health 93%' bar with no basis. The default /organism tab (OrganismDashboard.tsx:262-266) shows 93% big with the health_summary and a green FULL THROUGHPUT chip. On the same page, the Self-Healing card shows 'no data yet' (:336-337), which contradicts the 1.0 folded into the headline. The cognition card shows posture 'reduced' (GET /native-ai/homeostasis returned posture 'reduced', max_parallel 3), which contradicts 'Full cognitive power available'. The Metabolic card shows 'ATP ratio 64%' (:352-355) with no 'simulated' label. The chips also show '<preferred_provider> provider', a key that /organism/config key_wiring marks 'stored-only — nothing reads this key back'. OrganismAnatomy.tsx:230-236 does this correctly: it shows the measured-only figure first and then health_basis.
- **evidence:** GET /api/v1/organism/status and GET /api/v1/organism/health-summary return composite_health_terms and health_basis, but neither Home nor the /organism overview reads those fields. Code: agentic_core/organism/biobus.py:194-236 and 297-303; OrganismDashboard.tsx:262-285 and 334-356; DashboardNew.tsx:106-108 and 257-261.
- **disclosed to the user:** Only on the Anatomy tab (/organism?tab=anatomy). Home and the /organism default tab present 93% and FULL POWER as measured health.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, and the live state is now worse than the assessor saw. composite_health is still 60% weighted on unmeasured terms: self_healing defaults to 1.0 and metabolic is simulated. Home and the default /organism tab show that composite as measured health, with no basis shown. Right now the only measured term, immune, is 0.0 (CRITICAL), yet the headline says 60% and 'Organism nominal … Standard operations active.' The measured-only figure is served as composite_health_measured_only, but neither page reads it.
- **refuter's evidence:** GET /api/v1/organism/status returned composite_health=0.6, mode NOMINAL, health_summary 'Organism nominal — ACTIVE_FOCUS cycle, health 60%. Standard operations active.', and terms immune {0.0, measured:true}, self_healing {1.0, measured:false, 'defaulted to 1.0, not a measurement'}, metabolic {1.0, measured:false, 'simulated'}. GET /native-ai/homeostasis returned posture 'protected', max_parallel 1, immune_threat CRITICAL. grep for measured_only/health_terms/health_basis/simulated in OrganismDashboard.tsx and DashboardNew.tsx finds no hits. Code: DashboardNew.tsx:107,257,261; OrganismDashboard.tsx:261-266 (the big % and health_summary) and :279-281 (preferred_provider chip); OrganismHub.tsx:14 (overview is the default tab).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** On Home and the /organism overview, render composite_health_measured_only as the headline, with health_basis as the caption, and move the blended figure into a 'with default and simulated terms' tooltip. Label the Metabolic card 'simulated'. Have _health_summary say how much of the score is measured, and stop saying 'Full cognitive power' when native homeostasis reports 'reduced'.

### R4.2 · §17.2 evolution / self-improvement (the Cognition tab's 'organism self-aligns') — **PARTIAL** *(assessed STUB)* · tier 1

- **severity (assessor):** high
- **claim:** §1 and §17.2: the organism forever learns, improves and self-corrects. The Cognition tab claims the organism 'measures vision realisation, routes each gap to the tier that owns it'.
- **observed:** POST /api/v1/cognition/align {execute:false} returned overall_realisation=0.97 with one gap. The checks behind that number are route-mount and non-empty-store lambdas (agentic_core/api/transformation.py:141-157). _realise() itself names this 'API surface coverage … not delivery', but align() and /api/v240/evolution/metrics drop that 'measure' field. The metrics route adds status 'realised' 1.0 for pillars such as "Chief = Owner's digital twin" and "Reconfigurable, combinable resource fabric". CognitionIntegration.tsx:173 renders 'Overall realisation 97% · N gaps routed (evidence-based)'. :170 would render 'Fully aligned — no open vision gaps.' once one evolution cycle runs. :125 shows 'Code / Live-State realisation 97%'. HeartbeatMonitor.tsx:99 labels the same number 'API coverage (not delivery)', so two tabs of one hub disagree about what 97% means. align(execute=true) also routes the organism's self-improvement from this proxy, so gaps that coverage marks 'realised' are never targeted.
- **evidence:** POST /api/v1/cognition/align returned {overall_realisation:0.97, gaps_routed:[{gap:'One self-running…', realisation:0.667}], note:'Evidence-based routing of vision gaps…'}. GET /api/v1/cognition/knowledge returned code_live_state.overall_realisation 0.97. GET /api/v240/evolution/metrics returned vision_realisation 0.97 with 10 of 11 pillars 'realised'. Code: cognition.py:115-135, integration_surface.py:253-271, CognitionIntegration.tsx:59-60, 125, 168-185.
- **disclosed to the user:** No, not on the Cognition tab. It is disclosed only on the Heartbeat tab.
- **refutation: STOOD, verdict CORRECTED STUB → PARTIAL.** The tier-1 defect reproduces. The Cognition tab labels an API-surface-coverage proxy as 'Overall realisation 97% … (evidence-based)' and 'Code / Live-State realisation 97%', under header copy saying the organism 'measures vision realisation'. _realise() itself says this is coverage, not delivery, and HeartbeatMonitor labels it that way. Minor verdict correction: the alignment routing does run and routes the one partial pillar, so the surface mislabels a real measure rather than being a pure stub. PARTIAL at tier 1 fits better than STUB.
- **refuter's evidence:** POST /api/v1/cognition/align {execute:false} returned overall_realisation 0.97 and one gap, with no 'measure' field. GET /api/v240/evolution/metrics returned vision_realisation 0.97, with pillars such as "Chief = Owner's digital twin" and 'Reconfigurable, combinable resource fabric' at status 'realised' 1.0. Code: transformation.py:141-157 (route/store lambdas plus the 'measure' disclaimer); cognition.py:90 passes only overall_realisation; CognitionIntegration.tsx:59-60, 125, 170, 173; HeartbeatMonitor.tsx:99 'API coverage (not delivery)'. The tab is reached at OrganismHub.tsx:17.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Pass _realise()['measure'] through align, knowledge and v240 metrics. Relabel the Cognition tab figure 'API surface coverage (not delivery)' and replace 'Fully aligned — no open vision gaps' with 'no coverage gaps — delivery is measured at /api/v1/plan/state'. Rename the v240 fields (vision_realisation to api_coverage, 'realised' to 'surface present').

### R4.3 · §7 Forge (Build/Forge resources) + §17.2 Musculoskeletal — **DOC_OVERCLAIM** · tier 2

- **severity (assessor):** medium
- **claim:** §7 (W446 rule): modules are 'federated, never duplicated, never a prompt approximation of a real engine … every composed resource runs its REAL engine'. The Laboratory 'is realised as the Forge's analysis/synthesis bench (forge.py stage laboratory)'. §17.2: 'Musculoskeletal → the §7 facilities + digital twin — REAL'.
- **observed:** Every stage of POST /api/v1/forge/run is a persona prompt sent through gateway.query_meta ('You are the Digital Petri Dish…', 'Acting as: Digital Twin Simulator.'). It never calls /petri/culture, /reactor/*, /twin/simulate or the incubator engines that the Resource Fabric's adapters do call. Its 'Simulator — digital-twin scenario modelling' stage runs no simulation. The response's ai_provenance ({served_by:{native:5}}) is dropped by ForgePipeline.tsx, whose RunResult type has no provenance field, so there is no floor badge. 'governance allowed' (:80) is the gate's verdict on a fixed attestation string ('synthesised under constitutional supervision'), not on the output. The UI sends stages as {type} only, so each resource's declared params (hypothesis, variations, scenario) can never be set. Floor bigram lists are presented under 'Integrated Deliverable'.
- **evidence:** POST /api/v1/forge/run with stages [petri_dish, incubator, simulator] returned pipeline and stage_outputs whose outputs begin with the persona prompts; ai_provenance was {served_by:{native:5}} and governance was 'allowed'. Code: agentic_core/api/forge.py:135-205; ForgePipeline.tsx:8, 27, 80-95. Reached from the Resource Fabric studio grid (ResourceFabric.tsx:19) at /forge-pipeline.
- **disclosed to the user:** Partly. The floor marker line sits inside each output, but there is no provenance chip, and nothing says the stages are prompts rather than the fabric's engines.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. Every Forge stage is a persona prompt through gateway.query_meta, and none calls the real petri, twin or reactor engines. The UI drops ai_provenance, and 'governance allowed' is the gate's verdict on a fixed attestation string. Tier 2, not tier 1, because the floor output declares itself in text ('Native structured content for …, grounded in: <prompt>'), so a reader can tell it is templated. The shortfall that goes unsaid is that the stages are not the fabric's engines.
- **refuter's evidence:** POST /api/v1/forge/run {objective:'halal micro-lending cooperative', stages:[{type:'simulator'}]} returned governance 'allowed' and ai_provenance {native:3}. The stage output begins '_Acting as: Digital Twin Simulator._ ## Scenario Results Native structured content…', with bullets 'varroa treatment' and 'small beekeepers' leaking from unrelated earlier requests. No simulation ran. Code: forge.py:142-189 (_q prompts only; the attestation is a fixed string); ForgePipeline.tsx:8 (RunResult has no provenance), :27 (stages sent as {type} only), :80 (governance), and :40-42 (header claims the 'resources process').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Route Forge stages through resource_fabric's per-resource adapters, so the Forge federates rather than duplicates. Until then, render provenanceMapBadge(ai_provenance) and a 'prompt-persona stages — not the fabric's engines' note, and relabel governance as 'gate on intent, not output'. Correct §7 and §17.2 to exclude the Forge from 'REAL'.

### R4.4 · §6 autonomous workflow-tree (bespoke per solution) — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §6: 'Bespoke-per-solution swarms … synthesised, modelled and simulated bespoke to each solution's needs'; the 'autonomous workflow-TREE' runs goals through owned capabilities.
- **observed:** POST /api/v1/native-ai/tree returned planner='deterministic_template' for two unrelated goals. Both trees had the same frame, research, design, synthesise, review skeleton, with a 'risk' branch added only because the word 'halal' matched a keyword list (orchestrator.py:460-486). The backend is honest about this: it returns planner and only uses 'swarm_planned' when a real model produced a valid DAG. NativeAI.tsx never reads 'planner', yet its tree panel (:786-790) tells the user 'the native swarm autonomously decomposes it into a dependency tree … biomimetically mediated (… the learning loop)'. Governance, decision and consensus correctly abstain on the floor, and the per-node floor badges render.
- **evidence:** POST /api/v1/native-ai/tree {goal:'Design a halal micro-lending cooperative…'} gave 6 template nodes, planner 'deterministic_template'. {goal:'Write a peer-reviewed paper on protein folding kinetics'} gave 5 template nodes, same planner. grep of NativeAI.tsx for 'planner' finds no hits.
- **disclosed to the user:** No. The response field is dropped, and the panel copy claims autonomous decomposition.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The tree comes back with planner='deterministic_template' and the fixed frame/research/design/synthesise/review skeleton. NativeAI.tsx never reads 'planner', and the panel copy says the native swarm 'autonomously decomposes' the goal. Per-node floor badges are the only signal. The tier-2 invisible shortfall is correct.
- **refuter's evidence:** POST /api/v1/native-ai/tree {goal:'Write a peer-reviewed paper on protein folding kinetics'} returned planner 'deterministic_template' and nodes [frame, research, design, synthesise, review]. grep of NativeAI.tsx for 'planner' or 'deterministic_template' finds no hits. :786-790 is the 'autonomously decomposes … the learning loop' copy.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Render run.planner in TreeView as a chip ('keyword template — the floor cannot plan' vs 'model-planned'), and make the panel copy conditional on it.

### R4.5 · Command Center (the shell's Channels dock on every page) — **STUB** · tier 1

- **severity (assessor):** low
- **claim:** §9/§15.6: a multimodal UX, with honesty over polish and simulation labelled.
- **observed:** Most channels are now honest (packages/ui/src/CommandCenter.tsx), but untrue statements remain on a surface reached from every page (Shell, then FourthColumn, then <CommandCenter tiled/>). The Spatio-Temporal channel (components/organism/SpatioTemporal.tsx) renders the literals 'Sovereign Consensus Status: CROSS-PLANETARY SYNC ACTIVE' and 'Mars Relative: T + 842s' under an 'illustrative' header. After 'Apply REST Mode', the Predictive channel says 'Rest Mode applied. Cognitive durability optimization in progress.' (CommandCenter.tsx:425), but the handler only sets a UI mode flag. The heading still reads 'RL-Powered Suggestions' although no RL exists. Every channel has a 'Query <channel> channel…' input that echoes 'You asked' and nothing answers or says nothing will (:99-104, 160-181). The popup shows a pulsing green 'Live' dot (:243-244), and the Dashboard tile says 'Live Metrics' but opens OrganismVitals, which states NOT MEASURED. NeuralLink's 'SYNAPSE ACTIVE' and 'Subjective Qualia Feedback: Intuiting strategic surplus…' sit beside a 'decorative animation' disclaimer.
- **evidence:** Read packages/ui/src/CommandCenter.tsx:51-63, 99-104, 243-244, 415-436; apps/workstation-superapp/src/components/organism/SpatioTemporal.tsx:45-51; NeuralLink.tsx; FourthColumn.tsx:153; Shell.tsx:265.
- **disclosed to the user:** Partly: there are 'illustrative' and 'decorative' headers, but the status literals and the 'optimization in progress' sentence are stated flatly.
- **refutation: SURVIVED** (reproduced by the refuter). Mostly reproduced, with one correction. The pulsing green 'Live' dot (CommandCenter.tsx:243-244) is in the non-tiled popup branch. The only mount is <CommandCenter tiled/> (FourthColumn.tsx:153), so that dot is never reached; drop it from the evidence. What remains and is reached from every page: after Apply REST Mode, the Predictive channel says 'Rest Mode applied. Cognitive durability optimization in progress.' The handler (:353-356) only sets a UI store mode that greys the sidebar and header, so that statement is flatly untrue. The heading 'RL-Powered Suggestions' has no RL behind it. The 'Dashboard · Live Metrics' tile description is shown under each tile. The query box echoes 'You asked' and nothing answers or says it won't. 'CROSS-PLANETARY SYNC ACTIVE' and 'Mars Relative: T + 842s' sit under a header that does disclose 'illustrative: no data is mapped', so those two are partly disclosed. Tier 1 stands on the REST sentence, but it is low-consequence.
- **refuter's evidence:** Code: packages/ui/src/CommandCenter.tsx:50-62 (channel descriptions shown in tiles at :122-123), :99-104 and :160-181 (query echo only), :353-356 (handleApplyRest = setCurrentMode('REST') + setRestApplied), :421-426. SpatioTemporal.tsx:17 (the illustrative header) and :45-49 (the status literals). NeuralLink.tsx:47 and :74, beside the decorative disclaimer at :77. Consumers of currentMode are Header.tsx and Sidebar.tsx, both visual only.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Delete the SYNC ACTIVE and Mars literals. Change the REST text to 'REST mode set — this only changes the interface'. Drop 'RL-Powered' and the Live dot. Either remove the query box or label it 'nothing answers here yet'.

### R4.6 · §3A/§7 Synthesis Lab provenance — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §6: native-first with honest provenance wherever output reaches a user.
- **observed:** The /api/v1/synthesis/stream done frame carries served_by and is_external (agentic_core/synthesis/api.py:401). SynthesisStudio.tsx:243-252 builds finalResult from output_id, download_url and timestamp only, so served_by is discarded. On the floor, a user sees only the in-text marker '_[Workstation native structured engine — owned, no external dependency]_', which says 'owned', not 'not model analysis'. The non-streaming fallback (:218-221) spreads resp.data, but no provenance chip is rendered anywhere in the file.
- **evidence:** grep served_by/provenance/floor in SynthesisStudio.tsx finds no hits; synthesis/api.py:379-401 emits served_by in the done frame.
- **disclosed to the user:** No, apart from the ambiguous inline marker.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced from source. The stream's done frame carries served_by and is_external, but SynthesisStudio builds finalResult from output_id, download_url and timestamp only, and renders no provenance anywhere. The only floor signal is the inline '_[Workstation native structured engine — owned…]_' marker.
- **refuter's evidence:** agentic_core/synthesis/api.py:401 emits served_by and is_external in the done frame. SynthesisStudio.tsx:243-252 builds finalResult without them, and the fallback at :218-221 spreads resp.data but nothing renders it. grep for served_by/provenance/floor/Badge in SynthesisStudio.tsx finds no hits.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Keep ev.served_by and ev.is_external on finalResult and render provenanceBadge beside each result.

### R4.7 · §7 fabric model-and-simulate before commit (the compliance chip) — **PARTIAL** · tier 2

- **severity (assessor):** low
- **claim:** §7: the platform models and simulates the configuration before commit. §11/§15: faith-rooted halal values, never certify what could not be assessed.
- **observed:** POST /api/v1/resources/compose/simulate returned simulation.quality.compliance.overall='pass' and compliant=true, while the sharia_halal and uk_legal rows are 'review' with coverage 'none' ('no engine covers this area'). By design (compliance.py:282-296) rows with coverage 'none' cannot colour the overall result, and coverage_gaps lists them. ResourceFabric.tsx:487-491 renders a green 'compliance: pass' chip and does not show coverage_gaps; the halal gap is visible only in the hover title. bar_measured also marks 'safe' as met=true, measured=true with sharia_halal 'review'. Separately, compose() and deleteComposition() do not check r.ok (:296-303, 282), so a 4xx clears the tray silently, despite the W344 'actions never fail silently' comment. The QMS chip is honest here: qms_gate_passed=null renders 'QMS —' with the not-assessable basis.
- **evidence:** POST /api/v1/resources/compose/simulate {resource_ids:[spi,reactor,immune,native_swarm]} returned compliance overall 'pass', verdicts sharia_halal:review/none and uk_legal:review/none, bar_measured.safe.met=true, and commit_ready=false.
- **disclosed to the user:** Only in the hover tooltip.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, and it is slightly worse than reported. The simulate payload's quality.compliance does not even carry coverage_gaps: its keys are only overall, compliant and verdicts. So nothing in the payload the fabric reads names the halal and legal gaps, apart from the per-row statuses in the hover title. The green 'compliance: pass' chip shows even though sharia_halal and uk_legal are review/none. bar_measured marks 'safe' and 'compliant' as met and measured, but it is not rendered in the fabric. compose() and deleteComposition() ignore r.ok, as claimed. Tier 2 holds because the hover title does list sharia_halal:review. It is close to a tier-1 certification on a halal-values platform.
- **refuter's evidence:** POST /api/v1/resources/compose/simulate {name:'x', resource_ids:[spi,reactor,immune,native_swarm]} returned compliance overall 'pass', compliant True, no coverage_gaps key, and verdicts sharia_halal review/none, uk_legal review/none, regulatory pass/screen, ehs pass/screen, ethical pass/engine. bar_measured safe {met:true, measured:true, basis includes sharia_halal:'review'}. commit_ready False. Code: ResourceFabric.tsx:487-491 (chip, hover only) and :282, :296-303 (no r.ok check); lib/api.ts:84-86 (pass renders emerald).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When coverage_gaps is non-empty, render the chip as 'compliance: pass (screened) · not covered: sharia_halal, uk_legal' in neutral or amber rather than emerald. Check r.ok on compose and delete.

### R4.8 · §17.2 the seven layers (Nervous, Immune, Self-healing, Genome, Cardiovascular, Endocrine) as effectors — **PARTIAL** · tier 3

- **severity (assessor):** medium
- **claim:** §17.2: sense → think → decide (Nervous) with auto-triggered reflexes; Immune defends; self-healing heals; the Genome is VSB identity; homeostasis and a survival instinct.
- **observed:** The layers are real as sensors and buses, not as effectors. Nervous: GET /organism/nervous/status shows reflex_arcs_registered=0, so no reflex can fire (nervous.py:78 has a register method and nothing calls it). Immune: /organism/systems shows real error-window sensing, but POST /api/v1/cca/immune-reconfigure has no UI or autonomous caller; the playbook says 'recommended'. Self-healing: breakers wrap only 'model:<name>' (orchestrator.py:56-80) and the external providers, and the native floor is never recorded, so in native-first operation /organism/self-healing/status stays tracked_endpoints=0 and 'health unknown' after dozens of completions (disclosed via health_basis). Genome: in the fabric run, encoding the objective gave every trait 0.5, fitness 0.5 and a ten-way tie for dominant_trait, and GET /organism/genome shows total 0; the UI shows a green 'ran'. Cardiovascular: homeostasis genuinely caps max_parallel (3, then 2 under ELEVATED immune), which is real. Heartbeat: running, 7 beats, real. Organism config: most keys are 'stored-only — nothing reads this key back', disclosed via key_wiring.
- **evidence:** GET /api/v1/organism/{status,systems,nervous/status,self-healing/status,genome,config}, GET /api/v1/heartbeat/status and GET /api/v1/native-ai/homeostasis. Also the composition run's genome row and grep for immune-reconfigure callers.
- **disclosed to the user:** Mostly yes: health_basis, reflex_arcs, key_wiring and the Anatomy tab. The fabric's genome 'ran' chip does not flag the empty encoding.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The layers are real sensors and buses, not effectors, and that is largely disclosed. One correction: GET /organism/genome no longer shows total 0, because the composition runs added genomes. The store row itself does disclose encoded:false and fitness_provenance 'default-unencoded'. Only the fabric's genome row hides that behind a green 'ran' chip.
- **refuter's evidence:** GET /organism/nervous/status: reflex_arcs_registered 0. nervous.py:75 defines register_reflex, and grep finds no caller. GET /organism/self-healing/status: overall_health null, 'nothing measured, health unknown', tracked_endpoints 0. /cca/immune-reconfigure is defined at change_control.py:608 with no UI or autonomous caller (living_plan.py:57 admits this), and immune.py:115-117 phrases it as 'recommended'. The composition run's genome row had every trait 0.5, fitness 0.5, dominant_trait None and a ten-way tie. GET /organism/genome shows encoded:false and 'default-unencoded (no FITNESS line parsed)'. Homeostasis caps max_parallel for real (now 1, under CRITICAL immune).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** This is capability work (P2.7): register reflex arcs, wire an immune-reconfigure caller behind the CCA, and record native/ollama serves into the breakers. Meanwhile, have the genome adapter report 'no traits detected' when every trait ties.

### R4.9 · §6 native AI mandate (in-house-first, external optional, honest floor) — **DELIVERED**

- **severity (assessor):** info
- **claim:** §6: native, owned capability, with external providers as optional accelerants and never dependencies; the floor is honest about what it is.
- **observed:** Verified by execution. GET /native-ai/status returned posture in-house-first, external_allowed=false, selection_order [native], is_real_model=false, floor_active=true, floor_note 'NOT an LLM'. floor_active_basis moved from a prediction to 'most recent SUCCESSFUL recorded completion was served by native … history, not a live probe' after real calls. /native-ai/complete, /swarm and /tree returned served_by=native and is_external=false. /native-ai/models is honest that there are no local models. gateway.query_meta routes to the native orchestrator first. gateway._call (external-first) has no callers. The external SDKs appear only behind AI_ALLOW_EXTERNAL (gateway.stream_meta:375, avatars/api.py:309-335). NativeAI.tsx:546-559 renders the floor card, note and basis. provenanceBadge and provenanceMapBadge (lib/api.ts:65-102) turn floor runs amber, and there are 24 call sites. The shortfall is the environment, not a defect: with AI_DISABLE_LOCAL=1 there is no owned generative model, and 'owned models' in practice means locally hosted Ollama weights, which §6 permits ('owned/locally-runnable'). selfcheck 'live' is an import check, not a behavioural one.
- **evidence:** GET /api/v1/native-ai/{status,models,resources,capabilities,selfcheck,homeostasis,lifecycle}; POST /api/v1/native-ai/{complete,swarm,tree}. Code: agentic_core/ai/gateway.py:130-185 and 299-449, agentic_core/ai/native/orchestrator.py, NativeAI.tsx, lib/api.ts.
- **disclosed to the user:** Yes.
- **refutation: SURVIVED** (reproduced by the refuter). Verified by execution: in-house-first posture, external off, and an honest floor note and basis. The external-first gateway._call has no caller. One caveat for the whole region: the native floor injects recalled terms from unrelated earlier requests into new outputs ('varroa treatment' and 'small beekeepers' in Petri and Forge outputs about penguins or micro-lending). The floor note calls this 'honest structured reasoning'. That is an output-quality concern, not a failure of the native-first mandate, so DELIVERED stands.
- **refuter's evidence:** GET /api/v1/native-ai/status returned posture in-house-first, external_allowed False, selection_order ['native'], is_real_model False, floor_active True, floor_note '…NOT an LLM…', and floor_active_basis 'most recent SUCCESSFUL recorded completion was served by native … history, not a live probe'. POST /native-ai/complete {prompt:'hello'} returned the output with the native marker. grep for gateway._call finds only a comment at change_control.py:454 saying nothing invokes it.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None needed for truth. The capability gap (an owned generative model) is disclosed.

---

## R5 — §1–§3, §3A, §9, §14, §15, §17.1 — the offerings, the avatar/UX, democratisation, the founding principles, the 4×6×4 grid

**Assessor's region summary:** R5 (§1-3, 3A, 9, 14, 15, 17.1), read-only against HEAD 929508f0 on :8086. Three Tier-1 truth defects remain on reached surfaces, so M1 is NOT met for this region.  1. **Care falls-risk:** CareHub tells a clinician 'no falls history' under a green 'no trigger recorded' chip when falls history was never recorded. This is the default path, because the NEWS2 default keys stay in the box when the tool is switched to falls_risk (scoring.py:353-357; CareHub.tsx:80, :96-97). 2. **Living Marketplace:** the 4 'live' product cards advertise features nothing serves. The capital fund's card lists 'On-Chain Gateway' while its own dashboard says 'No on-chain integration exists'. QEP-SDK lists morphology, quiz and trust scoring, and OctoVeritas lists a Quad-Engine Bridge (catalog/api.py:40-72; LivingMarketplace.tsx:295). The counts themselves are honest: 4 live, 10 source, 6 legacy. 3. **Halal pre-assessment:** when served by the floor, the text echoes a truncated '(COMPLIANT /' verdict token and a 'Flagged Ingredients' list of product-name n-grams. Gelatin and E471 are never flagged, and there is no withholding or floor_note, unlike the tafsir tool beside it (religion.py:342).  Tier-2 shortfalls: - The avatar's honest 'image received but not analysed' note is computed, then lost: only the floor-ignored prompt receives it, and the UI never renders image_understood. The avatar also does not answer enterprise-grounded questions. - The domain-tool floor scaffolds are disclosed on each result (amber badge, 'QMS —'), but the Domains front door still promises 'best-in-class capability'. - The compliance screen shows a big green PASS for 'halal … gelatin … carmine', although each row is disclosed as a keyword screen. - The VSB Cockpit stamps 'operational' and 'commercialise' on a minutes-old entity.  Verified DELIVERED: - QEP: sourced Arabic, honest tafsir withholding, recitation never scored. - Care scoring: NEWS2 matches the RCP table by hand; MUST, Waterlow and NEWS2 lower bounds; honest 'no arithmetic' for dementia. - Honest voice 503s, an honest empty job search, disclosed i18n coverage, the explicit profile, and the auth posture.  Tier-3 gaps: - The Products axis of the 4×6×4 grid is MISSING (disclosed in the vision). LIVING_PLAN:274 overclaims it. - No §14 switch board. - interfaith and hypothesis routes are API_ONLY. - An orphaned v137 '100% compliant' JSON remains in docs/compliance.  Scratch evidence is in C:/tmp/w476_r5/. UI conclusions are reasoned from source; no browser was driven. Five of the six domains have no /services route, and nothing in the UI calls one, so that is not a defect.

### R5.0 · §3A Domain Working (Care) / §15 P6 — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** Domain tools are real, in-house AI-mediated working capability; never fabricate (§15 P6). The Care risk tool scores validated tables honestly.
- **observed:** The falls-risk scorer tells the user 'no falls history' when falls history was never recorded, and shows a GREEN chip. POST /api/v1/care/risk-assess {tool:falls_risk, patient_data:<the CareHub default NEWS2 keys>} and {tool:falls_risk, patient_data:{'fallen twice this year':'yes'}} both return total 0, band 'no trigger recorded', response 'no falls history and fewer than two factors recorded — reassess on change', factors_present [], missing = all 11 factors INCLUDING falls_history, warnings []. The unrecognised key is dropped without a warning. CareHub renders the band chip with a regex colour fallthrough (high→red, medium|at risk|warranted→amber, else EMERALD), so 'no trigger recorded' shows green, followed by the untrue response line. An amber 'Incomplete — missing…' line does appear under it, but the headline tells a clinician the patient has no falls history, and NICE CG161 treats falls history as the single trigger. Switching the tool select from news2 to falls_risk leaves the NEWS2 default keys in the key:value box, so this is the default path. With correct keys it works: {'falls history':'yes','age':'82'} gives 2, 'multifactorial assessment warranted'.
- **evidence:** Executed the three payloads above against :8086. agentic_core/care/scoring.py:353-355 (the response is chosen by `falls_history in present`, so a missing value reads as absent); scoring.py:357 warnings hard-coded []; apps/workstation-superapp/src/pages/domains/CareHub.tsx:80 (the emerald fallthrough), :96-97 (the tool select and the NEWS2-only default patient_data).
- **disclosed to the user:** Partly. The amber 'Incomplete — missing: falls_history …' line appears, but the green chip and the 'no falls history' sentence contradict it.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. When falls_history is MISSING, the response still says 'no falls history', and the band 'no trigger recorded' falls through to the emerald chip. An unrecognised key ('fallen twice this year') is dropped with warnings []. The amber Incomplete line does not undo a headline that states an absent fact as true on a clinical tool.
- **refuter's evidence:** POST /api/v1/care/risk-assess {tool:falls_risk, patient_data:{'fallen twice this year':'yes'}} and {…{respiratory_rate:18, spo2:96}} both return total 0, band 'no trigger recorded', response 'no falls history and fewer than two factors recorded — reassess on change', complete false, missing = all 11 factors including falls_history, warnings []. scoring.py:350-356: the band and response key on `'falls_history' in present`, so missing reads as absent, and warnings is the literal []. CareHub.tsx:80 is the regex chip (else emerald), :81 renders r.score.response unqualified, :90 the amber Incomplete line, and :97 the NEWS2-only default key:value text.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When falls_history is in missing, return band null and a response like 'falls history not recorded — cannot say whether assessment is warranted'. Colour only an explicit low band emerald; any other band goes neutral. Put a warning on every key the scorer does not recognise.

### R5.1 · §3 / §14 marketplace (Living Marketplace catalogue) — **PARTIAL** *(assessed DOC_OVERCLAIM)* · tier 1

- **severity (assessor):** high
- **claim:** Only wired, functional surfaces are shown (§9 Owner ruling). The marketplace counts only what is served (P1.13), and stubs never ship (§15 P10).
- **observed:** GET /api/v1/catalog/products returns counts {registered 20, live 4, source 10, legacy 6}. The counts and the status labels are honest. The four LIVE cards still list features that nothing serves. capital_fund (live, /capital) lists 'Autonomous Rebalancing', 'On-Chain Gateway' and 'Constitutional Evolution Voting'; the only matches for rebalanc / evolution-vot in agentic_core are the catalogue literal itself. The capital fund's own page says the opposite: CapitalDashboard.tsx:253-256 reads 'No on-chain integration exists' (the crypto_gateway/mainnet_settlement code under products/capital_fund/ is imported by nothing). qep-sdk (live, /qep-religion) lists 'Arabic Morphology Analysis', 'AI Quiz Generation' and 'Scholarly Annotation Trust Scoring'; the openapi (441 paths) has no morphology, quiz or annotation-trust route, and qep_analytics.py:19 records that such figures were once invented. OctoVeritasEngine (live, /constitution) lists 'Quad-Engine Bridge' and 'C-Suite Coordination'; quad-engine exists only as a v137 CLI flag in grand_synthesis_engine.py.
- **evidence:** Executed GET /api/v1/catalog/products. Read agentic_core/catalog/api.py:40-72 (CATALOG_OVERRIDES feature literals) and :13-18 (ROUTE_OVERRIDES that make these four 'live'). apps/workstation-superapp/src/pages/marketplace/LivingMarketplace.tsx:295-300 renders product.features as bullets with no qualifier on live cards. grep for rebalanc / on.chain / evolution.vot / morpholog / quad.engine across agentic_core and the openapi.
- **disclosed to the user:** No. The live cards carry no qualifier, and the header says 'Only an entry marked live opens to a served surface', which implies the listed features are served.
- **refutation: STOOD, verdict CORRECTED DOC_OVERCLAIM → PARTIAL.** Reproduced. The tier is right, but the verdict label is wrong: this is not a doc-only overclaim, because the Marketplace is a reached surface. Its four LIVE cards list unqualified features that nothing serves. Among them is 'On-Chain Gateway', which the capital page itself denies ('No on-chain integration exists'). That is a reached surface stating something untrue.
- **refuter's evidence:** GET /api/v1/catalog/products returns counts {registered 20, live 4, source 10, legacy 6}. The live features include capital_fund ['Autonomous Rebalancing','On-Chain Gateway','Constitutional Evolution Voting'], OctoVeritasEngine [...,'Quad-Engine Bridge'] and qep-sdk ['Arabic Morphology Analysis','AI Quiz Generation','Scholarly Annotation Trust Scoring']. catalog/api.py:42-72 holds the literals. LivingMarketplace.tsx:286-300 qualifies only the legacy and source cards and renders the features as bullets on live cards. CapitalDashboard.tsx:253-256 reads 'No on-chain integration exists'. The openapi has 441 paths and none matches morph/quiz/annot/rebal/chain/quad. The only morphology code (reactor/religion/quranic_studies.py MorphologyService) is not a served route.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Replace each live product's features with the capabilities its route actually serves, e.g. capital fund: 'virtual WST allocation · portfolio · AI report'. Drop On-Chain Gateway, Rebalancing, Morphology, Quiz, Trust Scoring and Quad-Engine, or move them under a labelled 'not built' line.

### R5.2 · §3A Domain Working (Religion — Halal pre-assessment) / §11 faith-content / §15 P6 — **STUB** · tier 1

- **severity (assessor):** medium
- **claim:** Religion-domain tools are honest under the faith-content constitution. Never fabricate a verdict. When the floor serves, a religion tool withholds what the floor cannot mean (the tafsir precedent: sections_withheld + floor_note).
- **observed:** POST /api/v1/religion/halal-review {product_name:'Choco bar', ingredients:['sugar','cocoa butter','gelatin','E471']}. The floor-served 'assessment' repeats the heading '## Halal Status Assessment (COMPLIANT /' five times: it is the prompt's '(COMPLIANT / REQUIRES REVIEW / NON-COMPLIANT)' enum cut short, so it reads as a COMPLIANT verdict. The '## Flagged Ingredients' section lists 'milk chocolate, chocolate bar, bar choco, choco bar, bar sugar' as though they were flagged, and never names gelatin or E471 (the two ingredients of real halal concern). The response has no floor_note and no sections_withheld. The sibling Tafsir tool on the same hub does withhold (sections_withheld ['Transliteration','Translation'] plus a floor_note saying the notes are 'a structured frame, not scholarship').
- **evidence:** Executed halal-review and quran-tafsir (surah 2, ayah_start 255) on :8086. agentic_core/api/religion.py:342 (the enum heading in the prompt); religion.py:242-279 (floor withholding exists for tafsir only). The UI renders the text through DomainTool with the amber 'structured floor — not model analysis' badge (lib/api.ts:69) and the certification disclaimer.
- **disclosed to the user:** Partly. The amber floor badge and 'not certification' disclaimer are shown, but the body text itself presents a verdict-shaped token and a false flagged-ingredients list.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, using the UI's real field set (product_description is required; the assessor's payload would 422, but the hub sends it). The body repeats '## Halal Status Assessment (COMPLIANT', which reads as a verdict. 'Flagged Ingredients' lists milk chocolate / chocolate bar / bar choco / choco bar / bar sugar and never gelatin or E471. There is no floor_note and no sections_withheld. The hub's own description promises 'ingredient flags'. The 8px amber badge does not neutralise a verdict-shaped token and a false flag list on a faith-content tool, and the tafsir sibling proves that withholding is implemented elsewhere.
- **refuter's evidence:** POST /api/v1/religion/halal-review {product_name:'Choco bar', product_description:'A milk chocolate bar', ingredients:['sugar','cocoa butter','gelatin','E471']} returns keys [assessment_id, product_name, target_markets, assessment, ai_provenance, generated_at, disclaimer] (no floor_note or sections_withheld); served_by native; qms_gate_passed null. ReligionHub.tsx:166-178 is the hub description: 'ingredient flags, process concerns'. lib/api.ts:69 is the badge.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Apply the tafsir pattern: when served_by is native, withhold 'Halal Status Assessment' and 'Flagged Ingredients' and add a floor_note. Or strip the verdict enum from the floor-facing prompt so no verdict token can be echoed.

### R5.3 · §9 multimodal enterprise-aware avatar — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** Image understanding is in-house-first and honesty-bound: an owned vision model when provisioned, otherwise an honest 'image received but not analysed', never a fabricated description. The avatar is enterprise-aware, and when the serving resource cannot honour the language, the answer says so.
- **observed:** The honest image note is computed and then dropped. avatars/api.py:292-340 builds image_note ('Image attached and received … not analysed') but only inserts it into the prompt. The deterministic floor ignores the prompt body, so POST /api/v1/avatar/chat with an image_base64 PNG and 'What is in this image?' returned a scaffold about 'the Workstation Sovereign Mesh avatar — a helpful, concise assistant' with image_understood:false. The UI hook (useAvatarSession.ts:233-238) reads only response/served_by/is_external/suggested_areas, so the user never sees that the image went unanalysed. Enterprise awareness fails the same way. With vsb_id vsb-24267609d1, 'What stage is my enterprise at…' returned grounded_in set, but the answer text is the system prompt's n-grams plus 'bees yorkshire / varroa mites'; the question is not answered. With language 'Arabic' the reply is English with language:null, and the UI does not render that. Settings does disclose that 'AI-generated content is still produced in English'. Voice is honest: /avatar/transcribe and /avatar/speak return explicit 503 'nothing was sent externally', and the browser Web Speech path is primary. No fabrication was found.
- **evidence:** Executed POST /api/v1/avatar/session, /avatar/chat (plain, with image, with vsb_id + language), /avatar/transcribe and /avatar/speak. Read agentic_core/avatars/api.py:279-384, apps/workstation-superapp/src/hooks/useAvatarSession.ts:209-252 and components/avatar/ConversationPanel.tsx:95 (amber floor badge per message). The UI is reasoned from source; no browser was driven.
- **disclosed to the user:** Partly. Each reply carries the amber 'structured floor — not model analysis' badge, and the language shortfall is disclosed in Settings. The image-not-analysed note is not shown anywhere.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The image_note is computed only into the prompt. The floor reply describes the avatar's own system prompt, and image_understood:false is never read by the UI hook. No image description is fabricated, and every reply carries the amber floor badge, so tier 2 (an invisible shortfall on the image) is correct.
- **refuter's evidence:** POST /api/v1/avatar/chat {message:'What is in this image?', image_base64:<1x1 PNG>, language:'Arabic'} returns image_understood false, served_by native, language null, and a response of 'Acting as: Workstation Sovereign Mesh avatar … Key factors: workstation sovereign, concise assistant…'. avatars/api.py:292-340 builds image_note; :346-353 puts it in the prompt only. useAvatarSession.ts:233-238 stores only response/served_by/is_external/suggested_areas.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Return image_note as its own response field (or prepend it to response when image_understood is false) and render it in ConversationPanel. Also render a 'requested <language> — answered in English' chip when language is null but one was requested.

### R5.4 · §3A offering 1 — the floor-served domain tools as a class — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** The Domains section gives 'best-in-class capability on demand' through in-house-AI-mediated research · analyse · generate · assess tools of comparable scope across the six domains.
- **observed:** One tool per domain was executed: halal-review, science/hypothesis, education/lesson-plan, law/analyse, employment/cv, care/risk-assess, plus education/feedback, employment/salary-negotiation and religion/hadith-study. Every one is floor-served (served_by native; qms_gate_passed null with the 'not assessable — floor-served' basis). The text is headings filled with keyword bigrams, often taken from the tool's own instruction rather than the user's input. law/analyse on a clause with unlimited indemnity and termination without notice returns 'Legal Risks: Exposure on england wales / solicitor specialising / comprehensive analysis' and never mentions either clause. No figures or grades are fabricated: salary gives no numbers, feedback's 'Indicative Level / Grade' is empty, hadith 'Grading' is empty, and job search returns 0 results with an honest basis. Every result carries the amber 'structured floor — not model analysis' badge and the 'QMS —' chip (DomainTool.tsx:239-253). The front door (DomainsHub.tsx:49, :84-86) still says 'Get best-in-class capability on demand' and 'Every tool runs on … native AI fabric', with no environment-level notice that only the floor is serving. Tool counts come from the one registry: 27 tools = 5+3+4+3+4+7 (the hub labelled 'Law' has 3; 'Care' 4).
- **evidence:** Executed the listed POST routes on :8086; responses saved under C:/tmp/w476_r5/*.json. Read apps/workstation-superapp/src/lib/toolRegistry.ts, pages/domains/DomainsHub.tsx:34-88, components/DomainTool.tsx:230-270 and lib/api.ts:65-72.
- **disclosed to the user:** Yes, per result (amber badge plus QMS '—'). Not at the front door, which promises best-in-class capability.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. Per-result disclosure exists (amber floor badge plus the QMS chip). The Domains front door still promises 'best-in-class capability on demand' and says 'Every tool runs on … native AI fabric', and no app-wide notice that only the floor is serving exists. The tools fabricate no figures, and the shortfall is disclosed per result, so tier 2 rather than tier 1 is fair.
- **refuter's evidence:** DomainsHub.tsx:49 ('Get best-in-class capability on demand') and :84-86 ('Every tool runs on Workstation IDBO's own native AI fabric'). DomainTool.tsx:237-251 holds the badge and QMS chip. A floor response was reproduced on care/risk-assess and halal-review (qms_basis 'not assessable — floor-served'). A grep for a global floor notice found none in layout components.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Have DomainsHub read the serving posture (e.g. /api/v1/native-ai status) and show 'only the structured floor is serving: results are scaffolds, not analysis' while no owned model is up.

### R5.5 · §11×§3A compliance screen reached from Governance (spot-check P6) — **PARTIAL** · tier 2

- **severity (assessor):** low
- **claim:** Compliance is continuously live, and a pass is never painted where nothing was assessed.
- **observed:** GET /api/v1/compliance/frameworks: each card describes its engine honestly ('keyword screen … not a certification'). This part is DELIVERED. POST /api/v1/compliance/check {subject:'A halal chocolate bar made with gelatin and E120 carmine'} returns overall 'pass' with sharia_halal 'pass', reason 'halal vocabulary present and no prohibited (haram) term'. The page renders this as a large emerald PASS: the word 'halal' in the subject is enough to pass gelatin and carmine. The uk_legal row says 'review — no engine covers this area' and coverage 'none', yet its reason text also reads '(UK engine-backed · audit …)', which contradicts itself.
- **evidence:** Executed both routes. Read apps/workstation-superapp/src/pages/governance/ComplianceChecker.tsx:36-100.
- **disclosed to the user:** Yes, row by row ('a keyword screen, not a certification'). The large green overall PASS is the only part that is not qualified.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The word 'halal' in the subject turns sharia_halal into 'pass' because gelatin and carmine are absent from _HARAM, and the overall renders as a large emerald PASS. The uk_legal row says coverage 'none' yet its reason carries '(UK engine-backed · audit …)'. The page header states 'It flags; it does not certify. A pass is a pass of the screen', and each row says 'keyword screen, not a certification'. With that disclosure, tier 2 stands. The pass is self-declared by the subject's own vocabulary, which is worth recording.
- **refuter's evidence:** POST /api/v1/compliance/check {subject:'A halal chocolate bar made with gelatin and E120 carmine'} returns overall 'pass', compliant true, sharia_halal pass 'halal vocabulary present and no prohibited (haram) term'; uk_legal review, coverage none, reason '...not legal advice (UK engine-backed · audit 53d75e96…)'. compliance.py:30-34 is the _HARAM list (no gelatin/carmine/E120). ComplianceChecker.tsx:38-40 is the header disclosure and :71-77 the overall card.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add doubtful-ingredient terms (gelatin, carmine/E120, E471, rennet, alcohol-derived flavourings) to the sharia screen as 'review'. Drop 'engine-backed' from uk_legal reasons whose coverage is 'none'.

### R5.6 · §3A offering 2 / §17.1 lifecycle — VSB Cockpit stage — **PARTIAL** · tier 2

- **severity (assessor):** low
- **claim:** All VSBs follow a Concept→…→Commercialise gated lifecycle. The canon (§17.1) records that every VSB is stamped stage 'commercialise' at birth and that nothing advances it, pending an Owner ruling (P3.0).
- **observed:** A VSB created today (vsb-6ae1b88504, created 13:04:39Z) returns status 'operational' and stage 'commercialise' from GET /api/v1/vsb, while its review-gates show only 'design' approved and every other stage not_gated. VSBCockpit.tsx:453-454 renders both as unqualified badges (emerald 'operational', aura 'commercialise'). A founder is told their minutes-old virtual enterprise is at the Commercialisation stage. The entity's domain is 'enterprise', which is a realm, not one of the six domains.
- **evidence:** Executed GET /api/v1/vsb and GET /api/v1/vsb/vsb-6ae1b88504/review-gates. Read agentic_core/api/genesis.py:843 and :1048 (the literal 'stage': 'commercialise', 'status': 'operational').
- **disclosed to the user:** No. The shortfall is recorded in the vision canon, not on the Cockpit.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. Every VSB is born with status 'operational' and stage 'commercialise' from literals, while its review-gates show only design approved. The Cockpit renders both as unqualified badges. The label is not wholly invented, since the genesis pipeline does run a 'commercialise' step, and the vision canon records the gap. The Cockpit does not disclose it, so tier 2.
- **refuter's evidence:** GET /api/v1/vsb: all entities show status operational, stage commercialise (e.g. vsb-0280b9f830, created 13:08:52Z). GET /api/v1/vsb/vsb-6ae1b88504/review-gates shows design approved and the other stages not_gated. genesis.py:840-843 and :1045-1047 hold the literals. VSBCockpit.tsx org tab: `<Badge color="emerald-500">{detail.status}` and `<Badge color="aura">{detail.stage}` with no qualifier.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Until the Owner rules, render the badge as 'journey scope: commercialise' with a tooltip saying the stage is not yet tracked, or derive the stage from the review-gate statuses.

### R5.7 · §3A / §9 / §15 — verified DELIVERED — **DELIVERED**

- **severity (assessor):** none
- **claim:** Authentic sourced Quran text and honest QEP; clinical tools computed from published tables; honest voice; honest job search; language coverage stated from real dictionaries; an honest catalogue count.
- **observed:** QEP: GET /api/v1/qep/ayah/2/255 returns the Arabic from alquran.cloud. qep/status declares that recitation is never scored. The tafsir tool withholds translation on the floor and carries a floor_note. Care: NEWS2 {rr24, spo2 93, air, sbp98, pulse115, alert, 38.6} gives 9 'high' and 'emergency response', hand-checked against the RCP 2017 table. An alias key ('heart rate':135) is honoured (total 10). MUST {bmi 17.5, loss 7%} gives 3 'high', flagged INCOMPLETE. Waterlow and NEWS2 partial inputs return lower bounds with no band. dementia_care returns 'no published arithmetic … not a score'. Voice: transcribe and speak return 503 'nothing was sent externally'; the Web Speech path is feature-detected. Job search: 0 results; the UI says 'not a live job board' and explains that the floor cannot compose listings. i18n: dictionaries exist for ar/fr/es/ur, and Settings.tsx:106-126 states coverage and that AI content stays in English. Catalogue counts are live 4 / source 10 / legacy 6, with source and legacy cards labelled. The explicit user profile (GET /api/v1/user/profile) is applied to prompts via gateway.py:154-156. Auth and signup posture is disclosed (/api/v1/auth/config, Login.tsx:85).
- **evidence:** Executed each route listed. Read the cited files.
- **disclosed to the user:** n/a (delivered)
- **refutation: SURVIVED** (reproduced by the refuter). Spot-checks reproduce the delivered claims: the NEWS2 arithmetic, authentic ayah text, and the honest 503 from voice.
- **refuter's evidence:** POST care/risk-assess news2 {resp_rate 24, spo2 93, air, sbp 98, pulse 115, alert, temp 38.6} returns 9, 'high', 'emergency response — urgent or emergency clinical review…'. GET /api/v1/qep/ayah/2/255 returns ref 2:255 with the Arabic text. POST /api/v1/avatar/speak returns 503 '…browser's speechSynthesis is the in-house path — nothing was sent externally.'
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** none

### R5.8 · §17.1 the 4 Realms × 6 Domains × 4 Products grid; §14 democratisation switches — **MISSING** · tier 3

- **severity (assessor):** low
- **claim:** A 96-cell grid (Realms × Domains × Products: Reactor · Incubator · Factory · Laboratory). §14: democratisation is measurable as which Owner switches have been flipped, each switch with a proven pre-flight.
- **observed:** agentic_core/taxonomy.py:10-11 and apps/workstation-superapp/src/lib/taxonomy.ts define only REALMS (4) and DOMAINS (6). There is no PRODUCTS axis, and Genesis offers realm × domain only (GenesisJourney.tsx:116-142, 205). The vision's own §17.1 discloses this as awaiting an Owner ruling. docs/WORKSTATION_IDBO_LIVING_PLAN.md:274 still says 'Products: Reactor/Factory/Incubator … all live' (DOC_OVERCLAIM, not reached by users). For §14, only /api/v1/auth/config exposes switch state (auth and self-serve signup). No surface shows AI_ALLOW_EXTERNAL, REAL_MONEY_ENABLED or pre-flight status: no switch, readiness or preflight route exists among the 441 openapi paths.
- **evidence:** Read taxonomy.py, taxonomy.ts, GenesisJourney.tsx and LIVING_PLAN.md:274. Scanned openapi paths. Executed GET /api/v1/auth/config.
- **disclosed to the user:** The grid gap is disclosed in the vision canon. The switch board is not surfaced; it is planned for P4.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. The taxonomy has only REALMS (4) and DOMAINS (6), with no Products axis. The vision canon discloses the grid gap. No switch or preflight surface exists beyond auth/config. The LIVING_PLAN line is an unreached doc overclaim.
- **refuter's evidence:** agentic_core/taxonomy.py:10-11 defines REALMS and DOMAINS only. docs/WORKSTATION_IDBO_LIVING_PLAN.md:274 matches 'Reactor/Factory'. The openapi scan (441 paths) has no switch or preflight route.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Amend LIVING_PLAN:274 to say the Products axis is 'facilities exist; the grid axis is not built'. Build the switch board in P4 as planned.

### R5.9 · §3A parity / canon docs — small residue — **API_ONLY** · tier 3

- **severity (assessor):** low
- **claim:** The six domains carry tool sets of comparable scope. Canon compliance documents are honest (the W473 rewrite).
- **observed:** POST /api/v1/religion/interfaith and /api/v1/science/hypothesis are served (hypothesis executed: 200, floor) but no hub mounts them (grep of pages/domains/*Hub.tsx endpoints), so they are API_ONLY. Science exposes 3 tools against Employment's 7. docs/compliance/MANDATES.md and MANDATES_FINAL.md are now honest inventories. docs/compliance/v137_floor_20_compliance_report.json still asserts compliance_rate 1.0 with all 10 articles compliant; it is referenced by no live code, doc, or test (only a stale .pyc), so it is an unreached DOC_OVERCLAIM. GET /api/v1/avatar/status reports ollama_online:true while AI_DISABLE_LOCAL=1 means Ollama never serves; the UI does not render this field.
- **evidence:** Executed science/hypothesis and avatar/status. Grepped hub endpoints and references to the v137 report.
- **disclosed to the user:** n/a (not reached)
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed as minor residue. religion/interfaith is served (422 on an empty body, so the route exists) and no UI calls it. ollama_online is reported true with local serving disabled. The UI reads that field only as part of an OR for the online indicator, where native:true already yields 'online', so nothing is misstated.
- **refuter's evidence:** POST /api/v1/religion/interfaith {} returns 422. A grep of src for 'interfaith' finds only a KnowledgeHub description string. GET /api/v1/avatar/status returns {online:true, native:true, ollama_online:true,...}. useAvatarSession.ts:129 is the only consumer of ollama_online.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Mount or retire interfaith and hypothesis. Archive the v137 report or add a 'superseded, unmeasured' header. Have avatar/status report local_serving_enabled alongside ollama_online.

---

## R6 — §8 + §12 + §17.5 — the biomimetic living organism, the economic organism, the ten architecture invariants

**Assessor's region summary:** R6 (§8 organism, §12 economy, §17.5 invariants), measured against the live backend at :8086 (HEAD 929508f0). No repository files were written. Scratch mutations went only into the backend DATA_DIR C:/tmp/w476b: - test VSB ids w476-r6-a/b/c/d/e - one 10 WST transfer (xfer-32506e4b42) into another auditor's living VSB, vsb-24267609d1 - one CCA hold, cca-56a4ee92a7 (the 250k materiality test) - the w476-r6-e ledger, deliberately truncated to test repair and left that way. Repair quarantined a copy. The w476-r6-a ledger and capital_fund.json were corrupted, probed and restored. The charity-directives file was created corrupt and then removed.  Economy books: DELIVERED. The six-stage waterfall arithmetic is exact. The trial balance and balance sheet balance across a cycle, a transfer and a period close. Materiality holds work, template bounds work, and an unreadable ledger gives 503, never zero.  Organism: the heartbeat really beats every 60s. Autonomy is opt-in, and the page says so.  M1 is NOT met for this region. FIVE Tier-1 truth defects remain on reached surfaces: (1) The composite health headline says 'Organism nominal, health 60%, Standard operations active'. At the same moment the only measured term (immune) read 0.0 with threat CRITICAL. 60% of the score is a defaulted self-healing value (1.0) plus a simulated metabolic value pinned at 1.0, so the score cannot fall below 0.6. (2) The ATP 'metabolic energy' only ever rises. So the §8→§12 survival instinct is unreachable, and the Economy page shows 'Metabolic Energy 100%' with no disclosure. (3) When the Owner's charity-directives store cannot be read, it is read as defaults. The surface says 'defaults', and money went to a cause the Owner had excluded. (4) When the capital fund cannot be read, it is silently reset to a fresh 10,000,000 WST HEALTHY pool. The 10M seed is itself an unfunded constant that the UI calls 'the real capital fund'. (5) The Sovereign Evolution page shows a defaulted 'proceed' verdict as 'Curated by C-Suite'.  Tier 2: venture money goes into a VSB that failed compliance and is held. The Owner's Dawah priority gets nothing, and grants whose compliance verdict is 'review' are still funded. The twin pre-validation fallback cannot fail on its health term.  Invariants §17.5: - Hold: single router mount, append-only UEG (both chains verify), signal-bus locking, plan staleness. - Twin pre-validation holds in form but is vacuous in its fallback. - Not held, and the vision's own §17.5 status says so (Tier 3): user isolation on the organism perimeter, the GaaS gate on every output, torch optionality, and the KPI gate (MISSING).

### R6.0 · §8 homeostasis / composite health (and §17.5 twin pre-validation, which consumes it) — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** §8: the organism continuously monitors and evaluates itself, and it defends, learns and heals. §15.6: honesty over polish, never fabricate, label simulation.
- **observed:** The headline organism health reports NOMINAL/FULL_POWER with a percentage mostly made of unmeasured values, even while the only measured term reads zero and CRITICAL. Live at 13:09Z: GET /api/v1/organism/status returned composite_health 0.6, mode NOMINAL, health_summary 'Organism nominal — ACTIVE_FOCUS cycle, health 60%. Standard operations active.' In the same response systems.immune was health 0.0, threat_level CRITICAL, errors_in_window 11, with a playbook saying 'engage immune_quarantine'. /health-summary: composite_health_measured_only 0.0. The terms: self_healing value 1.0 is measured:false ('defaulted to 1.0') and GET /organism/self-healing/status says overall_health null, 0 circuits. The metabolic value 1.0 is measured:false (simulated). The formula is 0.4*immune + 0.4*1.0 + 0.2*1.0, so the composite cannot fall below 0.6 and mode cannot fall below NOMINAL while no circuits are tracked. Earlier the same headline read 'Organism at peak … health 80%. Full cognitive power' with immune ELEVATED 0.5. Where the unqualified figure reaches users: DashboardNew.tsx:257,261 on the home page '/' ('NOMINAL · 60% health', 'Composite Health 60%'); OrganismDashboard.tsx:262-266 (the default tab of /organism, which shows the banner and summary but no basis); the VSBEconomy.tsx:586 board-pack posture; /fund/status organism. Only the Anatomy tab (OrganismAnatomy.tsx:230-239) shows the measured-only figure and health_basis. Knock-on: change_control.py:783, the twin pre-validation fallback, passes when composite_health >= 0.6 and threat is NOMINAL/ELEVATED. The composite half of that gate is always true, so it can never fail on health.
- **evidence:** curl /api/v1/organism/status and /api/v1/organism/health-summary (13:04Z: 0.8/FULL_POWER/'at peak' with immune 0.5 ELEVATED; 13:09Z: 0.6/NOMINAL with immune 0.0 CRITICAL). agentic_core/organism/biobus.py:190-235 (composite formula; sh_health = 1.0 if untracked) and :211-222 (mode thresholds). apps/workstation-superapp/src/pages/DashboardNew.tsx:107,257,261. pages/organism/OrganismDashboard.tsx:262-266. agentic_core/api/change_control.py:780-784.
- **disclosed to the user:** Only on the Anatomy tab. Not on the home Dashboard, the Organism overview tab, the Economy board-pack posture or /fund/status.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced live, and it is currently worse than the assessor's 13:09Z reading. GET /organism/status returned composite_health 0.8, mode FULL_POWER and the summary 'Organism at peak … health 80%. Full cognitive power available.' In the same response immune was 0.5 ELEVATED, and /health-summary gave composite_health_measured_only 0.5 and health_basis '40% measured'. So 60% of the headline is a default (self-healing 1.0, no circuits) plus a simulated ATP of 1.0. biobus.py:193-199 sets sh_health to 1.0 when nothing is tracked, which gives a floor of 0.4*immune + 0.6 while ATP sits at 1.0. The home Dashboard shows the unqualified figure: DashboardNew.tsx:106-107 reads composite_health, and :257 and :261 render it. The same goes for OrganismDashboard.tsx:262-266. The Economy board pack printed composite_health 0.96 / FULL_POWER at 13:14Z. Only OrganismAnatomy.tsx:230-236 renders the measured-only figure and its basis. The knock-on at change_control.py:783 is correct: while ATP is 1.0 and no circuits are tracked, the composite >= 0.6 test cannot fail, so only a HIGH or CRITICAL immune threat can fail that gate.
- **refuter's evidence:** curl /api/v1/organism/status and /organism/health-summary at 13:1xZ: 0.8 FULL_POWER 'at peak', measured_only 0.5. /organism/self-healing/status overall_health null, 0 circuits. biobus.py:185-222. DashboardNew.tsx:106-107,257,261. OrganismDashboard.tsx:262-266. change_control.py:783.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Compute mode and health_summary from composite_health_measured_only, or make an unmeasured term count as unknown rather than 1.0. Put health_basis beside every rendered composite. Make the twin fallback read immune health or measured_only instead of the composite.

### R6.1 · §8 survival instinct / §8→§12 economic survival instinct / metabolic energy — **STUB** · tier 1

- **severity (assessor):** high
- **claim:** §8 and §12: a survival instinct makes the organism adaptive. When energy is depleted the organism conserves, and the economic organism raises its reserves.
- **observed:** The ATP 'metabolic energy' behind every survival-instinct lever can only go up. ATPSimulator starts at ratio 5.0 (0.333 normalised). Each update adds production 0.5*efficiency (0.4–0.5) and subtracts consumption 0.1*load (at most 0.1). Every organism_context() read advances it, at most once a second, with load 0.2–1.0. Live, it rose with page polls: 0.396 → 0.456 → 0.831 → 1.0 within about 10 minutes of boot, and stayed at 1.0. So the following can never fire: the economy's reserve raise at atp < 0.3 (metabolism.py:158-163), the heartbeat's self_recovery at < 0.3 (heartbeat.py:194-203), native homeostasis 'protected' at < 0.3, and metabolic_throttle at < 0.2 (heartbeat.py:327). The number users see is a poll counter capped at 1.0. VSBEconomy.tsx:490 renders 'Metabolic Energy' as a percentage with tone 'good' and no basis. The board pack shows 'ATP 100%'. The cycle JSON carries metabolic_energy 1.0 and energy_state 'healthy'. Even with the w476-r6-a ledger unreadable, /economy/status reported metabolic_energy 1.0.
- **evidence:** agentic_core/molecular/atp_simulator.py:11-27. agentic_core/organism/biobus.py:151-166. agentic_core/economy/metabolism.py:156-163,362-371. agentic_core/ai/native/homeostasis.py:43-66. apps/.../enterprise/VSBEconomy.tsx:490,586. Live: organism/status atp 0.396 at 12:58, board-pack 0.456, cycle 0.831 at 13:01, health-summary 1.0 from 13:04 onward.
- **disclosed to the user:** Partly. biobus composite_health_terms.metabolic says 'simulated' (seen only on the Anatomy tab). The Economy page metric, the board-pack ATP and the cycle report carry no disclosure, and nothing says the survival instinct cannot trigger.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed from the code. ATPSimulator.update adds 0.5*efficiency, which is 0.4 or 0.5 (biobus.py:162), and subtracts 0.1*load. Every caller keeps load at or below 1.0: homeostasis.py:46 caps it at 1.0, app_mvp.py:436 uses cpu/100, and recover uses 0. So each update is net positive, at least +0.3 on the 0.5-15 scale, and the biobus ATP can never go down. The survival-instinct branches are therefore dead once the value rises: metabolism.py:160 (atp < 0.3), heartbeat.py:194 (< 0.3) and heartbeat.py:327 (< 0.2). Live /economy/status gave metabolic_energy 1.0, /fund/status atp_ratio 1.0 and the board pack atp_ratio 1.0. VSBEconomy.tsx:490 renders 'Metabolic Energy' with tone 'good' and no basis, and :586 renders 'ATP n%'. The biobus basis text itself is inaccurate: it says 'ATPSimulator on a constant metabolic_load', but the value is a monotone accumulator. A further minor point: integration_surface.py:415 builds a fresh ATPSimulator on every call, so that surface always reports 0.333.
- **refuter's evidence:** agentic_core/molecular/atp_simulator.py:14-26. biobus.py:151-166. homeostasis.py:46-48. metabolism.py:156-163. heartbeat.py:192-203,320-330. VSBEconomy.tsx:490,584-586. Live /economy/status metabolic_energy 1.0; /fund/status organism.atp_ratio 1.0.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either drive ATP from a real load signal that can fall (request rate, model-call latency or failures), or relabel metabolic energy as a simulation on every surface. Also state that the survival-instinct levers are inert until then.

### R6.2 · §12 charity: the Owner's directives (inclusions and exclusions honoured); an unreadable store — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** §12: the four causes, with a 100%-donation-only screen and Owner-set directives (inclusions and exclusions honoured).
- **observed:** An unreadable Owner charity-directives store is quietly replaced by defaults, and money then goes to causes the Owner excluded. I wrote a malformed C:/tmp/w476b/economy_charity_directives.json. It was a truncated object with exclusions ['dawah','conflict_relief'] and priorities ['clean_water']. Results: GET /api/v1/economy/charity/directives returned 200 with exclusions [] and source 'defaults (2026-06-21 Owner directive)'. POST /api/v1/economy/cycle {vsb_id:'w476-r6-e', revenue:100} then granted to conflict_relief, a cause the Owner had excluded, with no error. The cause is get_directives(), which uses load_json_tolerant(..., {}): a store that cannot be parsed reads as never set. approved_signals() (charity signals) and revenue.py:42 (the revenue-event queue) use the same tolerant load. The P1.15 'refuse, never replace' rule did not reach these economy stores. I removed the file afterwards.
- **evidence:** agentic_core/economy/charity.py:62-72,90-94. agentic_core/config.py:191-210. agentic_core/economy/revenue.py:42. Live probe output: directives source 'defaults', exclusions []; cycle grants ['clean_water','conflict_relief','orphan_sponsorship','famine_food','emergency_health'].
- **disclosed to the user:** No. The surface states that defaults are in force when the Owner's directives exist but cannot be read.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed from the code; I did not mutate the store myself. charity.py:65 get_directives() calls load_json_tolerant(_DIRECTIVES_STORE, {}). config.py:191-210 returns the default {} for any object that cannot be parsed, including a truncated one, because raw_decode fails and no exception is raised. With d == {}, exclusions become [] and source becomes 'defaults (2026-06-21 Owner directive)'. The Owner's exclusions are then silently dropped from every allocation, and the page says the Owner never set them: CharityDirectives.tsx:125 renders 'still on defaults · defaults (...)'. approved_signals() at charity.py:90-94 uses the same tolerant load. The live GET /economy/charity/directives currently returns source defaults and exclusions [], which is consistent with this.
- **refuter's evidence:** agentic_core/economy/charity.py:62-72,90-94. agentic_core/config.py:191-210. apps/.../enterprise/CharityDirectives.tsx:13,125. Live GET /api/v1/economy/charity/directives.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Read the directives, signals and revenue stores strictly (read_json_strict / StoreUnavailable). A cycle should refuse to allocate charity, or hold it, while the directives cannot be read, and the directives route should return 503.

### R6.3 · §12 Sovereign Capital Fund (the capital_fund waterfall stage compounds into it) — **STUB** · tier 1

- **severity (assessor):** high
- **claim:** §12: virtual/simulated money, honestly labelled. The capital_fund stage is 'energy storage' that compounds. Stores refuse rather than replace (P1.15 truth-first).
- **observed:** The fund's baseline is an unfunded 10M WST constant presented as 'the real capital fund', and an unreadable fund is silently reset to that constant. (a) When the store is absent, _load_fund() seeds total_capital and available at 10,000,000 WST. The live fund was created by my first cycle at 13:01:40 and reads total_capital 10,000,156 = 10,000,000 seed + 140 + 16 in cycle contributions. No cycle, owner or ledger funded the 10M. CapitalDashboard.tsx:170-194 shows it with the caption 'Figures are the real capital fund … in virtual WST'. Wallet.tsx:76 shows 'Total Pool 10M WST' and 'Fund Health HEALTHY'. /fund/status has no basis field. (b) I wrote a malformed capital_fund.json. GET /api/v1/fund/status returned 200 with total_capital 10000000, available 10000000, allocation_count 0, fund_health HEALTHY. The real file was renamed to capital_fund.corrupt-* and a fresh pool took its place. That is the replace-on-unreadable class P1.15 set out to kill, and the surface says nothing. I restored the backup afterwards.
- **evidence:** agentic_core/api/capital_fund.py:37-58 (seed plus rename-aside) and :107-124 (status). Live: /fund/status → total_capital 10000156.0; corrupt probe → 10000000 HEALTHY [200]; the directory then held capital_fund.corrupt-eb80f403 plus a fresh capital_fund.json. apps/.../enterprise/CapitalDashboard.tsx:166-194. pages/profile/Wallet.tsx:76.
- **disclosed to the user:** No. The 'virtual WST' label is present, but neither the seed nor the reset is disclosed. The caption calls the figure 'the real capital fund'.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. capital_fund.py:37-58: when the store is absent, or cannot be parsed after load_json_tolerant, it is renamed aside to .corrupt-<id> and a fresh 10,000,000 WST pool is returned, with no field in the response saying so. Live /fund/status returned total_capital 10000156.0, fund_health HEALTHY, allocation_count 0, and no basis or seed field. CapitalDashboard.tsx:190-193 captions the figure 'the real capital fund … in virtual WST'. Wallet.tsx:76 shows 'Total Pool 10M WST' and :73 shows 'Fund Health'. The 'virtual' label is honest about the currency. It does not disclose that 10M of the 10.000156M is an unfunded code constant, or that an unreadable fund is reset to a HEALTHY fresh pool. That reset is the replace-on-unreadable class P1.15 set out to remove.
- **refuter's evidence:** agentic_core/api/capital_fund.py:37-58. Live GET /api/v1/fund/status → total_capital 10000156.0, HEALTHY. CapitalDashboard.tsx:166-194. Wallet.tsx:73-76. Assessor's backup file at C:/tmp/w476_r6/capital_fund_backup.json.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Start the fund at 0 plus real cycle contributions, or disclose the seed as an Owner-set virtual endowment with its own basis field. Read the store strictly and return 503 when unreadable, never a fresh pool.

### R6.4 · §8 self-curation: Sovereign Evolution Office (introspect → AI CEO → C-Suite verdicts → CoE/BTO roadmap) — **STUB** · tier 1

- **severity (assessor):** medium
- **claim:** §8, recorded W446: Workstation's own evolution is delivered by its own living organisation. The Sovereign Evolution Office introspects, then AI CEO triage, then C-Suite verdicts, then the CoE/BTO roadmap.
- **observed:** The cycle's C-Suite verdict is a code default, but the page shows it as the C-Suite's decision. Under the native floor, POST /api/v1/sovereign-evolution/cycle {focus:'audit', submit_to_change_control:false} returned 200. The introspection showed immune health 0.0, CRITICAL. The only CEO directive was the code default 'Routine organism health review' (P2, COO), with rationale 'No parseable CEO directives; defaulting to a maintenance sweep.' Its verdict 'proceed' and effort 'M' are also code defaults (sovereign_evolution.py:243-245 fills 'proceed' when no C-Suite line matched). The response still says curated_by ['AI CEO','C-Suite','CoE','BTO'] and items_proceeding 1. bto_roadmap is the floor's generic go-to-market frame ('Positioning wedge…', 'Channels … CAC to be measured'), not an organism roadmap. SovereignEvolution.tsx:160-180 renders this under 'CEO Directives · Curated by C-Suite' with the chain 'AI CEO → C-Suite → CoE → BTO' and a 'PROCEED · M' verdict badge. 'Items Proceeding 1' has tone good. A routine P2 sweep is what gets 'curated' while the organism is CRITICAL.
- **evidence:** Live POST /api/v1/sovereign-evolution/cycle → C:/tmp/w476_r6/sev.json. agentic_core/api/sovereign_evolution.py:205-211,228-248,272. apps/.../pages/evolution/SovereignEvolution.tsx:152,160-180.
- **disclosed to the user:** Partly. The rationale line admits the CEO directive was defaulted. The verdict and the 'curated by C-Suite' attribution are not marked as defaults. The BTO text carries the floor's header, but nothing says it ignores the request.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed from the assessor's saved response and the code. C:/tmp/w476_r6/sev.json holds a single directive, 'Routine organism health review', with rationale 'No parseable CEO directives; defaulting…', verdict 'proceed', effort 'M' and an empty execution_note. That is the code default at sovereign_evolution.py:243-245, which applies when no C-Suite line matched. The BTO roadmap is the native floor's generic 'Structured go-to-market frame … Positioning wedge' template. It carries the header 'native structured engine — owned', which does not say the request was ignored. The response still asserts curated_by ['AI CEO','C-Suite','CoE','BTO'] and items_proceeding 1. SovereignEvolution.tsx:152 shows Items Proceeding with tone good. :160-180 render 'CEO Directives · Curated by C-Suite', the chain, and a 'PROCEED · M' badge, which also falls back to 'proceed' in the UI at :179. The defaulted CEO directive is disclosed through its rationale. The C-Suite verdict attribution is not disclosed.
- **refuter's evidence:** C:/tmp/w476_r6/sev.json (ceo_directives, bto_roadmap). agentic_core/api/sovereign_evolution.py:205-211,228-248,262-272. apps/.../evolution/SovereignEvolution.tsx:152,160-180.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Mark defaulted directives and verdicts with a source field (for example verdict_source 'default: no C-Suite verdict parsed'). Render it as 'no verdict'. Drop curated_by tiers that returned nothing parseable.

### R6.5 · §12 venture investment (the user_projects stage) against §11 live compliance — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §12 with §11: the VSB reinvests in its own growth and in users, as a compliant entity, with compliance continuously live.
- **observed:** The user_projects stage invested in a VSB that had failed compliance and whose own economy was held. POST /economy/cycle {vsb_id:'w476-r6-a', revenue:1000, costs:100} put the full 105 WST user_projects budget into 'vsb:vsb-24267609d1' (Varroa Guard R2, score 0.62). At that moment GET /economy/living-vsbs showed this entity with compliance.verdict 'fail' and economy_held.held true ('compliance_fail_hold … no economy cycle runs until a re-screen clears it'). GET /economy/ventures/candidates lists it with metrics_source 'derived deterministically from live stage/status/governance'. real_candidates() never reads compliance standing: 'governed' means only that ent.board and ent.economy exist. /economy/transfer to the same held entity was also accepted (10 WST, 200). The cycle report and portfolio give no warning.
- **evidence:** Live cycle JSON venture_investment.positions[0]; /economy/living-vsbs compliance fail + economy_held; /economy/ventures/candidates. agentic_core/economy/ventures.py:50-114.
- **disclosed to the user:** No.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced live. /economy/living-vsbs lists vsb-24267609d1 (Varroa Guard R2) and vsb-0280b9f830 with compliance verdict 'fail' and economy_held.held true. /economy/ventures/candidates lists both as investable. The w476-r6-a board pack's venture_portfolio holds 105 WST in vsb:vsb-24267609d1. ventures.py:90-110 computes 'governed' only from ent.board and ent.economy and never reads compliance or the hold. The tier stays 2 rather than 1 because the positions do not claim any compliance standing. The shortfall is simply invisible to the user.
- **refuter's evidence:** Live GET /api/v1/economy/living-vsbs (2 of 4 fail+held). GET /economy/ventures/candidates (both present). GET /economy/board-pack?vsb_id=w476-r6-a venture_portfolio.holdings[0].id vsb:vsb-24267609d1. agentic_core/economy/ventures.py:50-114.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Exclude from real_candidates() any living VSB whose compliance verdict is fail or whose economy is held, and list it as excluded with the reason. Decide explicitly whether transfers into a held entity are allowed, and say so.

### R6.6 · §12 charity allocation: the Owner's four causes and the compliance screen — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §12: the four causes (WATER · Orphan · Conflict/disaster · Dawah) with a 100%-donation-only screen and Owner-set directives honoured.
- **observed:** Under the Owner's default directives Dawah gets nothing, while two causes the Owner never prioritised are funded. With priorities ['clean_water','orphan_sponsorship','conflict_relief','dawah'], the default top-5 cut gave Dawah 0 WST (boosted score 0.8482). Famine_food (0.8987) and emergency_health (0.8642) received grants, and the cycle report still printed 'priorities: [… dawah …]' beside them. The priority boost is +0.12 capped at 1.0, so two causes tie at 1.0 and the cap destroys ordering. Every grant carried compliance 'review' and was funded anyway, while the method string says 'every grant compliance-screened (halal/ethical)'. Only 'fail' excludes. donation_100pct_verified 'not_checked' is honestly disclosed.
- **evidence:** Live cycle w476-r6-a giving_back.grants (5 grants, no dawah, all compliance 'review'). GET /economy/charity/candidates (dawah ranked 7th of 7). agentic_core/economy/charity.py:44-60,130-136,151-175.
- **disclosed to the user:** Partly. The curated-weights provenance and not_checked are disclosed. Neither the exclusion of an Owner-priority cause nor the funding of 'review' verdicts is disclosed.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced live. GET /economy/charity/candidates ranks clean_water 1.0, conflict_relief 1.0, orphan 0.9845, famine_food 0.8987, emergency_health 0.8642, refugee_support 0.8487 and dawah 0.8482, so dawah is 7th and falls outside the default top-5 in allocate(). The boost is capped by min(1.0, base+0.12) at charity.py:135, and that cap makes two Owner priorities tie. At charity.py:162-168 only a 'fail' verdict excludes a grant, so 'review' grants are funded, while the method text claims every grant is compliance-screened. A priority is a boost, not a guarantee, so this is not tier 1. But the surface does not disclose that an Owner-named cause received nothing.
- **refuter's evidence:** Live GET /api/v1/economy/charity/candidates (scores above). agentic_core/economy/charity.py:130-136,148-175.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Guarantee each Owner-priority cause a place (rank priorities first, then fill), and hold 'review'-verdict grants for Owner review instead of posting them.

### R6.7 · §12 double-entry books, waterfall, materiality, virtual-WST honesty, unreadable ledger — **DELIVERED**

- **severity (assessor):** low
- **claim:** §12: six-stage waterfall with Stage 0 reserves (costs + 20% of revenue) and a 20/30/20/15/15 split; Owner-adjustable within template bounds and UEG-logged; material distributions held for Change Control; virtual WST; double-entry books with period close and CFO statements.
- **observed:** The economy books work as specified, checked by execution. Cycle w476-r6-a (revenue 1000, costs 100): reserves 200, distributable 700, split 140/210/140/105/105. Trial balance 1000/1000 balanced, balance sheet 200 = 200. A 10 WST transfer left the sender reserve_fund at 190. The P&L includes transfer_out 10 and net 190, and it still balances. /close-period rolled 190 into retained_earnings, set the next period's postings to 0, balanced 190/190, with ueg_logged true. Revenue 1,000,000 was held_for_change_control (CRITICAL, threshold 250k, cca-56a4ee92a7). A waterfall POST with owner 0.5 for entity_type charity was refused 400; for an unknown id it resolved to caller_claimed. With a corrupt ledger, /ledger, /board-pack, /cycle, /close-period and /transfer all returned 503 with 'nothing was changed', /status said ledger.available false, and /transfers/open said ledger_unreadable true. Owner payments: real_money_rails DISABLED. Every surface says 'Virtual/simulated WST'. Minor shortfalls: the waterfall POST note always says 'logged to the UEG' even though its UEG write is try/except-pass (economy.py:262-278; close-period already reports ueg_logged). The cash-flow statement books the transfer to reserves as an 'operating payment', showing net cash 0 against a 190 reserve asset. Ledger repair on a half-truncated test ledger quarantined it and recovered nothing (409), which it discloses.
- **evidence:** Live: /economy/cycle, /economy/ledger/w476-r6-a, /economy/board-pack?vsb_id=w476-r6-a (before and after close), /economy/transfer (xfer-32506e4b42), /economy/close-period, /economy/waterfall POSTs, corrupt-ledger probes, /economy/ledger/w476-r6-e/repair. agentic_core/api/economy.py:240-286,1168-1205,1289-1311. agentic_core/economy/ledger.py:459-474.
- **disclosed to the user:** Yes, for the virtual-WST nature and the unreadable-ledger states.
- **refutation: SURVIVED** (reproduced by the refuter). Spot-checked live: the w476-r6-a board pack shows revenue 1000, reserves 200, costs 100, distributed 700, split 140/210/140/105/105 (source entity_template), owner real_money_rails DISABLED, currency 'WST (virtual)', and a new period opened after close with 0 postings. The minor shortfall the assessor names is real. At economy.py:268-285 the UEG write is try/except-pass, but the returned note always says 'logged to the UEG'. That false claim appears only when the UEG write fails, so it does not change the verdict. The corrupt-ledger 503 probes were not re-run by me (read-only), so I rely on the assessor for those.
- **refuter's evidence:** Live GET /api/v1/economy/board-pack?vsb_id=w476-r6-a and /economy/status. agentic_core/api/economy.py:258-286.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Return ueg_logged from the waterfall POST instead of an unconditional claim. Label the reserve movement separately in the cash-flow statement.

### R6.8 · §17.5 the ten architecture invariants — **PARTIAL** · tier 3

- **severity (assessor):** medium
- **claim:** §17.5: user isolation · mandatory GaaS gate on every output · append-only DCS audit · arms-length agency · twin pre-validation before major change · torch optionality · single router mount · signal-bus atomicity · living plan ≤5-min staleness · KPI gate before customer delivery.
- **observed:** HOLD: - Single router mount: include_router appears only in app_mvp.py; cross_platform.py mentions it only in a docstring. - Append-only audit: GET /api/v1/gaas/ueg/verify gives valid true over 84 events; /api/v1/ueg/verify gives chain_valid true, tail_anchored. - Signal-bus atomicity: nervous.py has deque(maxlen) with threading.Lock, and immune.py has a lock. - Plan staleness: /api/v1/plan/state reconciled_at equals request time.  HOLDS IN FORM ONLY: - Twin pre-validation: /twin/models is empty, so every HIGH/CRITICAL change uses the health-gate fallback, whose composite >= 0.6 test cannot fail (finding 1).  NOT HELD (all stated in the vision's own §17.5 status paragraph, so no over-claim): - User isolation: the organism perimeter has 0 auth dependencies. heartbeat.py has 5 routes including /stop and /configure (which switches on auto_economy); organism_status.py has 6; sovereign_evolution.py 3; business_plan.py 9; swarm.py 7. - GaaS gate: only 19 of 57 api modules import gaas at all. - Torch optionality: agentic_core/avatars/__init__.py:1 → core/recirculation_orchestrator.py:24 → governance/uci_interceptor.py:11 → mjm/recursive_meta_learner.py:4 → mjm/hd_omni_learner.py:1 'import torch', unguarded. - KPI gate: no kpi_gate code exists (MISSING).  ARMS-LENGTH: not re-falsified by execution. Autonomous evolution routes through the CCA, and evolution_auto_apply is governed by the CCA (/heartbeat/status).
- **evidence:** grep include_router. Live /api/v1/gaas/ueg/verify and /api/v1/ueg/verify. /api/v1/twin/models → total 0. agentic_core/api/change_control.py:745-790. grep of Depends counts on api/heartbeat.py, organism_status.py, sovereign_evolution.py, business_plan.py, swarm.py. The torch import chain above. vision §17.5 status paragraph (lines 727-739).
- **disclosed to the user:** In the canon (vision §17.5 status). There is no user-facing surface claiming the invariants hold.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. include_router is found only in app_mvp.py plus a mention in cross_platform.py. /twin/models returns {models:[], total:0}, so every HIGH/CRITICAL change uses health_gate_default. heartbeat.py, organism_status.py and sovereign_evolution.py each have 0 Depends. The torch chain is real, though the path is avatars/__init__ → avatars/core/recirculation_orchestrator.py:24 → governance/uci_interceptor.py:11 → mjm/recursive_meta_learner.py:4 → mjm/hd_omni_learner.py:1 'import torch'. There is no kpi_gate. Vision §17.5 (lines 727-739) records the unheld invariants honestly. One small canon overclaim: it lists twin pre-validation as 'hold in code' with an 'honest health_gate_default fallback', and the health half of that fallback cannot fail (finding 0). The threat half can still fail on HIGH or CRITICAL. No user surface claims the invariants hold, so tier 3 stands.
- **refuter's evidence:** grep include_router. Live GET /api/v1/twin/models. grep -c Depends (0,0,0). The torch import chain as read. docs/WORKSTATION_IDBO_WHOLE_VISION.md §17.5 status. change_control.py:783.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add an owner/admin dependency on the organism control routes (heartbeat start/stop/configure, genome mutate, sovereign-evolution cycle). Lazy-import torch in hd_omni_learner. Keep the KPI gate as an Owner ruling (P3.0).

### R6.9 · §8 self-running, defending, healing: heartbeat, immune, self-healing — **PARTIAL** · tier 3

- **severity (assessor):** medium
- **claim:** §8 and §1: once established, the organism forever runs, defends, heals, learns and improves itself.
- **observed:** The heartbeat really runs. /heartbeat/status: running true, interval 60s, beats 1→7 between 12:57:43 and 13:03:44. Each beat does pulse, homeostasis and transformation_tick, UEG-logged. But everything beyond the pulse is opt-in: auto_evolve, auto_economy, auto_align, auto_compliance and auto_ship are all false by default, and HeartbeatMonitor.tsx:100-110 says 'All five are OFF by default'. /living-vsbs says 'autonomous_cycles false … OFF'. Defence is signal-only. With immune at health 0.0 / CRITICAL, the beat only fires an alert signal when health < 0.5 (heartbeat.py:186-190). Quarantine stays 'recommended' in the playbook and needs a manual POST /cca/immune-reconfigure. Self-healing only covers AI-model and provider circuits (gateway.py, native orchestrator.py:72). /organism/self-healing/status reported 0 circuits and overall_health null after minutes of traffic, so on this deployment 'heals itself' has nothing to heal, and the status says so honestly. The immune 'threats' here were compliance_fail events from other auditors' VSB births, so compliance verdicts are being counted as attacks.
- **evidence:** Live /api/v1/heartbeat/status (13:04Z), /organism/self-healing/status, /organism/status systems.immune. agentic_core/organism/heartbeat.py:170-240. apps/.../organism/HeartbeatMonitor.tsx:18-30,100-110.
- **disclosed to the user:** Yes. The levers are shown as OFF and self-healing health as null or unknown. The survival-instinct inertness (finding 2) is not disclosed.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed live. /heartbeat/status shows running true, beats 17, interval 60s, and auto_evolve, auto_economy, auto_align, auto_compliance and auto_ship all false. It shows last_recovery and last_self_healing null, and evolution_auto_apply disabled and governed by the CCA. heartbeat.py:185-190 only fires an alert signal when health < 0.5. /organism/self-healing/status reports overall_health null and 0 circuits, with an honest basis. I also confirmed the assessor's side observation. The immune hot_endpoint_tied list in /organism/status contains 'compliance:genesis', 'compliance:vsb_mobile' and other compliance entries, so VSB compliance verdicts feed the immune 'threat' count. That count then drives the composite health headline (finding 0).
- **refuter's evidence:** Live GET /api/v1/heartbeat/status, /organism/self-healing/status, /organism/status systems.immune.hot_endpoint_tied. agentic_core/organism/heartbeat.py:165-203.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Give CRITICAL immune a real autonomous response (quarantine behind an Owner lever). Wire self-healing circuits around the platform's own store and route failures, not just model calls. Classify compliance verdicts separately from immune threats.

---

*Regenerated by W476 from the audit workflow's journal. Every entry above is an observation against
the booted HEAD named in the header — routes executed, handlers and components read, stores counted —
not a claim read from another document. No browser was driven: statements about what a user SEES
(a chip's colour, a tab's default, a rendered badge) are reasoned from the component source, and the
assessors and refuters say so where it matters.*
