# Vision Fidelity Ledger — v3 (2026-09-05)

**Supersedes v2 (2026-09-02, baseline `d937dd37`) in full.** v2 predated W435–W445 — eleven
workstreams including the whole W437–W444 reach campaign — and prompt v11 said to weigh it
accordingly. This edition is regenerated from a fresh six-region assessment against a backend
booted from HEAD `06c51109` (port :8024, single-user mode, `AI_DISABLE_LOCAL=1`). Under that flag the
gateway routes every model call to the deterministic native floor — the configuration CI runs and
the one any machine without a local model gets (it is NOT the shipped default: with the flag unset
and Ollama discoverable, the gateway serves from the local model). What IS assessable on the floor
is whether every floor-served surface discloses it. One caveat the audit itself found (R3.0/R4.0):
a surface that bypasses the gateway — the v138 AI-CEO chat — reached the host's Ollama directly and
returned real llama3.2 prose during this audit, so 'the floor served every call' is true of the
gateway path, not of every route.

## How this document was generated — and what that means for reading it

Six assessors ran one vision region each against the booted HEAD, explicitly barred from three
sources: the vision's own §16, the previous edition of this ledger, and `AUTONOMOUS_PROGRESS.md`
(a record of intent, not proof). They executed routes, read handlers and components, and counted
stores. The ASSESSMENT was capped at ten findings per region, most consequential first — and every
region returned exactly ten, so **60 is the size of the cap, not the size of the gap**; a region's
eleventh-worst thing is not in this ledger. **Every finding was then attacked by an independent
refuter instructed to default to refuted** (v2 refuted six per region), who had to reproduce the gap (execute the
route, read the code, count the store) before letting it stand, and who was told to correct the
verdict UP or DOWN when the assessor had it wrong.

Reading rules that follow from the method:

1. **DELIVERED is understated by construction.** Assessors were told their job was the gap that
   remains, but to report DELIVERED where they verified it. Read the DELIVERED entries as the
   floor of what works, not the ceiling.
2. **Every one of the 60 findings was individually refuted.** The refuters reproduced
   56 as stated and overturned 4 — 2 moved to a
   HARSHER verdict (a DELIVERED claim that was not), 2 to a MILDER one (a STUB that was real
   machinery with an undisclosed shortfall). The verdict in each heading is the one the REFUTER
   stands behind; the assessor's original is shown where it differs.
3. **The floor is the environment.** A finding that says 'floor scaffold reached the user' is
   not a complaint that no model ran — it is a finding that the surface did not SAY so, or
   certified what it could not assess. That is the §15 principle-6 line, and it binds.
4. **Refuters reproduced with their OWN inputs.** Several verdicts below carry evidence from a
   second journey, VSB, change-record or transfer the refuter created on the live backend —
   a finding that reproduces under a second, independently chosen input is stronger evidence
   than one observation.

## Summary

| standing verdict (after refutation) | count | as assessed |
|---|---|---|
| STUB | 10 | 12 |
| MISSING | 3 | 3 |
| DOC_OVERCLAIM | 5 | 5 |
| API_ONLY | 0 | 0 |
| PARTIAL | 36 | 32 |
| DELIVERED | 6 | 8 |
| **total** | **60** | **60** |

Per region:

| region | sections | findings | STUB | MISSING | DOC_OVERCLAIM | API_ONLY | PARTIAL | DELIVERED |
|---|---|---|---|---|---|---|---|---|
| R1 | §10 + §11 | 10 | 1 | 0 | 2 | 0 | 4 | 3 |
| R2 | §4 + §13 | 10 | 3 | 0 | 0 | 0 | 7 | 0 |
| R3 | §5 + §17.3 + §17.4 | 10 | 2 | 2 | 0 | 0 | 5 | 1 |
| R4 | §6 + §7 + §17.2 | 10 | 1 | 0 | 1 | 0 | 8 | 0 |
| R5 | §1–§3, §3A, §9, §14, §15, §17.1 | 10 | 2 | 0 | 1 | 0 | 6 | 1 |
| R6 | §8 + §12 + §17.5 | 10 | 1 | 1 | 1 | 0 | 6 | 1 |

The distilled, actionable form of the surviving gaps is **prompt v11 rev 2's `<ledger>` and
`<delivery_plan>`** (`docs/FABLE_DELIVERY_PROMPT.md`). This document is the evidence base behind
them: every `<ledger>` item cites the entries here it rests on by region.index, and every plan
workstream carries the region.index entries it closes (or says it rests on another instrument —
the reach audit for the scatter, the Owner's hand for P4).

---

## R1 — §10 + §11 — the solution-quality bar, continuous compliance, and the faith-content constitution

**Assessor's region summary:** R1 (§10 solution-quality bar + §11 live compliance and the faith-content constitution), assessed against HEAD 06c51109 on the live :8024 backend (floor-only, AUTH off) by executing every route in scope and reading the handlers and the four named components; UI behaviour is reasoned from source, not driven. The QEP core in the Religion domain is genuinely DELIVERED and honest end to end (sourced Qur'an text, real ayah-count refusals, real SM-2 hifz, written-recall that says nothing about recitation, a translation route that refuses before touching the gateway, floor-served lessons labelled as outlines, basis lines on every figure). The QMS defect→correct→re-verify loop and the federated compliance screen are DELIVERED and wired to UI. The gap that remains clusters in four places. (1) The Tafsir surface is the constitution's weakest point: on the floor it serves a scaffold under a 'Translation' heading, the sourced Arabic, range-cap note and any scholar-referral disclaimer never reach the screen, and it sits beside a green QMS chip — the same rule that makes /translate refuse is not applied here. (2) The compliance screen's 'Constitutional (gaas.v5)' verdict is a STUB for content — the gate never reads the subject, so it is a green pass on every check — and the 'UK Legal' engine is an eight-term employment vocabulary with an input-invariant audit hash, labelled as more. (3) The living-QMS gate cannot fail on floor output (heading echo → coverage 1.0): 174/174 native deliverables in the store pass, 'verified' is sealed as met, and the Deliverables/DomainTool chips are green — Genesis already fixed this class (verified:null 'not assessable') but the shared gate did not. (4) §10 itself is a DOC_OVERCLAIM against any instrument that exists: 12 of 16 criteria are never measured, and Genesis attests modelled/simulated/ranked as met on runs whose own payload says the three candidates were identical and the ranking carried no information — though this is disclosed on-surface with honest counts and the identical-candidates note. Continuous compliance exists as a heartbeat beat but is opt-in, off, VSB-only, and has run once. A compliance-FAILed deliverable is correctly routed to Change Control yet still lists green and exports with no marker. Net: the honesty layer is real and mostly reaches the user; the remaining misleads are a handful of green chips (QMS on floor, constitutional pass, list-level QMS on a failed deliverable) and one sacred-text surface that renders less than its own API discloses.

### R1.0 · §11 faith-content constitution rules 2/4/5 — Religion Hub → Tafsir tab (POST /api/v1/religion/quran-tafsir rendered via DomainTool) — **PARTIAL**

- **severity (assessor):** HIGH — sacred content; the one surface where the vision says honesty binds hardest
- **claim:** Floor-served output is never presented as scholarship or translation; a truncated range is disclosed as the covered range; AI content is labelled with provenance and learners are directed to qualified teachers.
- **observed:** The backend half is real: the Arabic is fetched from alquran.cloud and injected as given material, 1:8 is refused (422 'surah 1 has 7 ayaat — ayah 8 does not exist'), 2:1-20 is capped with range_note 'covers 2:1-10, not the requested end 20', and the prompt forbids producing Arabic from memory. But on the floor (the live environment) the returned `tafsir` field is a scaffold whose '## Translation' section reads 'Structured service frame for: … Quality measure for provided text … SLAs & assurance', and '## Exegesis' / '## Linguistic Analysis' are 'Native structured content for …' bullets. The UI (DomainTool) renders ONLY `result.tafsir` and `result.disclaimer`: `arabic_text`, `arabic_source`, `reference` and `range_note` are never shown, and the tafsir route returns NO `disclaimer` (fatwa-research, hadith-study and halal-review all do). So a user sees a floor template under a 'Translation' heading, no sourced Arabic, no 'consult a qualified scholar' line, and never learns that their 20-ayah request was cut to 10. The amber 'structured floor — not model analysis' badge is the only disclosure; beside it sits a GREEN 'QMS pass' chip while the screen's own compliance verdict was 'review' (hidden in the tooltip). Rule 4 is applied inconsistently: /api/v1/qep/translation/translate refuses (503) on the floor; /religion/quran-tafsir serves a floor 'Translation' section.
- **evidence:** Executed POST /api/v1/religion/quran-tafsir {surah:1,ayah_start:1,ayah_end:3} → 200, ai_provenance.served_by='native', quality_assurance.compliance_overall='review', response keys have no 'disclaimer'; tafsir text saved at C:/tmp/r1/tafsir.json shows the '## Translation … Structured service frame' scaffold. {surah:1,ayah_start:8} → 422. {surah:2,ayah_start:1,ayah_end:20} → reference 'Surah 2:1-10' + range_note. agentic_core/api/religion.py:147-239 (no disclaimer key; cf. lines 94-97, 139-143, 289-292 for the other tools). apps/workstation-superapp/src/components/DomainTool.tsx:118-119, 237-239, 245 (renders resultKey + disclaimer only; QMS chip keyed on qms_gate_passed). ReligionHub.tsx:134-147 (resultKey='tafsir').
- **disclosed to the user:** Partially — amber provenance badge only; the sourced Arabic, the range cap, and any scholar-referral disclaimer do not reach the screen; the green QMS chip contradicts the 'review' compliance verdict.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced in full. The backend half is real (sourced Arabic injected as given material, 1:8 refused 422, 2:1-20 capped to 2:1-10 with range_note), but the tafsir route is the only Religion tool without a `disclaimer` key, and DomainTool renders only result[resultKey] + result.disclaimer, so arabic_text, arabic_source, reference and range_note never reach the screen. On the floor the visible text carries a '## Translation' section reading 'Structured service frame for: ... Offering ... SLAs & assurance' beside the amber badge, while a GREEN 'QMS pass' chip sits next to a compliance_overall of 'review' that lives only in a tooltip. One nuance the assessor missed: for 2:1-20 the floor's grounding snippet happened to echo the prompt's Arabic block (98 Arabic tokens in the tafsir text) whereas 1:1-3 echoed none — so sourced Arabic sometimes reaches the screen by accident of the floor's snippet choice, never by design and never labelled as the sourced text. PARTIAL with partial disclosure is the right verdict; the amber badge is genuinely rendered so this is not a STUB.
- **refuter's evidence:** Executed POST /api/v1/religion/quran-tafsir {surah:1,ayah_start:1,ayah_end:3} -> 200; response keys ['tafsir_id','reference','arabic_text','arabic_source','surah','ayah_start','ayah_end','approach','tafsir','ai_provenance','generated_at'] (no 'disclaimer'); ai_provenance.served_by 'native', ai_provenance.quality_assurance {qms_gate_passed:true, compliance_overall:'review'}; tafsir text '## Translation\nStructured service frame for: ... Quality measure for provided text ... SLAs & assurance'. {surah:1,ayah_start:8} -> 422 'surah 1 has 7 ayaat'. {surah:2,ayah_start:1,ayah_end:20} -> reference 'Surah 2:1-10', range_note present, tafsir text does not contain '2:1-10', 'Surah 2' or '20' and contains no consult/qualified/teacher/mufti referral. POST /api/v1/qep/translation/translate {text,target_language} -> 503 'will not be presented as a translation'. Code: agentic_core/api/religion.py:147-239 returns no disclaimer (cf. :94-97, :139-143, :289-292 for fatwa/hadith/halal); apps/workstation-superapp/src/components/DomainTool.tsx:118-119 (resultText = result[resultKey]), :237-241 (QMS chip green on qms_gate_passed; compliance only in title), :245 (result.disclaimer only); pages/domains/ReligionHub.tsx:134-147 (resultKey='tafsir'); grep of src for arabic_text|range_note|arabic_source -> zero .tsx consumers; lib/api.ts:65-73 provenanceBadge amber label 'structured floor — not model analysis'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** (1) Add a `disclaimer` to the tafsir response mirroring the fatwa one; (2) when served_by=='native', either refuse like /translation/translate or drop the 'Translation'/'Transliteration' headings and prepend a floor_note; (3) render arabic_text + arabic_source + range_note in the Tafsir tab (a dedicated panel or DomainTool `extraKeys`).
- **status now:** FIXED W456 (P1.8) — §11 rule 4 applied to the tafsir route as it was to /translation: on the floor the Transliteration and Translation sections are withheld from the prompt and cut from the text, named, with a floor_note; a disclaimer key; the tab renders the sourced Arabic (rtl), its source, the reference, the range note and the floor note; the QMS chip is three-state since W449.
- **erratum (W448, verified against the code):** the refuter's 'the tafsir route is the only Religion tool without a disclaimer key' is wrong — interfaith (agentic_core/api/religion.py:302-347) returns none either; two of the five Religion POST tools lack one. The verdict is unaffected.

### R1.1 · §11 compliance screen — 'Constitutional (gaas.v5)' framework verdict (POST /api/v1/compliance/check → GovernanceCenter → Compliance tab) — **STUB**

- **severity (assessor):** HIGH — a green 'pass' on a control that cannot read the subject
- **claim:** Compliance engines (… Constitutional/gaas) are integrated into every synthesis/generative/operational workflow, not bolted on.
- **observed:** ConstitutionalPolicyGate.validate(action_type, ctx) computes `intent = str(action_type or ctx['intent'])`; the compliance router passes action_type=req.kind (default 'content', always truthy), so the subject text is never consulted. Every content check therefore returns 'Constitutional · pass · Constitutional gate clear.' — the ComplianceChecker paints it green with a CheckCircle. The prohibited list is nine agent-action tokens (delete_all, wire_funds, rm_rf_root …), not a content policy; and the router's `except Exception: pass` leaves a pass on engine failure.
- **evidence:** Executed POST /api/v1/compliance/check {subject:'wire_funds exfiltrate drop_database disable_governance bypass_constitution', kind:'content'} → constitutional 'pass'; {subject:'a bakery', kind:'wire_funds'} → 'fail' (proves only the kind is read). agentic_core/gaas/v5/policy_gate.py:15-19 (_PROHIBITED_INTENTS), :36 (intent derivation); agentic_core/api/compliance.py:188-196 (validate(req.kind, {'intent': subject[:80]}), silent except). UI: apps/workstation-superapp/src/pages/governance/ComplianceChecker.tsx:67-80 renders status/reason verbatim.
- **disclosed to the user:** No — rendered as a passing framework identical in appearance to the real Sharia/EHS screens.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced exactly. ConstitutionalPolicyGate.validate derives intent as str(action_type or ctx['intent']); the router passes action_type=req.kind whose default 'content' is always truthy, and the UI posts {subject} only, so from the Compliance screen the subject text is never consulted and the constitutional verdict is unconditionally 'pass · Constitutional gate clear.' The prohibited set is nine agent-action tokens, not a content policy, and the router's bare except leaves 'pass' on engine failure. Rendered green with CheckCircle, identical in appearance to the real Sharia/EHS verdicts, with no disclosure — a surface that pretends. STUB stands.
- **refuter's evidence:** Executed POST /api/v1/compliance/check {subject:'wire_funds exfiltrate drop_database disable_governance bypass_constitution', kind:'content'} -> constitutional status 'pass' reason 'Constitutional gate clear.' (overall 'review' only because ethical 'value: review'); {subject:'a bakery', kind:'wire_funds'} -> constitutional 'fail' "Intent 'wire_funds' is constitutionally prohibited" (only the kind is read). Code: agentic_core/gaas/v5/policy_gate.py:15-19 (_PROHIBITED_INTENTS, nine tokens), :36 (intent = str(action_type or context.get('intent',''))); agentic_core/api/compliance.py:88-92 (kind default 'content'), :188-196 (validate(req.kind, {'intent': req.subject[:80]}), const_status defaults 'pass', except Exception: pass); UI pages/governance/ComplianceChecker.tsx:24 posts body JSON.stringify({ subject }) only, :67-80 renders status/reason verbatim with STATUS_ICON.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Report the framework as 'not applicable to content — gaas.v5 gates agent actions, not text' (status 'not_checked', amber) unless kind is an action; or pass the subject through validate_output/_UNSAFE_OUTPUT and label what it actually checks.
- **status now:** FIXED W455 (P1.7) — the constitutional row is 'not_checked — not applicable to content; gaas.v5 gates agent actions' (the output screen still runs and fails an unsafe pattern); an action kind meets the action gate; an engine raise is 'error', never a pass; the overall never counts not_checked as a pass.

### R1.2 · §10 'verified · tested · validated' — the living-QMS gate as applied to Deliverables (/api/v1/deliverables/produce) and every domain tool (ai_text → assure_delivery) — **PARTIAL**

- **severity (assessor):** HIGH — a green certification that cannot fail on the environment that serves 100% of calls
- **claim:** Every solution is verified · tested · validated; delivered end-to-end to the Solution-Quality Bar.
- **observed:** assure_delivery measures coverage as 'declared section names present in the text'. The deterministic floor echoes the prompt's '## Section' lines verbatim into its output, so coverage is 1.0 by construction and the stub regex never matches the floor's own vocabulary. The gate therefore passes every floor-served delivery, records 'verified: met True, basis: QMS gate on real coverage/stub metrics', and seals that into the DCMS quality record. Genesis already recognised this defect class (W436: floor-served stage → verified:null 'not assessable') but the shared gate did not get the same fix, so the two surfaces disagree about the same floor output. UI: Deliverables shows GREEN 'Living-QMS gate: pass · cov 100%' and a green '● QMS' list dot beside the amber floor badge; DomainTool shows green 'QMS pass'.
- **evidence:** Executed produce {type:'report', brief:'A halal artisan bakery in Leeds…'} → served_by 'native', content is the heading-echo scaffold ('## Executive Summary\n## Background…' repeated inside every section), qms_gate_passed true, delivery_coverage 1.0, bar_measured '4 measured · 0 attested · 12 not measured' with 'verified' met:true. Store audit data/deliverables.json: 300 rows; 174 native rows all qms_gate_passed=true, ZERO native rows false (the only false is a verbatim-ingest). agentic_core/vbs/quality.py:112-119 (_delivery_coverage), :144-160 (gate + seal); agentic_core/api/genesis.py:87-113 (the fix that exists only there). Deliverables.tsx:211-219, 185-187; DomainTool.tsx:237-241.
- **disclosed to the user:** Partially — the amber 'structured floor' badge and the tooltip's per-criterion basis exist, but the gate chip itself is green and says 'pass', and the sealed record claims 'verified'.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced by execution on the live backend: a floor-served report is a heading-echo scaffold, yet the gate reports delivery_coverage 1.0, qms_gate_passed true, and seals 'verified: met True, basis QMS gate on real coverage/stub metrics' into the DCMS record; the UI paints 'Living-QMS gate: pass · cov 100%' green and a green list dot beside the amber floor badge, with nothing on Deliverables.tsx mentioning the floor or the gate's vacuity. The genesis stage verifier has the floor fix (verified:null) but assure_delivery does not — and I confirmed the two instruments disagree on the SAME Genesis payload (every stage 'not assessable', gate 'verified' met:true). One evidence correction: the store the assessor counted (data/deliverables.json, 300 rows) is the repo's historical store, last modified Aug 30; the live backend at :8024 writes to C:/tmp/hb18, whose store shows the same pattern (8 native rows, all qms_gate_passed=true, zero false). PARTIAL with partial disclosure stands.
- **refuter's evidence:** Executed POST /api/v1/deliverables/produce {type:'report', brief:'A halal artisan bakery in Leeds...'} -> id deliv-290844f5, served_by 'native', content begins '## Executive Summary ... ## Executive Summary\n## Background\n## Analysis\n## Findings\n## Recommendations\n## Next Steps' (headings echoed inside sections), quality.delivery_coverage 1.0, stub_found false, qms_gate_passed true, bar_measured.summary '4 measured · 0 attested · 12 not measured', criteria.verified {met:true, source:'gate'}. Genesis run (POST /api/v1/genesis/journey) -> stages_verified '0/0', stages_floor_served 5, every stage_verifications[*].verified null 'not assessable — floor-served', yet quality_assurance.quality.bar_measured.criteria.verified {met:true, measured:true}. Stores: repo data/deliverables.json 300 rows -> Counter {(native,True):174, (native,None):124, (verbatim-ingest,True):1, (verbatim-ingest,False):1}; live C:/tmp/hb18/deliverables.json 10 rows -> {(native,True):8, (verbatim-ingest,True):2}; GET /api/v1/deliverables total 10 confirms the live dir. Code: agentic_core/vbs/quality.py:104-111 (_delivery_coverage = declared section names present), :60-64 (measured('verified', qms_passed, 'QMS gate on real coverage/stub metrics')), :144-160 (gate + DCMS seal); agentic_core/api/genesis.py:87-113 (floor fix exists only in _verify_stage). UI pages/Deliverables.tsx:185-187 (green '● QMS' list dot), :211-219 (green 'Living-QMS gate: pass · cov 100%'); grep for floor|cannot fail|vacu in Deliverables.tsx -> none.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Thread served_by into assure_delivery; when the content was floor-served set qms_gate_passed=None and 'verified'/'specifically designed' to source 'none' with basis 'not assessable — floor emits the requested headings' (exactly as genesis._verify_stage does), and render the chip slate '—' instead of green.
- **status now:** FIXED W449 (P1.1) — assure_delivery takes served_by; floor-served content returns qms_gate_passed=None with basis 'not assessable', nothing counted as a gate run, the record still sealed (now per delivery: content hash + server inside the seal); every chip renders slate '—' with the basis. Guard: test_w449_floor_served_gate_is_not_assessable_both_ways.

### R1.3 · §11 — compliance FAIL on a material deliverable: routed to Change Control but the artifact still ships unmarked (Deliverables list + /export) — **PARTIAL**

- **severity (assessor):** MEDIUM-HIGH — a haram brief becomes a clean-looking exported document
- **claim:** Compliance engines are integrated into every operational workflow; solutions are ethically evaluated; founding ethics (Halal/Sharia) are enforced in the architecture, never as decoration.
- **observed:** A 'fine wine subscription club with a casino night' brief is produced: sharia_halal FAIL, overall FAIL, compliance_routed_to_cca true — and a real CCA request is queued (verified). But the deliverable is stored with qms_gate_passed=true, appears in the list with a GREEN '● QMS' dot (the list summary carries qms_gate_passed but not compliance), exports as md/html/pdf/docx with no compliance marker anywhere in the document, and the deliverable record does not carry the cca_id, so nothing on the artifact ties it to the pending review. Only the detail pane shows a red 'compliance: fail' chip.
- **evidence:** Executed produce {brief:'A fine wine subscription club with a casino night, for London'} → id deliv-a9f023e1, verdicts sharia_halal fail / ethical review, compliance_routed_to_cca true; GET /api/v1/cca/queue → cca-16ffc5a48a 'Compliance FAIL on deliverable delivery', status 'submitted', submitted_by 'compliance_screen'; GET /api/v1/deliverables → the row shows qms_gate_passed true and no compliance field; GET /api/v1/deliverables/deliv-a9f023e1/export?format=md → 200, body has no compliance line. agentic_core/vbs/quality.py:180-205 (route-to-CCA, flag never block); deliverables.py:302-305 (list summary), :318-330 (_to_markdown); Deliverables.tsx:185-187, 250-255.
- **disclosed to the user:** Partially — red chip in the detail pane only; list and every export format are silent; the CCA link is invisible from the deliverable.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced end to end on the live backend. The wine/casino brief produces sharia_halal FAIL, compliance_routed_to_cca true, and a real MEDIUM-tier CCA request is queued at the same second — but the deliverable is stored with qms_gate_passed=true, the list summary carries qms_gate_passed and no compliance field (so the list dot is green), the md export contains no compliance marker at all, the deliverable record carries no cca_id, and the CCA request carries no deliverable id (only affected_systems ['compliance','deliverable']) — nothing ties artifact to review in either direction. Only the detail pane's red 'compliance: fail' chip discloses it. PARTIAL with partial disclosure stands; flag-never-block is by design (quality.py comment) but the artifact shipping unmarked is the shortfall.
- **refuter's evidence:** Executed POST /api/v1/deliverables/produce {type:'report', brief:'A fine wine subscription club with a casino night, for London'} -> id deliv-264ad375, quality.qms_gate_passed true, compliance.overall 'fail', verdicts sharia_halal fail / ethical review, compliance_routed_to_cca true, no cca_id/cca key anywhere in the record. GET /api/v1/cca/queue -> cca-b0f73639d4 submitted_at 2026-09-05T11:39:38Z status 'submitted' submitted_by 'compliance_screen' title 'Compliance FAIL on deliverable delivery' (deliverable updated_at 2026-09-05T11:39:38Z — same second). GET /api/v1/deliverables -> row {id:'deliv-264ad375', served_by:'native', qms_gate_passed:true} with no compliance field. GET /api/v1/deliverables/deliv-264ad375/export?format=md -> 2592 bytes, grep -i 'compliance|sharia|fail|change control|cca' -> no matches. Code: agentic_core/vbs/quality.py:180-218 (FAIL -> submit_change with title/description/affected_systems only; sets compliance_routed_to_cca True; never stores the cca id); agentic_core/api/deliverables.py:302-306 (list summary fields), :318-330 (_to_markdown: title/provenance/brief/content/footer only). UI pages/Deliverables.tsx:185-187 (list dot keyed on qms_gate_passed), :250-255 (red compliance chip in detail pane only).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add `compliance_overall` to the list summary and export subtitle/header (e.g. '§11 compliance: FAIL — under Change Control review cca-…'), store the cca_id on the deliverable, and turn the list dot red when compliance is 'fail'.
- **status now:** FIXED W455 (P1.7) — a §11 FAIL rides on page one of every export ('COMPLIANCE VERDICT: FAIL … NOT cleared for use' with the Change-Control id, whose id the gate now keeps), on the list row and on the download button. Watermarked, not blocked — stated as a choice.

### R1.4 · §10 Solution-Quality Bar — 16 criteria vs what any instrument measures; Genesis attests modelled/simulated/ranked on a run whose own stage-5 says no comparison happened — **DOC_OVERCLAIM**

- **severity (assessor):** MEDIUM — disclosed on-surface, but the vision text and the sealed record both overstate
- **claim:** Every solution is specifically designed · modelled · simulated · optimised · categorised · ranked; best-in-class · innovative · effective · safe · efficient · commercially viable · compliant; verified · tested · validated.
- **observed:** On deliverables the gate measures 4 of 16 (specifically designed, verified, compliant, safe) and reports '12 not measured' — best-in-class, innovative, effective, efficient, commercially viable, optimised, tested, validated have no instrument anywhere. On Genesis (11/11 calls floor-served) the three candidates came back byte-identical (candidates_distinct 1), the ranking tied and was 'resolved by list order — NOT evidence', every stage verification is null/not assessable — yet _bar_evidence still attests 'modelled', 'simulated' (twin returned 1200 identical chars ×3) and 'ranked' (the tie sentence itself), and _measure_bar records each as met:true (source 'caller'), sealed into the QMS record. The UI renders '§10 bar: 4 measured · 4 attested · 8 not measured'. The Genesis UI does replace the ranked cards with the amber 'All 3 candidates returned IDENTICAL text…' note and shows 'The deterministic native floor served 11 of these calls' above the results — genuinely honest — but the bar chip still counts 'ranked' as attested-met.
- **evidence:** Executed POST /api/v1/genesis/journey {problem:'A halal artisan bakery…', establish:false} → stage_5.candidates_are_alternatives false, tie.detected true, selection_basis 'TIE at 0.891 … resolved by list order, NOT by evidence', stages_verified '0/0', stages_floor_served 5, bar_measured.summary '4 measured · 4 attested · 8 not measured', attested_criteria ['categorised','modelled','ranked','simulated'] with 'ranked' basis = the tie sentence. agentic_core/api/genesis.py:436-462 (attestations; 'ranked' and 'modelled' unconditional), agentic_core/vbs/quality.py:88-95 (attested → met:true). GenesisJourney.tsx:577-596, 665-671, 756-761.
- **disclosed to the user:** Yes, largely — counts, tooltips and the identical-candidates note are on screen; the residual mislead is 'ranked/simulated' counted as met when the payload says the ranking carries no information.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on my own Genesis run: the three candidates came back byte-identical (candidates_distinct 1, candidates_are_alternatives false), the ranking tied and was 'resolved by list order, NOT by evidence', all five stages are floor-served and 'not assessable' — yet _bar_evidence attests 'modelled' and 'ranked' unconditionally (ranked's basis IS the tie sentence) and 'simulated' passes its only guard (>=200 chars per candidate) on three identical 1200-char twin outputs, and _measure_bar records each attested criterion as met:true. The UI is genuinely honest about most of it (floor-served count, stages_note, the identical-candidates notice replacing the ranked cards, the 'measured · attested · not measured' chip and tooltip), so the residual mislead is narrow: 'ranked'/'simulated'/'modelled' counted as attested-met on a run whose own payload says no comparison happened. 8-12 of the 16 criteria have no instrument anywhere; the vision's §10 bar claims more than the system measures. DOC_OVERCLAIM stands.
- **refuter's evidence:** Executed POST /api/v1/genesis/journey {problem:'A halal artisan bakery in Leeds...', establish:false} -> stage_5_model_simulate_rank: candidates_distinct 1, candidates_are_alternatives false, tie {detected:true, tied_candidates:[pragmatic,innovative,lean], resolved_by:'list order — NOT evidence'}, selection_basis 'TIE at 0.897 across 3 candidates ... resolved by list order, NOT by evidence', 3 candidates each simulation length 1200 with 1 distinct simulation text; stages_verified '0/0', stages_floor_served 5, ai_provenance.served_by {native: 11}; quality.bar_measured.summary '4 measured · 4 attested · 8 not measured', attested_criteria ['categorised','modelled','ranked','simulated'], criteria.ranked {met:true, source:'caller', basis:'caller evidence: TIE at 0.897 ... NOT by evidence ...'}, criteria.simulated {met:true, basis:'digital-twin forward-simulation returned 1200-1200 chars per candidate'}, optimised/tested/validated met:null. Code: agentic_core/api/genesis.py:436-462 ('modelled' and 'ranked' unconditional; 'simulated' guarded only by length>=200; 'optimised' correctly withheld on tie; 'tested'/'validated' withheld because stages_verified 0 != 5); agentic_core/vbs/quality.py:88-95 (attested -> met:true, source 'caller'). UI pages/synthesis/GenesisJourney.tsx:577-596 (floor-served card + stages_note), :608-609 ('floor-served, not assessable'), :665-671 (identical-candidates notice replaces ranked cards), :756-761 ('§10 bar: {summary}' chip with measured/attested tooltip).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Do not attest 'ranked'/'optimised' when tie.detected or candidates_are_alternatives is false; do not attest 'simulated' when all twin outputs share one digest or were floor-served; in _measure_bar record attested criteria with met:None. Reword vision §10 to 'measured where an in-house instrument exists; the record names what was not'.
- **status now:** attestation half FIXED W449 (ledger 3.11) — Genesis withholds modelled/simulated/ranked/optimised on a tie or identical candidates with the reason (test_w449_bar_attestations_withheld_on_tie_both_ways); the §10 wording half is an OWNER RULING (P3.0).

### R1.5 · §11 'continuously monitored and evaluated live compliance' — the organism heartbeat's auto_compliance beat — **PARTIAL**

- **severity (assessor):** MEDIUM
- **claim:** Continuously monitored and evaluated live compliance to legal, regulatory, international and industry standards — AI-mediated, biomimetically supported, timely.
- **observed:** The continuous mechanism exists (heartbeat._compliance_beat re-screens one VSB per beat, round-robin, with regression → immune reflex) but it is opt-in and OFF on this deployment (auto_compliance false, last_compliance null after 48 beats); it covers established VSBs only — deliverables and domain-tool outputs are screened once at production and never again; the history store holds 5 VSBs each re-screened exactly once, all on 2026-08-28. The screens themselves are deterministic keyword rules, not standards tracking.
- **evidence:** GET /api/v1/heartbeat/status → running true, beats 48, auto_compliance false, last_compliance null; data/vsb_compliance_history.json → 5 entries, 1 history record each (2026-08-28T21:24:59Z). agentic_core/organism/heartbeat.py:246-256. UI: pages/organism/HeartbeatMonitor.tsx:28 ('Self-defend' toggle); pages/enterprise/VSBEconomy.tsx:549 tooltip 'Not screened is NOT the same as clean'.
- **disclosed to the user:** Yes on the VSB and Heartbeat surfaces; nothing on Deliverables says its screen was a one-off.
- **refutation: SURVIVED** (reproduced by the refuter). The verdict holds but the evidence needs correcting. Reproduced: the continuous mechanism exists (heartbeat._compliance_beat, round-robin re-screen, regression -> immune) but is opt-in and OFF on this deployment (auto_compliance false, last_compliance null — now after 504 beats, not 48), covers established VSBs only, and every VSB in the history store was screened exactly once. Corrections: the dates are not 'all on 2026-08-28' — the repo store's 5 entries span 2026-08-28 to 2026-09-01, and the LIVE backend's store (C:/tmp/hb18/vsb_compliance_history.json, which the assessor did not cite) holds 4 VSBs screened once each between 2026-09-04 and 2026-09-05. Disclosure is honest on the VSB and Heartbeat surfaces (a freshly registered live VSB shows never_screened:true and the UI renders 'not screened' with the 'Not screened is NOT the same as clean' tooltip); nothing on Deliverables says its screen was a one-off. PARTIAL stands.
- **refuter's evidence:** GET /api/v1/heartbeat/status -> running true, beats 504, interval_seconds 60, auto_compliance false, last_compliance null. Repo data/vsb_compliance_history.json -> 5 VSBs x 1 record: 2026-08-28T21:24:59Z, 2026-08-30T13:20:38Z, 2026-08-30T13:21:14Z, 2026-09-01T01:05:05Z, 2026-09-01T13:05:18Z. Live C:/tmp/hb18/vsb_compliance_history.json -> 4 VSBs x 1 record: 2026-09-04T20:38:33Z, 2026-09-04T20:44:48Z, 2026-09-05T06:42:04Z, 2026-09-05T11:38:19Z. GET /api/v1/economy/living-vsbs -> vsb-39bfd8035c compliance {verdict:null, screened_at:null, never_screened:true}. Code: agentic_core/organism/heartbeat.py:246-256 (if self.auto_compliance: self._compliance_beat()). UI pages/organism/HeartbeatMonitor.tsx:28-29 ('Self-defend' toggle for auto_compliance); pages/enterprise/VSBEconomy.tsx:547-555 ('not screened' + tooltip 'Not screened is NOT the same as clean'); grep of pages/Deliverables.tsx for screen|re-screen|one-off -> only the per-record compliance chip, no re-screen statement.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Default auto_compliance on (it is cheap and deterministic), extend the beat to living deliverables, and show 'last screened <time>' on each deliverable/VSB card.

### R1.6 · §11 compliance engine depth — 'UK Legal (London)' and 'Sharia/Halal' engines behind the federated screen — **DOC_OVERCLAIM**

- **severity (assessor):** MEDIUM — labels imply legal engines; the substance is keyword lists
- **claim:** Live compliance to legal, regulatory, international, and industry best-practice standards and guidance.
- **observed:** The 'UKLegalPrecisionEngine (invoked, statute vocabularies + SHA3-512 audit)' has three statutes — EqualityAct2010, ERA1996, ACASCode — i.e. eight employment-law terms (discrimination, harassment, victimisation, unfair_dismissal, redundancy, wage_deduction, disciplinary, grievance). 'a money laundering scheme' and 'process personal data without consent and breach GDPR' raise no engine flag (laundering is caught by the router's own 9-word regex; GDPR only reaches 'regulatory review'). The audit hash shown in every reason ('audit ed0614b07becece9…') is identical for a halal bakery, a laundering scheme and a produced deliverable — it hashes the (empty) flag set, not the subject — and only changes when an employment term hits. The Halal engine is a substring loop over a haram_content_filter list plus interest_rate>0. Regulatory/EHS are regexes. The frameworks card and each verdict's '(engine-backed)' suffix read as more than this.
- **evidence:** Python probe of UKLegalPrecisionEngineImpl(configs/legal_precision.yaml): statutes = {EqualityAct2010:[…], ERA1996:[…], ACASCode:[…]}; flags [] for the laundering and GDPR texts. Executed /api/v1/compliance/check for three subjects → uk_legal reasons all carry 'audit ed0614b07becece9…' except the employment text ('STATUTORY_BREACH: ERA1996 … audit 3fb19a52f4240436…'). agentic_core/api/compliance.py:29-57 (regexes + framework labels), :135-151; agentic_core/business/governance/halal_compliance_officer.py:18-35.
- **disclosed to the user:** No — the Frameworks card and verdict suffixes present engine-grade coverage; nothing says 'employment statutes only' or that the audit hash is not over the subject.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced exactly, and it is slightly worse than described: configs/legal_precision.yaml declares `rules:` (EMP/CIV/TAX/GDP entries) but no `statutes:` key, so UKLegalPrecisionEngineImpl silently falls to its hardcoded default of three statutes / eight employment terms — the yaml's own GDPR and Limitation Act rules are loaded into self.rules and never consulted by validate(). 'a money laundering scheme' and the GDPR text raise zero engine flags (laundering is caught by the router's 9-word _ILLEGAL regex; GDPR only reaches regulatory 'review'). The audit hash is sha3_512 over {intent_type, violations, layer} — the subject is never hashed — so every clean subject shows the identical 'audit ed0614b07becece9…'. The Halal engine is a three-substring loop plus interest_rate>0. The frameworks card and the '(engine-backed)' / 'audit …' suffixes present engine-grade, per-subject coverage. The vision's 'live compliance to legal, regulatory, international, and industry best-practice standards' claims far more than this; DOC_OVERCLAIM stands, with the audit-hash presentation compounding it at the surface (disclosed=No).
- **refuter's evidence:** Direct probe: UKLegalPrecisionEngineImpl('configs/legal_precision.yaml').statutes == {EqualityAct2010:[discrimination,harassment,victimisation], ERA1996:[unfair_dismissal,redundancy,wage_deduction], ACASCode:[disciplinary,grievance]}; flags/violations [] and audit ed0614b07becece9 for 'a money laundering scheme', 'process personal data without consent and breach GDPR', and 'A halal artisan bakery in Leeds'; 'unfair dismissal and harassment of an employee' -> flags [harassment, unfair_dismissal], violations [STATUTORY_BREACH: EqualityAct2010, STATUTORY_BREACH: ERA1996], audit f3d65e6ee733d423. Executed POST /api/v1/compliance/check for the same four subjects: uk_legal reasons carry 'audit ed0614b07becece9…' for all but the employment text; laundering -> uk_legal fail via "Unlawful element: 'laundering'" (regex); GDPR -> uk_legal pass, regulatory review. Code: agentic_core/legal/precision_engine.py:17-27 (config.get('statutes', default)), :36-72 (validate; audit_data = {intent_type, violations, context.layer}); configs/legal_precision.yaml top-level key 'rules' only; agentic_core/api/compliance.py:29-46 (_HARAM/_ILLEGAL/_REGULATED/_EHS regexes), :48-57 (_FRAMEWORKS labels 'UKLegalPrecisionEngine (invoked, statute vocabularies + SHA3-512 audit)'), :135-151 (flag derivation from statute vocabularies, audit suffix); agentic_core/business/governance/halal_compliance_officer.py:11-35 (haram_content_filter ['alcohol','gambling','interest-based-finance'], interest_rate>0). GET /api/v1/compliance/frameworks returns the same labels.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Relabel: 'UK Legal — keyword screen + employment-statute vocabulary (Equality Act 2010 / ERA 1996 / ACAS); not legal advice'; hash the subject into the audit; return 'review — no engine covers this area' for anything outside the vocabularies instead of 'pass'.
- **status now:** FIXED W455 (P1.7) — the Frameworks card labels each check by what it does (keyword screens, statute vocabulary, an action gate; 'not legal advice', 'not a certification'); the audit hash is SHA3-512 over the subject; a subject outside every vocabulary is 'review — no engine covers this area', never pass; coverage is recorded per row.

### R1.7 · §11 faith-content constitution rules 1/2/3/5 — QEP core in the Religion domain (Qur'an text, hifz, written recall, translation refusal, lesson provenance) at /religion → QEP Flagship and /qep — **DELIVERED**

- **severity (assessor):** n/a (residual noted)
- **claim:** Qur'an Arabic never AI-generated (sourced from alquran.cloud, labelled); nonexistent ayaat refused; recitation never scored — written recall only with its scope stated; floor-served output labelled; learners directed to qualified teachers.
- **observed:** Verified by execution: /qep/suwar and /qep/ayah/1/1 return alquran.cloud text with source label; /qep/ayah/1/8 → 422 'surah 1 has 7 ayaat — ayah 8 does not exist'; /qep/ayah/115/1 → 400; hifz/schedule [1,9] → 422; hifz/review '1:9' → 422; hifz/review q=4 → real SM-2 (interval 1d, efactor 2.5), +5 XP persisted, progress shows 3 scheduled / 1 memorised with basis 'a review count, not a hifz certification'; uid path traversal → 404. tajweed/analyse: exact → 1.0, partial → 0.3421 with 'madd (kasra+ya) sequence … absent', English → comparable:false; every payload states 'says NOTHING about recitation'. translation/translate → 503 refusal BEFORE any gateway call; translation/status computes availability. tajweed/lesson → floor_served true + floor_note 'structured OUTLINE … not scholarly content'. QEPStudio renders the honest-state strip, the constraints line, the availability chip, basis lines on every figure, and the 'Recitation itself is never scored here' footer; TajwidCoach shows 'Recitation assessment unavailable'; LearnTeachModule and QEPIntelligence show 'NOT ESTABLISHED' / 'no scholars are verified' rather than figures. /api/qep/analytics/overview says 'measured: false' plainly.
- **evidence:** Executed all routes listed (responses recorded above); agentic_core/religious_domain/api.py:67-75, 246-264, 344-379, 384-436, 445-518, 553-619; agentic_core/religious_domain/tajwid/coach.py:45-85; agentic_core/api/qep_intelligence.py:180-251, 343-394; QEPStudio.tsx:156-171, 261-274, 305-331, 334-338; QEPReligionHub.tsx:59-107; LearnTeachModule.tsx:60-70, 84-92, 110-120.
- **disclosed to the user:** Yes — every basis/scope line reaches the screen.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced every route the assessor listed and the UI honesty strings. Qur'an text is fetched from alquran.cloud and labelled; nonexistent ayaat are refused at the ayah, hifz-schedule and hifz-review routes; written recall is a real Levenshtein comparison with an explicit 'says NOTHING about recitation' scope and a qualified-teacher referral, and refuses to score non-Arabic; SM-2 review is real and persisted with a 'review count, not a hifz certification' basis; translation refuses 503 before any gateway call and status computes availability; the tajweed lesson carries floor_served:true + floor_note; analytics says measured:false plainly; uid traversal is 404. The UI (QEPStudio honest-state strip, availability chip, basis tooltips; QEPReligionHub 'Recitation assessment unavailable'; LearnTeachModule 'No scholars are verified'; QEPIntelligence 'NOT ESTABLISHED') renders it. DELIVERED stands for the QEP core; the tafsir tool in finding 0 is a separate Religion-domain surface.
- **refuter's evidence:** Executed: GET /api/v1/qep/suwar -> 114 entries with ayah_count; GET /qep/ayah/1/1 -> text_arabic + source 'alquran.cloud'; /qep/ayah/1/8 -> 422 'surah 1 has 7 ayaat — ayah 8 does not exist'; /qep/ayah/115/1 -> 400. POST /qep/hifz/schedule {uid,surah_number:1,ayaat_range:[1,9]} -> 422 'range must lie within [1, 7]'; [1,3] -> 3 scheduled. POST /qep/hifz/review {ayah_ref:'1:9'} -> 422; {ayah_ref:'1:1',quality:4} -> new_interval_days 1, new_efactor 2.5, xp_awarded 5, memorised_basis 'a review count, not a hifz certification'; GET /qep/hifz/progress/{uid} -> 3 in schedule / 1 memorised with the same basis; /qep/hifz/progress/../../etc and ..%2F..%2Fetc -> 404. POST /qep/tajweed/analyse: exact -> text_similarity 1.0; half-text -> 0.5 with missing_rule_markers ['madd (kasra+ya) sequence present in reference, absent in attempt']; English -> comparable:false 'contains no Arabic'; every payload scope 'TEXT comparison only. This says NOTHING about recitation ...' and disclaimer 'Recitation assessment requires a qualified teacher'. GET /qep/translation/status -> translation_available false with availability_basis; POST /qep/translation/translate -> 503. POST /qep/tajweed/lesson {rule_name:'madd'} -> floor_served true, floor_note 'structured OUTLINE ... not scholarly content'. GET /api/qep/analytics/overview -> measured:false. Code: agentic_core/religious_domain/api.py:344-379, 384-436, 445-518, 545-619. UI: components/QEPStudio.tsx:133-140 (503 refusal handled as 'the honesty working'), :154-187, :270, :311 (availability chip); pages/domains/QEPReligionHub.tsx:80; components/LearnTeachModule.tsx:114-119; components/qep/QEPIntelligence.tsx:225; App.tsx:175,181 (/religion, /qep routes); ReligionHub.tsx:86-91,199 (QEP Flagship tab -> /qep).
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Residual only: the /qep header 'Advanced AI Flagship • v1.0' and the Competitions tab's 'View Leaderboard' toast 'Global leaderboard launching with QEP Season 2' promise a launch nothing backs (cards are labelled PLANNED/preview); replace the toast with 'no tournament backend exists'.

### R1.8 · §10 QMS — persistent defects and the ISO 9001 §8.7/§10.2 correct → re-verify loop (/api/v1/vbs/qms/*, VBSSystemsPanel) — **DELIVERED**

- **severity (assessor):** n/a
- **claim:** Verified · tested · validated; continually improving quality within a real management system.
- **observed:** Executed the loop on DEF-9812b006: correct → status 'corrected' (not closed); reverify with 9-char content → measured coverage 0.0 / stub true → reopened, gate_failures 2→3, non-conformance 0.1667→0.2 (a failed correction raises the rate); correct again; reverify with 300-char content → closed, basis honestly 'measured_from_content (no stored section requirements — length + stub instruments only)'. Gate input is bounded (coverage 97 → 422). Defects persist (data/qms_state_vbs.json: 71 gates, 19 failures, labels studio/genesis/deliverable/composition). UI wires list/correct/reverify (VBSSystemsPanel.tsx:39-77). /vbs/systems declares real-vs-simulated per system. Shortfall is the floor-vacuity of the gate itself (finding 3), not the loop.
- **evidence:** POST /api/v1/vbs/qms/gate {coverage:0.5} → passed false, rate 0.1667; defects/{id}/correct, /reverify responses above; GET /api/v1/vbs/qms/defects summary before/after. agentic_core/vbs/qms.py:56-139; agentic_core/api/vbs_systems.py:31-135.
- **disclosed to the user:** Yes.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced the whole ISO 9001 §8.7/§10.2 loop on the live backend: my failing gate (coverage 0.5) opened a persistent defect; correct -> 'corrected' (not closed); reverify with 9-char content -> measured coverage 0.0 / stub true -> reopened to 'open', gate_failures 10 -> 11; correct again; reverify with 360-char content -> closed, basis honestly 'measured_from_content (no stored section requirements — length + stub instruments only)'. Gate input is bounded (97 -> 422). Defects persist and are wired in VBSSystemsPanel (list/gate/correct/reverify). One caveat on the assessor's rate figures only: the live QMS store is shared with concurrent activity (gates_run moved 156 -> 176 during my loop), so the rate movement is not isolable — but gate_failures incrementing on the failed re-verify confirms the code's claim directly. DELIVERED stands; the vacuity of the gate on floor output is finding 2's problem, not the loop's.
- **refuter's evidence:** Executed POST /api/v1/vbs/qms/gate {coverage:0.5} -> passed false, min_coverage 0.95, real true (opened DEF-ae8d0e33 label 'cockpit-gate', status 'open'); {coverage:97} -> 422 'less than or equal to 1'. POST /qms/defects/DEF-ae8d0e33/correct -> status 'corrected'; POST .../reverify {content:'too short'} -> status 'open', reverify_meta {coverage:0.0, stubs_found:true}, reverify_basis 'measured_from_content (no stored section requirements — length + stub instruments only)'; summary gate_failures 10 -> 11; correct again -> 'corrected'; reverify with 360-char content -> status 'closed', reverified true, passed true; summary closed 3 -> 4, open 4 -> 3. GET /api/v1/vbs/systems -> qms declares real ['ISO-9001-aligned quality gates', 'persistent traceable defects + the §8.7/§10.2 correct→re-verify loop (W307)', 'non-conformance rate'] vs simulated per system. Live store C:/tmp/hb18/qms_state_vbs.json; repo data/qms_state_vbs.json gates_run 71, gate_failures 19, labels {studio:15, genesis:2, deliverable:1, composition:1}. Code: agentic_core/vbs/qms.py:56-139; agentic_core/api/vbs_systems.py:31-135. UI components/VBSSystemsPanel.tsx:39 (GET defects), :52 (POST gate), :63-77 (correct/reverify).
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None needed for the loop.

### R1.9 · §11 federated compliance screen UI — GovernanceCenter → Compliance tab (/compliance → /governance-hub?tab=compliance) — **DELIVERED**

- **severity (assessor):** n/a (shortfalls are findings 2 and 7)
- **claim:** Compliance engines (Halal/Sharia · UK-Legal · Regulatory · EHS · Ethical · Constitutional) integrated into every workflow.
- **observed:** The screen is real, deterministic and explainable: 'interest-bearing loans and pork products … medical health claims' → sharia_halal FAIL ('interest-bearing'), regulatory REVIEW ('medical'), ethical per-dimension reasons, overall FAIL; a halal bakery → pass. The same screen_compliance is genuinely woven into assure_delivery (deliverables, domain tools, Genesis candidates — a FAILed candidate is vetoed), sealed to the UEG, and a FAIL on a material label routes to the CCA (verified queued). The ComplianceChecker renders overall + per-framework verdicts with reasons; route reachable via /compliance redirect. The vocabulary is a hardened keyword set (wine/casino/lottery etc.), so it is a screen, not a standards engine — see findings 2 and 7 for the two verdicts that overstate.
- **evidence:** Executed POST /api/v1/compliance/check (three subjects, results above); GET /api/v1/compliance/frameworks; agentic_core/api/compliance.py:99-178; agentic_core/vbs/quality.py:129-142, 165-205; agentic_core/api/genesis.py:63-70, 311-320; ComplianceChecker.tsx; App.tsx:225-227.
- **disclosed to the user:** Yes for the five keyword frameworks; no for the constitutional stub.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced: the federated screen is real, deterministic and explainable across the five keyword frameworks, reachable via the /compliance redirect into GovernanceCenter's Compliance tab, rendered with overall + per-framework verdicts and reasons. The same screen_compliance is woven into assure_delivery (deliverables, domain tools, Genesis — where a FAILed candidate is sorted out before ranking), sealed to the UEG, and a material FAIL routes to the CCA (verified queued in finding 3). DELIVERED stands for what it is — a hardened keyword screen honestly labelled per framework — with the two overstating verdicts (constitutional stub, engine-depth) carried by findings 1 and 6.
- **refuter's evidence:** Executed POST /api/v1/compliance/check: 'interest-bearing loans and pork products with medical health claims' -> overall fail, sharia_halal fail "Prohibited element: 'interest-bearing'", regulatory review 'medical'; 'A halal artisan bakery in Leeds' -> overall pass, ethical 'human: pass; environment: pass; quality: not_assessed; value: pass'; 'a money laundering scheme' -> uk_legal fail. GET /api/v1/compliance/frameworks -> six frameworks with engine labels. Code: agentic_core/api/compliance.py:99-178 (screen_compliance), :181-205 (/check); agentic_core/vbs/quality.py:129-142 (screen before QMS seal), :165-218 (UEG seal, immune record, CCA routing); agentic_core/api/genesis.py:63-85 (_screen_candidate, disqualified on fail), :311-325 (VETO sort before ranking, _vetoed list). UI: App.tsx:227 (/compliance -> /governance-hub?tab=compliance); pages/governance/GovernanceCenter.tsx:6,13 (Compliance tab -> ComplianceChecker); pages/governance/ComplianceChecker.tsx:60-85 (overall card + per-verdict cards with reasons).
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Covered by findings 2 and 7.

---

## R2 — §4 + §13 — the end-to-end lifecycle (Describe → … → Run forever) and what the output IS

**Assessor's region summary:** R2 (§4 lifecycle + §13 output), assessed by executing the live HEAD backend at :8024 (DATA_DIR resolves to C:\tmp\hb18; live stores after my probes: 4 vsb_entities, 4 living_vsbs, per-VSB vsb_repos with real git) and reading genesis.py, vsb.py, living_vsbs.py, heartbeat.py, GenesisJourney.tsx, VSBCockpit.tsx, VSBSpawnStudio.tsx — I could not drive a browser, so UI statements are reasoned from source. What is DELIVERED and verified: one POST /genesis/journey with establish=true really does run the §4 stage chain, establishes an entity (genome, Board+Chief, virtual economy, plan seed, native delivery swarm), runs birth vitals (first §11 screen + first governed zero-revenue cycle), ships five surfaces as one git-committed whole in ~4 s, and the journey page discloses the floor honestly (amber 'native ×11' card, '0/0 verified · 5 floor-served, not assessable', identical-candidates box); the standalone /establish path ships honest '_Not provided_' stubs; evolve routes to the CCA and re-ships; 12 export formats are live with mp4/mp3 honestly catalogued. The GAP, ranked by harm to a real person: (1) a journey-born VSB's PUBLIC body — website pages, web/phone app data, BUSINESS_PLAN.md — presents the floor's SAMAJH/SOCH/AQAL/HOSHIYARI headings and problem bigrams as the enterprise's concept, with no floor disclosure on any shipped surface and a §10 QMS 'pass' (the stub regex cannot see floor scaffold); (2) the Board Pack recommits the W434 class (subject became '## SAMAJH', raw provenance banner in the narrative) and is rendered verbatim under a DCS-registered badge; (3) Mode-3 review gates block nothing — zero consumers, and the journey runs all stages before a VSB exists; (4) there is not ONE lifecycle: the Spawn Studio path skips §4.3–§4.7 entirely, reports GaaS 'assumed', and ships the raw floor banner into the public web/phone app 'Organisation' tab; §4.6 Develop has no stage in either pipeline (the only thing built is a brochure site + a constant data-viewer app about the entity, not the user's solution); (5) §4.10 'forever' tending is off by default (auto_economy/compliance/ship/evolve all false, last_vsb_operated null) while every entity and the birth log claim the organism tends it; (6) §4.5's tie/identical-candidate disclosure stops at the page — EVIDENCE.md and the QMS attestations still say 'evidence-ranked / simulated'; (7) the Genesis page never shows the birth-ship and its first offered button ('Generate VSB Repository') regresses the shipped website to a one-line scaffold while the ship manifest says current; (8) the cognitive/MJM stages bypass the problem prefix and describe the engine (12/13 stage texts carry the user's problem, 1/13 pure scaffold, 0/13 any solution content); (9) exported journey reports lose floor provenance at the 'verbatim-ingest' hop; (10) the floor-fallback name 'VSB — I keep 40 beehives in Somerset and lose ' becomes the brand on every surface. Net: the lifecycle skeleton, gating-by-CCA, shipping and journey-page honesty are real; the honesty does not travel into the §13 body, the Board Pack, the Spawn path or the 'living' claim, and the review gates are decorative.

### R2.0 · §13 What the Output is (+ §4.8/§4.9, §10, §15.6) — **STUB**

- **severity (assessor):** critical
- **claim:** The canonical output is a living VSB repository bespoke to the solution, integrating a Website, Web app and Phone app as the entity's public surfaces — quality-gated (§10), compliance-screened (§11), honesty over polish (never fabricate; label simulation).
- **observed:** For a journey-established VSB the shipped PUBLIC body is the deterministic floor's internal scaffold presented as the enterprise's content, with no floor disclosure anywhere on the body, and the §10 gate certifies it. Website index 'What we do' reads 'Its approach: ## SAMAJH / I need a treatment and monitoring approach that (domain: agriculture) / - monitoring approach / - beehives somerset / - third colonies / - colonies every'; /about 'Concept' and /solution 'Overview' are the same SAMAJH/SOCH/AQAL/HOSHIYARI headings + problem bigrams; BUSINESS_PLAN.md Executive Summary and Concept, webapp/data.json and mobile/data.json (concept, business_plan.executive_summary) carry the same text; the page footer says 'quality-gated, compliance-screened, document-controlled'. Every surface: qms_gate_passed=true, stub_found=false, git-committed. The website generator's own floor-safety guard (`_public_copy` → `_entity_fallback_copy`) embeds `concept[:400]`, which IS the floor text, so the guard defeats itself; the W434 heading strip is line-anchored `(?m)^#{1,6}` and misses '## SAMAJH' mid-line after 'Its approach: '. The QMS stub regex is only TODO|TBD|FIXME|lorem ipsum|placeholder|coming soon|as an ai plus ≥200 chars (agentic_core/vbs/quality.py:31-32), so floor scaffold can never fail it. By contrast the standalone /establish path (no journey) ships honest copy ('Probe R2 Standalone is a purpose-built venture addressing …', EVIDENCE.md/OPERATIONS.md '_Not provided at establishment_') — the defect is specific to entities born from a floor-served journey.
- **evidence:** Executed: POST /api/v1/genesis/journey {establish:true, ship_output:true} → vsb-5d357ea055 (4.0 s, served_by native×11); GET /api/v1/vsb/vsb-5d357ea055/website/page/{index,about,solution}, /webapp/page/data.json, C:/tmp/hb18/vsb_repos/vsb-5d357ea055/BUSINESS_PLAN.md; website manifest quality.qms_gate_passed=true, stub_found=false, ai_provenance {native:3} (never rendered by GenesisJourney.tsx website card, which shows only QMS/compliance badges). Code: agentic_core/api/vsb.py:452-470 (_build_website_files hero/about use concept), :651-676 (_public_copy fallback), :721 (line-anchored heading strip), :870-903 (_entity_appdata), agentic_core/vbs/quality.py:31-32,126.
- **disclosed to the user:** NO on every shipped surface (website, webapp, PWA, BUSINESS_PLAN.md, Cockpit Plan tab renders plan.concept verbatim with no provenance). YES only on the Genesis journey page itself (amber 'What served this journey' card).
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on a fresh journey-born entity. The shipped public body (website x3 pages, webapp/mobile data.json, BUSINESS_PLAN.md, Cockpit Plan tab) presents the floor's SAMAJH/SOCH/AQAL/HOSHIYARI scaffold + problem bigrams as the enterprise's concept, with no floor disclosure on any of those surfaces, and the §10 gate passes it (stub_found=false). The website generator's own floor guard falls back to copy that embeds concept[:400] (the floor text) and the heading strip is line-anchored so 'Its approach: ## SAMAJH' survives. Standalone /establish (no journey concept) ships honest copy, so the defect is specific to journey-born entities exactly as claimed.
- **refuter's evidence:** Executed POST /api/v1/genesis/journey {establish:true, ship_output:true} -> vsb-14625ba62d (5.2 s, served_by native x11, initial_ship shipped/commit df84860b93d7). GET /website/page/index 'What we do': '...is a purpose-built venture addressing <problem>. Its approach: ## SAMAJH / - reliable proofing / - sourdough loaves / - want reliable / - proofing proc / - small bakery / SOCH ...'; /about 'Concept' and /solution 'Overview' same; footer 'quality-gated, compliance-screened, document-controlled'. /webapp/page/data.json concept + business_plan.executive_summary/concept = same text. C:/tmp/hb18/vsb_repos/vsb-14625ba62d/BUSINESS_PLAN.md Executive Summary/Concept = same. Website manifest qms_gate_passed=true, stub_found=false, ai_provenance {native:3} — GenesisJourney.tsx:855-875 renders only QMS/compliance badges for the site card. GET /api/v1/business-plan?scope=vsb-14625ba62d concept starts '## SAMAJH'; VSBCockpit.tsx:496-508 and BusinessPlan.tsx:93-100 render plan.concept verbatim, no provenance. Code: vsb.py _entity_fallback_copy (about: 'Its approach: {concept.strip()[:400]}'), _public_copy falls back to it whenever sb=='native'; heading strip `(?m)^\s{0,3}#{1,6}\s*` line-anchored; quality.py:31-32 _STUB_RE = TODO|TBD|FIXME|lorem ipsum|placeholder|coming soon|as an ai. Standalone vsb-981debd258 index: 'Probe R2 Standalone is a purpose-built venture addressing ...' with no scaffold; EVIDENCE.md/OPERATIONS.md '_Not provided at establishment_'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In genesis_establish/_stream, when provenance.served_by_agent marks concept/design/commercialisation as floor-served, store them in genesis_blueprint with a `floor_served: true` flag and have _blueprint()/_entity_appdata()/_build_website_files()/_seed_plan_from_journey() fall back to the honest entity-data copy (as the standalone path already does) and stamp a visible 'generated by the deterministic floor — not model reasoning' line on the pages; make _public_copy's fallback use `challenge` only, never `concept`, when concept is floor-served; and add the floor's own signature headings (SAMAJH/SOCH/AQAL/HOSHIYARI/INKASHAF/'Native structured') to the QMS stub regex so a floor scaffold cannot pass as 'specifically designed'.
- **status now:** gate half FIXED W449 — the §10 gate no longer seals a floor-served body as verified: website and board pack by their own generation provenance; repo / webapp / mobile by the provenance the journey now STORES on the entity (the refuter caught that the entity never carried it, so those three had still fallen to the old gate). A standalone /establish declares no origin and still gets the measured gate. Body half FIXED W450 (P1.2) — a floor-served field is an honest pending state at establishment, never the floor's text; the founder's words always ship; the README and footers no longer claim 'quality-gated, compliance-screened'. The body's SUBSTANCE still needs the owned model to compose it.

### R2.1 · §13 / §17.3 Board Pack (Living Business System on-demand layer) — **STUB**

- **severity (assessor):** high
- **claim:** Board Pack assembled fresh from live data, DCS-registered, with an in-house AI-CEO narrative — document-controlled and provenance-sealed.
- **observed:** The board-pack narrative is the W434 defect class recommitted on a fourth path: the prompt interpolates `Concept: {concept[:500]}` where the stored concept begins '## SAMAJH', the floor takes that as the subject, and the shipped narrative reads '_[Workstation native structured engine — owned, no external dependency]_ / _Acting as: AI CEO assembling a fresh Board Pack for the VSB._ / ## SOCH Native structured content for 'SOCH', grounded in: ## SAMAJH (domain: the stated domain). - samajh soch / ## Executive Summary Subject: ## SAMAJH … / ## Key Risks - Exposure on samajh soch.' The narrative is stored raw (never passed through _public_prose), QMS pass, dcs_registered=true, and GenesisJourney.tsx renders `pack.narrative.slice(0,1200)` verbatim under a green 'DCS-registered' badge with no provenance badge. The pack's 'strategic.ceo' layer is the same SAMAJH text.
- **evidence:** GET /api/v1/vsb/vsb-5d357ea055/board-pack → ai_provenance {native:1}, dcs_registered true, narrative as quoted; agentic_core/api/vsb.py:1007-1019 (prompt), :1030-1041 (narrative stored raw); apps/workstation-superapp/src/pages/synthesis/GenesisJourney.tsx:900-915 (render, no provenance).
- **disclosed to the user:** NO — the raw provenance marker is the only hint and it appears inside the 'narrative' as if it were board content; the UI shows DCS-registered + compliance badges only.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on the birth-shipped board pack of my fresh entity: the narrative is the raw floor output with its provenance banner and 'Subject: ## SAMAJH' (the floor took the concept's first heading as the subject), stored without _public_prose, QMS-passed and DCS-registered, and the Genesis page renders it verbatim under a green DCS-registered badge with no provenance badge.
- **refuter's evidence:** GET /api/v1/vsb/vsb-14625ba62d/board-pack -> dcs_registered true, ai_provenance {native:1}, narrative begins '_[Workstation native structured engine — owned, no external dependency]_ / _Acting as: AI CEO assembling a fresh Board Pack for the VSB._ / ## SOCH Native structured content for 'SOCH', grounded in: ## SAMAJH (domain: the stated domain). - loaves commercialisation - commercialisation soch - sourdough loaves - soch refuter ... ## Executive Summary Subject: ## SAMAJH (domain: the stated domain).' Code: vsb.py generate_vsb_board_pack prompt interpolates 'Concept: {concept[:500]}' and stores narrative = meta.get('output') raw (no scrub); layers.strategic.ceo = ceo_specification (the SOCH/AQAL text). UI: GenesisJourney.tsx:350-354 POSTs /board-pack; :983-995 renders `pack.narrative.slice(0, 1200)` with 'DCS-registered' (emerald) + compliance badges only.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Pass `narrative` through _public_prose before storing; feed the prompt `challenge` (not a floor-served concept) as the subject; when served_by=='native' replace the narrative with an honest entity-data summary and render the pack's ai_provenance beside the DCS badge (reuse provenanceBadge from lib/api).
- **status now:** narrative half FIXED W450 (P1.2) — on the floor the board pack's narrative is 'narrative pending the owned model' (its live layers stand), never the engine's marker/role/headings frame; a model-served narrative is scrubbed of markers. The empty-concept refusal and identical-hash disclosure remain P1.14.

### R2.2 · §4 (each stage 'verified, tested and validated') / §17.4 Mode 3 human review gates — **STUB**

- **severity (assessor):** high
- **claim:** Optional human review gates at any Concept→Commercialisation stage, set in the VSB genome; each stage of the one continuous workflow is gated (verified · tested · validated) before it advances.
- **observed:** Review gates are a config store with a UI and no enforcement. `_gate_status` computes `blocks_progress` but nothing in the repository reads it: grep of every .py outside vsb.py for review_gate/review-gate returns nothing, and the docstring '(used by the lifecycle to honour Mode 3)' has no caller. Structurally they cannot gate the journey: POST /api/v1/genesis/journey runs all eight stages in one request before any VSB (and therefore any gate config) exists; /api/v1/vsb/spawn likewise. A founder who gates 'design' and leaves it 'pending' sees 'blocks_progress: true' and a DCS hash — and every stage, ship, evolve and cascade proceeds regardless. The Genesis page presents it as 'Human review gates (Mode 3) — tap a stage to gate/ungate; each change is DCS-audited'.
- **evidence:** GET /api/v1/vsb/vsb-5d357ea055/review-gates (8 lifecycle stages, statuses with blocks_progress); agentic_core/api/vsb.py:1093-1103, :1152-1160; grep -rn 'review_gate|review-gate' --include=*.py (only vsb.py); genesis.py:154-358 single-request cascade; GenesisJourney.tsx:960-1000.
- **disclosed to the user:** NO — the panel implies gating is live.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced by breaking it: I gated 'design' and 'build' (pending, blocks_progress=true, DCS-hashed) on vsb-5d357ea055 and then shipped and evolved the entity — both proceeded with no mention of the gate, the evolve filed a CCA and re-shipped the repo, and the gates stayed pending. Nothing outside the gate endpoints reads review_gates; the journey and spawn run every stage in one request before any VSB (hence any gate config) exists. The UI panel presents it as live gating.
- **refuter's evidence:** POST /api/v1/vsb/vsb-5d357ea055/review-gates {stages:[design,build]} -> statuses design/build gated, pending, blocks_progress true, dcs_hash 2bf1bbdb...; GET .../review-gates/design -> blocks_progress true. Then POST .../repo/ship -> shipped true, coherent_whole true, commit c7654007cc93, response keys contain no gate/block field. POST .../evolve -> generation 3, evolution_pending_cca cca-7ff49eb974, repo re-shipped (ship commit b148433bf35d); entity review_gates still {'stages':['design','build'],'decisions':{}}. Grep review_gate|review-gate|blocks_progress over *.py: only agentic_core/api/vsb.py (gate endpoints :1093-1176 + a docstring :218) and integration_tests/test_mvp_spine.py; heartbeat.py, genesis.py, spawn (vsb.py:1310-1490) never consult it. genesis.py:154-358 runs all stages in one request; GenesisJourney.tsx:1000-1030 'Human review gates (Mode 3) — tap a stage to gate/ungate; each change is DCS-audited.'
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either consult the gate in the two places that advance a VSB (ship_vsb_repo / evolve_vsb / run_repo_cascade: refuse with 409 while any gated stage is pending or rejected) and split the journey into resumable per-stage calls that check the gate, or relabel the panel honestly ('records a review decision; does not yet block any stage').
- **status now:** gating half FIXED W452 (P1.4) — one shared guard; every lifecycle mover (ship, evolve, evolution apply, repo cascade, org cascade, fabric swarm run, plan orchestrate, the birth-ship, the heartbeat's autonomous evolve/re-ship) refuses with 409 {gate, status, blocks_progress} while any gated stage is pending or rejected; gates can be set at birth. The journey still runs every stage in one request before the entity exists — a per-stage pause mid-journey is not built.

### R2.3 · §4 The End-to-End Lifecycle as ONE continuous workflow (§4.3–§4.7) / §15.2 — **PARTIAL**

- **severity (assessor):** high
- **claim:** One continuous, intelligent, autonomous workflow with stages 1–10; the deliverable is a living VSB generated end-to-end.
- **observed:** There is not one lifecycle but two entry pipelines with four stage vocabularies, and neither is gated. (a) Genesis: concept → research → model/simulate/rank → design → operations → commercialisation → establish → ship (the §4 stages, minus §4.6 Develop — nothing builds the user's solution; the only 'build' is the entity's own brochure site/data-viewer). (b) VSB Spawn Studio (/vsb, /vsb-spawn → POST /api/v1/vsb/spawn): cognitive → MJM → GaaS → CEO spec → genome → swarm, 0.54 s, NO research, design, rank, operations or commercialisation stages, yet the entity is persisted status 'operational' stage 'build' and appears in the Cockpit beside journey-born entities; its GaaS gate reports 'Constitutional alignment assumed (configure GAAS_GENOME_PATH + GAAS_LEGAL_PATH for full gate)'. Its ceo_specification carries the raw floor banner ('_[Workstation native structured engine…]_ / _Acting as: AI CEO of Workstation IDBO._ / The request concerns: … / ## Native approach Workstation composes a structured response…') and ships verbatim into webapp/data.json and mobile/data.json `organisation.ceo` and ORGANISATION.md — the W376 defect recurring because _entity_appdata scrubs concept/challenge but not organisation.ceo. (c) The review-gate lifecycle labels (intake/research/design/build/validate/commercialise/genome/launch) match neither pipeline; (d) the Genesis page shows a 5-step progress map (Describe/Explore/Establish/Deliver/Operate) and a 6-stage rail that lights every stage 'done' on any result. The spawn path also does not ship a body at birth.
- **evidence:** POST /api/v1/vsb/spawn (SSE, 15 events, vsb-0bf07cfb56); GET /api/v1/vsb/vsb-0bf07cfb56 ceo_specification; POST …/repo/ship then GET …/webapp/page/data.json organisation.ceo (raw banner); agentic_core/api/vsb.py:62-71, :1310-1500, :870-903; genesis.py:154-358; GenesisJourney.tsx:427-500; App.tsx:185-189.
- **disclosed to the user:** PARTIAL — the spawn SSE shows the 'assumed' GaaS line; nothing tells a user that a Spawn-Studio VSB skipped §4.3–§4.7, and the shipped public surfaces carry the banner undisclosed.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced: two entry pipelines with different stage sets, four stage vocabularies, neither gated; §4.6 Develop is MISSING inside the one workflow (a Factory exists at /api/v1/factory/produce but the journey never calls it — the only 'build' is the entity's brochure site/data viewer). The Spawn Studio entity ships the raw floor banner as its AI CEO into public surfaces because _entity_appdata scrubs concept/challenge but not organisation.ceo. One nuance: VSBSpawnStudio.tsx:201 does state its own shorter pipeline ('Nine Cognitive Engines → MJM → GaaS → CEO Strategy → Genome'), so a user on that page is told what it runs — but nothing says the entity skipped §4.3–§4.7, and it is persisted 'operational' beside journey-born entities.
- **refuter's evidence:** POST /api/v1/vsb/spawn (fresh) -> vsb-39bfd8035c, 15 SSE events in 1.36 s: cognitive → mjm → gaas ('Constitutional alignment assumed (configure GAAS_GENOME_PATH + GAAS_LEGAL_PATH for full gate).', passed true) → ceo_strategy (content starts '_[Workstation native structured engine — owned...]_ _Acting as: AI CEO of Workstation ID') → genome → swarm → governance → complete; no research/design/rank/operations/commercialisation; status operational, stage build (vsb.py:1441-1462). Existing spawn entity vsb-0bf07cfb56: ceo_specification = raw banner + 'The request concerns: ... ## Native approach Workstation composes a structured response...'; /webapp/page/data.json organisation.ceo and repo ORGANISATION.md carry it verbatim (vsb.py _entity_appdata scrubs concept+challenge only). Genesis stages: genesis.py:154-358 concept→research→3 candidates+twins→design→operations→commercial→establish→ship, no Develop; grep 'factory' -> products.py:173 /api/v1/factory/produce, uncalled by genesis.py. Review-gate lifecycle ids vsb.py:62-71 (intake/research/design/build/validate/commercialise/genome/launch) match neither. GenesisJourney.tsx:427-432 5-step map; :456-460 6-stage rail `const done = !!result`.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Route the Spawn Studio through genesis_journey(establish=True) (or mark spawn-born entities `lifecycle: 'spawn-lite'` and show it in the Cockpit selector and repo README); scrub ceo_specification with _public_prose in _entity_appdata and ORGANISATION.md; derive the review-gate lifecycle from the real journey stage list so one vocabulary exists.

### R2.4 · §4.10 Run · Defend · Heal · Learn · Improve · Grow — forever — **PARTIAL**

- **severity (assessor):** high
- **claim:** The established VSB self-operates and forever evolves on live intelligence; the organism tends it on the circadian heartbeat.
- **observed:** Registration is real and evolution is genuinely gated, but autonomous tending is OFF by default and the entity asserts otherwise. Live heartbeat: running, 51 beats, auto_economy=false, auto_compliance=false, auto_ship=false, auto_evolve=false, last_vsb_operated=null; living_vsbs.json shows operating_cycles=1 for the journey VSB (the single birth cycle) and 0 for the spawn VSB — no entity has been operated since birth. Yet every entity record carries `living.autonomous_operation: 'registered — the organism tends this VSB on the circadian heartbeat (paced virtual economy cycles)'` and the SSE birth log announces 'Registered as a Living Entity — The organism tends this VSB on the circadian heartbeat (governed economy cycles)'. DELIVERED parts: birth vitals (first §11 screen 'review', first governed cycle with honest zero revenue 'no_activity_maintenance_cycle'); POST /evolve under the floor produces an evidence-based proposal ('remediate the §11 screen posture (currently review)'), files CCA cca-72ab695847 (mutations only on approval) and re-ships the repo (commit b25d773922eb).
- **evidence:** GET /api/v1/heartbeat/status; C:/tmp/hb18/living_vsbs.json; GET /api/v1/economy/living-vsbs (operating_cycles 0/1, never_screened true for spawn VSB); POST /api/v1/vsb/vsb-5d357ea055/evolve; genesis.py:736-742, :899-901; agentic_core/organism/heartbeat.py:130-135.
- **disclosed to the user:** NO at the VSB surfaces (Genesis birth log, entity record, Cockpit); the heartbeat page (another region) exposes the flags.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced: autonomous tending is off by default and no entity has been operated since its birth cycle after 500 heartbeats, yet every entity record and the SSE birth log assert the organism tends it on the heartbeat, and the VSBEconomy page says the organism 'operates continually … forever'. The delivered parts (birth vitals, honest zero-revenue first cycle, evidence-based /evolve filing a CCA and re-shipping) are real.
- **refuter's evidence:** GET /api/v1/heartbeat/status: running true, beats 500, auto_economy false, auto_compliance false, auto_ship false, auto_evolve false, last_vsb_operated null, last_vsb_evolved null. heartbeat.py:236-243 `if self.auto_economy:` is the only path to operate_one(); grep operate_one|operate_vsb( -> heartbeat.py:237-238 and genesis.py birth vitals only. C:/tmp/hb18/living_vsbs.json + GET /api/v1/economy/living-vsbs: all 6 entities operating_cycles 1 (birth) or 0, last_operated == registered_at. Entity vsb-14625ba62d living.autonomous_operation 'registered — the organism tends this VSB on the circadian heartbeat (paced virtual economy cycles)' (genesis.py:736-742); SSE 'Registered as a Living Entity — The organism tends this VSB on the circadian heartbeat (governed economy cycles).' (genesis.py:899-901); VSBEconomy.tsx:526 'the organism operates continually on the circadian heartbeat … forever'. Delivered: birth_vitals first_screen overall 'review', first_cycle revenue_basis 'no_activity_maintenance_cycle' governance passed; POST /evolve on vsb-5d357ea055 -> proposal 'remediate the §11 screen posture (currently review)' basis 'latest §11 compliance screen (evidence-based, owned)', CCA cca-7ff49eb974, re-shipped.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Have register()/the entity text read heartbeat.auto_economy at birth and say 'registered; autonomous tending is OFF — enable auto_economy/auto_compliance on the heartbeat' when false; show the entity's operating_cycles/last_operated in the Cockpit Organisation card.

### R2.5 · §4.5 Model · Simulate · Optimise · Rank (+ §10 bar, §15.5) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** Every candidate solution is designed, modelled, simulated, optimised, categorised and ranked so the best is selected on evidence (effectiveness, safety, efficiency, commercial viability, compliance).
- **observed:** Under the floor the three 'alternatives' are byte-identical (candidates_distinct=1), the ranking is a three-way tie at 0.86 resolved by list order, and the page discloses this well: an amber box replaces the ranked cards, selection_basis says 'TIE … resolved by list order, NOT by evidence', and 'Not measured at selection time: effectiveness · efficiency · commercial viability' is printed. But the disclosure stops at the page: the shipped EVIDENCE.md says '## Selected Candidate (§4.5 evidence-ranked) - id: pragmatic · rank: 1 · score: 0.86 … simulated evidence: 0.739' with no tie or identical-candidates note, and the QMS record attests 'modelled', 'simulated', 'ranked', 'categorised' from this run (4 measured · 4 attested · 8 not measured — optimised, best-in-class, innovative, effective, efficient, commercially viable, tested, validated) while the UI shows a green 'Living-QMS gate: pass · cov 100%'. Stage verifications are honestly '0/0 verified · 5 floor-served, not assessable'.
- **evidence:** journey_out.json stage_5_model_simulate_rank (candidates_are_alternatives false, tie.detected true), quality_assurance.quality.bar_measured.summary '4 measured · 4 attested · 8 not measured'; C:/tmp/hb18/vsb_repos/vsb-5d357ea055/EVIDENCE.md; genesis.py:104-207, :277-306; vsb.py:141-155; GenesisJourney.tsx:650-700, :744-760.
- **disclosed to the user:** YES on the Genesis page; NO in the shipped repo's EVIDENCE.md and in the QMS attestation wording.
- **refutation: SURVIVED** (reproduced by the refuter). Verdict stands, but one evidence claim is wrong and must be corrected: the QMS attestation wording DOES disclose the tie — bar_measured.ranked.basis carries 'TIE at 0.856 across 3 candidates … resolved by list order, NOT by evidence', and 'optimised' is honestly recorded met:null (not attested); the UI shows the '§10 bar: 4 measured · 4 attested · 8 not measured' chip beside the green gate badge with a tooltip separating measured from attested. The shortfall that survives is the shipped EVIDENCE.md, which reads '§4.5 evidence-ranked … rank: 1 · score: 0.856' with no tie or identical-candidates note because the repo builder never reads tie/candidates_distinct.
- **refuter's evidence:** journey2 stage_5: candidates_distinct 1, all three approach_digest d8fc54842e, score 0.856 each, tie.detected true, resolved_by 'list order — NOT evidence', comparison_note present; GenesisJourney.tsx:670-680 renders the amber box in place of cards, :713 selection_basis, :720-728 criteria_not_measured. quality_assurance.quality.bar_measured: ranked.basis = 'caller evidence: TIE at 0.856 across 3 candidates (pragmatic, innovative, lean) — resolved by list order, NOT by evidence. selected pragmatic on 0.40 form 0.7…'; optimised met None source none; summary '4 measured · 4 attested · 8 not measured'; GenesisJourney.tsx:744-770 green 'Living-QMS gate: pass · cov 100%' + '§10 bar: 4 measured · 4 attested · 8 not measured' chip. C:/tmp/hb18/vsb_repos/vsb-14625ba62d/EVIDENCE.md: '## Selected Candidate (§4.5 evidence-ranked) - id: pragmatic · rank: 1 · score: 0.856 … simulated evidence: 0.79' — no tie note; vsb.py:141-155 builds it from selected_candidate only. Stage verifications honestly '0/0' with 5 floor-served.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Write stage_5.tie / candidates_are_alternatives / comparison_note into EVIDENCE.md (selected_candidate already travels), and do not attest 'ranked'/'simulated' when candidates_distinct==1.
- **status now:** FIXED W449 — the tie / identical-candidates facts travel with the selected candidate and the shipped EVIDENCE.md prints them beside it; the QMS record no longer counts ranked/simulated as met on a tie.

### R2.6 · §13 shipped body coherence / §4.9 Deliver (Genesis page actions) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** The repo is shipped as one coherent, version-controlled whole; drift is never silent.
- **observed:** Clicking 'Generate VSB Repository' after a birth-ship regresses the public website: POST /repo alone rewrites web/index.html to the one-line scaffold ('Integrated Website — scaffold; full site generated in a later increment'), 1887 → 622 bytes, commits it, and the ship manifest still reports stale=false while the repo manifest says integrated_surfaces 'web/index.html (scaffold)'. The Genesis page invites exactly this click: it never reads `initial_ship` or `birth_vitals` (0 references in the frontend), so after a one-call establish the progress map shows Deliver/Operate undone and the first button offered under the newborn is 'Generate VSB Repository', followed by separate website/webapp/PWA buttons that regenerate what already shipped. The evolve re-ship restores the site (1887 bytes) — the mechanism is sound, the UI ordering causes the regression.
- **evidence:** Executed: GET website/page/index (1887) → POST /api/v1/vsb/vsb-5d357ea055/repo (commit a7dcde22e309) → GET website/page/index (622, scaffold text) → GET repo/ship stale=false → POST /evolve re-ship → 1887; vsb.py:176-181 (_build_repo_files writes web/index.html); grep initial_ship apps/workstation-superapp/src → none; GenesisJourney.tsx:423-432, :828-846.
- **disclosed to the user:** NO — the ship manifest says current while the home page is a scaffold; the birth-ship itself is never shown.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced byte-for-byte on my fresh entity: POST /repo after the birth ship overwrote the 3-page website's index with the one-line scaffold and committed it, while the ship manifest still reported stale=false; a re-ship restored it. The Genesis page never reads initial_ship/birth_vitals, so after a one-call establish the progress map shows Deliver/Operate undone and the first button offered is 'Generate VSB Repository'. Nuance: on the two-step SSE path the birth record does list 'Shipped: website — surface generated' lines, but the map and buttons still present delivery as not done.
- **refuter's evidence:** GET /api/v1/vsb/vsb-14625ba62d/website/page/index = 1810 bytes (3-page site). POST .../repo -> commit 4a6be2d3f238, integrated_surfaces website 'web/index.html (scaffold)'. GET .../website/page/index = 552 bytes: '<p><em>Integrated Website — scaffold; full site generated in a later increment.</em></p>'. GET .../repo/ship -> stale false, stale_reason None, commit still df84860b93d7. POST .../repo/ship -> commit 3cb22f6bcde6, index back to 1810; git log in C:/tmp/hb18/vsb_repos/vsb-14625ba62d shows 'repo: …' commit 4a6be2d between the two full ships. Code: vsb.py:176-181 _build_repo_files writes web/index.html scaffold; mark_repo_stale (vsb.py:1559) called only from evolve/apply paths (:1770-1834), never by /repo. UI: grep initial_ship|birth_vitals in apps/workstation-superapp/src -> 0 hits; GenesisJourney.tsx:190 setVsb(data.established_vsb) (one-call path); :427-432 `surfaces = !!(repo || site || webapp || pwa)`; :828 'Generate VSB Repository' first under the newborn; :286 SSE path only stores stage/label/content.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In _build_repo_files, skip writing web/index.html, webapp/README.md, mobile/README.md when the corresponding generated surface already exists (or have POST /repo call mark_repo_stale); render established_vsb.initial_ship in GenesisJourney.tsx and set repo/site/webapp/pwa state from it so the progress map and buttons reflect the body already shipped.
- **status now:** FIXED W450 (P1.2) — /repo drops its web/webapp/mobile scaffold placeholders when a generated surface is on disk (measured: it had overwritten the shipped three-page site with 471 bytes while ship.json said stale=false) and labels integrated_surfaces by what is on disk; a rename marks a shipped body stale. The file/zip endpoint and clickable tree remain P3.7.

### R2.7 · §4.2 Understand & Map (cognitive cascade + MJM) / §4 stage content vs scaffold (the W434 lesson) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** AI-mediated problem mapping: understand the person, the context, the real underlying problem, constraints, goals and success criteria; each stage is specific to the person's problem.
- **observed:** Count of the 13 journey texts (cognitive, MJM, concept, research, design, operations, commercial, 3 candidates, 3 twins): 12 carry the user's own problem terms (30–90 hits each — the W434 problem-prefix works through the `_q` seam), 1 (the cognitive cascade) carries none, and 0 contain any solution content beyond bigrams of the problem ('Component for monitoring approach / Component for beehives somerset / Component for colonies every'). The two helpers that bypass the realm/problem prefix (_ai_cognitive_prime, _ai_mjm_lifecycle in api/intelligence.py) still let the engine describe itself: cognitive subject 'Analyse the following thro', bullets 'architecture specialised · specialised intelligence · cognitive architecture · intelligence systems · operating biological'; MJM subject 'Process this through the three phases of Mus', bullets 'meta-judgement system · cognitive engines · mjm orchestrator'. Both render as Phase 1 'Cognitive Cascade (6 engines)' and 'MJM Assessment'. The 'Sovereign Journey Complete' card lists a constant engines_used badge set (Inkashaf…Iman, MJM, DDPIE, BDP, gaas.v5) regardless of what ran, and these two calls (plus the name call) are not counted in the 'native ×11' provenance line. This is the environment (floor only) — the assessable part is that the floor disclosure card is present and honest, but the content beneath it is the engine's self-description.
- **evidence:** journey_out.json phase_1_conceptualisation.cognitive_cascade / mjm_assessment; term counts run over the payload (cognitive: problem 0 / scaffold 6; MJM: 30 / 12; others: 50–90 / 0–1); agentic_core/api/intelligence.py:300-317, :320-335; genesis.py:52-63 (scrub but no re-subject), :350-353.
- **disclosed to the user:** PARTIAL — the amber 'deterministic native floor served 11 of these calls' card sits above the results; the cognitive/MJM/name calls are not in that count and the engines_used badges overstate.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced with a term count over my own journey: the cognitive cascade carries zero terms from the problem and 42 engine self-description terms; MJM 20 problem / 66 scaffold; the other eleven texts carry the problem (41–70 hits) but no solution content beyond bigrams. The floor disclosure card is present and honest for the 11 _q calls, but the cognitive/MJM/name calls are outside that count and the engines_used badges are a constant list.
- **refuter's evidence:** journey2 phase_1.cognitive_cascade: '## INKASHAF Analyse the following thro (domain: enterprise). - architecture specialised - specialised intelligence - cognitive architecture - intelligence systems - operating biological' repeated per lens (problem terms 0, scaffold 42); mjm_assessment: 'Process this through the three phases of Mus (domain: enterpr). - meta-judgement system - cognitive engines - mjm orchestrator' (20/66); design: 'Component for reliable proofing. - Component for sou…' (70/8). intelligence.py:300-335 _ai_cognitive_prime/_ai_mjm_lifecycle call gateway.query directly without the realm/problem prefix; genesis.py:189-197 scrubs them only. ai_provenance.served_by_agent lists exactly 11 agents (concept, research, 3 candidates, 3 twins, design, operations, commercial) — cognitive/MJM/name absent; engines_used constant at genesis.py:340-345. Amber card 'The deterministic native floor served 11 of these calls' at GenesisJourney.tsx:571-590.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Route _ai_cognitive_prime/_ai_mjm_lifecycle through the journey's local `_q` (so they get the Problem: field and provenance accounting), and derive engines_used from provenance.served_by_agent instead of a constant list.

### R2.8 · §4.1 Describe (multimodal input) + §4.9 Output in any selectable format — **PARTIAL**

- **severity (assessor):** low
- **claim:** Describe multimodally (text, voice, image, uploaded data — reports, reviews, data, media, sites, apps, models); output as Reports, Presentations, Videos, Websites, Platforms, Apps, Products, Services, each a living deliverable.
- **observed:** DELIVERED: text + voice dictation (DictateButton) + attach txt/md/json/pdf-with-text-layer on Genesis; 12 live export formats (md, html, slides, txt, json, video-html, pdf, docx, pptx, xlsx, svg, png) with mp4/mp3 honestly catalogued 'not yet produced'; Report/Presentation export of the journey works end-to-end (deliv-59b02680, html export served). Gaps: no image/media/site/app/model ingestion on Genesis (image only in the Cockpit 'Converse' tab); 'video' is an animated-HTML scene player, labelled as such; the exported journey report loses the floor provenance at the ingest hop — the deliverable is stamped 'verbatim-ingest' and the HTML header reads 'produced on Workstation IDBO's own AI fabric (in-house · verbatim-ingest)' while its body is the SAMAJH keyword text (0 occurrences of 'floor'/'native'/'provenance' in the export). Website/Platform/App outputs exist only as the entity's own brochure site + a fixed 4-tab data viewer (_WEBAPP_APP_JS is one constant for every VSB) and the same viewer as a PWA — honest in the manifests ('NOT a server/backend app, not deployed'), but not 'bespoke to the solution'.
- **evidence:** GET /api/v1/deliverables/output-formats; POST /api/v1/deliverables/produce + GET /api/v1/deliverables/deliv-59b02680/export?format=html; components/AttachDocument.tsx:118 accept list; GenesisJourney.tsx:205-238; vsb.py:770-800 (_WEBAPP_APP_JS), :826-845 manifest notes.
- **disclosed to the user:** YES for formats and app nature (manifest notes, 'video-html' label, attach hint); NO for the floor origin of an exported journey report.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced: 12 live export formats with mp4/mp3 honestly catalogued; text/voice/document attach on Genesis but no image/media/site/app/model ingestion; the journey report export is stamped 'in-house · verbatim-ingest' with no floor provenance. One nuance the assessor's count missed: the exported body does carry the floor's own phrase 'Structured phased frame (deterministic scaffold):' twice, a faint in-content hint — but it is not stated as provenance anywhere in the header or metadata. The web/mobile app is one constant viewer for every VSB.
- **refuter's evidence:** GET /api/v1/deliverables/output-formats -> live_ids [md, html, slides, txt, json, video-html, pdf, docx, pptx, xlsx, svg, png], catalogue_not_yet_produced [mp4, mp3]. AttachDocument.tsx:8,19,118 accept = .txt/.md/.markdown/.csv/.tsv/.json/.log/.yaml/.yml/.xml/.html/.htm + .pdf + text/* — no image/media; DictateButton wired at GenesisJourney.tsx:503-506. POST /api/v1/deliverables/produce (content built exactly as GenesisJourney.tsx:205-238 does) -> deliv-ef29edf2, ai_provenance {served_by:'verbatim-ingest', note:'content supplied verbatim by the caller — not regenerated'}; GET .../export?format=html (13,203 bytes): header 'produced on Workstation IDBO's own AI fabric (in-house · verbatim-ingest)'; counts floor 0, native 0, provenance 0, 'deterministic scaffold' 2 (inside Technology & Delivery Plan and First 90 Days). deliverables.py:226-235, :400. vsb.py _WEBAPP_APP_JS single constant reused by _build_mobile_files; manifests say 'NOT a server/backend app, not deployed/hosted'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Pass the journey's ai_provenance into /deliverables/produce (the UI already has it) and print it in the export header instead of 'verbatim-ingest'; accept image files on Genesis' AttachDocument and route them through the existing avatar vision path.

### R2.9 · §4.8 Establish (naming) — visible on every §13 surface — **PARTIAL**

- **severity (assessor):** low
- **claim:** Instantiate a bespoke, specifically-designed living VSB IDBO enterprise.
- **observed:** Under the floor `_derive_name` rejects every returned line (each contains ':', starts with '#'/'-', or says 'native structured engine') and always falls back to `VSB — {problem[:40]}`, so the beekeeper's enterprise is named 'VSB — I keep 40 beehives in Somerset and lose ' (trailing space, truncated mid-sentence). That string is the <title> and hero on all three website pages, the PWA manifest name, README/BUSINESS_PLAN headings, git commits, the board pack, the living roster and the Cockpit selector; the spawn path hard-codes 'VSB — {challenge[:60]}'. The standalone /establish path with a user-supplied name ('Probe R2 Standalone') is clean. DELIVERED and worth recording: ship-as-one-whole with real git version control (5 commits, per-surface QMS/compliance, unified manifest), owner-scoped 404s, honest '_Not provided at establishment_' stubs when no journey preceded, and the Cockpit's stale-body banner + re-ship/evolve/cascade actions.
- **evidence:** genesis.py:410-431 (_derive_name filter + fallback), vsb.py:1441 (spawn name); observed name in every fetched surface; establish_out.json (named path); GET /api/v1/vsb/vsb-5d357ea055/repo/ship version_control.
- **disclosed to the user:** NO — the name looks like a chosen brand everywhere it appears.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced: under the floor the name derivation rejects every returned line and falls back to 'VSB — {problem[:40]}', so my entity is 'VSB — Refuter R2 run 2: I run a small bakery i' (cut mid-word) on every surface; the spawn path hard-codes 'VSB — {challenge[:60]}'; a user-supplied name is clean. The delivered parts (git version control across ships, honest stubs, owner-scoped reads) are real.
- **refuter's evidence:** vsb-14625ba62d name 'VSB — Refuter R2 run 2: I run a small bakery i' in <title>/<h1> of index/about/solution, mobile manifest name, BUSINESS_PLAN.md/README headings, git commit messages, board pack, GET /api/v1/vsb list, /api/v1/economy/living-vsbs roster. genesis.py:410-431 _derive_name skips lines that start with '_[', '#', '-', '>' or contain ':' or 'native structured engine' or exceed 6 words — every line of the floor's structured reply qualifies, so `cand` is always empty and the fallback fires. Spawn vsb-39bfd8035c/vsb-0bf07cfb56 named 'VSB — {challenge[:60]}' (vsb.py:1449). Standalone vsb-981debd258 'Probe R2 Standalone'. Delivered: repo git log shows per-surface commits (commits_total 4 at birth, 5 after /repo, 9 after re-ship); EVIDENCE.md/OPERATIONS.md '_Not provided at establishment_' on the standalone path.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When the derived name falls back, use a neutral slug ('Somerset varroa apiary venture' from the first noun phrase, or 'Unnamed VSB vsb-…') trimmed of trailing whitespace, and prompt the user on the Genesis newborn card to name the enterprise before it ships.
- **status now:** FIXED W450 (P1.2) — the floor's fallback name `VSB — {problem[:40]}` is gone: a neutral whole-word slug from the founder's words, marked name_pending, ships NOTHING; the newborn card asks the founder and POST /vsb/{id}/name ships the deferred body (living register, swarm CEO label and plan opening follow). An optional name field precedes the journey.

---

## R3 — §5 + §17.3 + §17.4 — the living organisation, the living business system layers, the three integration modes

**Assessor's region summary:** R3 (§5 living organisation, §17.3 living business system, §17.4 integration modes) assessed against HEAD 06c51109 on the live :8024 backend (AUTH off, AI_DISABLE_LOCAL=1 → native floor), 433 OpenAPI paths. I executed every route in scope (board status/charter/chief-instruct, business-plan get/roadmap/progress/list/generate/orchestrate, swarm cascade + runs, cca submit/review/implement/detail/impact, vsb board-pack/board-packs/review-gates/decision, v138 ceo chat/vitals, federation twins, twin models) and read board.py, business_plan.py, swarm.py, change_control.py, vsb.py (board-pack + gates), v138/ceo.py, genesis.py seeding, heartbeat.py, plus BoardOfDirectors.tsx, CEOChat.tsx, SwarmIntelligence.tsx, LivingOrganisationHub.tsx, AgentHubPanel.tsx, BusinessPlan.tsx, ChangeControlAgency.tsx, VSBCockpit.tsx, GenesisJourney.tsx. UI behaviour is reasoned from source (no browser). Store counts via API: 7 business plans, 2 persisted org-cascade runs, 8 CCA records, 2 board packs on one VSB, 4 VSB entities each carrying a board, 0 twin models, 0 registered hub agents, board_directives store holding both chief_instruct and board_directive rows. What genuinely works: the org cascade executes the whole Chief→Build-to-Order chain with appraisals, real fabric requisitions, gaas gate (arms_length:true), DCS hashes and a UEG seal; chief_instruct lands objectives on the scoped living plan; the living roadmap is derived and integrated into the plan page and the Cockpit; board packs assemble with history + DCS; review-gate records + decisions are DCS-audited; the Agent Hub is candid about being an unoccupied bus. The gap that remains, in order of how badly a person is misled or blocked: (1) the DEFAULT 'AI CEO' tab is a detached Ollama 'Galactic Era' roleplay outside the native fabric, unconnected to the Board/plan/VSB, with a status pill that cannot see its own fallback; (2) Mode 3 human review gates never block anything — proven by delivering a 'Deliver the design' objective on a VSB whose design gate is rejected; (3) Change Control reads no identity: an 'ai_ceo' CRITICAL constitutional change was override-approved and implemented, the 'AI review' decision is a health-threshold default under the floor, and 'digital-twin pre-validation' is the same health gate; (4) 'Chief: AI-Generate' writes undisclosed floor prompt-echo into the Chief-owned Executive Summary · Concept · Vision with no owner-edit UI to undo it; (5) the Chief 'twin' is a prompt preamble with no autonomous loop, while the Board/Swarm chips paint the floor green although the platform's shared badge would paint it amber; (6) §17.3 cadences and the ≤5-min staleness invariant are unimplemented and the pack's 'action_plan' layer is the board roster; (7) board packs certify substanceless floor narratives as coverage 1.0; (8) Mode 2 expert twins do not exist. Legacy off-chain routes (/api/ceo/delegate, /api/csuite/*) are API-only orphans; /api/csuite/cto/infrastructure hardcodes ai_provider 'Ollama (local)'. The honest fixes are mostly small: route the CEO chat through the gateway, one shared gate guard, identity + decision_source in the CCA, provenanceBadge on the apex chips, and a refusal to persist floor text into the plan opening.

### R3.0 · §5 AI CEO tier (Living Organisation hub, default tab /ceo → CEOChat.tsx) — **STUB**

- **severity (assessor):** critical
- **claim:** The AI CEO receives the Board's action plan, integrates the living management systems (BMS·QMS·DCS·EMS) and delegates to the specialist C-Suite; every tier runs on Workstation's OWN native fabric (§6); honesty over polish (§15.6).
- **observed:** The default tab of the Living Organisation hub is a chat that (a) bypasses the native gateway entirely — agentic_core/api/v138/ceo.py calls Ollama directly at OLLAMA_BASE_URL with DEFAULT_MODEL='llama3.2', ignoring AI_DISABLE_LOCAL and the §6 provenance/posture; (b) prompts the model to 'respond as the AI CEO of the Galactic Era. Cite relevant constitutional articles' and greets the user 'Greeting, Guardian. I am the VSB AI CEO. Our collective resonance is reaching multi-dimensional thresholds.'; (c) reads nothing from the Board directives, the living business plan, any VSB or the cascade — it is not the AI CEO of the organisation; (d) on a 'wish i could'/'can you create' message it registers a lambda and tells the user 'I have autonomously created and registered tool_xxxx' (a fabricated capability). When Ollama is absent the reply is '[Offline Mode] The AI model is still loading…' but CEOChat.tsx only flips its status pill on the strings '(Ollama Offline)' / 'Simulated synthesis', so the header keeps showing 'Planetary Strategy Active' over a fallback reply.
- **evidence:** Live: POST /api/v138/ceo/chat #1 → '[Offline Mode] The AI model is still loading. This usually resolves after the…'; #2 → 'Greetings, fellow sentient beings of the Galactic Era. As the AI CEO… Article III, Section 2 of the Galactic Constitution…' (a real llama3.2 completion — proving the route sidesteps the AI_DISABLE_LOCAL fabric that /api/v1/native-ai/status reports as floor_active:true, ollama available:false). Code: C:/Users/rehan/Workstation/agentic_core/api/v138/ceo.py lines 28-31 (router 'AI CEO Galactic Era', OLLAMA_BASE_URL, DEFAULT_MODEL), 383-471 (generate_ollama_stream, 'Galactic Era' prompt, '[Offline Mode]' fallback), 386-389 (fake tool registration), 336-337 (lambda tool). UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/CEOChat.tsx lines 9, 45, 87-92 (detection strings), 159-161 ('Planetary Strategy Active'); C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/LivingOrganisationHub.tsx line 14-15 (default tab = ceo). grep: AI_DISABLE_LOCAL is honoured only in ai/gateway.py:310 and ai/native/model_resource.py:39 — not in v138/ceo.py.
- **disclosed to the user:** No. The surface presents itself as 'VSB AI CEO' with a green 'Planetary Strategy Active' pill; nothing tells the user this chat is a detached Ollama roleplay outside the native fabric, unconnected to the Board/plan/VSB; the fallback reply is not detected by the UI's status pill.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced live and it is worse than described. The default tab of /ceo (LivingOrganisationHub.tsx line 25 falls back to 'ceo') is a chat that bypasses the native fabric entirely: agentic_core/api/v138/ceo.py posts straight to OLLAMA_BASE_URL with DEFAULT_MODEL='llama3.2' and never consults AI_DISABLE_LOCAL (grep: honoured only in ai/gateway.py:310, ai/native/model_resource.py:39/64, ai/native/orchestrator.py:269). With Ollama running on this machine, my two probes returned real llama3.2 completions while /api/v1/native-ai/status reports ollama available:false. The persona is 'AI CEO of the Galactic Era' citing invented constitutional articles; a 'can you create' message registers a lambda (lines 336-337) and the model then narrates 'tool_87f3 has been successfully integrated… responsible for sending weekly emails to our learners' — a fabricated capability. It reads nothing from the Board directives, the living plan or any VSB. The UI's fallback detection (CEOChat.tsx 87-92) matches '(Ollama Offline)'/'Simulated synthesis' but the backend emits '[Offline Mode]…', so the pill stays green 'Planetary Strategy Active' (line 160) over a fallback. Not disclosed anywhere on the surface.
- **refuter's evidence:** Live: POST http://127.0.0.1:8024/api/v138/ceo/chat #1 → 'Greetings, user. I am the AI CEO of the Galactic Era… Article III, Section 2 of the Constitution states…'; #2 ('can you create a tool that emails our learners weekly?') → 'I have autonomously created and registered tool_87f3… has been successfully integrated into our systems'. curl http://127.0.0.1:11434/api/tags → ['llama2:latest','llama3.2:latest','llama3.2:1b']. GET /api/v1/native-ai/status → resources.ollama.available:false. Code: C:/Users/rehan/Workstation/agentic_core/api/v138/ceo.py lines 28-31, 331-337 (lambda tool), 383-389 (fake registration note), 416-419 ('Galactic Era' prompt), 452-466 ('[Offline Mode]' fallback); mounted at C:/Users/rehan/Workstation/agentic_core/app_mvp.py:77-78. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/CEOChat.tsx lines 9, 45, 87-92, 158-161; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/LivingOrganisationHub.tsx lines 14, 25. Built bundle apps/workstation-superapp/dist/assets/index-BsaMh3Mt.js contains both 'api/v138/ceo/chat' and 'Planetary Strategy Active'; GET /ceo → HTTP 200 text/html.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Route CEOChat through gateway.query_meta (honouring AI_DISABLE_LOCAL and provenance), ground the system prompt in the scoped living plan + latest board directive instead of 'Galactic Era', drop the fake tool-registration branch, match the pill on the real '[Offline Mode]' marker (or on served_by), and either make the AI CEO tab the cascade's CEO tier or move it off the default tab.
- **status now:** FIXED W451 (P1.3) — /api/v138/ceo/chat runs through gateway.stream_meta (in-house first, honours AI_DISABLE_LOCAL, breaker, learning loop, tenant memory, profile, guardrail), grounded in the Board's directives + living plan + business plan + the real meeting log; the terminal frame names served_by; persona, lambda tool registration, Redis mock and canned advisory deleted. The floor's answer is the floor's structured frame, labelled amber — substance needs the owned model.

### R3.1 · §17.4 Mode 3 — human review gates (vsb.py review-gates, GenesisJourney.tsx panel) — **STUB**

- **severity (assessor):** high
- **claim:** Mode 3 — optional human review gates at any Concept→Commercialisation stage (set in the VSB genome); per-stage status + blocks_progress; each decision DCS-audited.
- **observed:** Gate configuration, human approve/reject decisions and DCS hashes are recorded correctly (DELIVERED as a record), but no lifecycle process ever consults them: `review_gates`/`blocks_progress` are read only by the four review-gate endpoints themselves. A stage whose gate is REJECTED (blocks_progress:true) does not block delivery, evolution, shipping or plan orchestration.
- **evidence:** Live on vsb-2473e80e76: GET /api/v1/vsb/vsb-2473e80e76/review-gates/design → {gated:true, status:'rejected', blocks_progress:true}; then POST /api/v1/business-plan/objective/obj-6cb1f2de/orchestrate (objective 'Deliver the design', scope vsb-2473e80e76) → HTTP 200, status_advanced:true (planned→in_progress), qms_passed:true; the entity stays stage 'build' / status 'operational'. Earlier in the store the §5 cascade 0a921c5ec5 also delivered against this VSB at 20:45:56Z after the design gate was rejected at 20:44:27Z. Code: C:/Users/rehan/Workstation/agentic_core/api/vsb.py lines 1093-1103 (_gate_status computes blocks_progress) — ripgrep for review_gates|blocks_progress across agentic_core returns only vsb.py:1093-1180 (its own endpoints); business_plan.py orchestrate (lines 233-320), swarm.py cascade, vsb.py /evolve, /repo/ship never read the gates. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/synthesis/GenesisJourney.tsx lines 997-1030 renders status chips only.
- **disclosed to the user:** No. The API labels the field blocks_progress and the panel says 'Human review gates (Mode 3) … tap a stage to gate/ungate'; nothing says the gate is advisory. A founder who rejects the design gate reasonably believes the organisation stops.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced live with two independent lifecycle paths. With the design gate REJECTED (blocks_progress:true) the Chief workflow-tree orchestration of 'Deliver the design' ran to HTTP 200 and appended a new delivery review; with the launch gate PENDING (blocks_progress:true) the §5 cascade bound to 'Launch to market' ran to HTTP 200, wrote 'QMS gate passed' and advanced the plan binding. Neither response mentions a gate. ripgrep confirms review_gates/blocks_progress are read only by vsb.py's own four endpoints (plus one comment at line 218); business_plan.orchestrate, swarm.cascade, /evolve and /repo/ship never consult them. The Genesis panel text says only 'tap a stage to gate/ungate; each change is DCS-audited' — nothing says the gate is advisory. On top of the surface, the vision's own §17.4 tags Mode 3 '✅ DELIVERED (W126)' — a doc overclaim as well. The record-keeping is real; the gating is a pretence, so STUB is right.
- **refuter's evidence:** Live: GET /api/v1/vsb/vsb-2473e80e76/review-gates → stages ['design','launch'], design status 'rejected' blocks_progress true, launch 'pending' blocks_progress true. POST /api/v1/business-plan/objective/obj-6cb1f2de/orchestrate {scope:'vsb-2473e80e76'} → HTTP 200; plan now shows a review at 2026-09-05T11:40:59Z 'Chief workflow-tree delivery — decision=proceed…'. POST /api/v1/swarm/cascade {scope:'vsb-2473e80e76', objective_id:'obj-52dbd6fa', csuite_roles:[CSO,CFO]} → HTTP 200 in 4.0s; persisted run cd31e5bd02: governance 'allowed', plan_binding {advanced:true, result:'review_written'}, quality qms_gate_passed true; 'review_gate'/'blocks_progress' absent from the response. Code: C:/Users/rehan/Workstation/agentic_core/api/vsb.py lines 1093-1103 (_gate_status/blocks_progress), 1116-1180 (the only readers); rg 'review_gates|blocks_progress|review-gates' agentic_core → vsb.py only. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/synthesis/GenesisJourney.tsx lines 999-1030. Vision: C:/Users/rehan/Workstation/docs/WORKSTATION_IDBO_WHOLE_VISION.md §17.4 Mode 3 line.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add one shared guard (e.g. vsb._assert_stage_not_blocked(vsb_id, stage)) and call it from business-plan orchestrate, swarm cascade plan-binding, /evolve, /repo/ship and genesis stage advancement, returning 409 with the gate decision; until then rename blocks_progress→would_block and state 'advisory — not yet enforced' in the panel.
- **status now:** FIXED W452 (P1.4) — blocks_progress is consumed by every mover through vsb._refuse_gated; the panel says what a gate does; the Cockpit renders the refusal with the gate named.

### R3.2 · §5 Change Control (arms-length Agency) + §17.5 arms-length / twin pre-validation invariants — change_control.py, ChangeControlAgency.tsx — **PARTIAL**

- **severity (assessor):** high
- **claim:** Change Control governs all change at arm's length; the AI CEO cannot instruct the Board/genome; digital-twin pre-validation before major change; HIGH = AI review + constitutional check + manual flag; CRITICAL = blocked pending explicit admin approval (module docstring).
- **observed:** Mechanics (submit/queue/review/implement/audit trail, LOW auto-approval held under immune threat, config_change applied through the reconfiguration engine) work. But: (1) no identity is read anywhere in the CCA — submitted_by is free text and review/implement have no user dependency, so an 'ai_ceo' submitter can file a CRITICAL constitutional change and any caller can approve it with override_decision and implement it; (2) under the native floor the 'AI review' never emits [DECISION:…], so the decision is a default rule (non-CRITICAL + composite_health ≥ 0.5 → approved; CRITICAL → rejected) while the stored review_result is keyword salad with no reasoning; (3) the §17.5 'digital-twin pre-validation' degenerates the same way — no [TWIN:] marker → 'pass' whenever organism health ≥ 0.6 (source 'health_gate_default'); (4) the docstring's 'manual flag' for HIGH and 'admin approval' for CRITICAL do not exist in code. The ChangeControlAgency page says 'every organism change is tier-gated, AI-reviewed, and logged'.
- **evidence:** Live: POST /api/v1/cca/submit {change_type:'constitutional', submitted_by:'ai_ceo'} → cca-56b0a94d97 tier CRITICAL; POST …/review {override_decision:'approved'} → approved; POST …/implement → status 'implemented', twin_prevalidation 'pass' source 'health_gate_default', applied:null. Earlier record cca-1bc5ca7ab0 (organism_mutation, HIGH) approved by 'cca_ai' with a salad review then implemented. Code: C:/Users/rehan/Workstation/agentic_core/api/change_control.py lines 14-17 (tier promises), 486-524 (override + default-rule decision), 399-446 (_twin_prevalidate health-gate fallback), 553-580 (implement); grep shows no Depends/get_current_user in the file. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/ChangeControlAgency.tsx line 417 ('every organism change is tier-gated, AI-reviewed, and logged'), 176-185 (renders 'Review — approved' + raw review text).
- **disclosed to the user:** Partly. The review text carries the floor prefix line and the twin verdict shows '(health_gate_default)', but the UI presents 'approved' as the outcome of an AI review when it came from a health-threshold default; nothing discloses that CRITICAL can be override-approved by any caller or that arms-length is not enforced by identity.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed from the stored record and the code. The CCA's mechanics (submit/queue/review/implement, immune-held LOW auto-approval, config_change applied through the reconfiguration engine, twin pre-validation recorded with an honest source) work. But change_control.py reads no identity anywhere (imports only APIRouter/HTTPException; submitted_by is free text; review/implement have no user dependency), override_decision is honoured for every tier including CRITICAL, the floor-served 'AI review' falls to a default rule (non-CRITICAL + composite_health ≥ 0.5 → approved), and the §17.5 twin pre-validation degenerates to a health threshold ('health_gate_default'). The module docstring's 'HIGH … manual flag' and 'CRITICAL — blocked pending explicit admin approval' (and resource_fabric.py:236 'CRITICAL blocked') exist nowhere in code. The W345 arms-length test only proves AI-CEO-tier surfaces cannot mutate board/genome_spec; it does not cover an 'ai_ceo' submitter filing and any caller override-approving a constitutional change. The UI banner asserts every change is 'tier-gated, AI-reviewed, and logged' and renders 'Review — approved' with the twin verdict source shown — disclosure is partial.
- **refuter's evidence:** Live: GET /api/v1/cca/cca-56b0a94d97 → change_type 'constitutional', impact_tier CRITICAL, submitted_by 'ai_ceo', review_result 'Manual override: probe override', twin_prevalidation {verdict:'pass', source:'health_gate_default'}, status 'implemented', audit_trail [submitted, review_started, approved, twin_prevalidation_pass, implemented]. GET /api/v1/cca → by_status {submitted:7, implemented:3, rejected:1, approved:1}. Code: C:/Users/rehan/Workstation/agentic_core/api/change_control.py lines 14-17 (tier promises), 38-39 (imports — no auth dependency), 485-489 (override honoured for any tier), 518-524 (default rule), 437-441 (health_gate_default), 553-580 (implement); C:/Users/rehan/Workstation/agentic_core/api/resource_fabric.py:236. Test: C:/Users/rehan/Workstation/integration_tests/test_mvp_spine.py lines 4539-4560 (arms-length leg covers board/genome record only). UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/ChangeControlAgency.tsx lines 155-171 (Review — approved + raw text; twin source shown), 396 ('every organism change is tier-gated, AI-reviewed, and logged').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Require an Owner/admin identity (Depends(get_current_user) + role) for override_decision and for CRITICAL implement; refuse submissions whose submitted_by is an AI tier for genome/constitutional types; when no [DECISION:] marker is found, record decision_source:'health_default' and render it as such (amber) in the UI; correct the module docstring to what the code does.

### R3.3 · §5 / §15.9 Chief-owned Business Plan opening (Executive Summary · Concept · Vision), AI-generable in-house then owner-edited — business_plan.py /generate, BusinessPlan.tsx — **PARTIAL**

- **severity (assessor):** high
- **claim:** The Chief-owned plan opens with Executive Summary · Concept · Vision — the founder's idea framed by the digital-twin Chief — AI-generated in-house from the founder's description, then owner-edited.
- **observed:** 'Chief: AI-Generate' parses the model's '## Executive Summary/Concept/Vision/Mission/Strategy' blocks straight into the plan. Under the native floor the blocks are prompt-echo salad that ignores the owner's description (the floor keyed on the prompt preamble: 'drafting refreshing', 'scope b-gen-probe'), the floor's disclosure prefix is discarded by the parser (it precedes the first '##'), chief_draft is stored but never rendered, BusinessPlan.tsx shows the fields in a 'Chief's Opening' card with no provenance chip, and the 'never clobber non-empty fields' rule makes the salad permanent through the UI because no page calls POST /business-plan/set (no owner-edit surface exists). Genesis-seeded VSB plans open with template sentences ('X is a living VSB IDBO established to solve: …'), not a Chief framing.
- **evidence:** Live: POST /api/v1/business-plan/generate {scope:'r3b-gen-probe', context:'A halal meal-prep subscription for university students in Leeds'} → plan.executive_summary = "Subject: You are the Chief of the Board (the Owner's digital twin) drafting/refreshing the LIVING business plan for scope 'r3b-gen-probe'. (domain: the stated domain). Native structured synthesis grounded in the input's salient terms: - drafting refreshing - scope b-gen-probe …"; concept/vision/mission/strategy identical in kind; objectives_added 0. Code: C:/Users/rehan/Workstation/agentic_core/api/business_plan.py lines 332-370 (section parser drops pre-heading lines, fills only empty fields, stores chief_draft), 83-87 (_q uses gateway.query, no provenance). UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/BusinessPlan.tsx lines 87-97 ('Chief's Opening' card, no badge); grep 'business-plan/set' in apps/workstation-superapp/src → no caller; grep chief_draft in src → none. Genesis template: C:/Users/rehan/Workstation/agentic_core/api/genesis.py lines 602-611.
- **disclosed to the user:** No. The opening card is titled "Chief's Opening — Executive Summary · Concept · Vision" and shows the salad as the Chief's framing with no floor badge; the user cannot edit or clear it from the UI.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on a fresh scope. Under the floor every Chief-opening field becomes prompt echo that ignores the founder's description ('leeds' appears in none of the five fields), objectives_added is 0, the floor's disclosure prefix precedes the first '## ' heading and is discarded by the parser, and because the parser fills only EMPTY fields the salad is permanent through the UI. No page calls POST /business-plan/set (grep in src → nothing) and chief_draft is never rendered, so there is no owner-edit surface. BusinessPlan.tsx shows the fields in a 'Chief's Opening — Executive Summary · Concept · Vision' card with no provenance chip even though the shared provenanceBadge (W439) exists. Genesis-seeded VSB plans open with template sentences. The capability exists but the shortfall is undisclosed and un-correctable from the UI.
- **refuter's evidence:** Live: POST /api/v1/business-plan/generate {scope:'r3-refuter-gen', context:'A halal meal-prep subscription for university students in Leeds'} → HTTP 200; GET /api/v1/business-plan?scope=r3-refuter-gen → executive_summary and concept both begin "Subject: You are the Chief of the Board (the Owner's digital twin) drafting/refreshing the LIVING business plan for scope 'r3-refuter-gen'…", vision/mission/strategy begin "Native structured content for '…', grounded in: You are the Chief…", objectives 0, chief_draft begins '_[Workstation native structured engine — owned…]_' (dropped from every field). Code: C:/Users/rehan/Workstation/agentic_core/api/business_plan.py lines 334-350 (parser drops pre-heading lines, fills only empty fields), 355-357 (chief_draft stored), 83-87 (_q — no provenance); C:/Users/rehan/Workstation/agentic_core/api/genesis.py lines 602-611. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/BusinessPlan.tsx lines 87-97 (card, no badge); rg 'business-plan/set|chief_draft' apps/workstation-superapp/src → no matches; provenanceBadge at C:/Users/rehan/Workstation/apps/workstation-superapp/src/lib/api.ts lines 65-72.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Use gateway.query_meta in /generate, refuse to write sections when served_by=='native' (return chief_draft with an amber 'structured floor — not model analysis' badge instead), keep the prefix line with each stored section, and add an owner-edit form wired to POST /business-plan/set on BusinessPlan.tsx and VSBCockpit.tsx.

### R3.4 · §5 Chief = founder's digital twin, apex Board (board.py; BoardOfDirectors.tsx; VSBCockpit.tsx Chief panel) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** Chief = the founder's digital twin, continually improving, delivering the founder's role autonomously in the founder's presence AND absence; modelled, simulated and iterated on the founder's instructions and data; each tier manages/appraises/develops the one below.
- **observed:** Chief instruct works end-to-end: gaas.v5-gated, provenance-recorded, UEG-sealed, persisted, and it lands objectives on the scoped living plan (fallback = the instruction itself as one objective). The 'twin' is a static charter plus the last five instructions injected into the prompt (founder_profile); no founder model exists (/api/v1/twin/models total 0), nothing invokes the Chief without a click (the heartbeat only reads the plan for compliance re-screening), and per-VSB Chiefs are titled 'Digital Twin of default' (owner_id). The apex provenance chip in BoardOfDirectors.tsx and the cascade chip in SwarmIntelligence.tsx render the deterministic floor GREEN ('served by native×2 · in-house' / 'in-house') even though the platform's shared provenanceBadge renders served_by=='native' as amber 'structured floor — not model analysis'. The grounded specialist deliberation POST /api/v1/board/directive (directors read live system state) has no UI caller (API_ONLY); the Board tab only lists such deliberations if someone called the API.
- **evidence:** Live: GET /api/v1/board/status → hierarchy Owner→Chief→Board→AI CEO→C-Suite→CoE→BTO, recent_directives [chief_instruct served_by {native:2} gov 'allowed'; board_directive served_by {native:4}]; GET /api/v1/vsb/vsb-2473e80e76 → board.chief.title 'Chief of the Board — Digital Twin of default'; GET /api/v1/twin/models → total 0. Code: C:/Users/rehan/Workstation/agentic_core/api/board.py lines 128-146 (founder_profile = values string + last 5 instructions), 258-305 (chief_instruct fallback objective), 320-360 (board_directive) — grep for callers of /board/directive in src and agentic_core finds only a routing table in cognition.py (no caller); C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py lines 49-56 (plan read for compliance text only). UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/BoardOfDirectors.tsx lines 84-86 ('represents you faithfully in your presence and absence'), 160-163 (green chip); C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/SwarmIntelligence.tsx lines 251-253; shared badge C:/Users/rehan/Workstation/apps/workstation-superapp/src/lib/api.ts lines 65-72.
- **disclosed to the user:** Partly. The directive text opens with the floor's own prefix line, but the chip colour and 'in-house' wording read as a healthy owned model, and the page header asserts presence-and-absence autonomy that nothing implements.
- **refutation: SURVIVED** (reproduced by the refuter). Verified. Chief instruct is genuinely gated (gaas 'allowed'), provenance-recorded, UEG-sealed and lands objectives on the scoped plan (fallback = the instruction as one objective with placeholder KPI/timeline). But the 'twin' is a static values string plus the last five instructions (founder_profile); /api/v1/twin/models holds 0 models; per-VSB Chiefs are titled 'Digital Twin of default'; nothing invokes the Chief unprompted (heartbeat auto_align calls align(execute=False), which never reaches board_directive). POST /board/directive's only in-tree caller is cognition.align with execute=true, and the sole UI caller (CognitionIntegration.tsx:46) posts execute:false — so the grounded director deliberation is API_ONLY. BoardOfDirectors.tsx inlines a green 'served by native×2 · in-house' chip instead of the shared provenanceBadge that would render the floor amber ('structured floor — not model analysis'). I add one doc overclaim the assessor missed: the canon's own scorecard rates 'Chief = Owner's digital twin (apex, arms-length)' as 'strong' on the single evidence that the /api/v1/board route exists.
- **refuter's evidence:** Live: GET /api/v1/board/status → hierarchy Owner→Chief→Board→AI CEO→C-Suite→CoE→BTO; recent_directives[0] dir-170d128c chief_directive opens '_[Workstation native structured engine — owned…]_'; GET /api/v1/business-plan → obj-cc6df5e2 directive_id 'dir-170d128c', kpi '(KPI to be set by the Board)', timeline 'next review'; GET /api/v1/twin/models → {total:0}; GET /api/v1/vsb/vsb-2473e80e76 → board.chief.title 'Chief of the Board — Digital Twin of default', owner_id 'default'. Code: C:/Users/rehan/Workstation/agentic_core/api/board.py lines 128-146 (founder_profile), 258-283 (chief_instruct + fallback objective); C:/Users/rehan/Workstation/agentic_core/api/cognition.py lines 113-149 (execute default False; board_directive only under execute); C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/CognitionIntegration.tsx:46 (execute:false); C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py lines 266-272 (align(execute=False)). UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/BoardOfDirectors.tsx lines 84-86, 159-162 (inline green chip); shared badge C:/Users/rehan/Workstation/apps/workstation-superapp/src/lib/api.ts lines 65-72. Canon overclaim: C:/Users/rehan/Workstation/agentic_core/api/transformation.py lines 96-98; C:/Users/rehan/Workstation/agentic_core/api/living_plan.py:36.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Use lib/api.ts provenanceBadge on the Board and Swarm chips; soften the Board header to 'acts when you instruct it — no autonomous Chief loop yet'; title per-VSB Chiefs by the founder's name, not owner_id; add a 'Convene the Board' button wired to /board/directive so the directors' grounded inputs reach the user.
- **status now:** FIXED W453 (P1.5) — BoardOfDirectors' inline 'served by native×N · in-house' chip is the map helper: amber floor label on the floor, the models named otherwise.

### R3.5 · §17.3 Living Business System — Strategic (AI CEO; quarterly + market signal), Action Plan (BTO; weekly + KPI-triggered), §17.5 'living business plan ≤5-min staleness' — **MISSING**

- **severity (assessor):** medium
- **claim:** Four continuously-maintained layers WITH CADENCE: Constitutional (genome-locked) · Strategic refreshed quarterly + on market signal · Action Plan refreshed weekly + KPI-triggered · Board Pack on demand; invariant: living business plan ≤5-min staleness.
- **observed:** Only the on-demand Board Pack and the derived roadmap exist. No scheduler, heartbeat step or KPI trigger refreshes the strategic or action-plan layers; the plan changes only when a user clicks or a cascade/orchestration writes a review. In the Board Pack the 'action_plan' layer is the Board ROSTER (vsb.board) and 'strategic' is the salad ceo_specification, so the layers are labels over existing fields rather than maintained artefacts. The Constitutional layer's 'values' is a hardcoded constant identical for every VSB regardless of the founder's values.
- **evidence:** ripgrep quarterly|weekly|KPI-triggered|staleness across agentic_core → only management_systems.py prompt text (lines 96, 117), economy.py:553 and vsb.py repo staleness (unrelated); C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py drives evolution/compliance beats (interval 60s) but never refreshes the plan. Board Pack layers: C:/Users/rehan/Workstation/agentic_core/api/vsb.py lines 997-1003 (constant values string), 1028-1033 ('strategic': ceo_specification; 'action_plan': {'board': vsb.board}). Vision §17.3/§17.5 text at C:/Users/rehan/Workstation/docs/WORKSTATION_IDBO_WHOLE_VISION.md lines 452-457, 462-466.
- **disclosed to the user:** No. The Genesis Board Pack chip lists 'constitutional · strategic · action_plan · operational' as if four maintained layers were assembled; nothing states that no cadence exists.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed for the cadence-maintained layers, with one nuance. No scheduler, heartbeat step or KPI trigger refreshes a Strategic or Action-Plan layer: grep for quarterly/weekly/KPI-triggered hits only prompt text; generate_plan has no caller outside business_plan.py; heartbeat's only plan touches are the compliance re-screen (reads plan text) and opt-in auto_align → cognition.align(execute=False), which routes vision gaps and writes nothing to the plan. In the Board Pack, 'strategic' is the floor-salad ceo_specification and 'action_plan' is the board roster, and constitutional.values is one constant string for every VSB. Nuance: the §17.5 '≤5-min staleness' invariant was honestly restated in the W345 test as 'assembled fresh on demand' (read-time derivation), and that read-time freshness IS delivered — so that clause is a DOC_OVERCLAIM in the vision wording rather than a missing capability. The Strategic/Action cadence layers themselves are MISSING, and the Genesis chip lists four layer keys with no cadence statement.
- **refuter's evidence:** rg -i 'quarterly|weekly|KPI-triggered' agentic_core → only management_systems.py:96,117 and v310/business.py:50,84 (prompt/format text); rg 'generate_plan|business-plan/generate' agentic_core excluding business_plan.py → none. C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py lines 33-57 (compliance beat reads plan text), 130-135 (opt-in flags), 266-272 (align execute=False); C:/Users/rehan/Workstation/agentic_core/api/cognition.py lines 117-149. Board Pack: C:/Users/rehan/Workstation/agentic_core/api/vsb.py lines 997-1000 (constant values), 1028-1033 (strategic=ceo_specification, action_plan={'board':…}); live GET /api/v1/vsb/vsb-2473e80e76/board-pack → layers.strategic.ceo begins '_[Workstation native structured engine…]_', layers.action_plan keys ['board'], values 'Integrity · Compassion · Excellence · Halal/Sharia · Beneficence · Stewardship'. Staleness restatement: C:/Users/rehan/Workstation/integration_tests/test_mvp_spine.py lines 4595-4600 ('(4) PLAN FRESHNESS (the honestly-restated invariant: assembled fresh on demand)'). Vision: C:/Users/rehan/Workstation/docs/WORKSTATION_IDBO_WHOLE_VISION.md §17.3, §17.5. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/synthesis/GenesisJourney.tsx line 983.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either add a heartbeat step that re-derives the action-plan layer from KPI/review changes and stamps last_refreshed per layer (rendering staleness in the Cockpit), or relabel the pack layers to what they are ('board roster', 'CEO specification') and strike the cadence claims from §17.3 until built.

### R3.6 · §17.3 Board Pack (POST /api/v1/vsb/{id}/board-pack; Genesis 'Assemble Board Pack') — **PARTIAL**

- **severity (assessor):** medium
- **claim:** Board Pack assembled fresh from live data with an in-house AI-CEO narrative, QMS-gated, §11-compliance-screened, DCS-registered, with history; surfaced on the Genesis page.
- **observed:** Assembly, history, DCS hash, compliance screen and the Genesis button all work (verified). Under the floor the narrative is salad and, because this VSB's blueprint concept/commercialisation are empty, it is grounded in nothing ('Subject: . Commercialisation: .'); the §10 assure_delivery gate still reports delivery_coverage 1.0 and stub_found false, and the Genesis card shows green 'DCS-registered' + 'compliance: pass' chips over that narrative. Provenance is recorded (served_by native) but not rendered.
- **evidence:** Live: POST /api/v1/vsb/vsb-2473e80e76/board-pack → narrative '_[Workstation native structured engine — owned…]_ … ## Executive Summary | Subject: . Commercialisation: . … - commercialisation', quality_assurance.quality.delivery_coverage 1.0, stub_found false, dcs_registered true, ai_provenance {native:1}; GET …/board-packs → 2 packs. Code: C:/Users/rehan/Workstation/agentic_core/api/vsb.py lines 989-1051. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/synthesis/GenesisJourney.tsx lines 974-995 (chips + narrative, no provenance badge).
- **disclosed to the user:** Partly — the floor prefix line is visible in the rendered narrative, but the green QMS/compliance/DCS chips certify a pack with no substance.
- **refutation: SURVIVED** (reproduced by the refuter). Verified live. Assembly, history, DCS hash, compliance screen and the Genesis button all work. Under the floor the narrative is grounded in nothing on this VSB (blueprint keys are empty, so the prompt reads 'Concept: . Commercialisation: .') and the §10 assure_delivery gate still certifies it with delivery_coverage 1.0 and stub_found false; the Genesis card shows green 'DCS-registered' and 'compliance: pass' chips over that narrative, and served_by is recorded but never rendered. I add that all three 'assembled fresh' packs in the history carry the byte-identical DCS hash — under the floor the pack does not change between assemblies, so DCS registration cannot distinguish point-in-time packs. The only disclosure is the floor prefix line inside the narrative text.
- **refuter's evidence:** Live: GET /api/v1/vsb/vsb-2473e80e76/board-pack → narrative '_[Workstation native structured engine — owned…]_ … ## Executive Summary\nSubject: . Commercialisation: . (domain: the stated domain)…', quality_assurance.quality {qms_gate_passed:true, delivery_coverage:1.0, stub_found:false, document_controlled:true}, compliance.overall 'pass', ai_provenance {served_by:{native:1}}; GET …/board-packs → 3 packs (2026-09-04T20:44:21Z, 2026-09-05T01:39:04Z, 2026-09-05T06:37:24Z) all dcs_hash 'b4281ff525f6…'; GET /api/v1/vsb/vsb-2473e80e76 → blueprint keys [], concept ''. Code: C:/Users/rehan/Workstation/agentic_core/api/vsb.py lines 988-1051 (board pack; prompt at 1006-1012; qa at 1019-1022). UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/synthesis/GenesisJourney.tsx lines 974-995 (layers/DCS/compliance chips + narrative, no provenance badge).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Render provenanceBadge(pack.ai_provenance) beside the chips and have assure_delivery treat a served_by=='native' narrative as 'structure only — not assessed' rather than coverage 1.0; refuse to assemble when concept and commercialisation are both empty.
- **status now:** gate half FIXED W449 — the board pack's verdict is 'not assessable' on the floor, never 'pass' (its own generation provenance reaches the gate); the empty-concept refusal and the identical-hash disclosure remain P1.14.

### R3.7 · §5 org cascade Chief → Board → AI CEO → C-Suite → CoE → BTO → Build-to-Order → Products (POST /api/v1/swarm/cascade; SwarmIntelligence.tsx) — **PARTIAL** *(assessed DELIVERED)*

- **severity (assessor):** low
- **claim:** Delivery is performed by the whole chain, each tier managing/appraising/developing the tier below, integrating BMS·QMS·DCS·EMS, arms-length, reconfigurable with user design control, on the native fabric.
- **observed:** Verified by execution: one call runs every tier (Chief mandate, Board resolution, CEO directive, selected C-Suite officers each with a CoE, BTO programme, Build-to-Order, catalogue proposal), produces upward appraisals + persisted tier development, requisitions and RUNS real fabric facilities (organism status, compliance check), passes a gaas.v5 gate with arms_length:true, DCS-hashes four documents, seals a UEG hash, persists the run and can bind to a plan objective (advancing it on a QMS pass). The user picks the C-Suite roster and the plan scope (design control). Shortfall: under the floor all 16 tier calls are salad, the UI chips still show green 'in-house' + 'QMS gate: pass · cov 100%' (the floor is disclosed only inside the collapsed tier prose), and the UI hardcodes domain/realm 'enterprise'.
- **evidence:** Live: POST /api/v1/swarm/cascade {mission:'…halal meal-prep…', csuite_roles:[CSO,CFO]} → HTTP 200 in 9.9s; ai_provenance {native:16}; governance {status:'allowed', arms_length:true}; management_systems.integrated [bms,qms,ems,dcms,backbone]; fabric_requisitions ran /api/v1/organism/status and /api/v1/compliance/check; quality.delivery_coverage 1.0; appraisals present; GET /api/v1/swarm/cascade/runs → 2 persisted runs. Code: C:/Users/rehan/Workstation/agentic_core/api/swarm.py lines 300-960. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/SwarmIntelligence.tsx lines 128-160 (domain:'enterprise' hardcoded), 248-262 (chips), 287-300 (tier prose in <details>).
- **disclosed to the user:** Partly — each tier's text opens with the floor prefix line, but the summary chips ('in-house', 'QMS gate: pass · cov 100%') over-certify floor output and served_by is not rendered.
- **refutation: REFUTED — corrected to PARTIAL.** The execution claims hold — I re-ran the cascade myself and the mechanics the vision describes are real (every tier ran on the owned fabric, gaas gate allowed with arms_length true, real facilities requisitioned and run, QMS gate, UEG seal, appraisals, plan binding advanced, user-chosen C-Suite roster and scope). But the verdict is wrong by the stated vocabulary: DELIVERED means 'works as the vision says', and the assessor's own text lists a shortfall — all 16 tier calls were floor-served yet the UI renders a green 'in-house' chip and 'QMS gate: pass · cov 100%', served_by is never rendered, and the composer hardcodes domain/realm 'enterprise'. The platform's own W439 rule (provenanceBadge: the deterministic floor must never wear the green in-house badge) is violated on this surface, and the §10 assure_delivery gate certifies floor salad with stub_found:false — so the summary chips over-certify. That is a PARTIAL with the shortfall only partly disclosed (the floor prefix sits inside the collapsed tier prose).
- **refuter's evidence:** Live (my run): POST /api/v1/swarm/cascade {mission:'R3 refuter: launch the halal-compliant learner onboarding service…', csuite_roles:[CSO,CFO], scope:'vsb-2473e80e76', objective_id:'obj-52dbd6fa'} → HTTP 200 in 4.03s; GET /api/v1/swarm/cascade/runs → run cd31e5bd02: served_by {native:16}, any_external false, governance 'allowed', fabric_requisitions [{resource:'metabolic', ran:'/api/v1/organism/status'},{resource:'compliance', ran:'/api/v1/compliance/check'}], quality {qms_gate_passed:true, delivery_coverage:1.0, stub_found:false}, plan_binding {advanced:true, result:'review_written'}, ueg_hash 82d8895c…, duration_ms 3968; scoped plan obj-52dbd6fa gained review '§5 org cascade cd31e5bd02 delivered against this objective — QMS gate passed'. Code: C:/Users/rehan/Workstation/agentic_core/api/swarm.py lines 300-420 (cascade, provenance, plan grounding), 795-805 (governance arms_length). UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/SwarmIntelligence.tsx lines 137-141 (domain/realm 'enterprise' hardcoded), 250-253 (green 'in-house' chip keyed only on any_external), 258-262 (QMS chip), 298-301 (tier prose in <details>); W439 rule at C:/Users/rehan/Workstation/apps/workstation-superapp/src/lib/api.ts lines 62-72.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Render provenanceBadge(served_by) in the cascade chip row and let the user choose domain/realm; have the QMS chip say 'structure-checked only' when served_by=='native'.
- **status now:** chip half FIXED W453 (P1.5) — SwarmIntelligence's cascade 'in-house' chip and the run rows route through the helpers (the gate half closed W449).

### R3.8 · §17.4 Mode 2 — Expert → Digital-Twin human node (24/7 expertise without the expert present) — **MISSING**

- **severity (assessor):** medium
- **claim:** Mode 2 — an expert becomes a digital-twin human node providing 24/7 expertise; stated as the basis of the Chief twin.
- **observed:** No implementation of an expert twin exists. The only 'twin' is the Chief prompt-preamble (finding 5). /api/v210/federation/spawn-twin honestly persists a reference record and says so ('not a modelled twin'); /api/v1/twin/* models systems/processes, not people, and holds zero models; ripgrep for expert twin / human node across agentic_core returns nothing.
- **evidence:** Live: GET /api/v210/federation/twins → 4 entries that are just the VSB list; GET /api/v1/twin/models → total 0. Code: C:/Users/rehan/Workstation/agentic_core/api/integration_surface.py lines 344-372 (spawn_twin reference-only note, twin_blueprint = VSB record). Vision text C:/Users/rehan/Workstation/docs/WORKSTATION_IDBO_WHOLE_VISION.md lines 459-462.
- **disclosed to the user:** There is no user surface claiming Mode 2, so nothing misleads at the UI; the overclaim lives in the vision's §17.4 listing it as one of three delivered-shape modes.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed: nothing implements an expert digital-twin human node. grep for expert-twin / human-node across agentic_core returns nothing; /api/v1/twin models systems and holds zero models; /api/v210/federation/twins is simply the VSB list; spawn-twin honestly persists a reference and says it is not a modelled twin; the Agent Hub's only 'human' mention is that a handoff record may later be picked up by a human. One correction to the assessor's framing: the vision §17.4 does NOT tag Mode 2 as delivered (only Mode 3 and the Board Pack carry the ✅ markers), so there is no doc overclaim here — the capability is simply absent and no UI pretends otherwise. MISSING stands.
- **refuter's evidence:** rg -i 'expert.{0,20}twin|human.node|twin.{0,20}expert' agentic_core → no matches. Live: GET /api/v1/twin/models → {models:[], total:0}; GET /api/v210/federation/twins → 5 rows (vsb-639f5497a4, vsb-5d357ea055, vsb-0bf07cfb56, vsb-981debd258, vsb-2473e80e76) = the VSB list. Code: C:/Users/rehan/Workstation/agentic_core/api/digital_twin.py lines 1-9 (system modelling API); C:/Users/rehan/Workstation/agentic_core/api/integration_surface.py lines 344-362 (spawn_twin 'not a modelled twin'); C:/Users/rehan/Workstation/agentic_core/api/agent_hub.py:174. Vision: C:/Users/rehan/Workstation/docs/WORKSTATION_IDBO_WHOLE_VISION.md §17.4 (Mode 2 carries no delivery marker).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Mark Mode 2 as 'not built' in §17.4 (DOC_OVERCLAIM), or build the minimal node: an expert profile store + a gateway agent persona grounded in that profile, surfaced as a selectable advisor in the cascade roster.

### R3.9 · §5 Strategy + living Roadmap, Board→plan delegation, per-VSB scoping (business_plan.py roadmap; BusinessPlan.tsx; VSBCockpit.tsx; AgentHubPanel.tsx) — **DELIVERED**

- **severity (assessor):** low
- **claim:** The Chief delivers the plan via Strategy and a living Roadmap (phases · per-phase progress · current phase · next milestone) that updates as the plan progresses, integrated into the plan and the VSB Cockpit; Board direction lands as objectives; the org is scoped per VSB; the respiratory Agent Hub is honest about its occupancy.
- **observed:** Verified: GET /business-plan and /business-plan/roadmap derive phases from real objectives (never persisted, recomputed per read), with overall %, current phase and next milestone; the Business Plan page and the VSB Cockpit both render it; chief_instruct appends objectives tagged with the directive_id to the scoped plan and the page shows the '⌘ dir-…' trace; the Cockpit instructs THAT VSB's Chief (scope=vsb_id); governed runs (cascade, orchestrate) write reviews back and advance planned→in_progress (never auto-done); the Agent Hub tab states plainly that no module consumes hub messages and work-orders are records, not executions. Shortfall: KPI/timeline for floor-landed objectives are placeholders ('(KPI to be set by the Board)', 'next review'), so roadmap phases collapse to 'next review'/'Unscheduled'.
- **evidence:** Live: GET /api/v1/business-plan/roadmap → {living:true, phases:[{timeline:'next review', progress_pct:0, count:1}], current_phase:'next review', next_milestone:{…}}; GET /api/v1/business-plan?scope=vsb-2473e80e76 → 3 objectives, one with a cascade review 'org cascade 0a921c5ec5 delivered… QMS gate passed', roadmap phase 'Unscheduled'; GET /api/v1/hub/agents → registered [] + platform roster. Code: C:/Users/rehan/Workstation/agentic_core/api/business_plan.py lines 93-141 (_roadmap), 284-300 (status advance); C:/Users/rehan/Workstation/agentic_core/api/board.py lines 270-283. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/BusinessPlan.tsx lines 118-140; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/VSBCockpit.tsx lines 145-157, 523-544; C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/AgentHubPanel.tsx lines 126-131.
- **disclosed to the user:** Yes — the roadmap note says it is derived from objectives; placeholder KPI/timeline text is shown verbatim; the Agent Hub discloses its emptiness in amber.
- **refutation: SURVIVED** (reproduced by the refuter). Verified by execution and source. The roadmap is derived live from real objectives (never persisted) with overall %, current phase and next milestone; both the Business Plan page and the VSB Cockpit render it; the Cockpit instructs THAT VSB's Chief with scope=selected; chief_instruct lands directive-tagged objectives on the scoped plan (fallback = the instruction itself with visible placeholder KPI/timeline); my own orchestrate and cascade runs wrote reviews back and advanced status (never auto-done); the Agent Hub tab states in amber that no module consumes hub messages and work-orders are records. One nit the assessor's disclosure note overstates: BusinessPlan.tsx does not render roadmap.note (only VSBCockpit does), but the placeholder '(KPI to be set by the Board)' / 'next review' strings are shown verbatim, so the derivation is visible. DELIVERED stands.
- **refuter's evidence:** Live: GET /api/v1/business-plan/roadmap?scope=vsb-2473e80e76 → {living:true, phases:[{timeline:'Unscheduled', progress_pct:0, count:3}], current_phase:'Unscheduled', next_milestone:{title:'Validate the concept'}, note:'Living roadmap derived from the plan's objectives…'}; GET /api/v1/business-plan → obj-cc6df5e2 directive_id 'dir-170d128c', kpi '(KPI to be set by the Board)', timeline 'next review', status 'planned'; scoped plan reviews after my runs: obj-6cb1f2de 2026-09-05T11:40:59Z (workflow tree), obj-52dbd6fa cascade cd31e5bd02 11:41:10Z; GET /api/v1/hub/agents → registered [] + platform_roster. Code: C:/Users/rehan/Workstation/agentic_core/api/business_plan.py lines 91-141 (_roadmap), 143-152 (integrated into GET), 284-292 (planned→in_progress on QMS pass); C:/Users/rehan/Workstation/agentic_core/api/board.py lines 270-283. UI: C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/BusinessPlan.tsx lines 118-140 (roadmap card, no note), 149-153 ('⌘ dir-…' trace); C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/VSBCockpit.tsx lines 141-156 (scope: selected), 523-544 (roadmap + note); C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/AgentHubPanel.tsx lines 126-131.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None required for correctness; optionally let the Board directive path ask the Chief for a real timeline/KPI before falling back to 'next review' so phases carry dates.

---

## R4 — §6 + §7 + §17.2 — the native AI mandate, the reconfigurable resource fabric, the seven biomimetic layers

**Assessor's region summary:** R4 (§6 + §7 + §17.2), assessed against the live backend at 127.0.0.1:8024 (HEAD 06c51109, AI_DISABLE_LOCAL=1) by executing routes and reading handlers/components. The native-AI mandate's CORE is genuinely delivered: gateway.query_meta routes everything through the in-house orchestrator (ollama → native floor → external only when AI_ALLOW_EXTERNAL=true; the legacy external-first cascade has no callers; external calls are budget-capped), the model estate has a managed lifecycle, and /native-ai discloses the floor honestly ('Deterministic floor active … NOT an LLM'), with a working Primitive Console and a real §8→§6 homeostasis loop. User reconfiguration of cascades and resource compositions is real on /native-ai and /resource-fabric (define·edit·simulate·run·retire, per-stage owned-model routing). The gap is in what reaches people elsewhere. Most consequential: the AI CEO chat — the default tab of the Living Organisation hub — bypasses the fabric with a raw Ollama call (hard-coded model and 'Galactic Era' persona, ignores AI_DISABLE_LOCAL, no provenance, no guardrails/breaker/learning loop) and, when Ollama is down, streams a canned 'sovereign mesh advisory' that the UI cannot detect, so the status pill stays 'online'. Second: under the floor every multi-stage cascade (swarm, VSB delivery swarm, Forge, the 8-stage BDP engine) grounds stages 2+ in the floor's own provenance marker — 'grounded in: external dependency, structured engine' — and 0/8 BDP stages mention the user's challenge; the deliverables are about the engine, not the venture. Third: provenance stops at the API boundary for 47 gateway.query() sites and seven output pages (Intelligence Lab, Forge, Synthesis Studio/Nexus, BTO, Sovereign Evolution) where the only disclosure is an inline marker that reads as a brand line. The Visual Composer tab is a disconnected local canvas with fictional model names and a hard-coded 'GaaS COMPLIANT' badge. 'Bespoke per solution' is one fixed 4-stage template per VSB, and the tree's 'autonomous decomposition' is a keyword template whose planner flag the UI never renders. Of the seven layers: Immune, Self-healing, Nervous (bus), Musculoskeletal and Genome are real (Nervous reflex arcs have zero registrations and can never fire; Genome is a trait toy, not the arms-length VSB genome); Respiratory is the honest-but-hollow Agent Hub; Cardiovascular is a relabelled CPU-idle number plus a self-declared simulated allocator; Endocrine is an unwired PID file — and vbs/quality.py's W434 note vouching for Endocrine/Respiratory as 'real implementations' is itself an overclaim stamped into every quality record. Smaller: a config-disabled local model is scored as an AI failure (one API call dropped immune health to 0.8). DELIVERED items are reported alongside the gaps so the picture is complete.

### R4.0 · §6 native mandate · §15.1 · §5 (AI CEO chat, the default tab of the Living Organisation hub at /ceo) — **PARTIAL** *(assessed STUB)*

- **severity (assessor):** critical
- **claim:** §6: 'Native, owned capability — not a façade over third-party APIs'; every result 'reports served_by'; external/local models flow through the in-platform orchestrator. §15.6: never fabricate; label simulation.
- **observed:** The AI CEO chat bypasses the native fabric entirely. agentic_core/api/v138/ceo.py:383-477 opens its own httpx stream to http://localhost:11434/api/generate with a hard-coded DEFAULT_MODEL='llama3.2' (line 31) and the persona 'AI CEO of the Galactic Era' (line 427), ignoring AI_DISABLE_LOCAL, the gateway rate limiter, guardrails.validate_response, the self-healing breaker, the learning loop, tenant-scoped memory and served_by/is_external. Live: POST /api/v138/ceo/chat returned genuine model prose ('Greetings, esteemed bakery owner! As the AI CEO of the Galactic Era…') while GET /api/v1/native-ai/status on the same process reports floor-only (owned_resources_available=['native']). When Ollama is down the handler emits a canned advisory — '[Offline Mode] … the sovereign mesh advisory framework suggests: maintain current strategic trajectory, verify all subsystem alignments…' (lines 461-470) — typed out character-by-character to look like a stream. CEOChat.tsx:92-96 detects fallback only by the strings '(Ollama Offline)' / 'Simulated synthesis', which the backend never emits, so the header pill stays 'Planetary Strategy Active' (lines 161-163) over fabricated advice.
- **evidence:** C:/Users/rehan/Workstation/agentic_core/api/v138/ceo.py:30-31, 383-477, 493; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/CEOChat.tsx:46, 92-96, 161-169; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/LivingOrganisationHub.tsx:14 (CEO is the default tab); live curl POST http://127.0.0.1:8024/api/v138/ceo/chat vs GET /api/v1/native-ai/status; grep for '11434|/api/generate' outside ai/gateway.py + ai/native/: v138/ceo.py, avatars/api.py, config.py.
- **disclosed to the user:** No. No served_by/badge on the chat; the offline fallback is undetected by the UI and the status pill reads 'online'; the model name is never shown.
- **refutation: REFUTED — corrected to PARTIAL.** The substance holds but STUB is the wrong verdict. Live, the surface is a genuine LLM chat, not a pretence: POST /api/v138/ceo/chat streamed real llama3.2 prose ('Greetings, esteemed bakery owner of Leeds! As the AI CEO of the Galactic Era…') because ceo.py opens its own httpx stream to localhost:11434 (Ollama is up on this host) and ignores AI_DISABLE_LOCAL — while the same process's /api/v1/native-ai/status lists ollama available:false, local_models:[]. That is a §6/§15.6 mandate breach (bypasses the owned orchestrator, gateway rate limiter, guardrails, memory scoping; no served_by; model name never shown) — a working chat with an undisclosed provenance shortfall, i.e. PARTIAL. The offline path is a fabricating canned advisory, but its text begins with the visible prefix '[Offline Mode] Ollama is not running. Start it with `ollama serve`…', so 'disclosed=No' is also too strong: the inline prefix discloses the offline state; what stays undisclosed is the header pill ('Planetary Strategy Active' — CEOChat.tsx:92-96 looks for '(Ollama Offline)'/'Simulated synthesis', strings the backend never emits) and the fact that the advice after the prefix is canned.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/api/v138/ceo.py:30-31 (OLLAMA_BASE_URL, DEFAULT_MODEL='llama3.2'), 427 (persona), 433-445 (direct /api/generate stream), 447-471 (fallback typed char-by-char); C:/Users/rehan/Workstation/agentic_core/app_mvp.py:77-78 (router mounted at /api/v138); C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/CEOChat.tsx:46, 92-96, 161-166; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/LivingOrganisationHub.tsx:14, 25 (CEO default tab at /ceo); live: curl http://localhost:11434/api/tags (llama2, llama3.2 present); live POST /api/v138/ceo/chat vs GET /api/v1/native-ai/status (owned_resources_available ['native']).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Route generate_ollama_stream through gateway.stream()/orchestrator.complete() (in-house-first, honours AI_DISABLE_LOCAL, guardrails, breaker, learning loop, tenant memory) and emit a final SSE event {served_by, is_external}; delete the canned '[Offline Mode] … sovereign mesh advisory' paragraph (let the native floor answer, honestly badged); in CEOChat.tsx set aiStatus from that provenance event and render provenanceBadge beside each assistant message.
- **status now:** FIXED W451 (P1.3) — CEOChat renders a provenanceBadge on every answer and the pill reads from the last answer's provenance ('no answer yet' before one); no 'Planetary Strategy Active', no 'Guardian' greeting, no dead fallback detector; the SSE parser buffers split lines.

### R4.1 · §6 cascades · §7 digital resources · §17.2 Nervous (the deterministic floor inside multi-stage cascades) — **PARTIAL**

- **severity (assessor):** high
- **claim:** §6: 'dynamic, adaptive … cascade trees' that are 'effective'; the floor 'never invents facts — it frames the problem … grounded in the prompt's own content' (engine.py docstring).
- **observed:** Every stage after the first in any floor-served cascade is grounded in the floor's OWN marker text, not the user's problem. engine.py:_field('Prior context') captures the first line of the carried context, which is the provenance marker, so keyword/phrase extraction yields 'external dependency', 'structured engine', 'owned external', 'engine owned'; _role() likewise picks the previous stage's persona. Reproduced live on four surfaces: POST /api/v1/native-ai/swarm (designer stage: 'Variable external dependency / structured engine / owned external'); POST /api/v1/resources/swarm/run for the beehive VSB's delivery swarm (centre-of-excellence and build-to-order stages: 'grounded in: - external dependency - s…'); POST /api/v1/forge/run (the Incubator stage output reads '_Acting as: Digital Petri Dish._ ## Observations …' — role and section leaked from the prior stage); POST /api/v1/intelligence/bdp (8/8 floor-served stages, 0/8 mention 'bakery' or 'Leeds'; every subject line is the role scaffold 'You are the CMO and Chief Strategy Officer of Workstation IDBO…', because intelligence.py:408-412 formats {challenge} without a label the floor's _subject recognises). The 'Build-to-Order delivery plan' and 'Integrated Deliverable' a user receives are therefore about the engine, not their venture.
- **evidence:** C:/Users/rehan/Workstation/agentic_core/ai/native/engine.py:84-125 (_field/_role/_subject/_CONTENT_LABELS incl. 'Prior context'), 141-165; C:/Users/rehan/Workstation/agentic_core/ai/native/orchestrator.py:730-745 (swarm prompt 'Prior context:\n{carry}'); C:/Users/rehan/Workstation/agentic_core/api/forge.py:150-175 (context carry); C:/Users/rehan/Workstation/agentic_core/api/intelligence.py:395-421; live curl outputs quoted above.
- **disclosed to the user:** Partly. served_by='native' is disclosed on /native-ai and /resource-fabric (amber 'structured floor — not model analysis' badge), but nothing says the later stages are self-referential; on Forge and the Intelligence Lab not even the badge exists (see next finding).
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on three of the four surfaces and it is worse than stated on the VSB path. Native swarm (2 stages, bakery context): stage 2's 'Key factors' were 'external dependency / structured engine / owned external / engine owned'. The beehive VSB's saved delivery swarm (swarm-48ea0e7e): stage 1 mentions beehives/varroa 4x with 2 marker-term hits; the final build-to-order stage — the plan the founder receives — mentions beehives/varroa 0x and carries 27 marker-term occurrences. BDP: 8/8 floor stages, 'bakery' appears only in the init echo, every subject is the role scaffold ('You are the CMO and Chief Strategy Officer of Workstation IDBO…'), and section headings are truncated to '## Ma' / '## Mar'. Two extra causes the assessor missed: (a) orchestrator.py:735 builds 'Prior context:\\n' inside an f-string expression, so the prompt carries a literal backslash-n and even stage 1's subject reads 'Prior context:\nA small artisan bakery…'; (b) gateway.query_meta's memory recall (gateway.py:98-108, 140) injects earlier floor output into unrelated single-shot calls — a fresh POST /api/v1/ai/query answered with '_Acting as: AI CEO synthesising a compelling investor-ready executive sum._' and marker terms recalled from the prior BDP run. One correction: my Forge run did not reproduce the role leak (the Incubator stage reads 'Acting as: Digital Incubator', because forge.py puts the stage prompt first so _role() matches it); Forge's real defect is that stage subjects are the stage-prompt scaffold ('As a Digital Petri Dish, run a small-scale controlled experiment…'), with 'artisan bakery' surfacing once. PARTIAL stands: the cascade mechanism and provenance work; later-stage content is about the engine, not the venture.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/ai/native/engine.py:24 (_MARKER), 80-86 (_field), 89-92 (_role), 110-118 (_CONTENT_LABELS incl. 'Prior context'); C:/Users/rehan/Workstation/agentic_core/ai/native/orchestrator.py:734-740; C:/Users/rehan/Workstation/agentic_core/api/forge.py:161-170; C:/Users/rehan/Workstation/agentic_core/api/intelligence.py:405-417; C:/Users/rehan/Workstation/agentic_core/ai/gateway.py:98-108, 140; live POST /api/v1/native-ai/swarm, POST /api/v1/resources/swarm/run {swarm_id:'swarm-48ea0e7e'}, POST /api/v1/intelligence/bdp (32,770 bytes SSE), POST /api/v1/forge/run, POST /api/v1/ai/query.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In engine.py strip the marker line, '_Acting as:' lines and '## <role> output' headers from prior/carried context before _subject/_keywords/_role run, and add 'Task', 'Objective', 'Challenge' to the _subject label set; in intelligence.py prefix the challenge with 'Challenge:' in every stage template so the floor grounds on the user's input (the W336 fix applied to domain routers but not here). Add a regression test asserting a 3-stage floor swarm's stage-3 output contains the user's subject and not 'external dependency'.

### R4.2 · §6 'every result reports served_by' · §15.6 label simulation (provenance coverage across all AI-output surfaces) — **PARTIAL**

- **severity (assessor):** high
- **claim:** §6 ✅ block: 'Every result reports served_by'; §15.6 'honesty over polish (never fabricate; label simulation)'.
- **observed:** Provenance is honest where it exists but does not reach most surfaces. Backend: 47 gateway.query() call sites (provenance-discarding) vs 22 gateway.query_meta(); files with query() and zero served_by: ai_query.py (SolutionsPlatform's /api/v1/ai/query returns {answer} only — live verified), intelligence.py (11 sites; the BDP/SPI/APIE/DDPIE/Nexus engines — the §17.2 Nervous layer — stream 31.6KB of SSE with served_by count 0), business_plan.py, change_control.py, digital_twin.py, management_systems.py (7), transformation.py, sovereign_evolution.py, integration_surface.py. Frontend: IntelligenceLab.tsx, SynthesisStudio.tsx, SynthesisNexus.tsx, BTOCatalog.tsx, ForgePipeline.tsx, SovereignEvolution.tsx, OrganismDashboard.tsx each have 0 served_by/provenanceBadge/floor handling; ForgePipeline.tsx's RunResult interface (line 8) omits the ai_provenance the API does return. The only disclosure on those pages is the inline marker '_[Workstation native structured engine — owned, no external dependency]_', which reads as a brand claim and never says 'not model analysis'. DELIVERED where the W439 class-kill landed: provenanceBadge in 10 files / 22 uses (DomainTool, NativeAI, ResourceFabric, Deliverables, VSBCockpit, VSBSpawnStudio, LivingMarketplace, LawHub, OperationalExcellence, ConversationPanel), amber for the floor.
- **evidence:** grep -c 'gateway.query(' / 'gateway.query_meta(' across agentic_core (47 / 22); per-file counts listed above; C:/Users/rehan/Workstation/agentic_core/api/intelligence.py:366-422; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/developers/ForgePipeline.tsx:8, 78-95; C:/Users/rehan/Workstation/apps/workstation-superapp/src/lib/api.ts:62-73; live POST /api/v1/intelligence/bdp and /api/v1/ai/query.
- **disclosed to the user:** Only via the inline marker text on the affected pages; no badge, no 'not model analysis' wording; ai_provenance dropped on Forge.
- **refutation: SURVIVED** (reproduced by the refuter). Counts and reachability verified. Backend: 47 gateway.query() sites vs 22 query_meta(); files with query() and zero served_by exactly as listed (intelligence.py 11, management_systems.py 7, change_control.py 3, digital_twin.py 3, integration_surface.py 2, plus business_plan, transformation, sovereign_evolution, ai_query). Live: BDP stream 32.7KB with served_by count 0 and 8 floor markers; /api/v1/ai/query returns {answer, query} only. One attribution error that does not change the verdict: that route is served by integration_surface.py:91, not ai_query.py (ai_query.py returns {'response'} — also without served_by). Frontend: served_by/provenanceBadge/floor counts are 0 in all seven named pages, all seven are routed in App.tsx (/intelligence, /synthesis, /nexus, /forge-pipeline, /sovereign-evolution, /bto), and ForgePipeline.tsx:8 drops ai_provenance even though the live Forge response carries {served_by:{native:4}}. provenanceBadge is defined in lib/api.ts:69 ('structured floor — not model analysis', amber) and consumed by 10 files as stated.
- **refuter's evidence:** grep -c 'gateway\.query(' / 'gateway\.query_meta(' across C:/Users/rehan/Workstation/agentic_core (47 / 22) with per-file served_by counts; C:/Users/rehan/Workstation/agentic_core/api/integration_surface.py:91; C:/Users/rehan/Workstation/agentic_core/api/ai_query.py:19-20; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/developers/ForgePipeline.tsx:8, 27; C:/Users/rehan/Workstation/apps/workstation-superapp/src/App.tsx:199-229; C:/Users/rehan/Workstation/apps/workstation-superapp/src/lib/api.ts:69; live POST /api/v1/intelligence/bdp, POST /api/v1/ai/query, POST /api/v1/forge/run.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Convert the remaining gateway.query() call sites to query_meta() and carry {served_by,is_external} in each response (for SSE engines add it to every stage event and the 'complete' event); render provenanceBadge in IntelligenceLab, ForgePipeline (already in payload), SynthesisStudio/Nexus, BTOCatalog and SovereignEvolution; consider changing the floor marker text to '…— structured floor, not model analysis'.

### R4.3 · §6/§7 'Reconfigurable by users' · §5 (Living Organisation hub → Composer tab, /ceo?tab=composer) — **STUB**

- **severity (assessor):** high
- **claim:** §6: 'Reconfigurable by users … of the AI Agent Swarm cascades, workflows and pipelines'; §11/§17.5: 'mandatory GaaS gate on every output'.
- **observed:** VisualAgentComposer.tsx is pure local React state: seed nodes named 'Nematron-1B' and 'Nemoclaw-3B' (fictional models, lines 21-22), '+ Brain/+ Immune/+ Tool' add local nodes, the temperature slider edits local params, and every selected node shows a hard-coded green 'GaaS COMPLIANT' badge (line 143) although nothing is evaluated. 'Export Swarm Blueprint' only downloads a JSON (lines 37-47); nothing is posted to /api/v1/resources/swarm/define, run, simulated or governed, and there is no disclosure text. (The orphaned AgentForge.tsx does disclose 'local draft only; nothing is deployed' — but it is imported nowhere.) By contrast the working cascade designer (define/edit/run, per-stage model) lives on /native-ai and compositions on /resource-fabric.
- **evidence:** C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/VisualAgentComposer.tsx:21-22, 37-47, 141-145; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/LivingOrganisationHub.tsx:19; grep shows AgentForge.tsx has no importer.
- **disclosed to the user:** No. The badge asserts compliance; nothing says the canvas is disconnected.
- **refutation: SURVIVED** (reproduced by the refuter). Fully confirmed. VisualAgentComposer.tsx is pure local React state: seed nodes 'Nematron-1B' and 'Nemoclaw-3B' (fictional; the live model estate is llama2/llama3.2), '+ Brain/+ Immune/+ Tool' push local objects, the temperature slider edits local params only, every selected node renders a hard-coded green 'GaaS COMPLIANT' block with nothing evaluated, and 'Export Swarm Blueprint' only triggers a browser download. The file contains no fetch/axios/apiJson call at all; nothing reaches /api/v1/resources/swarm/define or run. It is reachable as the Composer tab of the default hub (/ceo?tab=composer). AgentForge.tsx (which does disclose 'local draft only') has no importer. The working cascade designer lives on /native-ai and /resource-fabric, so a user on the Composer canvas is designing nothing.
- **refuter's evidence:** C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/VisualAgentComposer.tsx:21-22, 27-36, 38-48, 141-143 (grep for fetch|axios|apiJson|/api/v1 → 0 hits); C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/LivingOrganisationHub.tsx:6, 19; grep -rn AgentForge apps/workstation-superapp/src → no importer; live GET /api/v1/native-ai/models (llama2/llama3.2 are the only real local models known to the estate).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either wire Export/Deploy to POST /api/v1/resources/swarm/define (nodes → stages) and show the returned cascade id + a link to run it, or replace the 'GaaS COMPLIANT' badge with 'not evaluated — local draft' and label the seed names as placeholders; simplest honest option: retire the tab and link to the /native-ai cascade designer.

### R4.4 · §6 'Bespoke-per-solution / per-founder swarms' · §7 user design control in Synthesis Lab · Build-to-Order · Forge — **PARTIAL**

- **severity (assessor):** medium
- **claim:** §6: the swarm cascade is 'synthesised, modelled and simulated bespoke to each solution's needs … and bespoke to each founder'; §7: users reconfigure cascades in 'Synthesis Lab, Build-to-Order, and Forge'; the tree 'autonomously decomposes' a goal.
- **observed:** DELIVERED: /native-ai lets a user define, edit (PUT), save and run cascades with per-stage owned-model routing; /resource-fabric composes 20+ real resources with reconfigurable params, model-and-simulate before commit, run history and retire (all live-verified). SHORTFALLS: (a) the VSB delivery swarm registered at establishment is one fixed 4-stage template (ai-ceo → c-suite → centre-of-excellence → build-to-order, genesis.py:554-557) for every VSB — bespoke only by prepended context (live: the beehive VSB's cascade is that exact template); (b) the workflow tree's decomposition is a keyword template under the floor — the payload honestly says planner='deterministic_template' (orchestrator.py:456-468) but NativeAI.tsx's TreeRun interface omits `planner` and the page copy says 'the native swarm autonomously decomposes it' (lines 767-771); (c) Synthesis Studio and BTO have no cascade/stage editing at all (0 matches for stages/cascade/swarm in SynthesisStudio.tsx and BTOCatalog.tsx); Forge lets a user pick and order 7 resource types but sends only {type} (per-stage config accepted by the API is not exposed) and offers no rerun although /forge/runs/{id}/rerun exists.
- **evidence:** C:/Users/rehan/Workstation/agentic_core/api/genesis.py:540-562; C:/Users/rehan/Workstation/agentic_core/ai/native/orchestrator.py:415-468, 719-760; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/developers/NativeAI.tsx:26-33, 767-771; ForgePipeline.tsx:27; live GET /api/v1/resources/swarm, POST /api/v1/native-ai/tree (planner='deterministic_template').
- **disclosed to the user:** Partly: planner not rendered on the tree view; template nature of VSB swarms not stated; Forge/BTO/Synthesis omissions are silent.
- **refutation: SURVIVED** (reproduced by the refuter). Every sub-claim verified. (a) genesis.py:554-559 registers the same four fixed stages (ai-ceo → c-suite → centre-of-excellence → build-to-order) for every VSB; live GET /api/v1/resources/swarm shows the beehive VSB's cascade is exactly that template, bespoke only via prepended context; VSBCockpit.tsx:440 and VSBSpawnStudio.tsx:528 present it as 'Native delivery swarm (owned, in-house) · 4 stages' with no mention that it is a shared template. (b) live POST /api/v1/native-ai/tree returned planner='deterministic_template' with all 7 nodes served by native; NativeAI.tsx has zero occurrences of 'planner' and its copy (lines 767-769) says the native swarm 'autonomously decomposes' the goal. (c) SynthesisStudio.tsx and BTOCatalog.tsx have 0 matches for stages/cascade/swarm; ForgePipeline.tsx:27 posts stages as {type} only, although the API accepts and honours per-stage config (live run with {variations:3} echoed it back), and forge.py:238 exposes /runs/{id}/rerun with no UI control. DELIVERED parts also hold: /native-ai swarm define/run and /resource-fabric saved-cascade run executed live with per-stage served_by.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/api/genesis.py:539-565; C:/Users/rehan/Workstation/agentic_core/ai/native/orchestrator.py:418-468 (_plan_tree keyword template, _plan_tree_adaptive → 'deterministic_template'); C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/developers/NativeAI.tsx:26-33, 767-769; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/developers/ForgePipeline.tsx:27; C:/Users/rehan/Workstation/agentic_core/api/forge.py:238-239; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/enterprise/VSBCockpit.tsx:440; live GET /api/v1/resources/swarm, POST /api/v1/native-ai/tree, POST /api/v1/forge/run.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Render `planner` on TreeView ('planned by: deterministic template / swarm'); in genesis register_swarm derive extra stages from the concept using the same keyword rules _plan_tree uses (risk, economics, implementation) and surface 'Edit cascade' on the VSB cockpit; add per-stage config inputs + a Rerun button to ForgePipeline.

### R4.5 · §17.2 seven biomimetic layers — Cardiovascular and Endocrine (and the canon comment that vouches for them) — **DOC_OVERCLAIM**

- **severity (assessor):** medium
- **claim:** §17.2: 'Cardiovascular — dynamic resource allocation (distribute)'; 'Endocrine — signal bus / shared context + MJM (coordinate)'; vbs/quality.py:225-262 (W434) asserts 'Endocrine biomimicry/geospheric/regulator.py — a real PID regulator … Not a stub' and 'Respiratory molecular/triad_integration.py — the metabolic-respiratory core', and bakes 'Endocrine, Musculoskeletal and Respiratory all have real implementations elsewhere' into every quality record's layers_note.
- **observed:** Cardiovascular: the only live 'cardiovascular' value is biometrics.resource_flow = 100 − CPU% (app_mvp.py:594, 471) — a relabelled CPU-idle number — and /api/v1/optimizer/allocate self-describes as 'a single-node simulated inventory baseline (no real cloud quota)' (live). Nothing distributes resources dynamically. Endocrine: no runtime path imports biomimicry/geospheric/regulator.py — the only references are the vbs/quality.py comment itself; the `Regulator` imported by change_control/uci_interceptor is a different class; the only geospheric imports are drad (ceo.py) and orchestrator_legacy (architecture/enriched_layers.py). Respiratory: molecular/triad_integration.py is imported by nothing outside its package; the Agent Hub (the vision's own W443 note) IS live at /api/v1/hub/* and honestly says 'records only — no executor is subscribed'. So the W434 'correction' that replaced an earlier 'no implementation' note is itself unverified: the files exist, they are not wired, and the note now travels in every delivery record.
- **evidence:** C:/Users/rehan/Workstation/agentic_core/vbs/quality.py:28-29, 220-262; grep 'biomimicry.geospheric|triad_integration' across agentic_core (only vbs/quality.py comment, v138/ceo.py drad, architecture/enriched_layers.py orchestrator_legacy); C:/Users/rehan/Workstation/agentic_core/app_mvp.py:471, 594; live GET /api/v1/biometrics/status, POST /api/v1/optimizer/allocate, GET /api/v1/hub/handoffs.
- **disclosed to the user:** No for Cardiovascular/Endocrine (the quality record says the opposite); Yes for Respiratory (Agent Hub panel: 'Records only — no executor is subscribed').
- **refutation: SURVIVED** (reproduced by the refuter). The core claim holds: the W434 comment vouches for files nothing wires. biomimicry/geospheric/regulator.py is imported by no module at all (the only 'regulator' imports are change_control.regulator, a different class); molecular/triad_integration.py is imported by nothing outside its package (the only molecular import repo-wide is atp_simulator); the 'cardiovascular' biometric is resource_flow = 100 − CPU% (app_mvp.py:427) and /api/v1/optimizer/allocate self-declares simulated_capacity:true. The note does travel live: POST /api/v1/petri/culture returned quality_assurance.biomimetic with layers ['Immune'], layers_not_contributing listing Cardiovascular/Respiratory/Endocrine, and the sentence 'Endocrine, Musculoskeletal and Respiratory all have real implementations elsewhere'. Two calibrations the assessor's evidence overstates: (1) 'nothing distributes resources dynamically' — homeostasis.assess (homeostasis.py:38-66) genuinely caps admitted parallelism from ATP/immune state and the live tree payload carries it, which is a form of dynamic allocation, just never labelled cardiovascular; (2) §17.2 defines Endocrine as 'signal bus / shared context', and the biobus (fire_signal → NervousSystem, organism_context()) is a live, widely-wired signal bus and shared context — so the overclaim is the specific files the W434 note vouches for, not a total absence of the function. User exposure is limited: no frontend renders layers_note (grep 0), though SwarmIntelligence.tsx:265 titles its chip '7 biomimetic layers'.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/vbs/quality.py:28-29, 222-262; grep -rn 'import.*regulator|from.*regulator' agentic_core (only change_control.regulator / regulator_v2 / genetic_immune regulatory_block); grep -rn 'agentic_core.molecular' (only atp_simulator, 3 sites); C:/Users/rehan/Workstation/agentic_core/app_mvp.py:427, 471; C:/Users/rehan/Workstation/agentic_core/ai/native/homeostasis.py:38-66; C:/Users/rehan/Workstation/agentic_core/organism/biobus.py:94, 170; C:/Users/rehan/Workstation/apps/workstation-superapp/src/components/organism/SwarmIntelligence.tsx:264-266; live GET /api/v1/biometrics/status (resource_flow 32.7), POST /api/v1/optimizer/allocate, GET /api/v1/hub/handoffs ('records only — no executor is subscribed'), POST /api/v1/petri/culture.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Change the vbs/quality.py layers_note to state what is wired ('Immune, Nervous, Self-healing, Musculoskeletal engaged; Cardiovascular and Endocrine have no live implementation; Respiratory = Agent Hub, records only') and drop the W434 claims; or actually wire the PID regulator into homeostasis.assess() and surface it, and make the optimizer's allocation feed max_parallel (a real 'distribute').

### R4.6 · §17.2 Nervous · §8 'reflex arcs' (auto-triggered responses) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** nervous.py docstring / §8: 'Reflex arcs → auto-triggered responses to threshold events'; the organism 'defends itself, learns, and improves'.
- **observed:** The NervousSystem is a real thread-safe ring buffer with honest arousal thresholds (live: ALERT, 0.97 sig/s, totals per type) and the biobus fires genuine signals from the orchestrator, homeostasis, QMS and heartbeat. But register_reflex() has zero callers anywhere in the repo (grep), so reflex_arcs_registered is 0 on the live process and _check_reflexes can never fire a callback: the 'auto-triggered responses' part of the layer is a mechanism with no arcs. The only autonomous reactions that exist are hand-coded in heartbeat.beat() (immune<0.5 alert signal, ATP<0.3 recovery, open-circuit probing), which run only once per beat, not on threshold events.
- **evidence:** C:/Users/rehan/Workstation/agentic_core/organism/nervous.py:76-108; grep 'register_reflex' across agentic_core → only the definition; live GET /api/v1/organism/nervous/status → reflex_arcs_registered: 0; C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py:175-216.
- **disclosed to the user:** Partly: the number 0 is in the payload and OrganismDashboard prints '{n} reflex arcs'; nothing explains that none can ever fire.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. register_reflex() is defined at nervous.py:76 and called nowhere in the repo; live GET /api/v1/organism/nervous/status reports reflex_arcs_registered: 0, so _check_reflexes (nervous.py:90-108) iterates an empty list on every signal and can never invoke a callback. A reader is actively misled by the same payload: total_signals shows reflex: 542 and by_type_last_60s reflex: 1 — but 'reflex' there is only a signal CATEGORY emitted as a record by 12+ sites (heartbeat.py:169/183/212, homeostasis.py:79/112, vsb.py:1376/1581, biobus.py:115, management_systems.py:334…), not an arc firing. The only autonomous reactions are the hand-coded once-per-beat checks in heartbeat.beat() (immune<0.5 alert, ATP<0.3 recover, open-circuit probe at heartbeat.py:175-216). OrganismDashboard.tsx:329 prints '{n} reflex arcs' via organism_status.py:268 and explains nothing.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/organism/nervous.py:76-108, 156; grep -rn register_reflex agentic_core → definition only; grep -rn '"reflex"' agentic_core (12 emitters); C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py:175-216; C:/Users/rehan/Workstation/agentic_core/api/organism_status.py:268; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/organism/OrganismDashboard.tsx:329; live GET /api/v1/organism/nervous/status (reflex_arcs_registered 0, total_signals.reflex 542).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Register at least the reflexes the heartbeat already hand-codes (e.g. nervous.register_reflex('immune_alert','reflex',threshold=…, callback=…) at startup) so threshold events react between beats, and show 'reflex arcs: 0 — none registered, no auto-responses' when the count is zero.

### R4.7 · §6 native fabric core — routing, gating, status honesty, Primitive Console — **PARTIAL** *(assessed DELIVERED)*

- **severity (assessor):** info
- **claim:** §6: native AI resource fabric with in-platform orchestrator; external providers 'optional accelerants, never dependencies'; owned floor always serves; §17.5 single control plane.
- **observed:** Verified by execution and code: gateway.query_meta() routes every completion through ai/native/orchestrator (in-house-first: ollama → native floor → external only if AI_ALLOW_EXTERNAL=true; registry.select() places externals last; the legacy external-first ModelGateway._call has zero callers); an hourly external-call cap (_external_budget_check) guards real spend; GET /api/v1/native-ai/status honestly reports mode=deterministic_floor with floor_active_basis ('history, not a live probe') and a floor_note ('NOT an LLM'); /models, /lifecycle (evaluate·promote·retire·reinstate), /complete (model tier choice), /ensemble, /swarm, /tree (with owned QMS/DCMS governance, difflib validation, minimax, consensus, Hill transform, UEG hash — all present in the live payload), /selfcheck (13/13 backing modules import). NativeAI.tsx shows an amber 'Deterministic floor active' banner, the resource list with owned/external chips, and the Primitive Console runs all 10 primitives rendering whole payloads including basis/null-with-reason. Homeostasis is a real closed loop (live posture 'reduced'/'protected', ATP simulated and labelled). Organism composite health is honestly split (40% measured; SIMULATED chips on OrganismAnatomy).
- **evidence:** C:/Users/rehan/Workstation/agentic_core/ai/gateway.py:109-160, 172-240 (dead _call); C:/Users/rehan/Workstation/agentic_core/ai/native/model_resource.py:22-24, 145-165; orchestrator.py:198-260, 355-381; C:/Users/rehan/Workstation/agentic_core/api/native_ai.py:23-85, 289-330; NativeAI.tsx:264-342, 525-538; live GET/POST on all routes above.
- **disclosed to the user:** Yes on /native-ai (floor banner, badges, basis strings).
- **refutation: REFUTED — corrected to PARTIAL.** Routing, gating and the primitive console hold (registry.select places externals last; ModelGateway._call has zero callers; _external_budget_check at orchestrator.py:350; /lifecycle, /models, /selfcheck 13/13, /tree with governance/validation/decision/consensus/signal_response/ueg_hash all live). But the status-honesty half of the DELIVERED claim is false on the live process right now: GET /api/v1/native-ai/status returns mode='real_model', mode_measured='real_model', measured_recent_server='ollama', floor_active=false, floor_active_basis 'most recent recorded completion was served by ollama' — while the floor serves everything (AI_DISABLE_LOCAL=1, owned_resources_available ['native'], is_real_model=false). Cause: native_ai.py:36-45 takes the most recent model_health row by last_at without checking success, and every ollama row is a FAILURE (live /api/v1/operations/model-health: ollama runs 3, success_rate 0.0, last_at 11:42:32Z, later than native's 11:41:08Z). NativeAI.tsx:528-536 keys its banner on floor_active, so the /native-ai page currently renders a green 'Real model: native' pill and hides floor_note — the exact inversion the assessor credited as 'honest'. Any API/curl call with model='local' (or a saved cascade stage naming ollama) puts the status into this state; the assessor's own [8] probe did. Verdict corrected to PARTIAL: the fabric is delivered; its disclosure is state-dependent and currently wrong.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/api/native_ai.py:29-72; C:/Users/rehan/Workstation/agentic_core/api/operational_excellence.py:143-183 (rows keyed by served_by regardless of success), 282-295; C:/Users/rehan/Workstation/agentic_core/ai/native/orchestrator.py:30-39 (_record_model writes served_by=name on failure too), 197-228, 350; C:/Users/rehan/Workstation/agentic_core/ai/native/model_resource.py:150-168; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/developers/NativeAI.tsx:13, 528-536; grep '\._call(' agentic_core → 0 callers; live GET /api/v1/native-ai/status (before and after a model='local' completion), GET /api/v1/operations/model-health, GET /api/v1/native-ai/selfcheck, POST /api/v1/native-ai/tree.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** None required for the core; keep the floor banner wording ('NOT an LLM') and extend the same badge to the surfaces in finding 3.
- **status now:** FIXED W458 (P1.10) — the status-honesty half: /native-ai/status no longer reads the newest per-model aggregate row regardless of success. agentic_core/api/operational_excellence.last_successful_server() returns the most recent row that actually SERVED, and mode / floor_active / the basis string follow it — naming the row read and the failed attempts recorded since. The native floor serve is recorded, so a stale row can be displaced; the basis reaches the screen (data-testid native-status-basis).

### R4.8 · §6 × §8 — a deployment-disabled resource is scored as an AI failure (immune + breaker) — **PARTIAL**

- **severity (assessor):** low
- **claim:** §8: immune/self-healing signals reflect real threats; §6 learning loop scores 'real recorded' model performance.
- **observed:** With AI_DISABLE_LOCAL=1, orchestrator._run_model returns '' for any 'ollama'/'local' route; complete() treats the empty output as a failure and calls _record_model + _organism_report, which writes an immune 'ai_failure' and a model:ollama breaker failure. Live: one POST /api/v1/native-ai/complete {model:'local'} moved immune errors_in_window 1→2 and immune health to 0.8, which then appeared in the sovereign-evolution cycle's introspection as a hot endpoint tie. A configuration choice degrades the organism's health score and the learning loop's ollama success rate. The UI hides the local tier when nothing is discovered, so only API/curl callers reach it; prefer='local' from saved cascades (stage.model) would too.
- **evidence:** C:/Users/rehan/Workstation/agentic_core/ai/native/orchestrator.py:213-236, 266-270; live sequence: GET /api/v1/organism/systems → POST /native-ai/complete (model=local) → GET /organism/systems (by_type gained ai_failure:1; self_healing circuits {model:ollama: 1/1 failed}); POST /sovereign-evolution/cycle introspection.immune.health=0.8.
- **disclosed to the user:** No — the resources_tried list shows 'ollama' as tried, not 'disabled'.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced live and the consequence is larger than described. Sequence: GET /organism/systems (immune health 0.9, by_type {compliance_fail:1}, breaker model:ollama total_failures 2) → POST /api/v1/native-ai/complete {model:'local'} (served_by 'native', resources_tried lists 'ollama' as tried) → GET /organism/systems: health 0.8, by_type gained ai_failure:1, hot_endpoint_tied gained 'model:ollama', breaker total_failures 3/3 calls, failure_rate 1.0. Code path: _run_model returns '' when AI_DISABLE_LOCAL is set (orchestrator.py:266-270), complete() treats empty output as failure and calls _record_model + _organism_report (orchestrator.py:224-228), and 'ollama' is appended to tried before the disable check runs (line 197). Beyond immune/breaker, the failure row is what flips /native-ai/status to 'real_model / floor_active:false' (see finding 7), and at ≥5 rows the learning loop would deprioritise a healthy owned model on the basis of a config flag. No 'disabled' marker anywhere in the payload.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/ai/native/orchestrator.py:30-39, 52-60, 197, 213-228, 266-270; C:/Users/rehan/Workstation/agentic_core/api/operational_excellence.py:287-294 (deprioritised rule); live GET /api/v1/organism/systems → POST /api/v1/native-ai/complete (model=local) → GET /api/v1/organism/systems → GET /api/v1/native-ai/status → GET /api/v1/operations/model-health (ollama success_rate 0.0).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In _run_model, when AI_DISABLE_LOCAL is set raise a distinct sentinel that complete() treats like circuit-open — tried.append('ollama (disabled by config, skipped)') with no _record_model/_organism_report — so disabled ≠ failed.
- **status now:** FIXED W458 (P1.10) — a resource that was never ATTEMPTED is a skip, not a failure: _run_model raises ResourceSkipped under AI_DISABLE_LOCAL (also for an unknown resource, and from the hourly external spend guard), and complete() annotates resources_tried 'ollama (disabled by config, skipped)' while recording nothing — no learning-loop row, no immune ai_failure, no breaker failure. One model=local call now leaves immune health at 1.0. A REAL failure of an enabled resource still records all three.

### R4.9 · §17.2 Genome (arms-length controlled VSB identity) and Sovereign Evolution under the floor — **PARTIAL**

- **severity (assessor):** low
- **claim:** §17.2: 'Genome — VSB identity/capabilities/constraints/evolution (arms-length controlled)'; §7 'Sovereign Evolution' as an organism system that self-improves.
- **observed:** The organism genome (organism/genome.py) is a 10-axis trait-vector toy with exemplary honesty (fitness_provenance, trait_provenance, encoding_note 'NOT an analysis of the entity' under the floor; live genome shows encoded:false) but it is not the VSB's identity/constraints and its encode/mutate/crossover routes carry no CCA/arms-length gate; the VSB-level genome is a separate GenomicRegistry in api/vsb.py. Sovereign Evolution runs (live cycle 225 ms) but under the floor its CEO directives default to 'Routine organism health review — No parseable CEO directives; defaulting to a maintenance sweep' and bto_roadmap is floor scaffold text; sovereign_evolution.py uses gateway.query() (no served_by) and SovereignEvolution.tsx renders the roadmap with no provenance. The heartbeat's autonomy switches (auto_evolve/economy/align/compliance/ship) default OFF and are persisted; HeartbeatMonitor exposes them honestly with a persisted flag and a read-only evolution_auto_apply lever.
- **evidence:** C:/Users/rehan/Workstation/agentic_core/organism/genome.py:126-219 (no governance dependency), C:/Users/rehan/Workstation/agentic_core/api/vsb.py:21, 45; live GET /api/v1/organism/genome; live POST /api/v1/sovereign-evolution/cycle (served_by count 0, marker count 1); C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/evolution/SovereignEvolution.tsx:173, 195; C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py:131-147, 505-522.
- **disclosed to the user:** Genome: yes (provenance chips, 'not encoded' warnings). Evolution: partly (the 'No parseable CEO directives' rationale is shown; the roadmap's floor origin is not badged). Heartbeat defaults: yes.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed on all three parts. Genome: organism/genome.py exposes /genome/encode, /genome/crossover, /genome/mutate with no Depends/CCA/change_control/arms-length reference anywhere in the file; live GET /api/v1/organism/genome lists trait-vector genomes with encoded:false and fitness_provenance 'default-unencoded (no FITNESS line parsed)' — honest, but not the VSB identity/constraints, which live in a separate GenomicRegistry (api/vsb.py:26, 31). Sovereign Evolution: live POST /api/v1/sovereign-evolution/cycle (2,740 bytes) had served_by count 0 and one floor marker; ceo_directives collapsed to 'Routine organism health review — No parseable CEO directives; defaulting to a maintenance sweep.'; bto_roadmap is floor scaffold ('_Acting as: Centres of Excellence._ ## CoE Quality Gate Structured service frame for: You are the Centres of Excellence…'); sovereign_evolution.py:125 uses gateway.query() and SovereignEvolution.tsx:173/195 renders rationale and roadmap with no provenance. Heartbeat autonomy switches default False and are persisted (heartbeat.py:131-147, 505-522); HeartbeatMonitor.tsx:10-11, 112, 138-142 surfaces autonomy_persisted and the read-only evolution_auto_apply lever.
- **refuter's evidence:** C:/Users/rehan/Workstation/agentic_core/organism/genome.py:100-310 (grep Depends|cca|change_control|governance → 0 hits); C:/Users/rehan/Workstation/agentic_core/api/vsb.py:26, 31; C:/Users/rehan/Workstation/agentic_core/api/sovereign_evolution.py:125, 210, 263-271; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/evolution/SovereignEvolution.tsx:173, 195; C:/Users/rehan/Workstation/agentic_core/organism/heartbeat.py:131-147, 505-522; C:/Users/rehan/Workstation/apps/workstation-superapp/src/pages/organism/HeartbeatMonitor.tsx:10-11, 112, 138-142; live GET /api/v1/organism/genome, POST /api/v1/sovereign-evolution/cycle.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Return {served_by,is_external} from run_cycle and badge the roadmap in SovereignEvolution.tsx; route organism genome mutate/crossover through the CCA when the genome is bound to a VSB (or rename the toy 'trait genome' in the UI so it is not read as the §17.2 VSB genome).

---

## R5 — §1–§3, §3A, §9, §14, §15, §17.1 — the offerings, the avatar/UX, democratisation, the founding principles, the 4×6×4 grid

**Assessor's region summary:** R5 (§1-§3, §3A, §9, §14, §15, §17.1) assessed against the live HEAD backend at :8024 (floor-only serving; UI reasoned from apps/workstation-superapp/src). The core Offering-1 promise is DELIVERED as a surface: six domain routers, 24 DomainTool forms, honest floor badges, disclaimers, refine/persist/export, the Religion QEP flagship with authentic Qur'an text and a refusal to fake recitation scoring. The gap that remains is concentrated in disclosure rather than reach: (1) the Employment hub's default tab promises 'live, real-time search across public job boards' over a backend that synthesises listings and renders generated documents with no provenance at all; (2) seven surfaces — including the Law hub's default tab and the persistent avatar — paint floor output GREEN while the label says 'not model analysis', contradicting the W439 class-kill claim; (3) the QMS gate cannot see a scaffold (coverage measured against no sections, stub regex only), so every floor output, including a NEWS2 assessment that computed no score, shows a green 'QMS pass'. The avatar is genuinely enterprise-grounded and voice/vision are honest, but under the floor it answers its own persona line and silently drops the user's language preference. §17.1's 4×6×4 grid is a DOC_OVERCLAIM: there is no product axis in code, configs/realms.yaml is an orphan with drifted domain-shaped realms, realm never reaches Offering-1 tools, and five hub CTAs post domains as realms into a projects API with its own vocabulary. The marketplace counts legacy product directories (self-declared 'PRODUCTION_READY', 'WCAG 2.2 AAA') as '20 Live Products'. §9 language coverage (4 languages × 90 chrome keys, English AI output) is disclosed honestly in Settings; the W428 profile is stored and shown but inert under the floor with no signal to the user. Barred sources (§16, VISION_FIDELITY_LEDGER.md, AUTONOMOUS_PROGRESS.md) were not consulted.

### R5.0 · §3A offering 1 / §15 principle 6 — Employment hub, default tab (Application Studio) — **STUB**

- **severity (assessor):** high
- **claim:** Domain tools are real, in-house-AI-mediated working capabilities; never fabricate, label simulation (§3A, §15.6).
- **observed:** The Employment hub opens on the 'Application Studio' tab (EmploymentHub.tsx:13 default 'studio'). Its Job Search Engine panel tells the user it is 'Live, real-time search across public job boards' and, on an empty result, 'No live listings matched. Try broadening your terms.' The backend route it calls is explicitly not a job board: career.py's job_search docstring says 'Uses AI synthesis — not a live job board' and asks the model to 'Generate exactly N realistic job listings' with fabricated URLs/salaries/publish dates, returning sources_used=['AI Career Intelligence']. Executed: POST /api/v1/career/job-search {query:'data engineer',location:'Leeds'} → results:[], sources_used:['AI Career Intelligence'], QMS pass. Under a capable model this panel would present invented listings as live search results. The same tab's document generator (POST /api/v1/career/generate) executed and returned floor scaffold ('The request concerns: Middle: 2 concrete achievements…') with ai_provenance served_by:'native' — but ApplicationStudio renders doc.content with no provenance badge at all (ApplicationStudio.tsx:498-511), unlike every DomainTool tab beside it.
- **evidence:** apps/workstation-superapp/src/components/employment/ApplicationStudio.tsx:393 ('Live, real-time search across public job boards'), :413 ('Searched … across {sources_used}'), :418 ('No live listings matched'), :498-511 (results rendered without ai_provenance); agentic_core/api/career.py:232-236 (docstring 'not a live job board'), :249-262 (prompt fabricating listings incl. url/salary/published), :290 (sources_used 'AI Career Intelligence'); live execution of /api/v1/career/job-search and /api/v1/career/generate on :8024.
- **disclosed to the user:** No. The UI copy asserts the opposite of what the backend does; the generated documents carry no provenance badge on this tab.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced in full. The Employment hub opens on 'studio' (EmploymentHub.tsx:13). ApplicationStudio's Job Search Engine copy says 'Live, real-time search across public job boards' and, on empty results, 'No live listings matched' — while the route it calls (career.py job_search) is documented in its own docstring as 'AI synthesis — not a live job board' and prompts the model to 'Generate exactly N realistic job listings' with fabricated url/salary/published fields, hard-coding sources_used=['AI Career Intelligence']. Executed: POST /api/v1/career/job-search {query:'data engineer',location:'Leeds'} → results:[], sources_used:['AI Career Intelligence'], served_by native. POST /api/v1/career/generate {output_types:['cv']} → floor scaffold ('The request concerns: Structure: Professional Summary…') with ai_provenance served_by:'native', and ApplicationStudio renders doc.content in a <pre> with no provenance badge; a grep of ApplicationStudio.tsx for floor|native|AI-generated|disclaimer|synthes returns zero hits, so nothing on the default Employment tab discloses either the synthesis or the floor. The verdict is correct: the surface asserts the opposite of what the backend does.
- **refuter's evidence:** apps/workstation-superapp/src/pages/domains/EmploymentHub.tsx:13; components/employment/ApplicationStudio.tsx:393, :413, :418, :172 (posts /career/generate), :205 (posts /career/job-search), :497-513 (doc.content rendered, no badge); agentic_core/api/career.py:229-236 (docstring), :249-262 (prompt), :289 (sources_used literal); live: POST :8024/api/v1/career/job-search → {results:[],sources_used:['AI Career Intelligence'],total:0,ai_provenance.served_by:'native'}; POST /career/generate → content '_[Workstation native structured engine…' served_by native.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Change the panel copy to 'AI-synthesised example listings — not a live job board; verify every URL', render the served_by badge (via provenanceBadge().cls) on each generated document and on the job results, and stop labelling synthesis as a 'source'.
- **status now:** FIXED W454 (P1.6) — the hub opens on the CV tools; the job search synthesises ILLUSTRATIVE listings (no url / posting date, salary an estimate, sources_used empty, provenance on the response) and the page says 'not a live job board', links nothing and badges the search and every generated document.

### R5.1 · §15 principle 6 — provenance badge colour on floor-served output — **PARTIAL**

- **severity (assessor):** high
- **claim:** Honesty over polish: floor-served output must not read as model analysis (the W439 class-kill in DomainTool claims 'every badge in the app routes through provenanceBadge').
- **observed:** provenanceBadge() (lib/api.ts:65-72) returns an amber class and the label 'structured floor — not model analysis' for served_by:'native'. Seven surfaces take only the .label and pick their own colour by is_external alone, so floor output wears a GREEN badge with a label that says it is not model analysis. Two of them are primary surfaces: the Law hub's default tab (LawHub.tsx:17 default 'compliance' → Document Analyser, badge at :133) and the persistent avatar footer on every page (ConversationPanel.tsx:95). Executed /api/v1/law/analyse and /api/v1/avatar/chat: both served_by 'native', is_external false → green on those surfaces per source.
- **evidence:** apps/workstation-superapp/src/lib/api.ts:65-72; DomainTool.tsx:225-229 (the claim 'every badge in the app routes through it'); colour chosen by is_external only at pages/domains/LawHub.tsx:133, components/avatar/ConversationPanel.tsx:95, pages/Deliverables.tsx:204, pages/enterprise/VSBCockpit.tsx:870, pages/synthesis/ResourceFabric.tsx:689, pages/developers/NativeAI.tsx:696, pages/OperationalExcellence.tsx:160.
- **disclosed to the user:** Partially — the label text is honest, the colour contradicts it (green = the class the W439 audit called 'read as scholarship').
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, and the assessor undercounted. provenanceBadge() (lib/api.ts:65-72) returns an amber class + 'structured floor — not model analysis' for served_by 'native', but the seven cited surfaces take only .label and colour by is_external alone, so floor output is green. Executed /api/v1/law/analyse and /api/v1/avatar/chat: both served_by 'native', is_external false → green at LawHub.tsx:133 (Law's default 'compliance' tab) and ConversationPanel.tsx:95 (mounted in Shell.tsx:190/:283, i.e. every page). Three further sites the assessor missed are worse than colour-only: MyWork.tsx:132 renders 'in-house' in emerald with NO floor label at all (a label regression, not just a colour one); Generator.tsx:101/:187 renders 'Native floor (in-house)' inside an emerald Badge; OrganismAnatomy.tsx:535 renders a Chip tone 'ok' reading 'served by native'. DomainTool.tsx:226-227 still claims 'every badge in the app routes through it'. PARTIAL (label honest, colour contradicts, and on My Work the label is not honest either) stands.
- **refuter's evidence:** apps/workstation-superapp/src/lib/api.ts:65-72; components/DomainTool.tsx:225-229; colour-by-is_external at pages/domains/LawHub.tsx:133, components/avatar/ConversationPanel.tsx:95 (Shell.tsx:190, :283), pages/Deliverables.tsx:204, pages/enterprise/VSBCockpit.tsx:870 & :875, pages/synthesis/ResourceFabric.tsx:689, pages/developers/NativeAI.tsx:696, pages/OperationalExcellence.tsx:160; additional: pages/MyWork.tsx:132 ('in-house', emerald, no floor label), pages/developers/Generator.tsx:101 & :187 (emerald Badge 'Native floor (in-house)'), pages/organism/OrganismAnatomy.tsx:535 (Chip tone 'ok'); live: POST /law/analyse and /avatar/chat → served_by 'native', is_external false.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Use provenanceBadge(...).cls (and .title) at the seven call sites instead of a local is_external ternary; add a test that greps for provenanceBadge(...).label used without .cls.
- **status now:** FIXED W453 (P1.5) — the Law hub's analysis badge (and every other .label-only site) renders the helper's cls/title: amber on the floor, never emerald; a source-grep guard fails on the old shape.

### R5.2 · §10/§11 quality gate on Offering-1 output (every domain tool, /refine) — **PARTIAL** *(assessed STUB)*

- **severity (assessor):** high
- **claim:** Every output passes a real QMS + compliance gate; the bar is measured, not asserted (§10, §15.5).
- **observed:** Every floor-served domain response carries quality_assurance {qms_gate_passed:true, delivery_coverage:1.0, stub_found:false} and DomainTool renders a green 'QMS pass' badge next to the amber 'structured floor' badge. The gate cannot see a scaffold: ai_text() calls assure_delivery(output, None) so required_sections is None and coverage is 1.0 by construction; the stub detector only matches TODO/TBD/FIXME/lorem ipsum/placeholder/coming soon/'as an ai' or <200 chars. Executed: NEWS2 risk assessment that computed no score (output literally 'Native structured content for Score Calculation, grounded in: Provide: ## Score Calculation…') → QMS pass, coverage 1.0, stub_found false; same for tafsir, lesson plan, contract analysis, care plan, CV, refine.
- **evidence:** agentic_core/api/_ai_provenance.py:40-52 (assure_delivery(output, None,…)); agentic_core/vbs/quality.py:31 (_STUB_RE), :32 (_MIN_SUBSTANTIVE=200), :105-126 (coverage vs None sections; stub rule); DomainTool.tsx:232-237 (green 'QMS pass'); live executions on :8024 of /care/risk-assess, /religion/quran-tafsir, /education/lesson-plan, /law/analyse, /care/care-plan, /employment/cv, /refine.
- **disclosed to the user:** No — a green 'QMS pass' sits beside the floor badge on clinical and legal outputs; nothing says the gate measured section presence only.
- **refutation: REFUTED — corrected to PARTIAL.** The gap is reproduced exactly as described, but STUB is the wrong label: the gate is not a surface that pretends, it is real machinery that is fed nothing to measure. Confirmed: ai_text() calls assure_delivery(output, None) (_ai_provenance.py:40-42); with required_sections None, _delivery_coverage returns 1.0 whenever the text is ≥200 chars (quality.py:107-110) and stub_found only fires on TODO/TBD/FIXME/lorem ipsum/placeholder/coming soon/as an ai or <200 chars (quality.py:31-32, :126) — the floor's own scaffold phrases match none of these. Executed the NEWS2 case (RR 22, SpO2 94, SBP 105, HR 95, T 38.2): output contained no score, only 'Native structured content for Score Calculation…', yet quality_assurance = {qms_gate_passed:true, delivery_coverage:1.0, stub_found:false}; same for cv, care-plan, lesson-plan, hypothesis, tafsir, refine, law/analyse (8/8 pass). DomainTool.tsx:232-237 renders a green 'QMS pass' whose tooltip says only 'coverage 1.0 · compliance pass' — nothing says coverage was measured against no sections. However, behind the badge a real, stateful gate runs on every call: qms.run_quality_gates opens persistent defects on failure and can flip the badge to amber 'flagged', the §11 compliance screen runs and did discriminate (compliance_overall 'review' on tafsir and refine vs 'pass' elsewhere), the record is SHA3-sealed and UEG-logged (quality.py:136-190). That is a gate whose Offering-1 content measure has collapsed to a length check — a PARTIAL with an undisclosed shortfall — not a façade with nothing behind it.
- **refuter's evidence:** agentic_core/api/_ai_provenance.py:40-52; agentic_core/vbs/quality.py:31-32 (_STUB_RE, _MIN_SUBSTANTIVE=200), :104-110 (_delivery_coverage → 1.0 if ≥200 chars when sections None), :123-126, :136-190 (compliance screen, qms.run_quality_gates, control_document, UEG); apps/workstation-superapp/src/components/DomainTool.tsx:231-237; live on :8024: POST /care/risk-assess (no score; qms_gate_passed true, coverage 1.0, stub_found false), /employment/cv, /care/care-plan, /education/lesson-plan, /science/hypothesis, /law/analyse (all pass), /religion/quran-tafsir and /refine (pass, compliance_overall 'review').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Treat served_by:'native' (or the floor's own header line) as stub_found:true / qms_gate_passed:'not measured' in assure_delivery, pass the router's required section list into ai_text so coverage is real, and render 'QMS: not assessable (floor)' instead of 'pass'.
- **status now:** FIXED W449 (P1.1) — ai_text() measures coverage against the PROMPT's declared sections (the floor's own extractor) and threads served_by, so a floor-served NEWS2/tafsir/CV is 'QMS —', never 'pass'.

### R5.3 · §3A Care domain — Clinical Risk Assessment (NEWS2 / MUST / Waterlow / falls) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** Care domain offers 'validated risk scoring' (DomainsHub blurb); AI Tools catalogue: 'Score and interpret a validated tool (NEWS2, MUST, Waterlow…)'.
- **observed:** The tool computes nothing: risk-assess builds a prompt asking the AI to 'show working for each component' and forwards observations verbatim. NEWS2 is a deterministic lookup table, yet no arithmetic exists in care.py. Executed with RR 22, SpO2 94, SBP 105, HR 95, T 38.2 (a NEWS2 of 7 — urgent response): the response contained no score, only 'Native structured content for Score Calculation… Exposure on interpretation recommended'. The amber floor badge and the disclaimer ('AI-generated risk assessment aid only') are rendered; the green 'QMS pass' (finding 3) is rendered too. Also the CareHub form key names (resp_rate/spo2/…) are free text the prompt forwards, so nothing validates units or completeness.
- **evidence:** agentic_core/api/care.py:103-142 (RiskAssessRequest → prompt only; no scoring); apps/workstation-superapp/src/pages/domains/DomainsHub.tsx:18 ('validated risk scoring'); pages/AIToolsCatalogue.tsx:24 ('Score and interpret a validated tool'); live execution of POST /api/v1/care/risk-assess on :8024.
- **disclosed to the user:** Yes for provenance (amber 'structured floor' badge + disclaimer in DomainTool); no for the capability claim — the copy still promises scoring the system never performs, even with a model.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, with one arithmetic correction. care.py risk_assessment (:101-142) builds a prompt that asks the AI to 'show working for each component' and forwards patient_data verbatim; there is no scoring table or arithmetic anywhere in agentic_core (grep -i news2 → only the _TOOLS literal, the request default, and a docstring). Executed with RR 22 / SpO2 94 / SBP 105 / HR 95 / T 38.2 / alert / air: the response held no score, only 'Native structured content for Score Calculation…' and 'Likelihood × impact to be scored'. Correction: those observations give NEWS2 = 6 (RR 2 + SpO2 1 + SBP 1 + HR 1 + Temp 1), not 7 — still the 5-6 'urgent ward-based response' band, so the consequence stands. The capability copy overclaims at three points: CareHub.tsx:79 ('Workstation's own AI scores and interprets the risk'), DomainsHub.tsx:18 ('validated risk scoring'), AIToolsCatalogue.tsx:24 ('Score and interpret a validated tool'). The observations field is a free key:value block (CareHub.tsx:85) with no unit or completeness validation — pydantic silently drops unknown fields, and an empty patient_data still returns a 'pass'. Provenance IS disclosed (amber floor badge + the 'AI-generated risk assessment aid only' disclaimer, DomainTool.tsx:225-229, :256); the scoring claim is not. PARTIAL stands.
- **refuter's evidence:** agentic_core/api/care.py:23 (NEWS2 'NHSI 2017' in _TOOLS), :95-98 (RiskAssessRequest: tool, patient_data dict, clinical_context), :101-142 (prompt-only handler, disclaimer); grep -i news2 agentic_core → 3 hits, none arithmetic; apps/workstation-superapp/src/pages/domains/CareHub.tsx:79, :85; pages/domains/DomainsHub.tsx:18; pages/AIToolsCatalogue.tsx:24; live: POST :8024/api/v1/care/risk-assess {tool:'NEWS2', patient_data:{…}} → no numeric score in 'assessment', disclaimer present, ai_provenance served_by native.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Implement NEWS2/MUST/Waterlow arithmetic in-house (they are published tables) and return the computed score as a separate deterministic field the UI shows first; let the AI narrate interpretation only. Until then, change the copy to 'structures an assessment; does not compute the score'.
- **status now:** FIXED W457 (P1.9) — agentic_core/care/scoring.py computes NEWS2 / MUST / Waterlow from the published tables (NICE CG161 falls as a labelled factor count), validates units/ranges/completeness, and the route returns the score block first with the AI told not to recompute; the hub renders the block first; the assessor's case scores 6 · medium (key threshold for urgent response); a lower-bound total gets no band.

### R5.4 · §9 — enterprise-aware, multimodal avatar (/api/v1/avatar/*, components/avatar/*) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** Enterprise-aware avatar with text · voice · image interaction and guided navigation, personalised to the user's language.
- **observed:** What works (executed): session creation, owner-scoped sessions, VSB grounding (grounded_in:'vsb-5d357ea055' asserted only when the grounding block builds, and the VSB's mission words reached 'Key factors'), whitelisted navigation suggestions with honest match reasons, browser-native STT/TTS as the in-house voice path, /speak 503 with an honest reason, image path that refuses to fabricate. What does not: under the floor the avatar's body answers its own persona line — three executions (contexts general/domains/vsb) all returned 'The request concerns: You are the … avatar — a helpful, concise assistant…' with the user's question absent; the prompt is assembled persona-first with the message last (api.py:340-346) and the floor's subject extraction picks the persona. Language: the hook sends the user's preferred language on every turn; the backend correctly returns language:null when the floor could not honour it, but the hook never reads that field, so an Arabic-preference user gets English scaffold with no notice. The footer badge for this floor reply is green (finding 2). /avatar/status reports ollama_online:true while AI_DISABLE_LOCAL=1 routes nothing to it — the status pings Ollama directly, unaware of the disable flag.
- **evidence:** agentic_core/avatars/api.py:330-346 (prompt assembly), :388-399 (grounded_in/language honesty), :405-422 (status pings Ollama regardless); apps/workstation-superapp/src/hooks/useAvatarSession.ts:224-238 (sends language, ignores resp.data.language), :166-175 (speechSynthesis in-house TTS); components/avatar/ConversationPanel.tsx:95 (green class); live executions: POST /avatar/session, /avatar/chat ×4, /avatar/speak (503), GET /avatar/status, /avatar/sessions.
- **disclosed to the user:** Partly — the floor label is present (green), the language shortfall is not surfaced in the panel, and the reply body itself reads like a non-answer.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced by execution. Session creation works (POST /avatar/session → session_id + avatar DID). Chat 'How do I start a new project in the Law domain?' (context general) returned '## Understanding\nThe request concerns: You are the Workstation Sovereign Mesh avatar — a helpful, concise assistant…' with Key factors 'workstation sovereign / concise assistant / …' — the user's question is absent; the prompt is assembled persona-first with 'User: {message}' last (api.py:340-346) and the floor's subject extraction takes the persona. VSB-grounded chat (context vsb, vsb_id vsb-5d357ea055) honestly set grounded_in and the VSB's words ('beehives somerset') reached Key factors, but Understanding was still 'You are the VSB … avatar'. Language: sent language 'Arabic' → backend returned language:null (honest, api.py:395-398), but useAvatarSession.ts:224-238 reads only served_by/is_external/suggested_areas and never resp.data.language, so the panel shows an English scaffold with no notice. GET /avatar/status → ollama_online:true (it pings Ollama's /api/tags directly, api.py:405-422, unaware AI_DISABLE_LOCAL=1 routes nothing there). POST /avatar/speak → 503 with an honest reason naming browser speechSynthesis as the in-house path (hook :166-175 implements it). suggested_areas honest ('matched: law'). Footer badge green (finding 1). PARTIAL stands.
- **refuter's evidence:** agentic_core/avatars/api.py:330-346 (prompt assembly), :388-399 (grounded_in / language honesty), :405-422 (status pings Ollama); apps/workstation-superapp/src/hooks/useAvatarSession.ts:166-175 (speechSynthesis), :224-238 (sends language, ignores resp.data.language); components/avatar/ConversationPanel.tsx:95; live on :8024: POST /avatar/session, POST /avatar/chat ×2 (general → persona-as-subject, language null; vsb → grounded_in 'vsb-5d357ea055', language null), GET /avatar/status → ollama_online true, POST /avatar/speak → 503 'External voice output is unavailable… speechSynthesis is the in-house path'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Put the user's message under a 'User:' label at the top of the floor prompt (or pass the message as the subject explicitly) so the floor grounds in the question; when resp.data.language is null but a language was requested, set the panel notice to 'Answered in English — your language needs the owned model'; make /status report the effective serving mode (floor) when AI_DISABLE_LOCAL is set.

### R5.5 · §17.1 — the 4 Realms × 6 Domains × 4 Products grid (taxonomy.py, configs/realms.yaml, catalogue) and §3A realm-aware domain working — **DOC_OVERCLAIM**

- **severity (assessor):** medium
- **claim:** 4 Realms × 6 Domains × 4 Products = 96 combinations, all on one Concept→Design→Build→Launch→Commercialise lifecycle; domain tools usable 'throughout the Enterprise / Learning / Developing / Scholarship realms'.
- **observed:** Realms and Domains exist as one source on both sides (taxonomy.py / taxonomy.ts) and realm has teeth in Genesis (REALM_REGISTER). The Products axis does not exist anywhere: no PRODUCTS constant, no 'laboratory' route (only /reactor, /incubator, /factory, /reactor-studio; 'IntelligenceLab' at /intelligence is unrelated), no 96-cell surface. /api/v1/catalog/products is a flat list of 20 SDK/platform directories (count:20), not the four product modes. configs/realms.yaml still encodes six domain-shaped 'realms' (bio, legal, climate, materials, religion, education) and has no code consumer; src/organism/config/sovereign_config.yaml:9 points at src/organism/config/realms.yaml, which does not exist. Offering-1 tools never see a realm: DomainTool posts only its fields and none of the six domain routers read one, so 'domain working across realms' is a Genesis-only property. Five hub CTAs post domains AS realms (realm=science/law/education/care/religion with domain=research/policy/curriculum/service/content) into a projects API that keeps its own realm vocabulary (technology/science/…/general) and stores req.realm.lower() unvalidated — the ProjectsHub realm <select> lists canonical realms, so the user sees 'Enterprise' selected while 'science' is posted.
- **evidence:** agentic_core/taxonomy.py:10-11 (REALMS, DOMAINS only); apps/workstation-superapp/src/lib/taxonomy.ts:5-6; App.tsx:202-207 (routes); GET /api/v1/catalog/products (count 20); configs/realms.yaml:1-13; src/organism/config/ (only sovereign_config.yaml present; :9 references a missing realms.yaml); grep 'realm' over agentic_core/api/{religion,science,education,law,employment,care}.py → only care.py's patient_profile field; ScienceHub.tsx:33, LawHub.tsx:48-49, EducationHub.tsx:177-178, CareHub.tsx:305, ReligionHub.tsx:43 (domains as realms); pages/projects/ProjectsHub.tsx:90 (initialRealm='technology'), :456; agentic_core/projects/api.py:50-59 (REALM_PROMPTS), :290.
- **disclosed to the user:** No — nothing tells a user the product axis is absent or that a hub CTA's realm is silently non-canonical.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced with two evidence corrections. Realms and Domains exist as one source (taxonomy.py:10-11, taxonomy.ts:5-6) and realm has teeth via realm_directive() — consumed by genesis.py:175, deliverables.py:104 and ceo_generate.py:199 (the mechanism is realm_directive(), which reads REALM_REGISTER; the assessor named the constant, the substance is right). The Products axis does not exist: no PRODUCTS constant; App.tsx:202-207 routes /reactor, /incubator, /reactor-studio, /factory, /intelligence — no laboratory; GET /api/v1/catalog/products count 20 = a directory scan of products/ (catalog/api.py:107-140). configs/realms.yaml holds six domain-shaped ids (bio/legal/climate/materials/religion/education) with no consumer; src/organism/config/sovereign_config.yaml:9 points at src/organism/config/realms.yaml, which does not exist. grep 'realm' across the six domain routers → zero reads. Hub CTAs post domains as realms: ScienceHub:30/:36, LawHub:48-54, EducationHub:29-35, CareHub:30/:35 navigate to /projects?realm=science|law|education|care (StartProjectCTA.tsx:27) — correction: my grep finds no such CTA in ReligionHub.tsx, so four hubs, not five. ProjectsHub.tsx:456 → initialRealm 'science' into a <select> whose options are enterprise/learning/developing/scholarship (:140-148), so the browser shows the first option while state posts 'science'; projects/api.py:290 stores req.realm.lower() unvalidated and REALM_PROMPTS (:50-59) is keyed by domain names and used at :348. Nothing tells the user. DOC_OVERCLAIM stands.
- **refuter's evidence:** agentic_core/taxonomy.py:10-11, :38 (REALM_REGISTER), :68 (realm_directive); consumers: agentic_core/api/genesis.py:27,175; api/deliverables.py:43,104; api/v290/ceo_generate.py:199; apps/workstation-superapp/src/App.tsx:202-207; GET :8024/api/v1/catalog/products → count 20; configs/realms.yaml:1-13; src/organism/config/sovereign_config.yaml:9 (realms.yaml missing); grep realm agentic_core/api/{religion,science,education,law,employment,care}.py → 0; pages/domains/ScienceHub.tsx:30,36; LawHub.tsx:48-54; EducationHub.tsx:29-35; CareHub.tsx:30,35; components/StartProjectCTA.tsx:27; pages/projects/ProjectsHub.tsx:90, :140-148, :456; agentic_core/projects/api.py:50-59, :290, :348.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either add PRODUCTS to taxonomy.py/.ts and expose the grid (a realm×domain×product picker feeding Genesis), or amend §17.1 to say the product axis is a design intent; delete configs/realms.yaml (orphan) and fix the sovereign_config path; make the five hub CTAs pass a canonical realm from the user's default and a canonical domain; have DomainTool send the user's default realm so realm_directive applies to Offering-1.

### R5.6 · §3 / §14 / §15.6 — Marketplace catalogue ('Sovereign Economy · 20 Live Products') incl. six 'Domain Signature Products' — **STUB**

- **severity (assessor):** medium
- **claim:** Products are living, delivered capabilities; never present a façade as a product.
- **observed:** The marketplace header counts every directory under products/ as a 'Live Product'. Six entries ('Care Domain Signature Product', feature 'VSB-SIG-CARE Series', etc.) are literals in catalog/api.py CATALOG_OVERRIDES; their only substance is a legacy directory (e.g. products/Care/VSB-SIG-CARE-SEXTA-1.0) whose manifest self-declares status 'PRODUCTION_READY', 'WCAG_2.2_AAA', nine 'injection_formats' incl. MP4/MP3, 'octo_veritas_integrated: true' — none of which any route serves. 'Open' merely navigates to the domain hub. The remaining entries are SDK source-file pointers (route:null) shown as products.
- **evidence:** agentic_core/catalog/api.py:12-26 (ROUTE_OVERRIDES), :30-95 (literal names/features), PRODUCTS_DIR scan; products/Care/VSB-SIG-CARE-SEXTA-1.0/manifest.json (PRODUCTION_READY, WCAG_2.2_AAA, injection_formats); apps/workstation-superapp/src/pages/marketplace/LivingMarketplace.tsx:173 ('{n} Live Products'), :286-303 (features + Open); GET /api/v1/catalog/products (20 entries).
- **disclosed to the user:** No — 'Live Products' with tier/feature bullets reads as a commercial catalogue.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. list_products() (catalog/api.py:130-140) returns every non-dot/underscore directory under products/ (20 on disk) and the marketplace header prints '{catalogProducts.length} Live Products' (LivingMarketplace.tsx:173) under the sentence 'every entry runs on Workstation's own native AI fabric and opens to a live surface. Listings reflect what is actually built.' (:176-177). Executed GET /api/v1/catalog/products: 20 entries, 11 with route null (no Open button, :298-303) — contradicting 'opens to a live surface' on its own page. The six 'Domain Signature Product' entries are literals in CATALOG_OVERRIDES (:66-98, features 'VSB-SIG-CARE Series' etc.); their only substance is a legacy directory — products/Care/VSB-SIG-CARE-SEXTA-1.0/manifest.json self-declares status PRODUCTION_READY, accessibility_level WCAG_2.2_AAA, nine injection_formats incl. MP4/MP3, octo_veritas_integrated true; its assets/dashboard/ directory is empty; no StaticFiles mount serves products/ (app_mvp.py:596 mounts only the SPA /assets); grep VSB-SIG|SEXTA in agentic_core → only the catalog literals. 'Open' for those six navigates to the domain hub. The remaining entries are metadata.json pointers to SDK source files (e.g. molecular_sdk → agentic_core/biochemical/molecular_comm.py) presented with tier/feature bullets. STUB stands.
- **refuter's evidence:** agentic_core/catalog/api.py:12-26 (ROUTE_OVERRIDES), :30-98 (CATALOG_OVERRIDES literals), :107-140 (metadata.json / directory scan); products/ (20 dirs); products/Care/VSB-SIG-CARE-SEXTA-1.0/manifest.json (status, accessibility_level, injection_formats, octo_veritas_integrated), assets/dashboard/ empty; products/molecular_sdk/metadata.json source pointer; agentic_core/app_mvp.py:590-596 (only SPA /assets mounted); apps/workstation-superapp/src/pages/marketplace/LivingMarketplace.tsx:173, :176-177, :286-303; live: GET :8024/api/v1/catalog/products → count 20, 11 route:null.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Rename the header to 'Registered product directories', drop or badge the six Domain Signature entries as 'legacy archive — not a served product', and only count entries with a live route as 'live'.

### R5.7 · §9 / §14 — accessible to all languages, personalised to each user (i18n + explicit profile W428) — **PARTIAL**

- **severity (assessor):** low
- **claim:** All languages; dynamically personalised to each user's instructions, preferences, needs and abilities.
- **observed:** i18n: four dictionaries (ar, fr, es, ur) of 90 keys each cover navigation + the two front doors; useT() is used by only three pages (Sidebar, DashboardNew, DomainsHub); the six domain hubs, DomainTool, Settings and the avatar are English-only; 12 languages are offered in Settings, 8 with no dictionary; RTL is applied at document level. Settings states this coverage truthfully, including 'AI-generated content is still produced in English'. Font scale is a real rendered effect; guided/tone are honest labels. Profile (W428): PUT /api/v1/user/profile works and the server-built preamble is shown verbatim; gateway prepends it to every generation. But the floor ignores it (executed: after saving a nurse-in-Leeds profile, /employment/cv output contained none of it) and gateway's 'profile_applied' flag is dropped by ai_text(), so no domain response — and no badge — can tell the user whether their profile shaped the output. Settings' 'Exactly what is added to your prompts' is true; 'used to shape what the platform generates' is not, under the floor.
- **evidence:** apps/workstation-superapp/src/lib/i18n.tsx:177 (DICTS en/ar/fr/es/ur; measured 22/90/90/90/90 keys), :198-202 (coverageFor); grep useT() → 3 pages; lib/userPrefs.ts:24-37 (12 LANGUAGES); pages/Settings.tsx:107-121 (honest coverage copy), :160-166 ('Used to shape what the platform generates'); agentic_core/ai/gateway.py:151-153 (preamble prepended), :174-176 (profile_applied returned); agentic_core/api/_ai_provenance.py:30-36 (profile_applied not carried into provenance); live PUT/DELETE /api/v1/user/profile and /employment/cv on :8024.
- **disclosed to the user:** Yes for language coverage (Settings is honest); no for the profile's effect under the floor.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, with a measurement correction: the four non-English dictionaries hold 64 keys each (ar/fr/es/ur; en 22), not 90 — counted from i18n.tsx (const ar at :43, fr :113, es :186, ur :256, DICTS :326). The Settings figure is computed at runtime from Object.keys (coverageFor, :347-351), so it stays honest whatever the count. useT() is consumed by exactly three pages (Sidebar, DashboardNew, DomainsHub); the domain hubs, DomainTool, Settings and the avatar are English-only; userPrefs.ts:24-37 offers 12 languages, 8 without a dictionary; Settings.tsx:107-121 states coverage truthfully including 'AI-generated content is still produced in English'. Profile: PUT /api/v1/user/profile {about_you:'Registered nurse in Leeds…', context:'…LTHT'} returned the exact preamble_preview; executed POST /employment/cv immediately after → output contained neither 'Leeds' nor 'LTHT' (the floor ignores the prepended preamble), and its ai_provenance carried no profile flag: gateway.query_meta returns profile_applied (gateway.py:181) but ai_text() builds provenance from served_by/is_external only (_ai_provenance.py:30-36) — grep shows gateway.py:181 is the field's only occurrence in code. Settings.tsx:176 'Used to shape what the platform generates for you' is therefore untrue under the floor and undisclosed. PARTIAL stands.
- **refuter's evidence:** apps/workstation-superapp/src/lib/i18n.tsx:18, :43, :113, :186, :256 (dict starts; measured 22/64/64/64/64 keys), :326, :347-351; grep useT() → components/layout/Sidebar.tsx, pages/DashboardNew.tsx, pages/domains/DomainsHub.tsx; lib/userPrefs.ts:24-37; pages/Settings.tsx:107-121, :176, :197; agentic_core/ai/gateway.py:151-153, :181; agentic_core/api/_ai_provenance.py:30-36; live on :8024: PUT /user/profile (preamble_preview returned), POST /employment/cv {target_role:'ICU Charge Nurse', experience:'Staff nurse 8 years, ICU'} → no 'Leeds'/'LTHT', ai_provenance lacks profile_applied; DELETE /user/profile → cleared.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Carry profile_applied into ai_provenance and show 'profile: applied / not usable by floor' in DomainTool; state in Settings that the profile takes effect when the owned model serves.

### R5.8 · §3A Offering-1 front door hygiene — DomainsHub / AI Tools counts, and the 'QEP Flagship' tab on five non-Religion hubs — **PARTIAL**

- **severity (assessor):** low
- **claim:** The Domains section surfaces domain-specific tools; QEP is the Religion domain's flagship (Owner directive 2026-09-03).
- **observed:** Counts disagree at every step: DomainsHub advertises 'Browse all 23 tools' (Law listed as 2), /ai-tools then shows '18 tools', while the hubs actually mount 24 DomainTool forms (Religion 4, Science 3, Education 4, Law 3, Care 4, Employment 6) — the catalogue omits hadith study, experiment design, marking, safeguarding, legal research and salary negotiation. Separately, Science, Law, Education, Care and Employment each keep a 'QEP Flagship' tab that is not QEP at all: it renders a 'Quadruple Engine Pillar' of four engines that, when clicked, say 'not wired to a backend yet — nothing was run', plus 'planned' immersive tools and an 'Audit Framework' button that toasts not-implemented. Honest after a click, but a permanently dead tab under a flagship name, contrary to the directive that QEP lives in Religion.
- **evidence:** apps/workstation-superapp/src/pages/domains/DomainsHub.tsx:13-21 (tools: 4,3,4,2,4,6 → 23); pages/AIToolsCatalogue.tsx:12-42 (18 entries); hub tab lists: ReligionHub.tsx:78-83, ScienceHub.tsx:48-51, EducationHub.tsx:192-196, LawHub.tsx:63-66, CareHub.tsx:319-323, EmploymentHub.tsx:64; components/QEPDashboard.tsx:35-43 ('NOT_WIRED'), :99-105; components/QEPImmersiveTools.tsx:31-36, :64.
- **disclosed to the user:** Counts: no. Dead tab: yes, on click (amber 'Not wired').
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. DomainsHub.tsx:13-21 declares tools 4/3/4/2/4/6 → TOTAL 23, printed at :43 ('{TOTAL} tools across 6 domains') and :56 ('Browse all 23 tools'); AIToolsCatalogue.tsx:12-45 lists 2+2+3+3+3+5 = 18, printed at :53. The hubs actually mount 23 <DomainTool> forms (Religion 4: dialogue/tafsir/halal/hadith; Science 3; Education 4; Law 2; Care 4; Employment 6) plus Law's custom compliance analyser = 24 tool forms; the catalogue omits hadith study, experiment design, marking/feedback, safeguarding, legal research and salary negotiation. Science, Law, Education, Care and Employment each keep a 'QEP Flagship' tab that renders QEPDashboard (click → status NOT_WIRED, 'This engine is not wired to a backend yet — nothing was run', :35-43, :99-105) and QEPImmersiveTools ('planned', both buttons → notImplemented, :31-36, :64). ReligionHub's 'qep' tab, by contrast, renders the real QEP (ReligionHub.tsx:91-100, W439 comment). Honest on click, dead by design, contrary to the directive that QEP lives in Religion. Counts undisclosed. PARTIAL stands.
- **refuter's evidence:** apps/workstation-superapp/src/pages/domains/DomainsHub.tsx:13-21, :43, :56; pages/AIToolsCatalogue.tsx:12-45, :53; <DomainTool mount counts per hub: ReligionHub 4, ScienceHub 3, EducationHub 4, LawHub 2 (+ custom compliance form :60-66/:125-140), CareHub 4, EmploymentHub 6 (tab list :64); QEPDashboard/QEPImmersiveTools imported+rendered in ScienceHub, EducationHub, LawHub, CareHub, EmploymentHub; components/QEPDashboard.tsx:35-43, :99-105; components/QEPImmersiveTools.tsx:31-36, :64; pages/domains/ReligionHub.tsx:81-86 (tabs), :91-100 (real QEP).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Derive both counts from one shared tool registry (the same list the hubs mount); remove the 'QEP Flagship' tab from the five non-Religion hubs or rename it 'Roadmap' and move it below the working tools.

### R5.9 · §3A offering 1 / §17.1 Domains — six Domains with in-house-AI-mediated tools, incl. the Religion QEP flagship — **DELIVERED**

- **severity (assessor):** none
- **claim:** Each Domain offers immediate, usable, in-house-AI-mediated tools (research · analyse · generate · plan · assess · author …) without establishing an enterprise; Religion carries the Quran Education Platform under the faith-content constitution.
- **observed:** Verified by execution: all six domains have routers under /api/v1/{religion,science,education,law,employment,care}, metadata routes (schools/methodologies/frameworks/templates/tools/services) answer, and one tool per domain plus /refine ran and returned ai_provenance {served_by:'native', is_external:false}. The hubs mount 24 DomainTool forms that render the amber 'structured floor — not model analysis' badge, the router disclaimers, copy/download, refine (verified: the floor preserves the draft verbatim and labels its additions), My Work persistence and the Offering-1→Genesis bridge. QEP: /qep/status is honest (SM-2 active, tajweed text-only, recitation never scored); tafsir returned arabic_source 'alquran.cloud — authentic sourced text, never AI-generated' with real Arabic; QEPReligionHub's Tajwid Coach refuses to score and says why. Shortfall, which is the environment not a defect: with AI_DISABLE_LOCAL=1 every tool's body is a deterministic scaffold that echoes its own prompt template ('Native structured content for X, grounded in: … Produce a care plan with these sections:'), so the usable capability today is form + provenance + persistence, not domain work — disclosed in DomainTool, undisclosed where findings 1-3 apply. (UI behaviour reasoned from source; no browser driven.)
- **evidence:** Live executions on :8024: GET /religion/schools, /science/methodologies, /education/frameworks, /law/templates, /care/tools, /employment/services, /qep/status; POST /religion/quran-tafsir, /science/hypothesis, /education/lesson-plan, /law/analyse, /care/care-plan, /employment/cv, /refine; agentic_core/api/{religion,science,education,law,employment,care}.py; apps/workstation-superapp/src/components/DomainTool.tsx:225-237, :270-283; pages/domains/*.tsx; pages/domains/QEPReligionHub.tsx:47-96.
- **disclosed to the user:** Yes on every DomainTool tab (amber floor badge + disclaimer); the floor's leaked prompt template inside the body is itself an honest tell, if an ugly one.
- **refutation: SURVIVED** (reproduced by the refuter). Verified by execution and source. Metadata routes all 200 with real content: /religion/schools, /science/methodologies, /education/frameworks, /law/templates, /care/tools, /employment/services, /qep/status. One tool per domain plus /refine executed on :8024 — /religion/quran-tafsir (arabic_text real, arabic_source 'alquran.cloud — authentic sourced text, never AI-generated'), /science/hypothesis, /education/lesson-plan, /law/analyse, /care/care-plan, /employment/cv, /refine — every one returned ai_provenance {served_by:'native', is_external:false}; /refine preserved the draft verbatim under a labelled 'Refinement additions (in-house structured floor…)' block. DomainTool renders the amber 'structured floor — not model analysis' badge (:225-229), the router disclaimer when present (:256), copy/download, saves to My Work (saveOutput :108, :139) and bridges to Genesis with a seeded record (:279-281). QEP: /qep/status states SM-2 active, tajweed text-only, recitation never scored; QEPReligionHub's TajwidCoach refuses to score and says why (:59-85). Two minor precisions the assessor glossed: /science/hypothesis and /education/lesson-plan carry no disclaimer field (only law/care/religion do), and ReligionHub's default tab is 'wisdom' with QEP as a tab plus the /qep-religion route. Under AI_DISABLE_LOCAL=1 every body is a scaffold echoing its own prompt template — the environment, not a defect, and disclosed on every DomainTool tab. DELIVERED stands for the DomainTool surfaces; the undisclosed exceptions are findings 0-3.
- **refuter's evidence:** live on :8024: GET /api/v1/{religion/schools,science/methodologies,education/frameworks,law/templates,care/tools,employment/services,qep/status} → 200; POST /religion/quran-tafsir {surah:1,ayah_start:1} → arabic_source alquran.cloud; POST /science/hypothesis, /education/lesson-plan, /law/analyse, /care/care-plan, /employment/cv, /refine → served_by 'native', is_external false; agentic_core/api/{religion,science,education,law,employment,care}.py; apps/workstation-superapp/src/components/DomainTool.tsx:108, :139, :225-229, :256, :279-281; pages/domains/QEPReligionHub.tsx:59-85; pages/domains/ReligionHub.tsx:21 (default 'wisdom'), :91-100.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None for the surface; for the floor, stop echoing the prompt's section instructions into the body ('Produce a care plan with these sections…') and emit 'no content composed for this section' instead.

---

## R6 — §8 + §12 + §17.5 — the biomimetic living organism, the economic organism, the ten architecture invariants

**Assessor's region summary:** R6 (§8 organism · §12 economy · §17.5 invariants), assessed by executing routes on the live HEAD backend (8024) and reading handlers/components. The ECONOMY is the strongest part of the region and is DELIVERED: a real 6-stage waterfall under a gaas.v5 gate, double-entry books that balance across cycle → inter-VSB transfer → receiver consumption → period close (verified trial balance and balance sheet at every step), refusals for insufficient funds/void receivers, owner accrual with rails DISABLED, capital-fund compounding, charity ranking that honours the Owner's directives with curated-weight provenance disclosed, and a UEG chain that verifies (807 events). Six of the ten §17.5 invariants hold in code (single mount, append-only UEG/DCMS, bus atomicity, twin pre-validation, live plan/board-pack assembly, economy-side tenant scoping). The GAP is the organism and the other four invariants: (1) the 'GaaS gate on every output' is true for 8 of 57 API modules — the gateway every other output takes runs only a three-word regex that also blocks legitimate business text ('exploit the opportunity'); (2) the §8 survival instinct is a dead branch — every 'rest/throttle/conserve' path keys off an ATP simulator whose production always exceeds its capped consumption, so the threshold is unreachable, and only the Anatomy tab says 'simulated' while the Economy page and board pack show 'ATP 100%'; (3) torch optionality is violated outright (boot fails without torch via avatars → uci_interceptor → hd_omni_learner); (4) the organism's control perimeter (heartbeat, genome, board, CCA, sovereign evolution) has no auth dependency and no global middleware, so under multi-user any caller could stop the heart or switch on economy cycles across all tenants; (5) no KPI gate before delivery exists anywhere; (6) the immune system recommends but never engages its own defence (no caller for immune-reconfigure); (7) the user_projects waterfall stage leaves the investor's books and never reaches the investee. The self-running claim is honest at the Heartbeat tab (all five autonomy flags OFF and persisted OFF; genome generation 0; evolution roadmap is the floor fallback) but overstated by the plan pillar 'strong'. Environment caveats: single-user, AUTH off, floor-only model serving — the floor is disclosed inside generated text (native-engine banner, 'No parseable CEO directives'), not as a UI badge on the Sovereign Evolution page. I could not drive a browser; UI statements are from reading apps/workstation-superapp/src.

### R6.0 · §17.5 invariant — mandatory GaaS gate on every output (+ §11 ethics architecturally binding) — **PARTIAL**

- **severity (assessor):** high
- **claim:** §17.5: 'mandatory GaaS gate on every output' is an absolute invariant; §15.6 ethics architecturally binding.
- **observed:** The constitutional interceptor (UnifiedConstitutionalInterceptorV16Omega / ConstitutionalPolicyGate) is invoked in 8 of 57 API modules only (board, compliance, constitutional_gaas, economy, forge, genesis, swarm, transformation_orchestration). Every other model output — business plan, deliverables, mgmt DCS/risk register, sovereign-evolution CEO/C-suite/BTO passes, CCA twin simulation, board narratives — flows through gateway.query_meta, whose only gate is guardrails.validate_response: a three-regex keyword filter (malware|hacker|exploit) that is not constitutional and blocks legitimate business text. Verified: validate_response('We will exploit the market opportunity in Q3.') -> False; 'A certified ethical hacker training course syllabus.' -> False — those outputs are replaced with '[POLICY VIOLATION]…'. Live risk-register call returned floor text (no keyword hit) so the false positive is code-verified, not observed on this call.
- **evidence:** agentic_core/ai/gateway.py:172-173 (validate_response is the sole gate; no interceptor import anywhere in gateway.py); agentic_core/ai/guardrails.py:2-10 (three patterns); grep of interceptor/PolicyGate use: 8 files under agentic_core/api/; economy.py:609 board-pack governance line says 'gaas.v5-gated cycles' (true for cycles only).
- **disclosed to the user:** No. Nothing at any surface says most outputs bypass the constitutional gate; the vision states every output is gated.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced and strengthened. The constitutional interceptor is invoked in exactly 8 of 57 API modules (board, compliance, constitutional_gaas, economy, forge, genesis, swarm, transformation_orchestration), and even there it wraps an intent/context ({intent: board_chief_instruct | economy_distribution | inter_vsb_transfer | genesis_journey | org_cascade ...}) with a regex post-gate (policy_gate.validate_output → _UNSAFE_OUTPUT). The 22 API modules that call gateway.query/query_meta/stream (business_plan, deliverables, management_systems, sovereign_evolution, digital_twin, products, intelligence, synthesis_studio, qep_intelligence, mega_project, ...) get only guardrails.validate_response, a three-regex filter. The assessor's false positive was code-only; I observed it LIVE: POST /api/ai/query {message:'Write one sentence on how we exploit the market opportunity in Q3.'} → {"response":"[POLICY VIOLATION] The generated response was blocked by safety guardrails."} — the floor echoed the prompt and the guardrail destroyed a legitimate business sentence. Not disclosed anywhere; worse, the Compliance page copy claims the federated check is 'Used by the economy, synthesis, Genesis, and the Forge to keep every output halal, lawful, and ethical', and the board pack's governance line reads 'gaas.v5-gated cycles'. PARTIAL (undisclosed) is the right verdict.
- **refuter's evidence:** agentic_core/ai/gateway.py:8,172-173 (validate_response is the only gate; no interceptor import); agentic_core/ai/guardrails.py:5-7 (malware|hacker|exploit); grep 'UnifiedConstitutionalInterceptor|ConstitutionalPolicyGate|.intercept(' over agentic_core/api/*.py → 8 files; grep 'gateway.(query|query_meta|stream)' → 22 files; agentic_core/gaas/v5/uci_v16_omega.py:81,101 (pre gate on context, post gate regex on output); agentic_core/economy/governance.py:246 (intent economy_distribution); live curl POST /api/ai/query → '[POLICY VIOLATION]…' (this session); python: validate_response('Reduce labour exploitation risk in the supply chain.') → False; apps/workstation-superapp/src/pages/governance/ComplianceChecker.tsx:38 ('keep every output halal, lawful, and ethical'); agentic_core/api/economy.py:609 + live GET /economy/board-pack governance 'gaas.v5-gated cycles'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Run ConstitutionalPolicyGate('gateway').validate + a UEG checkpoint inside gateway.query_meta (the sync pre-gate already exists in economy/governance.py:governed_cycle_sync), and replace the bare-substring regex with word-boundary/context-aware checks or drop it — then the §17.5 line is true.

### R6.1 · §8 survival instinct · homeostasis loops (immune↔nervous↔metabolic) · §8→§12 economic survival instinct — **STUB**

- **severity (assessor):** high
- **claim:** §8: homeostasis loops and a survival instinct make the organism dynamic/adaptive; the heartbeat 'rests and restores its own energy'; §12 economy 'conserves more when organism energy is low'.
- **observed:** Every survival-instinct branch keys off ATPSimulator.ratio/15 < 0.3: heartbeat step 2 (rest/recover), homeostasis.assess cognition throttle (atp<0.3), the metabolic_throttle lever (atp<0.2), and metabolism.run_cycle's reserve boost. ATPSimulator.update adds production 0.5 (0.4 off-focus) and subtracts 0.1×load per update, and load is clamped to 0.2+0.08×nodes ≤ 1.0 — so ATP is monotonically non-decreasing from its start of 5.0 (0.33) to the 15.0 cap (1.0). The <0.3 threshold (raw <4.5) is unreachable; the branches can never fire. Live confirmation: atp_ratio 1.0, last_recovery null, last_heal null across 511 beats; organism/status labels the metabolic term 'ATPSimulator on a constant metabolic_load — simulated, not a measurement'. POST /organism/homeostasis returns adjustments_made [] .
- **evidence:** agentic_core/molecular/atp_simulator.py:12-25; agentic_core/ai/native/homeostasis.py:45 (load = max(0.2, min(1.0, 0.2+nodes*0.08))); agentic_core/organism/heartbeat.py step 2 (atp < 0.3) and metabolic_throttle (<0.2); agentic_core/economy/metabolism.py (metabolic_energy < 0.3 → effective_reserve); live GET /api/v1/organism/status composite_health_terms.metabolic.measured=false; GET /heartbeat/status last_recovery=null.
- **disclosed to the user:** Partly. The Anatomy tab shows a 'SIMULATED' chip and the basis string (OrganismAnatomy.tsx:236-241). The Organism overview tab renders the blended composite (OrganismDashboard.tsx:261, no measured/blended distinction), the Economy page shows 'Metabolic Energy 100%' and '§8→§12 survival instinct' copy, and the board pack shows 'ATP 100%' — none say the number is a simulator that cannot fall.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed by the arithmetic and by every reachable path. ATPSimulator.update: production = 0.5 × circadian_efficiency where biobus passes efficiency 1.0 (ACTIVE_FOCUS) or 0.8 (all other phases) → production ≥ 0.4; consumption = 0.1 × metabolic_load, and every caller clamps load ≤ 1.0 (homeostasis.assess: max(0.2, min(1.0, 0.2+0.08·nodes)); biobus default 0.3) → consumption ≤ 0.1. Net ≥ +0.3 per update, dt=1.0, start 5.0, cap 15.0 — ratio is monotonically non-decreasing and reaches 1.0; no other code writes .ratio (grep). Every survival branch keys on atp < 0.3 (raw < 4.5): heartbeat.py:190 rest/recover, homeostasis.py assess cap=1, organism_status.py:427 RPM reduction, heartbeat.py:292 metabolic_throttle (<0.2), metabolism.py:129 reserve boost — all unreachable. Live at 788 beats: atp_ratio 1.0, last_recovery null, last_heal null; composite_health_terms.metabolic.measured=false with basis 'ATPSimulator on a constant metabolic_load — simulated, not a measurement'. One evidence correction: POST /organism/homeostasis {} today returns adjustments_made with one entry (scheduler 'defer_non_urgent' — Outside peak focus window ACTIVE_REST) rather than []; that is the circadian branch (line 58), not a survival-instinct branch, so the finding stands. Disclosure is partial as stated: the Anatomy tab shows measured-vs-blended; the Organism overview shows composite 100% and an 'ATP ratio' tile with no simulated label, and its cognition copy says 'each run expends ATP, which recovers on the circadian cycle' — a claim that is false in effect since ATP never net-falls; VSBEconomy shows 'Metabolic Energy 100%' (tone good) and the survival-instinct line only renders when energy_state != 'healthy', which cannot happen; the board pack shows 'ATP 100%'.
- **refuter's evidence:** agentic_core/molecular/atp_simulator.py:12-25; agentic_core/organism/biobus.py:151-165 (efficiency 1.0/0.8, dt=1.0, default load 0.3); agentic_core/ai/native/homeostasis.py:45,67-69; agentic_core/organism/heartbeat.py:189-194,288-294; agentic_core/api/organism_status.py:426-431,458; agentic_core/economy/metabolism.py:126-130,238-245; grep '\.ratio\s*[-+]?=' → only atp_simulator.py:24-25; live GET /organism/status composite_health_terms.metabolic {value 1.0, measured false}; live GET /heartbeat/status beats 788, last_recovery null; live POST /organism/homeostasis {} → adjustments_made [scheduler defer_non_urgent]; OrganismDashboard.tsx:234-235,261,352-355; VSBEconomy.tsx:384-389,471; OrganismAnatomy.tsx:228-231.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either drive metabolic load from a measured quantity (the _introspect cpu/memory percent already collected, request rate, queue depth) with a consumption term that can exceed production, or label every ATP figure outside Anatomy 'simulated — does not deplete' and remove the survival-instinct copy from VSBEconomy until the branch can fire.

### R6.2 · §17.5 invariants — user isolation (scoped to user_id) and Arms-Length Agency, at the organism's control perimeter — **PARTIAL**

- **severity (assessor):** high
- **claim:** §17.5: user isolation and arms-length (AI CEO cannot instruct board/genome) are absolute; §14 multi-user front door.
- **observed:** Auth is a per-route Depends(get_current_user); the app registers only CORSMiddleware (no global auth middleware, no router-level dependencies on any of 79 include_router calls). The heartbeat router (status/beat/start/stop/configure), genome router (encode/mutate/crossover), organism_status, sovereign-evolution cycle, board chief/instruct + directive, and change_control submit/review/implement carry zero auth dependencies. Under auth an unauthenticated caller could stop the heartbeat, POST /heartbeat/configure {auto_economy:true} (which then runs economy cycles on EVERY tenant's living VSB via operate_one), mutate genomes, file+AI-review+implement CCA changes that apply live organism levers, and instruct the Chief. By contrast the economy router is properly owner-scoped (404-never-403). Arms-length is honoured by construction in-process (no AI-tier code writes genome/board; evolution application sits behind CCA approval) but the HTTP perimeter is open. Not observable live here (AUTH off, single-user) — code-verified.
- **evidence:** agentic_core/app_mvp.py:36 (only CORSMiddleware); grep 'dependencies=' on include_router: 0; agentic_core/api/heartbeat.py:27-63 (no Depends); grep get_current_user|require_admin → 0 in organism_status.py, organism/genome.py, sovereign_evolution.py, board.py, change_control.py; economy.py:33-51 _require_economy_access (scoped correctly); living_vsbs.operate_one iterates all tenants' VSBs.
- **disclosed to the user:** No.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed in code (not observable live: AUTH off, single-user). app_mvp.py registers only CORSMiddleware; all 79 include_router calls carry no dependencies=, and no APIRouter(...dependencies=...) exists anywhere in agentic_core. heartbeat.py (status/beat/start/stop/configure), organism_status.py, organism/genome.py (encode/crossover/mutate), sovereign_evolution.py, board.py (chief/instruct, directive) and change_control.py (submit, immune-reconfigure, review, implement) contain zero get_current_user/require_admin/Depends references; genome.py has no owner/user/tenant scoping at all. get_current_user only raises 401 when a route declares it. heartbeat.beat() runs living_vsbs.operate_one() whenever auto_economy is true, and operate_one selects the least-recently-operated VSB across the whole registry with no owner filter — so an unauthenticated POST /heartbeat/configure {auto_economy:true} would cycle every tenant's VSB. The economy router is the contrast (24 auth references; _require_economy_access 404-never-403). Arms-length is honoured by construction (the chief_instruct path itself runs through the interceptor; evolution application sits behind CCA approval). PARTIAL with no disclosure is right.
- **refuter's evidence:** agentic_core/app_mvp.py:36-37 (only CORSMiddleware); grep 'include_router' → 79, 'include_router.*dependencies' → 0; grep 'APIRouter(.*dependencies' → 0; grep -c 'get_current_user|require_admin|Depends(' → heartbeat.py 0, organism_status.py 0, organism/genome.py 0, sovereign_evolution.py 0, board.py 0, change_control.py 0, economy.py 24; agentic_core/organism/genome.py:100-310 routes, no owner refs; agentic_core/auth/core.py:205-212; agentic_core/organism/heartbeat.py:235-238; agentic_core/economy/living_vsbs.py:105-118 (operate_one, no owner filter); agentic_core/api/economy.py:33-51.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Attach an admin-only dependency at include_router for the heartbeat, genome, organism-status, sovereign-evolution, board and CCA routers (dependencies=[Depends(require_admin)]) — one line each in app_mvp.py — and add a test that boots with AUTH on and asserts 401 on POST /heartbeat/stop.

### R6.3 · §17.5 invariant — torch optionality — **DOC_OVERCLAIM**

- **severity (assessor):** high
- **claim:** §17.5 lists 'torch optionality' among the ten absolute architecture invariants.
- **observed:** Booting the app with torch unavailable fails at import. Executed: sys.modules['torch']=None; import agentic_core.app_mvp → ModuleNotFoundError via app_mvp.py:93 (from agentic_core.avatars import api) → avatars/__init__.py:1 (imports AvatarRecirculationOrchestrator) → avatars/core/recirculation_orchestrator.py:24 (governance.uci_interceptor) → governance/uci_interceptor.py:11 (mjm.recursive_meta_learner) → mjm/recursive_meta_learner.py:4 → mjm/hd_omni_learner.py:1 'import torch' (unguarded). The live instance boots only because torch 2.11.0+cpu happens to be installed. Two of the three torch users are guarded (optimal_transport via entropy_regularised_gaas try/except); this chain is not.
- **evidence:** Traceback from the torchless boot (scratch DATA_DIR): agentic_core/app_mvp.py:93 → avatars/__init__.py:1 → avatars/core/recirculation_orchestrator.py:24 → governance/uci_interceptor.py:11 → mjm/recursive_meta_learner.py:4 → mjm/hd_omni_learner.py:1.
- **disclosed to the user:** No — a deployer without torch gets an import crash, not a message.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced exactly. With sys.modules['torch']=None, `import agentic_core.app_mvp` raises ModuleNotFoundError through app_mvp.py:93 → avatars/__init__.py:1 → avatars/core/recirculation_orchestrator.py:24 → governance/uci_interceptor.py:11 → mjm/recursive_meta_learner.py:4 → mjm/hd_omni_learner.py:1 (`import torch`, unguarded). The dependency manifests also contradict the invariant: requirements.txt pins torch==2.11.0 and pyproject.toml declares torch = "^2.11.0" as hard dependencies. The only documented graceful handling is torchsde in the minimisation chain, not torch on the app path. Nothing at any surface says torch is required.
- **refuter's evidence:** Executed in a scratch DATA_DIR: traceback frames app_mvp.py:93, avatars/__init__.py:1, recirculation_orchestrator.py:24, uci_interceptor.py:11, recursive_meta_learner.py:4, hd_omni_learner.py:1; agentic_core/mjm/hd_omni_learner.py:1 'import torch'; agentic_core/governance/uci_interceptor.py:11; requirements.txt:266; pyproject.toml:31; docs/WORKSTATION_IDBO_WHOLE_VISION.md:466-470 ('torch optionality' listed as absolute).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Make avatars/__init__.py lazy (import the orchestrator inside the function that needs it) or wrap hd_omni_learner's torch import in try/except with a clear 'MJM omni-learner needs torch' error at first use; add a CI test that boots with sys.modules['torch']=None.

### R6.4 · §12 — 'reinvests in its own growth and in users' (user_projects waterfall stage → venture intelligence) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** §12: the VSB reinvests in users; §6 of the economic model: user-project investment seeds offspring ventures whose returns recycle.
- **observed:** Executed a cycle on r6-audit-vsb-6238 (revenue 1000, costs 100): 105 WST posted as distribution_user_projects and five 'positions' of 21.26 WST recorded against living VSBs including vsb-981debd258. That investee's next cycle consumed 0 WST from ventures (intake 50.0 = only the explicit inter-VSB transfer; venture_returns_recycled 0). ventures.record_positions writes only the investor's portfolio store; no pending intake is queued on the investee (contrast transfers.record_transfer, which does queue receiver intake). The WST leaves the investor's books and arrives nowhere — an investment that never funds anyone.
- **evidence:** agentic_core/economy/ventures.py:243-276 (_record_positions_locked writes holdings only); agentic_core/economy/transfers.py:record_transfer (queues _PENDING_STORE for the receiver — the pattern ventures lacks); live cycle reports for r6-audit-vsb-6238 and vsb-981debd258.
- **disclosed to the user:** Partly. Portfolio note says 'Virtual/simulated venture portfolio — no real funds moved' but nothing says the investee is never credited; the board pack shows 'Venture portfolio … positions' as if capital were deployed.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced live with a fresh VSB. POST /economy/cycle on r6-refuter-vsb-15919 (revenue 1000, costs 100) posted 105 WST to distribution_user_projects and recorded five 21.26 WST positions including vsb:vsb-981debd258; the live venture portfolio store gained holdings only under the investor. I then transferred 50 WST explicitly to vsb-981debd258 and ran its cycle (revenue 0): intake_revenue 50.0, inter_vsb_received_wst 50.0, venture_returns_recycled_wst 0.0 — the 21.26 WST 'investment' never arrived. Code: _record_positions_locked writes only the investor's holdings/invested_total; transfers.record_transfer is the pattern that queues receiver intake and ventures has no equivalent. The only recycling that exists is the manual /ventures/return route → pending_returns → consumed at the investor's next cycle, so half the loop is real, which keeps this at PARTIAL rather than STUB. Disclosure: portfolio and board pack say 'Virtual/simulated' but the board pack renders 'Venture portfolio (§6) 105 WST · 5 positions' with named investees as if capital were deployed.
- **refuter's evidence:** Live cycle report (this session) venture_investment.positions[1] = {id vsb:vsb-981debd258, amount_wst 21.26}; C:/tmp/hb18/economy_ventures_portfolio.json (investor-side holdings only); live POST /economy/transfer 50 → receiver_pending_wst 50; live POST /economy/cycle vsb-981debd258 → intake 50.0 / inter_vsb_received 50.0 / venture_returns_recycled 0.0; agentic_core/economy/ventures.py:243-275 (_record_positions_locked), 172-240 (record_return/consume_pending_returns — investor-side only); agentic_core/economy/transfers.py:52-98,122-136 (_PENDING_STORE queued for receiver); apps/workstation-superapp/src/pages/enterprise/VSBEconomy.tsx:457-460.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In record_positions, also queue each position amount as pending intake for the investee VSB (reuse transfers' pending store) so it enters the investee's next waterfall — or relabel positions 'recorded, unfunded' on the API and the VenturePortfolioPanel.

### R6.5 · §8 — 'defends itself' (immune system → defensive reconfiguration) — **PARTIAL**

- **severity (assessor):** medium
- **claim:** §8: the organism defends itself; §17.2 Immune layer = GaaS + self-healing (defend).
- **observed:** The immune system measures error events and reports a threat level with a 'recommended:' playbook (W438 honesty). The one real defence — POST /api/v1/cca/immune-reconfigure, which auto-approves and applies reversible levers (HIGH→metabolic_throttle, CRITICAL→immune_quarantine) — has no caller: the heartbeat never invokes it (no reference in heartbeat.py) and no frontend file references it. Under HIGH/CRITICAL threat the organism fires a reflex signal and waits for someone to curl the route. The ELEVATED lever (temperature_bias) has no consumer at all (recorded as lever_set_no_consumer). Self-healing is real but narrow: per-model-endpoint circuit breakers; healing_log empty, only 'model:ollama' tracked.
- **evidence:** agentic_core/api/change_control.py:281-350 (immune_reconfigure route); agentic_core/organism/immune.py:105-120 (playbook is 'recommended'); grep immune_reconfigure in organism/heartbeat.py: none; grep immune-reconfigure in apps/workstation-superapp/src: none; live /organism/self-healing/status healing_log [].
- **disclosed to the user:** Partly — the API playbook says 'recommended'; the HeartbeatMonitor's 'Self-defend' toggle describes compliance re-screening, not immune defence; the Organism overview shows THROTTLE/FULL THROUGHPUT chips only.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. POST /api/v1/cca/immune-reconfigure (change_control.py:281) is the only real defensive reconfiguration and has no caller: grep across agentic_core, apps/workstation-superapp/src, tests and scripts finds only its own definition and the immune playbook strings that recommend curling it. immune._response_playbook returns 'recommended: …' strings by design (W438). The heartbeat reads immune.status().health and fires an alert reflex when low; it never reconfigures. temperature_bias (the ELEVATED lever) is defined in reconfiguration.py and acknowledged in change_control.py as having no reader. Live self-healing: 1 tracked circuit (model:ollama), healing_log [], open_circuits 0. The HeartbeatMonitor 'Self-defend' toggle is auto_compliance ('Re-screens every living VSB against §11 compliance each beat'), not immune defence. PARTIAL, partly disclosed, is right.
- **refuter's evidence:** agentic_core/api/change_control.py:281 (route), 169-182 (temperature_bias no consumer note); agentic_core/organism/immune.py:105-120; agentic_core/organism/heartbeat.py:179-183 (health read → alert reflex only), no 'immune_reconfigure' reference; grep 'immune-reconfigure|immune_reconfigure' in agentic_core (outside change_control.py), src, tests, scripts → none; agentic_core/organism/reconfiguration.py:56; live GET /organism/self-healing/status healing_log []; HeartbeatMonitor.tsx:28-29.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Have heartbeat.beat call the immune_reconfigure core when threat ≥ HIGH (it is already CCA-governed, reversible and UEG-logged) and add an 'Engage immune defence' action on the Organism tab; until then, rename the recommendation so no surface implies autonomous defence.

### R6.6 · §17.5 invariant — KPI gate before customer delivery — **MISSING**

- **severity (assessor):** medium
- **claim:** §17.5: 'KPI gate before customer delivery' is an absolute invariant.
- **observed:** No code gates any delivery on KPIs: grep -i kpi across agentic_core/vbs/, api/marketplace.py, api/deliverables.py returns nothing. What exists is the QMS assure_delivery gate (required sections / stub detection / coverage, vbs/quality.py:115) on deliverables and the §11 compliance hold on marketplace listings — quality and compliance gates, not KPI gates. The business plan's live objective carries the placeholder KPI '(KPI to be set by the Board)' (GET /business-plan/progress).
- **evidence:** grep results (zero KPI hits in delivery paths); agentic_core/vbs/quality.py:115-160 (assure_delivery criteria); live GET /api/v1/business-plan/progress objectives_detail[0].kpi.
- **disclosed to the user:** No — §17.5 asserts it; no surface says it is absent.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. grep -i kpi over agentic_core/vbs/, api/marketplace.py and api/deliverables.py returns nothing; no 'kpi_gate' or 'kpi.*gate' exists anywhere in agentic_core. The delivery gates that do exist are assure_delivery (coverage / stub detection / §11 compliance screen / QMS gate — vbs/quality.py:115-175) and /qms/gate ('cockpit-gate': coverage + stubs). KPI appears only as a free-text field on business-plan objectives (business_plan.py, genesis.py:617/628, vsb.py:1290 — often ''), and the live workstation plan's objective KPI is the board.py:329 fallback placeholder '(KPI to be set by the Board)'. No surface says the KPI gate is absent.
- **refuter's evidence:** grep results (zero kpi hits in vbs/, marketplace.py, deliverables.py; zero 'kpi_gate'); agentic_core/vbs/quality.py:115-175; agentic_core/api/vbs_systems.py:38-52; agentic_core/api/board.py:329; live GET /api/v1/business-plan/progress objectives_detail[0].kpi = '(KPI to be set by the Board)'; docs/WORKSTATION_IDBO_WHOLE_VISION.md:469-470.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either implement it (deliverable/marketplace release blocked unless the owning VSB's objective KPIs are set and tracked, with the check recorded in the deliverable's quality_assurance) or remove the phrase from §17.5 and living_plan.py.

### R6.7 · §8 self-running · self-improving · evolving (heartbeat, sovereign evolution, genome) — the OrganismHub Heartbeat/Anatomy tabs — **PARTIAL**

- **severity (assessor):** medium
- **claim:** §8: once established, ever self-managing, improving, growing, evolving; §1 'forever runs, defends, heals, learns, improves and grows itself'.
- **observed:** The heartbeat genuinely runs (511 beats at 60 s, circadian phase modulation, per-beat pulse/homeostasis/genome_scan/transformation_tick, every beat SHA3-chained into the UEG — verify valid, 807 events). But every autonomous behaviour the vision names is opt-in and OFF on this instance and persisted OFF: auto_economy (operate VSBs), auto_compliance (re-screen), auto_evolve (only in MAINTENANCE phases every 30 beats, proposals to CCA), auto_align, auto_ship, and evolution_auto_apply (CCA lever). Consequences visible live: genome max_generation 0, last_evolution null, last_vsb_operated null, roadmap_items 0; the Sovereign Evolution roadmap's single directive is the floor fallback 'Routine organism health review — No parseable CEO directives; defaulting to a maintenance sweep' and its BTO roadmap is the native-engine banner text. Genesis /establish registers a VSB into the living roster but does not switch tending on for it.
- **evidence:** agentic_core/organism/heartbeat.py:118-124 (all flags default False) and beat() branches gated on them; live GET /heartbeat/status (auto_* all false, autonomy_persisted true); GET /organism/status genome max_generation 0; GET /sovereign-evolution/roadmap ceo_directives[0].rationale; apps/…/HeartbeatMonitor.tsx:104-107.
- **disclosed to the user:** Yes, mostly: HeartbeatMonitor states 'All five are OFF by default and take effect on the NEXT beat'; the fallback rationale is rendered verbatim on the Sovereign Evolution page. Overclaimed by the plan pillar 'One self-running, self-healing, self-improving organism: strong' (living_plan.py:37) and the page copy 'makes Workstation run itself continuously'.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed. The heartbeat is genuinely alive (788 beats, ACTIVE_REST, per-beat pulse/homeostasis/genome_scan/transformation_tick, UEG verify valid at 1096 events), but auto_evolve/auto_economy/auto_ship/auto_align/auto_compliance all default False (heartbeat.py:131-135) and are all false live; auto_evolve additionally runs only in MAINTENANCE phases (heartbeat.py:317). Consequences live: last_evolution null, last_vsb_operated null, genome max_generation 0; the Sovereign Evolution roadmap's single directive is the floor fallback ('No parseable CEO directives; defaulting to a maintenance sweep') and its BTO roadmap is the native-engine banner. Genesis /establish registers the VSB, runs one birth cycle + screen, and never enables tending; its API response even says 'registered — the organism tends this VSB on the circadian heartbeat (paced virtual economy cycles)', which is untrue while auto_economy is OFF (operate_one is gated on that flag) — GenesisJourney.tsx does not render that string, so the overclaim is API-level. living_plan.py:37 rates the pillar 'strong' and the Living Plan doc row 5 says 'Heartbeat auto-runs the organism'; the HeartbeatMonitor discloses 'All five are OFF by default and take effect on the NEXT beat'. PARTIAL, mostly disclosed, is right.
- **refuter's evidence:** agentic_core/organism/heartbeat.py:131-135,235-238,317; live GET /heartbeat/status (auto_* all false, autonomy_persisted true, last_evolution null, last_vsb_operated null, last_genome.max_generation 0); live GET /sovereign-evolution/roadmap ceo_directives[0].rationale + bto_roadmap banner; agentic_core/api/genesis.py:739-742 (register + 'tends this VSB' copy), 767-768 (first cycle), no heartbeat.configure call; grep 'autonomous_operation|tends this VSB' in src → none; agentic_core/api/living_plan.py:37; docs/WORKSTATION_IDBO_LIVING_PLAN.md:203; HeartbeatMonitor.tsx:70,110-111; live GET /api/v1/gaas/ueg/verify valid true.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Have /establish (or the first registration) enable auto_economy + auto_compliance for the new entity by default with a visible 'tending on' state, and set the living-plan pillar to 'partial' until an instance has evolved at least one generation.

### R6.8 · §11×§12 living-VSB compliance standing on the economy surface (living-vsbs registry rows) — **PARTIAL**

- **severity (assessor):** low
- **claim:** §8/§11: compliance continuously monitored and evaluated live, visible to the entity's owner (W421 intent).
- **observed:** list_living reads compliance keys screened_at / at / verdicts, but screen_living_vsb persists last_at / overall / history. Result: every screened entity is served with screened_at null and verdicts [] (live: vsb-639f5497a4 verdict 'review', screened_at null; store holds last_at 2026-09-05T06:42:04Z). VSBEconomy.tsx therefore tooltips 'no framework detail recorded' and omits the time for entities that were screened.
- **evidence:** agentic_core/economy/living_vsbs.py:list_living (h.get('screened_at') or h.get('at'); h.get('verdicts')); agentic_core/organism/heartbeat.py:screen_living_vsb (writes overall/last_at/regression/history); data/vsb_compliance_history.json keys; apps/…/VSBEconomy.tsx:547-555.
- **disclosed to the user:** No — the user sees a verdict chip with a tooltip claiming no detail exists, while detail (time, history) does.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed — a plain read/write key mismatch. living_vsbs.list_living builds compliance from h.get('screened_at') or h.get('at') and h.get('verdicts'); heartbeat.screen_living_vsb persists {overall, last_at, regression, history:[{overall, at}]}. So every screened entity is served with screened_at null and verdicts []. Live: vsb-639f5497a4 and vsb-14625ba62d both verdict 'review', screened_at null, verdicts [], never_screened false; the live store keys are ['overall','last_at','regression','history']. VSBEconomy.tsx then tooltips '§11 — no framework detail recorded' and omits the 'screened <time>' line. Note per-framework verdicts are never persisted at all (history entries hold only overall+at), so what is lost to the user is the screen time and history, exactly as the assessor states.
- **refuter's evidence:** agentic_core/economy/living_vsbs.py:79-92; agentic_core/organism/heartbeat.py:76-80; C:/tmp/hb18 store (via live rows) + data/vsb_compliance_history.json keys ['overall','last_at','regression','history']; live GET /api/v1/economy/living-vsbs rows vsb-639f5497a4 / vsb-14625ba62d compliance {verdict 'review', screened_at null, verdicts []}; apps/workstation-superapp/src/pages/enterprise/VSBEconomy.tsx:547-556.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Read last_at in list_living and either persist the per-framework verdict list in screen_living_vsb or drop the empty 'verdicts' field and tooltip.

### R6.9 · §12 economic organism — cycle · 6-stage waterfall · transfer · period close · double-entry integrity · virtual-WST honesty · charity intelligence · owner payments; plus the §17.5 invariants that hold (single router mount, append-only DCS/UEG, signal-bus atomicity, twin pre-validation, plan staleness) — **DELIVERED**

- **severity (assessor):** info
- **claim:** §12: each VSB is a hybrid Waqf/Trust/Multinational entity that self-manages finances (owner share, reinvest, capital, users, charity by urgency, 100%-donation-only), virtual until real rails; §17.5 single mount, append-only audit, bus atomicity, twin pre-validation, ≤5-min plan staleness.
- **observed:** Executed on a fresh VSB: POST /economy/cycle {revenue 1000, costs 100} → gaas 'allowed' + checkpoint; reserves 300 (costs + 20%), distributable 700 split owner 140 / self_investment 210 / capital_fund 140 / user_projects 105 / charity 105 (exactly the waqf_ltd_hybrid template); trial balance 1000=1000 and balance sheet balanced; owner accrual 140 with real_money_rails DISABLED; capital contribution 140 landed in the fund; charity grants honour the 2026-06-21 directives (clean_water/conflict/orphan boosted, 100%-only filter, every grant compliance-screened 'pass', provenance 'curated' + 'not_checked' disclosed on API and the CharityDirectives page). POST /transfer 50 → sender reserve 300→250 (Dr transfer_out / Cr reserve_fund), receiver pending 50, consumed as intake on its next cycle (TB 200=200, BS balanced); 9999 → 400 insufficient funds; unregistered receiver → 404. POST /close-period → income/expenses rolled to retained_earnings 300, next period clean, UEG-logged. UEG verify valid (807 events, SHA3-512 chain + anchor). Single mount: all 79 include_router calls in app_mvp.py. Signal bus: nervous.fire under threading.Lock; agent hub store_lock + bounded queues. Twin pre-validation: CCA review auto-runs it for HIGH/CRITICAL and /implement 409s without a recorded pass, floor fallback honestly labelled 'health_gate_default'. /plan/state and the board pack are assembled live on request. VSBEconomy states 'All flows are virtual/simulated WST — no real money moves' and renders held/blocked/rejected governance states distinctly.
- **evidence:** Live calls on r6-audit-vsb-6238 and vsb-981debd258 (this session); agentic_core/economy/ledger.py:post/trial_balance/statements/close_period; economy/governance.py:governed_cycle; economy/transfers.py; economy/charity.py; api/change_control.py:520-575; gaas/v5/ueg.py:104-127; app_mvp.py include_router count.
- **disclosed to the user:** Yes — virtual-WST disclaimers on every economy surface; provenance labels on charity weights. Residual, minor: the ledger memo 'cycle intake (revenue)' does not distinguish user-typed simulation revenue (the UI pre-fills 10,000/1,000) from marketplace-recognised revenue, so the board pack's 'Revenue (cumulative)' mixes both; the per-VSB business-plan content carries no age indicator (workstation plan last updated 2026-09-04T20:38Z) even though the plan view is live-assembled.
- **refutation: SURVIVED** (reproduced by the refuter). Re-executed end-to-end on a fresh VSB (r6-refuter-vsb-15919) and every claim held. Cycle {revenue 1000, costs 100}: governance status 'allowed' + checkpoint; homeostasis_reserves 300, distributable 700, circulation owner 140 / self_investment 210 / capital_fund 140 / user_projects 105 / charity 105 (waqf_ltd_hybrid template); capital_fund_contribution 140 landed; charity grants over curated weights with provenance disclosed ('curated — … no needs, impact or trust data is measured or sourced'); metabolic_energy 1.0; disclaimer 'Virtual/simulated WST — no real funds moved'. Ledger trial balance 1000=1000 balanced; balance sheet balanced. Transfer 50 → sender reserve 300→250, receiver pending 50, consumed as intake (50.0) on the receiver's next cycle; 9999 → HTTP 400; unregistered receiver → HTTP 404. Close-period → P&L income 1000 / expenses 750 / net 250 rolled to retained_earnings, next period 0 postings, ueg_logged true; TB after close 250=250. UEG verify valid (1096 events, SHA3 root). 79 include_router calls all in app_mvp.py; nervous.fire under threading.Lock; /implement raises 409 '§17.5 invariant … no recorded digital-twin pre-validation'; /plan/state reconciled_at equals the request time (assembled live). VSBEconomy renders 'All flows are virtual/simulated WST — no real money moves'; CharityDirectives renders 'weights are curated editorial values, not measurements' and 'verified: not_checked'. The assessor's residuals (simulation revenue vs recognised revenue share one ledger memo; no per-VSB plan age indicator) are accurate and minor. DELIVERED stands.
- **refuter's evidence:** Live calls this session: POST /economy/cycle, GET /economy/ledger/r6-refuter-vsb-15919 balances {revenue 1000, reserves 300, owner 140, self_investment 210, capital_fund 140, user_projects 105, charity 105}, POST /economy/transfer (50 → 200 OK; 9999 → 400; vsb-does-not-exist → 404), POST /economy/cycle vsb-981debd258 intake 50.0, POST /economy/close-period, GET /api/v1/gaas/ueg/verify {valid true, events 1096}, GET /api/v1/plan/state reconciled_at 2026-09-05T16:42:04Z == now; python VirtualLedger.trial_balance/statements against DATA_DIR=C:/tmp/hb18 (balanced before and after close); agentic_core/app_mvp.py (79 include_router); agentic_core/organism/nervous.py:52,68; agentic_core/api/change_control.py:552-580; agentic_core/economy/ledger.py, governance.py:244-252, transfers.py, charity.py; VSBEconomy.tsx:198; CharityDirectives.tsx:185,209.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Tag ledger postings with source (api_simulation | recognised_event | heartbeat) and show the split in the board pack; show updated_at/age on the business-plan view.

---

*Regenerated by W446 from the audit workflow's journal (status lines added from W449 onward). Every entry above is an observation against
the booted HEAD named in the header — routes executed, handlers and components read, stores counted —
not a claim read from another document. No browser was driven: statements about what a user SEES
(a chip's colour, a tab's default, a rendered badge) are reasoned from the component source, and the
assessors and refuters say so where it matters.*
