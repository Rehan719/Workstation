# Vision Fidelity Ledger — v4 (2026-09-19) — MILESTONE M1

**Supersedes v3 (2026-09-05, baseline `06c51109`) as the current assessment.** v3 is kept whole at
`VISION_FIDELITY_LEDGER_v3.md` because prompt v11's `<ledger>` and `<delivery_plan>` cite its entries by
region.index and its status lines record how Phase P1 closed each one. This edition is the phase-boundary
re-run the plan's verification rule V6 requires (MILESTONE M1: after P1.1–P1.16, 'fidelity workflow re-run →
Tier-1 count 0'): a fresh six-region assessment against a backend booted from HEAD `cdd7619f` (port :8083,
single-user mode, `AI_DISABLE_LOCAL=1`). Under that flag the gateway routes every model call to the
deterministic native floor — the configuration CI runs and the one any machine without a local model gets
(it is NOT the shipped default: with the flag unset and Ollama discoverable, the gateway serves from the
local model). What IS assessable on the floor is whether every floor-served surface discloses it.

**Tiers.** Each finding carries the tier the refuter stands behind — 1: a TRUTH DEFECT (a reached surface
tells the user something untrue, fabricates a figure, or certifies what it could not assess); 2: an
INVISIBLE SHORTFALL (partial, and the surface does not say so); 3: a CAPABILITY GAP that is disclosed or
not reached. DELIVERED entries carry no tier.

## How this document was generated — and what that means for reading it

Six assessors ran one vision region each against the booted HEAD, explicitly barred from three
sources: the vision's own §16, the previous edition of this ledger, and `AUTONOMOUS_PROGRESS.md`
(a record of intent, not proof). They executed routes, read handlers and components, and counted
stores. The ASSESSMENT was capped at ten findings per region, most consequential first — and every
region returned exactly ten, so **60 is the size of the cap, not the size of the gap**; a region's
eleventh-worst thing is not in this ledger. **Every finding was then attacked by an independent
refuter instructed to default to refuted** (v2 refuted six per region), who had to reproduce the gap (execute the
route, read the code, count the store) before letting it stand, and who was told to correct the
verdict UP or DOWN when the assessor had it wrong.

Reading rules that follow from the method:

1. **DELIVERED is understated by construction.** Assessors were told their job was the gap that
   remains, but to report DELIVERED where they verified it. Read the DELIVERED entries as the
   floor of what works, not the ceiling.
2. **Every one of the 60 findings was individually refuted.** The refuters reproduced
   53 as stated and overturned 7 — 3 moved to a
   HARSHER verdict (a DELIVERED claim that was not), 1 to a MILDER one (a STUB that was real
   machinery with an undisclosed shortfall). The verdict in each heading is the one the REFUTER
   stands behind; the assessor's original is shown where it differs.
3. **The floor is the environment.** A finding that says 'floor scaffold reached the user' is
   not a complaint that no model ran — it is a finding that the surface did not SAY so, or
   certified what it could not assess. That is the §15 principle-6 line, and it binds.
4. **Refuters reproduced with their OWN inputs.** Several verdicts below carry evidence from a
   second journey, VSB, change-record or transfer the refuter created on the live backend —
   a finding that reproduces under a second, independently chosen input is stronger evidence
   than one observation.

## Summary

| standing verdict (after refutation) | count | as assessed |
|---|---|---|
| STUB | 8 | 8 |
| MISSING | 0 | 0 |
| DOC_OVERCLAIM | 4 | 3 |
| API_ONLY | 0 | 1 |
| PARTIAL | 38 | 37 |
| DELIVERED | 10 | 11 |
| **total** | **60** | **60** |

Standing tiers (non-DELIVERED entries, the tier the refuter stands behind):

| tier | count | meaning |
|---|---|---|
| **1** | **14** | truth defect on a reached surface — the M1 measure (target 0) |
| 2 | 19 | invisible shortfall (Phase P2's tier) |
| 3 | 17 | disclosed or unreached capability gap (Phase P3/P4) |

Per region:

| region | sections | findings | STUB | MISSING | DOC_OVERCLAIM | API_ONLY | PARTIAL | DELIVERED |
|---|---|---|---|---|---|---|---|---|
| R1 | §10 + §11 | 10 | 1 | 0 | 0 | 0 | 6 | 3 |
| R2 | §4 + §13 | 10 | 1 | 0 | 1 | 0 | 8 | 0 |
| R3 | §5 + §17.3 + §17.4 | 10 | 2 | 0 | 0 | 0 | 6 | 2 |
| R4 | §6 + §7 + §17.2 | 10 | 2 | 0 | 1 | 0 | 6 | 1 |
| R5 | §1–§3, §3A, §9, §14, §15, §17.1 | 10 | 1 | 0 | 1 | 0 | 5 | 3 |
| R6 | §8 + §12 + §17.5 | 10 | 1 | 0 | 1 | 0 | 7 | 1 |

The distilled, actionable form of the surviving gaps is **prompt v11 rev 2's `<ledger>` and
`<delivery_plan>`** (`docs/FABLE_DELIVERY_PROMPT.md`). This document is the evidence base behind
them: every `<ledger>` item cites the entries here it rests on by region.index, and every plan
workstream carries the region.index entries it closes (or says it rests on another instrument —
the reach audit for the scatter, the Owner's hand for P4).

---

## R1 — §10 + §11 — the solution-quality bar, continuous compliance, and the faith-content constitution

**Assessor's region summary:** R1 (§10 + §11), assessed against HEAD cdd7619f on the live floor-only backend at :8083, read-only. The faith-content constitution is substantially DELIVERED where the vision says it binds hardest: authentic text only (alquran.cloud, nonexistent ayaat refused 422, surah range 400), written-recall-only assessment with its scope stated on every response and on screen, translation refusing 503 with the availability chip shown before the user types, lesson outlines badged 'outline, not a lesson', SM-2 hifz real and persisted, the never-scored footer on the page. The §10 bar is honestly rendered as '2 measured · 0 attested · 14 not measured' with 'QMS —' on floor output, and the Genesis §4.5 ranking replaces its cards with the identical-candidates note. Three Tier-1 TRUTH DEFECTS remain on reached surfaces: (1) the Sharia/Halal screen is negation-blind — 'avoids riba' returns FAIL 'Prohibited element: riba', which seals the deliverable, routes it to Change Control and stamps 'COMPLIANCE VERDICT: FAIL — NOT cleared for use' on page one of the export; (2) on the floor the Tafsir tab's body repeats a Qur'anic ayah cut mid-word (220-char subject truncation in engine.py) five times under the label 'THE AUTHENTIC ARABIC TEXT' — the full text above it is correct, the fragment in the exported/saved body is not, and the cut is undisclosed; (3) /management › QMS (ISO 9001) generate returns a document for 'Zed Bakery' containing 'Quality measure for beekeepers yorkshire' (native-memory recall mined as salient terms by the floor) with no provenance, no floor note and a green 'Framework Generated' header. Tier 2: the halal-review/hadith/fatwa floor bodies are badged amber but shaped like assessments with mis-archetyped content (gelatin unflagged, an SLA frame under 'Text (Matn)') beneath tab copy that still promises engine-grade analysis. Tier 3: the constitutional row is absent from the woven delivery screen (five of six frameworks), and the honest /qep/compliance/audit is API_ONLY. I could not drive a browser; UI claims are reasoned from the component source (files:lines cited) against the executed responses.

### R1.0 · §11 compliance engines — Sharia/Halal screen (reached via Governance › Compliance, and woven into every /deliverables/produce record + export) — **PARTIAL** · tier 1

- **claim:** Compliance engines (Halal/Sharia · UK-Legal · …) are integrated into every synthesis/generative/operational workflow; solutions are ethically evaluated for well-being, safety, quality and value.
- **observed:** The halal screen is a negation-blind regex: any occurrence of a haram term fails the subject even when the subject says it AVOIDS it. A brief 'A UK savings app compliant with Sharia that avoids riba and offers profit-sharing' is returned as sharia_halal: fail, reason 'Prohibited element: \'riba\' (engine-backed)', overall fail, compliant:false. The deliverable produced from that brief is sealed with compliance: fail, the §10 criteria 'compliant' and 'safe' are recorded met:false, the record is routed to Change Control (cca-7bc9c59a76), the Deliverables list row shows a red 'compliance FAIL' chip, the Download button reads 'Download (carries FAIL verdict)', and the exported Markdown opens with '> **COMPLIANCE VERDICT: FAIL — NOT cleared for use — routed to Change Control … sharia_halal: Prohibited element: 'riba''. The same subject in the Governance › Compliance screen renders a red FAIL card with 'Prohibited element: riba'. The user is told, with red certainty, that a Sharia-compliant concept contains a prohibited element.
- **evidence:** Executed POST /api/v1/compliance/check {subject:'A halal fintech savings app avoiding riba with profit-sharing; …'} → overall 'fail', verdicts[0] {framework:'sharia_halal', status:'fail', reason:"Prohibited element: 'riba' (engine-backed)", coverage:'vocabulary'}. Executed POST /api/v1/deliverables/produce (type report, brief above) → quality_assurance.quality.compliance.overall 'fail', bar_measured.criteria.compliant.met false, safe.met false, compliance_routed_to_cca true; GET /api/v1/deliverables/deliv-e118cdbd/export?format=md → first line after title is the FAIL banner quoting 'riba'. Source: agentic_core/api/compliance.py:30-34 (_HARAM regex) and :137-138 (`if _HARAM.search(text): halal_status = 'fail', reason "Prohibited element: '<term>'"`) — no negation or context handling. Rendered by ComplianceChecker.tsx:71-89 (red XCircle + reason verbatim) and Deliverables.tsx:191, 305-307.
- **disclosed to the user:** Partly. /frameworks and the Compliance page header say it is a 'haram-term screen … not a certification'. But the verdict line itself asserts a fact about the subject ('Prohibited element'), the status is 'fail' not 'review', and the deliverable/export surfaces carry only 'COMPLIANCE VERDICT: FAIL — NOT cleared for use' with no mention that a keyword matched a negated phrase. Nothing on any surface says the screen cannot distinguish 'avoids riba' from 'charges riba'.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced end to end. The halal row is a negation-blind regex; a subject that says it AVOIDS riba is returned 'fail' with the factual assertion "Prohibited element: 'riba'", the deliverable is sealed compliant:false, routed to Change Control, and the export opens with 'NOT cleared for use'. The /frameworks copy ('haram-term screen … not a certification') discloses the method, not the failure mode: nothing on any surface says the screen cannot tell 'avoids riba' from 'charges riba'. One reinforcing detail the assessor missed: compliance.py appends '(engine-backed)' whenever the engine merely executed, even when it found no violation, so the false verdict wears an engine's endorsement.
- **refuter's evidence:** POST /api/v1/compliance/check {subject:'A UK savings app compliant with Sharia that avoids riba and offers profit-sharing'} → overall 'fail', compliant:false, verdicts[0] {sharia_halal, fail, "Prohibited element: 'riba' (engine-backed)", coverage 'vocabulary'}. POST /api/v1/deliverables/produce (type report, same brief) → id deliv-bfffdcdd, quality.compliance.overall 'fail', bar_measured.criteria.compliant.met false, safe.met false, basis "§11 screen verdicts: {'sharia_halal': 'fail', …}". GET /api/v1/deliverables/deliv-bfffdcdd/export?format=md → line 3: '> **COMPLIANCE VERDICT: FAIL — NOT cleared for use — routed to Change Control (cca-e085f39c2e) — sharia_halal: Prohibited element: 'riba' (engine-backed)**'. Source agentic_core/api/compliance.py:30-34 (_HARAM), :137-138 (search → 'fail'), :146-152 (halal_reason += ' (engine-backed)' after any successful engine call, violations or not). UI apps/workstation-superapp/src/pages/governance/ComplianceChecker.tsx:26 (posts), :71-89 (red XCircle, v.reason verbatim); pages/Deliverables.tsx:191 ('compliance FAIL' chip), :305-307 ('Download (carries FAIL verdict)').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In screen_compliance, when a haram term is matched only inside a negation window ('avoid(s|ing)', 'no ', 'free of', 'without', 'never', 'non-', 'prohibits') return status 'review' with reason "haram-vocabulary term 'riba' appears; the screen cannot tell whether it is offered or avoided — review"; otherwise keep 'fail'. Reword every fail reason from 'Prohibited element' to 'haram term present in the text'.

### R1.1 · §11 faith-content constitution rule 2 (sourced-only, exactly) — Religion Hub › Tafsir tab, floor-served — **PARTIAL** · tier 1

- **claim:** Qur'an Arabic text is only fetched from the authoritative source and injected as given material; sourced-only, exactly; a truncated range is disclosed as the covered range.
- **observed:** When the floor serves a tafsir for an ayah longer than ~220 characters, the study-notes body repeats the ayah CUT MID-WORD, five times, each time under the label 'THE AUTHENTIC ARABIC TEXT (sourced from alquran.cloud — quote ONLY this; never produce Quranic Arabic from memory)'. For 2:285 the body shows '(285) ءَامَنَ ٱلرَّسُولُ … كُلٌّ ءَامَنَ بِٱللَّه (domain: the stated domain).' — the ayah stops at 'بِٱللَّه' with the rest of the verse (and all of 286) missing, and no ellipsis or truncation note. The full sourced text IS shown above the body via renderExtra (that part is honest), but the body text — which is what the <pre> renders, what Copy/Download export, and what My Work saves — presents a mutilated fragment of scripture labelled authentic. The prompt instruction text also leaks verbatim into the user-facing body.
- **evidence:** Executed POST /api/v1/religion/quran-tafsir {surah:2, ayah_start:285, ayah_end:290} → arabic_text field complete (285 and 286 whole); tafsir field contains '(285) ءَامَنَ ٱلرَّسُولُ بِمَآ أُنزِلَ إِلَيْهِ مِن رَّبِّهِۦ وَٱلْمُؤْمِنُونَ ۚ كُلٌّ ءَامَنَ بِٱللَّه (domain: the stated domain).' under 'grounded in: THE AUTHENTIC ARABIC TEXT …' in each of 5 sections. Cause: agentic_core/ai/native/engine.py:107-119 `_subject()` returns `longest.strip()[:220]` (a hard 220-char cut with no marker), and :251 `f"Native structured content for '{title}', grounded in: {subject} …"` embeds it. Rendered by DomainTool.tsx:131,271-273 (resultKey 'tafsir' → <pre>), exported at :116-119 and :169-187; ReligionHub.tsx:136-165.
- **disclosed to the user:** Partly. The amber 'structured floor — not model analysis' badge, floor_note and disclaimer are all rendered (ReligionHub.tsx:153, DomainTool.tsx:238-244, 274). The truncation itself is not disclosed anywhere; the fragment is labelled 'THE AUTHENTIC ARABIC TEXT'. The 10-ayah range cap IS honestly disclosed (range_note rendered at ReligionHub.tsx:152; ayah_end clamped to 286 with reference 'Surah 2:285-286').
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced exactly. The study-notes body repeats the ayah cut mid-word at 220 characters, five times, each under the label 'THE AUTHENTIC ARABIC TEXT (sourced from alquran.cloud — quote ONLY this…)', with no ellipsis or truncation marker; the rest of 285 and all of 286 are absent from the body. The arabic_text field and renderExtra are complete and honest, but the body is what the <pre> shows, what Copy/Download export, and what My Work saves (DomainTool.tsx:116-119 persists resultKey text + floor_note + disclaimer — the truncated Arabic travels with it). The floor_note discloses 'structured frame, not scholarship'; it does not disclose that the Arabic inside the frame is mutilated. A truncated Qur'anic verse labelled authentic on a reached, exportable surface is a §11 rule-2 truth defect.
- **refuter's evidence:** POST /api/v1/religion/quran-tafsir {surah:2, ayah_start:285, ayah_end:290} → reference 'Surah 2:285-286', ayah_end clamped to 286, arabic_text 696 chars (both ayaat whole), tafsir 2497 chars containing 5 occurrences of 'THE AUTHENTIC ARABIC TEXT' each followed by '(285) ءَامَنَ ٱلرَّسُولُ … كُلٌّ ءَامَنَ بِٱللَّه (domain: the stated domain).' — stops at 'بِٱللَّه' (missing the final kasra and everything after). Cause agentic_core/ai/native/engine.py:107-119 (_subject → field[:220] / longest.strip()[:220]) and :251 (f"… grounded in: {subject} …"). UI pages/domains/ReligionHub.tsx:136-165 (resultKey 'tafsir'; renderExtra shows full r.arabic_text, r.floor_note, sections_withheld); components/DomainTool.tsx:131 (resultText from resultKey), :271-273 (<pre>{displayText}</pre>), :116-119 (saveOutput persists the body).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In quran_tafsir (religion.py:201-225) never place the Arabic inside the floor-visible prompt subject: pass the sourced text only as `arabic_text` (already returned) and give the floor a subject like 'Surah 2:285-286 (Arabic shown above)'. Alternatively make engine.py `_subject` never cut inside Arabic — if the 220-char window would split a sequence in the Arabic block, drop the Arabic entirely and append '[Arabic omitted — see the sourced text above]'.

### R1.2 · §10 verified·tested·validated / QMS — Management Systems hub › QMS (ISO 9001) generate — **STUB** · tier 1

- **claim:** Every solution is verified · tested · validated; floor-served output is never presented as more than it is; AI content is labelled with provenance.
- **observed:** Two defects on one reached surface. (a) Cross-request contamination: the QMS framework generated for 'Zed Bakery / food / sourdough bread' contains 'Quality measure for beekeepers yorkshire' and 'Quality measure for small beekeepers' — content from some earlier, unrelated interaction recalled from native memory and mined by the floor as the document's 'salient terms'. The same appeared for 'Test Co'. A user downloads a 'management_framework.txt' for their bakery that names another organisation's business. (b) No provenance on the surface: the response carries no ai_provenance, no floor_note, no disclaimer, and no QMS record; the page renders a green CheckCircle 'Framework Generated' over the raw text and offers a download. The only hint is the floor marker line '_[Workstation native structured engine — owned, no external dependency]_' inside the text, which does not say 'not model analysis'.
- **evidence:** Executed POST /api/v1/mgmt/qms/generate {organisation_name:'Zed Bakery', domain:'food', products_services:'sourdough bread'} → keys exactly {doc_id, organisation_name, standard, framework, generated_at}; 'beekeep' in framework == True ('- Quality measure for beekeepers yorkshire.' / '- Quality measure for small beekeepers.'). Same with {organisation_name:'Test Co', domain:'food'}. Handler agentic_core/api/management_systems.py:53-89 calls `gateway.query(prompt, agent='mgmt_qms')` (not the provenance-carrying ai_text) and returns no provenance; gateway.py:93-108 prepends '[native memory recall — prior Workstation interactions matched by token overlap…]' lines to the prompt; engine.py:139-145 `_content()` falls back to the whole prompt for keyword extraction when no content label matches, so recall lines become the 'salient terms'. UI: ManagementSystemsHub.tsx:112-128 posts and shows res.data.framework; ResultPanel :59-98 renders green 'Framework Generated' + <pre>{result}</pre> + download, no badge. Route registered App.tsx:192 (/management).
- **disclosed to the user:** No. The green 'Framework Generated' header, the absence of any provenance badge, and the foreign organisation's terms presented as this organisation's quality measures are all undisclosed.
- **refutation: SURVIVED** (reproduced by the refuter). Verdict and tier stand; the evidence for (a) needs a correction. (b) is fully reproduced: the response carries no ai_provenance/floor_note/disclaimer, the 'framework' never even names the organisation it is for, and the page renders a green CheckCircle 'Framework Generated' over the raw scaffold with a download — a reached surface presenting floor scaffold as a generated ISO 9001 QMS. (a) I could NOT re-trigger the beekeeper text: the live store (C:/tmp/w474b/memory.json, 419 entries, 52 mentioning beekeepers) now holds 10 QMS prompts (Zed Bakery, Acme Widgets, Test Co, Northstar… all owner 'platform') which outrank the beekeeper entries on token overlap. But I reproduced the CLASS by execution: memory.query_memory for a Northstar Dental Clinic QMS prompt returns Zed Bakery's and Acme Widgets' prompts, and engine._content() falls back to the whole augmented prompt because none of the QMS prompt's labels (Organisation/Size/Products/Services) are in _CONTENT_LABELS. Cross-organisation recall into a shared, unscoped platform namespace is live; which foreign terms surface depends on the store's state at the time.
- **refuter's evidence:** POST /api/v1/mgmt/qms/generate {organisation_name:'Zed Bakery', domain:'food', products_services:'sourdough bread'} and {…'Northstar Dental Clinic', domain:'healthcare', size:'medium'} → keys exactly {doc_id, organisation_name, standard, framework, generated_at}; 'bakery'/'northstar' absent from framework; body opens 'Structured service frame for: You are a lead ISO 9001 quality consultant. Design a complete QMS framework..' with '- Quality measure for quality consultant.' Handler agentic_core/api/management_systems.py:53-89 (gateway.query, no provenance returned). gateway.py:93-108 (_augment prepends recall lines; owner_id None → platform namespace). memory.py:105-125 (query_memory: allowed={'platform'}). Offline read of the live store with WORKSTATION_DATA_DIR=C:/tmp/w474b: query_memory(Northstar QMS prompt) → 3 recalls = the Northstar, Zed Bakery and Acme Widgets QMS prompts. engine.py:122-138 (_CONTENT_LABELS lacks Organisation/Size; _content returns whole prompt). UI pages/enterprise/ManagementSystemsHub.tsx:112-128 (posts, onResult(res.data.framework)), :59-98 ResultPanel (green CheckCircle2 'Framework Generated', <pre>{result}</pre>, download 'management_framework.txt', no badge). Route App.tsx:192.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** management_systems.py: switch all six generators to `ai_text(...)` (returns provenance) with `augment=False`/no memory recall for document generation (the gateway already exempts 'ship/persist' callers at gateway.py:144-149), return ai_provenance + a floor_note, and run assure_delivery over the text; ManagementSystemsHub.tsx: render provenanceBadge/qmsChip through the shared helpers and drop the unconditional green 'Framework Generated'.

### R1.3 · §11 faith-content constitution rules 1, 3, 4 — QEP Studio (Religion Hub › QEP Flagship): authentic text, written recall, translation refusal, lesson provenance — **DELIVERED**

- **claim:** Qur'an Arabic never AI-generated; nonexistent ayaat refused; recitation never scored — written recall only with scope stated; translation refuses (503) on the floor; every AI-served surface renders provenance.
- **observed:** All verified by execution and matched to what QEPStudio renders. Authentic text is fetched from alquran.cloud with source labelled; nonexistent ayah 2:290 → 422 'surah 2 has 286 ayaat'; surah 115 → 400. Written recall: exact typed text → text_similarity 1.0, a partial → 0.3421 with madd marker note, non-Arabic → comparable:false 'nothing is scored that cannot be compared'; every response carries kind 'written_recall_check' and the scope 'says NOTHING about recitation'. Translation: /translation/status → translation_available:false with basis; /translate → 503 'the deterministic native floor cannot translate and its output will not be presented as a translation'. Tajweed lesson → floor_served:true, floor_note 'a structured OUTLINE … not scholarly content', rendered as an amber 'outline, not a lesson' chip. Hifz SM-2 schedule/review/progress are real and persisted, and 'memorised' carries its basis ('a review count, not a hifz certification'). /qep/status's constraints line is rendered on the page.
- **evidence:** GET /api/v1/qep/ayah/1/1 → source 'alquran.cloud'; GET /api/v1/qep/ayah/2/290 → 422; GET /api/v1/qep/surah/115 → 400; POST /api/v1/qep/tajweed/analyse (UTF-8 body) ×3 → comparison.text_similarity 1.0 / 0.3421 / comparable:false; GET /api/v1/qep/translation/status → translation_available:false; POST /api/v1/qep/translation/translate → 503; POST /api/v1/qep/tajweed/lesson {rule_name:'idgham'} → floor_served:true; POST /api/v1/qep/hifz/schedule {uid:'audit-r1', surah_number:1, ayaat_range:[1,3]} → 3 scheduled; POST /api/v1/qep/hifz/review {ayah_ref:'1:1', quality:4} → new_interval_days 1, next_review_date 2026-09-20; GET /api/v1/qep/hifz/progress/audit-r1 → due_today 2, total_ayaat_memorised 1 with memorised_basis. UI: QEPStudio.tsx:156-171 (status + constraints), 261-274 (recall, scope line), 295-300 (lesson chips + floor_note), 311-323 (availability chip + refusal), 334-338 (never-scored footer). Backend: religious_domain/api.py:550-569.
- **disclosed to the user:** Yes — every limit is on screen.
- **refutation: SURVIVED** (reproduced by the refuter). Every claim reproduced by execution with the actual request schemas (the assessor's evidence did not state the tajweed payload; the route requires ayah_text + recited_text, and /translate requires text — both 422 otherwise, which is honest input validation, not a defect). Authentic text sourced and labelled; nonexistent ayah and surah refused; written recall scored as text-only with the scope line; translation refused 503 on the floor; lesson floor_served with floor_note; hifz SM-2 persisted with memorised_basis. QEPStudio renders the constraints, the scope, the floor chip and the refusal.
- **refuter's evidence:** GET /api/v1/qep/ayah/1/1 → source 'alquran.cloud'; GET /api/v1/qep/ayah/2/290 → 422 'surah 2 has 286 ayaat — ayah 290 does not exist'; GET /api/v1/qep/surah/115 → 400. POST /api/v1/qep/tajweed/analyse {surah:1, ayah:1, ayah_text:<1:1>, recited_text:…} ×3 → comparison.text_similarity 1.0 / 0.3684 (missing_rule_markers: madd) / comparable:false 'nothing is scored that cannot be compared'; every response kind 'written_recall_check', scope 'TEXT comparison only. This says NOTHING about recitation…'. GET /api/v1/qep/translation/status → translation_available:false; POST /api/v1/qep/translation/translate {text, target_language} → 503 'the deterministic native floor cannot translate and its output will not be presented as a translation'. POST /api/v1/qep/tajweed/lesson {rule_name:'idgham'} → floor_served:true, floor_note 'a structured OUTLINE … not scholarly content'. POST /api/v1/qep/hifz/schedule {uid:'refute-r1', surah_number:1, ayaat_range:[1,3]} → 3 scheduled; POST /hifz/review {ayah_ref:'1:1', quality:4} → new_interval_days 1, next_review_date 2026-09-20, memorised_basis 'a review count, not a hifz certification'; GET /hifz/progress/refute-r1 → due_today 2, total_ayaat_memorised 1. UI components/QEPStudio.tsx:156-171 (components + constraints), recall block ('written similarity … (normalised Levenshtein)'), lesson.floor_note chip, translation chip + 503 note, footer 'Recitation itself is never scored here'; mounted by ReligionHub.tsx:8,95.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None needed for these rules.

### R1.4 · §10 solution-quality bar — Living Deliverables (produce, gate, bar, export) — **PARTIAL** · tier 3

- **claim:** Every solution is specifically designed · modelled · simulated · optimised · categorised · ranked; best-in-class …; verified · tested · validated.
- **observed:** On the floor the system measures 2 of 16 criteria (compliant, safe — both from the §11 keyword screen), records 'specifically designed' and 'verified' as not assessable, and the other 12 as 'not measured by this gate'; qms_gate_passed is null with the basis stated. The Deliverables page renders 'QMS —' (grey) and '§10 bar: 2 measured · 0 attested · 14 not measured' with the per-criterion breakdown in the tooltip, so the shortfall is visible. The content itself is floor scaffold: the section 'grounded in' text embeds the entire requested heading list as the subject, so each of six sections repeats '## Executive Summary ## Background ## Analysis …' as if it were the brief — a deliverable no one could use, but honestly badged amber 'structured floor — not model analysis'.
- **evidence:** POST /api/v1/deliverables/produce → quality_assurance.quality: not_assessable true, qms_gate_passed null, bar_measured {measured:2, attested:0, not_measured:14, summary '2 measured · 0 attested · 14 not measured'}, criteria['modelled'|'simulated'|'optimised'|'ranked'|'best-in-class'|…].basis 'not measured by this gate'. Content field: every section contains 'Subject: Brief: … Domain: enterprise\n\n## Executive Summary\n## Background\n## Analysis\n## Findings\n## Recommendations\n## Next Steps (domain: enterprise).' Source: agentic_core/vbs/quality.py:40-43, 126-176, 243-248; engine.py:107-119 (_subject picks the longest 'sentence', which is the heading block). UI: Deliverables.tsx:208 (provenanceBadge), 213-230 (qmsChip + bar summary), api.ts:69-70, 114-125.
- **disclosed to the user:** Yes — the badge, 'QMS —' and '2 measured · 0 attested · 14 not measured' are on the page; the gate's basis is in the tooltip.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. On the floor the gate measures only 'compliant' and 'safe' (both from the §11 screen), records 'specifically designed' and 'verified' as not assessable with the basis, the other 12 as 'not measured by this gate', qms_gate_passed null; and the content embeds the whole heading list as the 'subject' of every section. The Deliverables page renders the amber floor badge, the grey 'QMS —' chip with the basis in its title, and the '§10 bar: N measured · N attested · N not measured' chip with the per-criterion lists in the tooltip. The shortfall is real and on screen.
- **refuter's evidence:** POST /api/v1/deliverables/produce → quality_assurance.quality.not_assessable true, qms_gate_passed null, bar_measured.criteria['specifically designed'|'verified'].basis 'not assessable — floor-served…', ['modelled'|'simulated'|'optimised'|'categorised'|'ranked'…].basis 'not measured by this gate', compliant/safe measured:true source 'gate'. Export line 12-19: '## Executive Summary\nSubject: Brief: … Domain: enterprise\n\n## Executive Summary\n## Background\n## Analysis\n## Findings\n## Recommendations\n## Next Steps (domain: enterprise).' Source engine.py:107-119 (_subject picks the longest 'sentence' = the heading block). UI pages/Deliverables.tsx:208 (provenanceBadge → 'structured floor — not model analysis', lib/api.ts:69-70), :213-230 (qmsChip 'QMS —' via api.ts:114-125 null branch; '§10 bar: {summary}' with measured/attested lists in title).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** engine.py `_subject`: exclude lines beginning with '## ' from the longest-sentence fallback so the floor grounds in the brief, not in the heading list.

### R1.5 · §10 modelled · simulated · optimised · ranked — Genesis journey stage 5 (GenesisJourney.tsx) — **PARTIAL** · tier 3

- **claim:** Candidate solutions are modelled, simulated, optimised and ranked, and the best is selected on evidence.
- **observed:** On the floor the three candidates are identical text; the payload says so and the page renders the amber comparison note IN PLACE of the ranked cards ('All 3 candidates returned IDENTICAL text, so no comparison was possible and the ranking below carries no information'); the tie is declared 'resolved by list order — NOT evidence'; three of five §4.5 criteria are named as not measured; stages_verified is '0/0' with 'stages_note: 5 of 5 stages were served by the deterministic floor and are not assessable'; the 'What served this journey' card shows 'native ×11'. One §10 criterion ('categorised') is caller-attested from the realm×domain pair, and the page labels attestation separately from measurement.
- **evidence:** POST /api/v1/genesis/journey {problem:'A halal community meal service for elderly Londoners that avoids waste', establish:false, ship_output:false} → stage_5_model_simulate_rank {candidates_are_alternatives:false, candidates_distinct:1, tie.resolved_by:'list order — NOT evidence', criteria_not_measured {effectiveness, efficiency, 'commercial viability'}}, candidates all score 0.855 / simulation_score 0.742; stages_verified '0/0'; ai_provenance.served_by {native:11}; bar_measured '2 measured · 1 attested · 13 not measured', attested_criteria ['categorised']. UI: GenesisJourney.tsx:616-640 (provenance card + amber floor sentence), 641-662 (three-state verification chips), 697-756 (comparison_note replaces cards; honesty + criteria_not_measured rendered), 784-804 (bar summary).
- **disclosed to the user:** Yes — every shortfall in this run reaches the screen.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. Three identical candidates, tie 'resolved by list order — NOT evidence', three §4.5 criteria named as not measured, stages_verified '0/0' with the stages_note, all 11 calls native, bar '2 measured · 1 attested · 13 not measured' with 'categorised' the only attested criterion. GenesisJourney.tsx renders the provenance card, the floor sentence, stages_note, the three-state verification chips, comparison_note in place of ranked cards, honesty + criteria_not_measured, and the bar chip with measured/attested split. Every shortfall in this run reaches the screen.
- **refuter's evidence:** POST /api/v1/genesis/journey {problem:'A halal community meal service for elderly Londoners that avoids waste', establish:false, ship_output:false} → stage_5_model_simulate_rank {candidates_are_alternatives:false, candidates_distinct:1, tie{detected:true, resolved_by:'list order — NOT evidence'}, criteria_not_measured{effectiveness, efficiency, 'commercial viability'}, comparison_note 'All 3 candidates returned IDENTICAL text…'}, candidates all (score 0.86, simulation_score 0.776); stages_verified '0/0', stages_note '5 of 5 stages were served by the deterministic floor and are not assessable…'; ai_provenance.served_by {native:11}; quality_assurance.quality.bar_measured.summary '2 measured · 1 attested · 13 not measured', attested_criteria ['categorised']. UI pages/synthesis/GenesisJourney.tsx:616-640 (served_by card, floor sentence, stages_note), :641-662 (verified chips), :697-756 (comparison_note replaces cards; honesty; criteria_not_measured), :784-804 (bar chip, measured vs attested in title). Route App.tsx:190.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** None for honesty. The capability gap (no real modelling/simulation on the floor) is environmental here.

### R1.6 · §11 halal-review / hadith-study / fatwa-research tools — Religion Hub › Halal Review, Hadith, Dialogue tabs — **PARTIAL** · tier 2

- **claim:** Floor-served output is never presented as scholarship; AI content labelled with provenance; solutions are effective and specifically designed.
- **observed:** All three return floor scaffold with the amber badge, 'QMS —' chip and a disclaimer, so provenance is honest. But the bodies are misassembled by heading-keyword archetype and can read as an assessment that was never made: the Halal Review for gummy bears with 'gelatin' never mentions gelatin (the one real halal concern) while emitting '## Cross-Contamination Risks … Exposure on gummy bears … Exposure on bears sugar'; the Hadith study's '## Text (Matn)' section is filled with a *service-SLA* frame ('Offering: what is delivered … SLAs & assurance'); prompt scaffolding ('Conduct a pre-assessment and provide: ## Halal Status Assessment (COMPLIANT / REQUIRES R') leaks verbatim into the visible text. The tab descriptions promise 'ingredient flags, process concerns, certification guidance' and 'researches its narration, isnad, grading and sharh' — neither is delivered on the floor, and the description does not change when the floor serves.
- **evidence:** POST /api/v1/religion/halal-review {product_name:'Gummy bears', ingredients:['sugar','gelatin','glucose syrup']} → assessment contains no 'gelatin' flag in Critical Issues/Flagged Ingredients bodies; contains '- Exposure on bears sugar.'; ai_provenance.served_by 'native'. POST /api/v1/religion/hadith-study {hadith:'Actions are judged by intentions'} → '## Text (Matn) — …\nStructured service frame for: … - Offering: what is delivered and the scope boundary. - Delivery model: …'. Cause: engine.py:204-209 (`has('quality', 'sla', …)` archetype matches the heading 'Text (Matn) — … translation'? no — matches the 'authentication … Quality' vocabulary) and :172-176 (risk archetype). UI: ReligionHub.tsx:169, 184 (descriptions); DomainTool.tsx:238-254 (badge + QMS chip), 274 (disclaimer).
- **disclosed to the user:** Partly — the amber badge and disclaimer are shown, but the tab copy still promises engine-grade analysis and the floor text is shaped like an assessment with wrong content.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced with the surface's own schema (product_description is required — the assessor's quoted payload 422s, but the Halal tab form sends it, so the surface path holds). The halal body echoes 'gelatin' only inside the 'grounded in:' input echo and never flags it; the Critical Issues / Flagged Ingredients bullets are n-grams of the product name ('gummy sweets', 'bears sugar'-style); the prompt scaffold 'Conduct a pre-assessment and provide: ## Halal Status Assessment (COMPLIANT /' leaks verbatim. The hadith '## Text (Matn)' section is a service-SLA frame; I found the exact cause the assessor guessed at: engine.py:204 matches has('sla') and the heading 'Text (Matn) — the Arabic if known, plus an English translation' contains 'sla' inside 'tran-sla-tion'. Provenance (amber badge, 'QMS —', disclaimer) is honest, but the tab descriptions promise ingredient flags / isnad research unconditionally and the body is shaped like an assessment with the one real concern absent. Tier 2 stands.
- **refuter's evidence:** POST /api/v1/religion/halal-review {product_name:'Gummy bears', product_description:'Chewy fruit gummy sweets', ingredients:['sugar','gelatin','glucose syrup']} → assessment: 'gelatin' appears 6× but only within the echoed 'Ingredients:' list under 'grounded in:'; Critical Issues bullets '- gummy sweets - sweets gummy - chewy fruit - fruit gummy - gummy bears'; Cross-Contamination '- Exposure on gummy sweets. … - Exposure on fruit gummy.'; text contains 'Conduct a pre-assessment and provide:\n## Halal Status Assessment (COMPLIANT / (domain: the stated domain).'; ai_provenance.served_by 'native', qms_gate_passed null. POST /api/v1/religion/hadith-study {hadith:'Actions are judged by intentions'} → '## Text (Matn) — the Arabic if known, plus an English translation\nStructured service frame for: … - Offering: what is delivered and the scope boundary. - Delivery model: … - Quality measure for hadith number. … - SLAs & assurance…'. Cause engine.py:204-209 (has('offering','delivery model','sla',…,'quality') — 'sla' ⊂ 'translation'), :172-176 (risk archetype). UI ReligionHub.tsx:169 ('ingredient flags, process concerns, certification guidance'), :184 ('researches its narration, isnad, grading and sharh'); DomainTool.tsx:238-254 (badge + QMS chip), :274 (disclaimer).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When served_by is 'native', have DomainTool prepend a one-line notice above the body ('The floor produced a study checklist from your input; no ingredient, isnad or grading analysis was performed') and have halal-review return an explicit `flagged_ingredients: []` with basis 'not assessed on the floor' so the page can say so.

### R1.7 · §11 constitutional row (gaas.v5) inside the delivery gate — every deliverable/tafsir/fatwa record — **PARTIAL** · tier 3

- **claim:** Constitutional/gaas compliance is integrated into every synthesis/generative/operational workflow.
- **observed:** The compliance screen woven into assure_delivery runs five frameworks; the constitutional row is applied only on the standalone /compliance/check route (and there it reports 'not_checked' for content, honestly). So every produced deliverable's sealed compliance record lists five verdicts, not six, and the frameworks page names six. The vision's 'integrated into every workflow' is true for five of six.
- **evidence:** POST /api/v1/deliverables/produce → quality.compliance.verdicts has 5 entries (sharia_halal, uk_legal, regulatory, ehs, ethical); POST /api/v1/compliance/check → 6 entries incl. constitutional 'not_checked'. Source: compliance.py:118-230 (screen_compliance — five rows; docstring 'The gaas.v5 Constitutional gate is applied separately by the org cascade / Change Control') vs :250-287 (check adds the sixth).
- **disclosed to the user:** Not explicitly on the deliverable surface (the chip tooltip lists the five rows that ran; nothing says the sixth did not). Disclosed in the /check note.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced: the woven screen runs five rows; the constitutional row exists only on /compliance/check, where it is honestly 'not_checked' for content. Since that sixth row can never read content anyway, its absence from deliverable records changes no verdict a user sees — a capability gap, not a truth defect. One small overclaim worth noting on the Compliance page header: it lists all six rows and says the screen is 'Used by the economy, synthesis, Genesis and the Forge', while those workflows get five; immaterial for content subjects.
- **refuter's evidence:** POST /api/v1/deliverables/produce → quality.compliance.verdicts = 5 entries (sharia_halal, uk_legal, regulatory, ehs, ethical). POST /api/v1/compliance/check → 6 entries incl. {constitutional, not_checked, 'not applicable to content — gaas.v5 gates agent actions…', coverage 'none'}. Source compliance.py:118-124 (screen_compliance docstring: 'The gaas.v5 Constitutional gate is applied separately…') vs :251-287 (check appends the sixth row). UI Deliverables.tsx:253-256 (tooltip lists only the verdicts that ran); ComplianceChecker.tsx:38-41 (header names six rows and the consumers).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Append a constitutional row with status 'not_checked' and coverage 'none' inside screen_compliance so the sealed record and the Deliverables tooltip show the same six-row shape as the Compliance page.

### R1.8 · §10 QMS — VBS Living-QMS routes (/api/v1/vbs/qms/*) — **DELIVERED**

- **claim:** A living QMS that gates, records defects and controls documents.
- **observed:** Gate, defects and document-control routes are real and stateful: an empty gate fails (coverage 0 < 0.95) and moves the non-conformance rate to 1.0; a 0.97/no-stub gate passes and the rate drops to 0.5; a 0.5/stub gate fails and the rate rises to 0.6667; defect summary counts follow; document-control reports persistent registered_artifacts/audit_integrity and says which counter is per-process. Floor-served deliverables correctly do NOT run this gate (gates_run stayed 0 after produce), so the rate is never moved by a gate that cannot fail.
- **evidence:** POST /api/v1/vbs/qms/gate {} → passed:false, non_conformance_rate:1.0; {coverage:0.97, stubs_found:false} → passed:true, rate 0.5; {coverage:0.5, stubs_found:true} → passed:false, rate 0.6667; GET /api/v1/vbs/qms/defects → summary.gates_run 0 before those gates; GET /api/v1/vbs/qms/document-control → qms_owns_dcms true, note names the per-process counter. UI: VBSSystemsPanel.tsx:39-77 reaches all of them.
- **disclosed to the user:** Yes.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced against the live, persisted state (my absolute numbers differ from the assessor's because the store already held 15 gates and 2 defects; the deltas match exactly): an empty gate fails and raises the rate, a 0.97/no-stub gate passes and lowers it, a 0.5/stub gate fails and raises it; defects_total and gate_failures increment with each failure; document-control reports persistent registered_artifacts/audit_integrity and names the per-process counter. Reached from the VSB Cockpit.
- **refuter's evidence:** GET /api/v1/vbs/qms/defects → summary {gates_run 15, defects_total 2, gate_failures 2, non_conformance_rate 0.1333}; POST /api/v1/vbs/qms/gate {} → passed:false, rate 0.1875 (3/16); {coverage:0.97, stubs_found:false} → passed:true, 0.1765 (3/17); {coverage:0.5, stubs_found:true} → passed:false, 0.2222 (4/18); GET /defects after → {gates_run 18, defects_total 4, gate_failures 4, open 4}; GET /api/v1/vbs/qms/document-control → qms_owns_dcms true, registered_artifacts 52, audit_integrity 1.0, controlled_documents 140, note 'registered_artifacts and audit_integrity are PERSISTENT (dcms store); controlled_documents is a per-process counter since start'. UI components/VBSSystemsPanel.tsx:39-40 (defects, document-control), :52 (gate POST), :67,77 (correct/reverify); mounted pages/enterprise/VSBCockpit.tsx:430 and :692.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None.

### R1.9 · §11 QEP compliance audit — /api/v1/qep/compliance/audit — **DELIVERED** *(assessed API_ONLY)*

- **claim:** Continuously monitored and evaluated live compliance.
- **observed:** The route is honest — compliant:null, three of four controls 'not_checked' with the reason 'No … check exists to run; a pass was previously asserted' — but no component in apps/workstation-superapp/src fetches it, so its honesty reaches no user.
- **evidence:** GET /api/v1/qep/compliance/audit → compliant:null, checks[0..2].status 'not_checked', note 'compliant is null when a control could not be checked'. Grep of apps/workstation-superapp/src for 'qep/compliance' → no matches (QEPStudio.tsx fetches status, translation/status, hifz, gamification, suwar only).
- **disclosed to the user:** N/A — not reached.
- **refutation: REFUTED — corrected to DELIVERED.** The assessor's grep claim is false: a component DOES fetch /api/v1/qep/compliance/audit and renders it honestly — 'NOT ESTABLISHED — controls could not run' (amber) when compliant is null, each check with its status and reason, and the route's note. It is mounted on the QEP Religion Hub's 'Intelligence' tab, which is a registered route linked from the command palette and the search mesh. The route's honesty reaches a user, so API_ONLY is wrong; the surface is DELIVERED (the audit is honest about three of four controls being unrunnable, which is a disclosed capability gap, tier 3). One stale comment (LearnTeachModule.tsx:64 says /qep-religion is not a route) is wrong — App.tsx:183 registers it.
- **refuter's evidence:** GET /api/v1/qep/compliance/audit → compliant:null, checks[0..2].status 'not_checked' ('No explainability check exists to run; a pass was previously asserted', …), checks[3] 'Recommendation weights normalised' pass, note 'compliant is null when a control could not be checked…'. Grep 'qep/compliance' in apps/workstation-superapp/src → components/qep/QEPIntelligence.tsx:44 jget('/api/v1/qep/compliance/audit').then(setAudit); rendered :215-239 (chip: compliant===true 'compliant' / false 'NOT compliant' / else 'NOT ESTABLISHED — controls could not run'; :229 maps audit.checks; :239 audit.note). Mounted pages/domains/QEPReligionHub.tsx:23 (tab {id:'intel', label:'Intelligence'}), :40 ({activeTab === 'intel' && <QEPIntelligence />}). Route App.tsx:183 (<Route path="/qep-religion" element={<QEPReligionHub />} />); linked components/layout/CommandPalette.tsx:57 and components/SearchMeshModal.tsx:32.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Either render the four controls under the QEP 'honest state' card (QEPStudio.tsx:156-171) or retire the route (vision §15.10 makes retirement a first-class resolution).

---

## R2 — §4 + §13 — the end-to-end lifecycle (Describe → … → Run forever) and what the output IS

**Assessor's region summary:** R2 (§4 + §13) assessed on a live HEAD backend at :8083 by executing the full Genesis journey three ways (founder-named, unnamed/slug, standalone with founder text), the SSE establish, and every VSB mover (ship, name, concept, board-pack, review-gates + decision, evolve), then reading the entity, plan, roster, and the repo files on disk. The truth-first machinery of P1 largely holds: floor-served bodies ship as the founder's words plus an explicit pending state on every surface checked; slugs never ship; the journey says 0/0 assessable; identical candidates are disclosed; gates and evolution are real and governed. Two Tier-1 truth defects remain. (1) Every birth tells the founder 'the organism tends this VSB on the circadian heartbeat' while the heartbeat's auto_economy lever is off by default and the live status shows last_vsb_operated null — a present-tense claim of an action that is not occurring, undisclosed on the Genesis page. (2) On the non-floor path the repo/webapp/mobile QMS gate is vacuous (required sections are the generator's own headings; the stub threshold is met by boilerplate), so an enterprise with an empty Concept/Design/Commercialisation ships a sealed QUALITY.md certifying 'QMS gate: PASS · specifically designed · verified', and the same PASS chip is rendered for any founder-written entity in the Cockpit and Genesis pages. Tier-2 shortfalls: an invented Concept and constant Vision seeded into the Chief's Opening without a pending marker (and a second, different Vision on the repo/board pack); the engine's persona prompt leaking as the cognitive cascade's 'Subject' and into the concept card; EVIDENCE.md shipping ranking/simulation figures under an 'evidence-ranked' heading for a comparison that did not happen; contradictory scaffold labels inside the shipped body; and a lifecycle that is not one — two birth paths with different stage sets and review gates named after stages they cannot hold. Stage content count on the floor: 5 of 7 stage texts restate the user's problem verbatim as 'Subject:' plus its bigrams, 0 contain reasoning, and the cognitive/MJM cards are mostly the engine's own prompt. Tier-3: multimodal intake is text/voice/text-PDF only; export formats are honest about what is live versus catalogue.

### R2.0 · §4.8 / §4.10 — Establish → Run forever (the 'living' registration) — **PARTIAL** · tier 1

- **severity (assessor):** high
- **claim:** §4.10: the established VSB 'self-operates and forever evolves'; §4.8 instantiates a 'living' enterprise.
- **observed:** Every establishment tells the founder, in the present tense, that the organism is tending the new enterprise — but the heartbeat lever that does the tending is OFF by default, and nothing at the Genesis surface says so. The only cycle that ever runs is the one fired at birth.
- **evidence:** Executed POST /api/v1/genesis/journey (establish=true, name given) → established_vsb.living = {"autonomous_operation": "registered — the organism tends this VSB on the circadian heartbeat (paced virtual economy cycles)"}; POST /api/v1/genesis/establish/stream emits stage 'living' → "The organism tends this VSB on the circadian heartbeat (governed economy cycles)." (genesis.py:1075-1077; rendered verbatim in the birth log, GenesisJourney.tsx:851-863 and 912-925 'Birth record — every step really happened'). GET /api/v1/heartbeat/status on the live backend → running:true, auto_economy:false, auto_evolve:false, auto_ship:false, auto_compliance:false, last_vsb_operated:null. heartbeat.py:139 `self.auto_economy = False  # opt-in` and :242 `if self.auto_economy:` guards operate_one(). GET /api/v1/economy/living-vsbs shows operating_cycles:1 for each entity — the birth cycle (genesis.py:931-935), never a heartbeat one.
- **disclosed to the user:** No. The Genesis page and the entity record state the tending as a fact; the opt-in lever is visible only on the separate Heartbeat Monitor page (HeartbeatMonitor.tsx:26 'Self-run').
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The tending claim is stated unconditionally at birth while the lever that does the tending is off by default and nothing on the Genesis, Cockpit or Economy surfaces says so. An additional reached surface (VSBEconomy living panel) makes the same untrue present-tense claim, so the tier-1 finding is if anything under-scoped.
- **refuter's evidence:** Executed POST /api/v1/genesis/establish/stream {problem, domain, name:'Sheffield Spoke Co-op'} → vsb-651cbe9b66; SSE stage 'living' content 'The organism tends this VSB on the circadian heartbeat (governed economy cycles).' (genesis.py:1075-1077, rendered GenesisJourney.tsx:851-863/912-925). Entity file vsb_entities/vsb-651cbe9b66.json living.autonomous_operation = 'registered — the organism tends this VSB on the circadian heartbeat (paced virtual economy cycles)' (genesis.py:903, vsb.py:1552). GET /api/v1/heartbeat/status → beats:131, auto_economy:false, last_vsb_operated:null, autonomy_persisted:true; heartbeat.py:139 auto_economy=False, :242 `if self.auto_economy:` guards operate_one(). GET /api/v1/economy/living-vsbs → 11 living rows, every one operating_cycles ≤ 1 (the birth cycle; my SSE 'First Economy Cycle' cycle:1). grep auto_economy/heartbeat across GenesisJourney.tsx and VSBCockpit.tsx → no hits; VSBEconomy.tsx:651 copy 'Established VSB IDBO enterprises the organism operates continually on the circadian heartbeat — each runs paced virtual economy cycles, forever' with no auto_economy check. Only HeartbeatMonitor.tsx:26 ('Self-run') exposes the opt-in.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** At establishment read heartbeat.auto_economy and write/emit the truth: 'registered on the living roster; autonomous economy cycles are OFF — enable Self-run on the Heartbeat page' (and the same on GET /economy/living-vsbs).

### R2.1 · §13 × §10 — the shipped repository's quality record on the non-floor path — **STUB** · tier 1

- **severity (assessor):** high
- **claim:** §13: the repo is 'quality-gated (§10), compliance-screened (§11), document-controlled and provenance-sealed'.
- **observed:** For any entity whose provenance is not the floor, the vsb_repo QMS gate cannot fail: its required sections are the generator's own headings and the stub threshold is met by README boilerplate. An enterprise established with NO concept, design or commercialisation ships a BUSINESS_PLAN.md with three empty sections, a public Solution page saying 'Its solution is developed and delivered in-house, end to end.', and a sealed compliance/QUALITY.md that certifies 'QMS gate: PASS', 'specifically designed: met=True' and 'verified: True'.
- **evidence:** Executed POST /api/v1/genesis/establish/stream {problem, domain, name} (no body fields) → vsb-a7d0213698; SSE 'Shipped: repo … qms_gate_passed: true', 'webapp … true', 'mobile … true'. GET /api/v1/vsb/vsb-a7d0213698/repo → quality.qms_gate_passed:true, served_by:'unspecified', delivery_coverage:1.0, bar_measured '4 measured'. On disk C:\tmp\w474b\vsb_repos\vsb-a7d0213698\BUSINESS_PLAN.md: '## Concept\n\n## Design & Development\n\n## Commercialisation\n' (empty); compliance/QUALITY.md: 'QMS gate: PASS'. Cause: vsb.py:355-357 assure_delivery(combined, ["Business Plan","Organisation","Identity","Executive Summary"]) — every one is a heading _build_repo_files always emits (vsb.py:75-83,138-141); quality.py:134-138 marks 'specifically designed'/'verified' met on coverage≥0.95 and not stub; quality.py:57-58 treats served_by None as assessable. Same PASS stamped on the founder-written entity vsb-11492e76aa (GET …/repo → qms_gate_passed:true), which the Cockpit 'Ship the repo' (VSBCockpit.tsx:789-791) and the Genesis 'Generate VSB Repository' chip (GenesisJourney.tsx:938-940 → qmsChip 'QMS pass · cov 100% · doc-controlled', emerald) render as a pass.
- **disclosed to the user:** No — the record and the chips say PASS; the W449 'not assessable' honesty applies only when served_by is the floor.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced end-to-end on a fresh entity: an establishment with no concept/design/commercialisation ships an empty BUSINESS_PLAN.md and a sealed QUALITY.md certifying QMS PASS, 'specifically designed: met=True' and 'verified: met=True', because the required sections are the generator's own headings and served_by=None is treated as assessable. The PASS reaches the founder through the repo file, the SSE birth log and the Cockpit/Genesis QMS chips.
- **refuter's evidence:** Executed POST /api/v1/genesis/establish/stream {problem:'Refuter R2 probe: a bicycle repair co-op…', domain:'community', name:'Sheffield Spoke Co-op'} → SSE 'Shipped: repo' data.qms_gate_passed:true (file_count 13), 'webapp' true, 'mobile' true. GET /api/v1/vsb/vsb-651cbe9b66/repo → quality.qms_gate_passed:true, served_by:'unspecified', delivery_coverage:1.0, stub_found:false, not_assessable:false, bar_measured.criteria['specifically designed'].met:true (source 'gate'), ['verified'].met:true. On disk C:/tmp/w474b/vsb_repos/vsb-651cbe9b66/BUSINESS_PLAN.md: '## Concept\n\n## Vision\n<problem>\n\n## Design & Development\n\n## Commercialisation\n' (three empty sections); compliance/QUALITY.md: '- QMS gate: PASS\n- Basis: QMS gate on real coverage/stub metrics (served_by=unspecified)'; web/solution.html: 'Its solution is developed and delivered in-house, end to end.' followed by '<p></p>'. Same on the assessor's vsb-a7d0213698 and vsb-11492e76aa (GET …/repo → true). Cause confirmed: vsb.py:355-357 assure_delivery(combined, ['Business Plan','Organisation','Identity','Executive Summary']) — all four are headings _build_repo_files always emits (vsb.py:75-83,138-141); quality.py:57-58 floor_served(None)→False; quality.py:31 _STUB_RE only matches TODO/TBD/placeholder/etc; quality.py:134-138 marks both criteria met on coverage≥0.95 and not stub. Rendered as a green pass by lib/api.ts:114-122 qmsChip via GenesisJourney.tsx:938-940 and the Cockpit ship panel (VSBCockpit.tsx:789-791).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Measure the repo/webapp/mobile gates against the entity's OWN body (concept, design, commercialisation present and not 'content pending'), or pass served_by='template' for generator-composed documents so the gate records 'not assessable' as it already does for the floor.

### R2.2 · §4.8 / §15.9 — the Chief's Opening (Executive Summary · Concept · Vision) and the board pack's constitutional layer — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §15.9: the Chief-owned plan opens with the founder's idea framed by the digital-twin Chief; §13: the body is bespoke to the solution.
- **observed:** On the standalone establish path the plan's Concept is an invented sentence and its Vision a constant slogan, both shown in the Cockpit as the enterprise's own with no pending marker; the repo and board pack meanwhile print the raw problem statement as 'Vision', so one enterprise carries two different Visions. The board pack ascribes a fixed values string to every enterprise.
- **evidence:** genesis.py:756 plan['concept'] = req.concept or f"Optimal solution concept for: {problem}"; :757 plan['vision'] = 'A self-running {entity_type} VSB IDBO that commercialises this solution beneficently.' Executed GET /api/v1/business-plan?scope=vsb-a7d0213698 → concept:'Optimal solution concept for: Community tool library…', vision:'A self-running waqf_ltd_hybrid VSB IDBO…', provenance.body_pending:[]. VSBCockpit.tsx:544-550 renders these under 'Concept' / 'Vision'. vsb.py:1147 BUSINESS_PLAN.md '## Vision\n{challenge}'; board pack constitutional.vision = challenge and values = 'Integrity · Compassion · Excellence · Halal/Sharia · Beneficence · Stewardship' (vsb.py:1135-1137), verified in GET /api/v1/vsb/vsb-782577eb83/board-pack.
- **disclosed to the user:** No for the invented concept/vision (body_pending is empty on this path); the board-pack values are presented as the enterprise's constitution.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. On the standalone establish path the plan's Concept is a generated placeholder sentence and its Vision a constant slogan, shown in the Cockpit under 'Concept'/'Vision' with body_pending empty so no pending marker renders; the repo and board pack print the problem statement as Vision, and the board pack ascribes a constant values string. Not tier 1: the placeholder text is not a factual claim about the enterprise and the board pack itself labels its grounding honestly.
- **refuter's evidence:** Executed GET /api/v1/business-plan?scope=vsb-651cbe9b66 → concept:'Optimal solution concept for: Refuter R2 probe: a bicycle repair co-op for students in Sheffield', vision:'A self-running waqf_ltd_hybrid VSB IDBO that commercialises this solution beneficently.', provenance:{served_by:null, body_pending:[], name_source:'founder'} (genesis.py:756-757; same on vsb-a7d0213698). VSBCockpit.tsx:544-550 renders Concept/Vision from plan and shows the pending marker only when body_pending is non-empty (:541-543). GET /api/v1/vsb/vsb-651cbe9b66/board-pack → layers.constitutional.vision = the raw problem statement, values = 'Integrity · Compassion · Excellence · Halal/Sharia · Beneficence · Stewardship' (vsb.py:1135-1137); on disk BUSINESS_PLAN.md '## Vision\n<problem>' (vsb.py:1147). Mitigation noted: the pack's concept_label reads 'Founder's problem statement (no concept recorded yet)' when no blueprint concept exists (vsb.py:1049-1060, 1133).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Seed Concept/Vision only from founder- or model-written text; otherwise write the pending state and list them in provenance.body_pending; derive one Vision and use it on plan, repo and pack; label the values line as the platform's default until the founder sets them.

### R2.3 · §4.1-§4.7 — what a floor-served journey shows as the stages' content — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §4.2-§4.7: each stage understands, researches, designs, models and operationalises the person's problem.
- **observed:** Counted on the live run: 5 of 7 stage texts restate the founder's problem verbatim ('Subject: …') plus its own bigrams; none contains reasoning; the Cognitive Cascade and MJM cards are dominated by the engine's own persona prompt — the 'Subject' shown is 'You are the Cognitive Architecture — 6 specialised intelligence systems…' and the 'Optimal Solution Concept' lists 'architecture specialised · cognitive architecture · intelligence systems'. The provenance card describes this as 'honest structured composition from your inputs', which overstates it.
- **evidence:** journey1.json (problem: Yorkshire beekeepers/varroa): cognitive_cascade contains 2/10 problem terms and opens 'Subject: You are the Cognitive Architecture — … Analyse the following thro (domain: agriculture)'; concept/research/design/operations/commercialisation each 10/10 terms but only as 'Subject:' lines and bigram bullets under headings 'INKASHAF/SAMAJH/SOCH'. Rendered by GenesisJourney.tsx:428-430 (Section cards) under the banner at :629-634. Payload honesty is present: stages_verified '0/0', stages_floor_served 5, stages_note; stage_5.candidates_distinct 1 with comparison_note rendered in place of ranked cards (:709-715).
- **disclosed to the user:** Partly — the floor banner and 'not assessable' chips are shown; the engine-prompt leak into the concept card and the 'from your inputs' wording are not.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on a fresh floor journey: the Cognitive Cascade card is composed from the engine's own persona prompt (surfaced as a '[recalled prompt]' subject), the MJM card from the MJM prompt, and every stage text is the problem restated as 'Subject:' lines plus its own bigrams under stage headings; the provenance banner's 'honest structured composition from your inputs' overstates what the cascade card contains. The floor banner and 0/0 chips are shown, so the undisclosed part is the prompt leak and the banner wording — tier 2.
- **refuter's evidence:** Executed POST /api/v1/genesis/journey {problem:'Refuter R2 probe: winter road salt damage to allotment soil in Bradford', domain:'agriculture', establish:false} (saved C:/tmp/w474_R2/journey_r2.json). phase_1_conceptualisation.cognitive_cascade opens '## INKASHAF\n Analyse the following thro (domain: - [recalled prompt] You are the Cognitive Architecture — 6 specialised intelligence systems operating in biological cascade sequence. Analys).\n- architecture specialised\n- specialised intelligence\n- cognitive architecture…' (source prompt intelligence.py:308; floor 'Subject: … (domain: …)' shape engine.py:248); mjm_assessment bullets 'meta-judgement system · beekeepers yorkshire · cognitive engines · mjm orchestrator' — note 'beekeepers yorkshire' is recalled from a PREVIOUS journey's problem, not mine. concept/research: '## Optimal Solution Concept\nSubject: Refuter R2 probe: … (domain: agriculture).\n- damage allotment\n- allotment soil…'. Payload honesty present: stages_verified '0/0', stages_floor_served 5, stages_note, stage_5 candidates_distinct 1 with comparison_note (rendered in place of cards, GenesisJourney.tsx:709-715). Banner wording at GenesisJourney.tsx:629-634.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Scrub the persona line from _ai_cognitive_prime/_ai_mjm_lifecycle output the way the stage prompts are labelled (genesis.py:245-247), and reword the banner to 'headings and your own words restated — no reasoning was performed'.

### R2.4 · §4.5 × §13 — EVIDENCE.md shipped in the repository — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §4.5: candidates are modelled, simulated, ranked so the best is selected on evidence.
- **observed:** The shipped evidence file heads its first section '§4.5 evidence-ranked' and prints rank 1, score 0.833 and 'simulated evidence: 0.751 (modelled 0.732; declared weights 60/40)' for a run in which the same file then says the simulation is pending and the three candidates were identical text. Numbers for a comparison and a simulation that did not occur are shipped as evidence.
- **evidence:** C:\tmp\w474b\vsb_repos\vsb-782577eb83\EVIDENCE.md lines 3-9 (generated by vsb.py:96-116); the disclosure lines 'simulation pending the owned model…' and '- comparison: TIE — list order — NOT evidence; candidates_distinct=1 …' follow in the same file. The web UI does not show this file; it is in the repo the founder receives.
- **disclosed to the user:** Adjacent disclosure in the same file; the heading and figures still assert an evidence ranking.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The shipped EVIDENCE.md heads a section '§4.5 evidence-ranked' and prints rank/score and a 'simulated evidence' figure for a run whose own following lines say the simulation is pending and the candidates were identical. The figures are computed (text-shape proxies over floor output, genesis.py:355-370), not random, and the disclosure sits in the same section, so tier 2 is defensible; it is the closest of the tier-2 findings to tier 1 because the heading itself certifies an evidence ranking that did not happen.
- **refuter's evidence:** C:/tmp/w474b/vsb_repos/vsb-782577eb83/EVIDENCE.md: '## Selected Candidate (§4.5 evidence-ranked)\n- id: pragmatic · rank: 1 · score: 0.833\n- coverage 1.0 · specificity 0.465 · structure 1.0\n- simulated evidence: 0.751 (modelled 0.732; declared weights 60/40)' then '### Simulation excerpt\nsimulation pending the owned model — the deterministic floor served the digital-twin stage…\n- comparison: TIE — list order — NOT evidence; candidates_distinct=1'. Generator vsb.py:96-116 prints rank/score/simulated evidence whenever selected_candidate exists. My journey_r2.json confirms the mechanism: three candidates each score 0.833 / simulation_score 0.728 / modelled 0.744 with candidates_distinct:1 and served_by_agent genesis_twin_* all 'native'. The web UI hides the ranked cards in this case (GenesisJourney.tsx:709-715); the repo does not.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When candidates_distinct ≤ 1 or the twin was floor-served, omit the scores and title the section 'Selected candidate — no comparison possible'.

### R2.5 · §13 — coherence of the shipped body's own descriptions — **PARTIAL** · tier 2

- **severity (assessor):** low
- **claim:** §13: one coherent, version-controlled whole integrating Website, Web app and Phone app.
- **observed:** The shipped body contradicts itself about what it contains: README.md always labels web/, webapp/, mobile/ as '(scaffold)'; webapp/README.md and mobile/README.md say 'scaffold; built in a later increment (not a compiled app)' and stay on disk beside the generated app.js/data.json/sw.js; QUALITY.md heads '§11 Compliance (pass)' above rows that say 'review — no engine covers this area'.
- **evidence:** vsb.py:87 README line 'web/, webapp/, mobile/ — … (scaffold)' unconditional; vsb.py:151-155 scaffold READMEs; the repo generator drops them only when app.json exists at ship time (vsb.py:341-348) but the birth ship runs repo first, so they persist: ls C:\tmp\w474b\vsb_repos\vsb-11492e76aa\webapp → README.md, app.js, app.json, data.json, index.html, styles.css. QUALITY.md for vsb-782577eb83: '## §11 Compliance (pass)' with sharia_halal/uk_legal 'review … no engine covers this area'.
- **disclosed to the user:** The GET /repo manifest labels surfaces correctly after generation (W450); the files on disk do not.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on a fresh birth: the shipped body labels its three surfaces as scaffolds while real generated app files sit beside them, because the birth ship writes the repo scaffold before the surface generators and the scaffold-dropping logic only fires on a later repo regeneration.
- **refuter's evidence:** C:/tmp/w474b/vsb_repos/vsb-651cbe9b66/README.md line 16: '`web/`, `webapp/`, `mobile/` — integrated Website / Web app / Phone app (scaffold)' (vsb.py:87 unconditional); webapp/ contains README.md, app.js, app.json, data.json, index.html, styles.css and webapp/README.md reads '# Sheffield Spoke Co-op — Web app (scaffold)… built in a later increment (not a compiled app)' (vsb.py:151-155); same for vsb-782577eb83 and vsb-11492e76aa (mobile/README.md 'Spec → scaffold'). The drop logic vsb.py:341-348 keys on app.json existing at repo-generation time; SSE order in my establish run was board_pack → mobile → repo → webapp → website, so webapp/website did not exist when the repo was written. compliance/QUALITY.md '## §11 Compliance (pass)' above 'sharia_halal: review — no engine covers this area' and 'uk_legal: review'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Write the README surface line and drop the scaffold READMEs from what is on disk (not just from the manifest) after the surface generators run; head the §11 section with 'screened: 3 of 5 frameworks could read the subject'.

### R2.6 · §4 — ONE gated lifecycle? — **PARTIAL** · tier 3 *(assessed tier 2)*

- **severity (assessor):** medium
- **claim:** §4: one continuous, autonomous workflow; §17.4 Mode 3 human review gates at any Concept→Commercialisation stage.
- **observed:** There are two birth paths with different stage sets (genesis/journey+establish: concept→research→3 candidates→twin→design→operations→commercial; vsb/spawn: cascade→MJM→GaaS→CEO→genome→swarm), and the review-gate lifecycle names a third set of 8 stages (intake…launch) that never execute as stages. Because the journey runs every stage inside one POST before an entity exists, a gate on 'Solution Design' cannot hold design; it holds the ship/evolve/cascade afterwards. The gate mechanism itself works.
- **evidence:** Executed POST /api/v1/vsb/vsb-782577eb83/review-gates {stages:["design"]} → pending; POST …/repo/ship → 409 {gate:'design', status:'pending'}; POST …/review-gates/design/decision {decision:'approve'} → approved; POST …/repo/ship → 200. Stage list: vsb.py:62-71 _CONCEPT_TO_COMMERCIALISE_STAGES; spawn path vsb.py:1595-1800; journey stages genesis.py:281-469. GenesisJourney.tsx:1083 discloses that gates block movers; the chips (:1094-1097) still present 'Solution Design · pending' with 'Design CoE architects the optimal solution'.
- **disclosed to the user:** Partly — the mover semantics are stated on the Genesis page; the stage names imply a stage-level hold that does not exist, and the two-path birth is not stated anywhere.
- **refutation: REFUTED — corrected to PARTIAL.** The gate mechanism works and what a gate does is stated on the surface where gates are set; the remaining shortfalls (an 8-name lifecycle that never executes as stages, a gate that holds movers rather than a journey stage, two birth paths with different stage sets) are capability gaps against §4/§17.4, and the VSB Spawn Studio is a separately-labelled page, not a hidden second path. Tier 2 requires an undisclosed shortfall a user would be misled by; the Genesis copy already tells the user a pending gate blocks ship/evolve/cascades, so this is tier 3.
- **refuter's evidence:** Executed on vsb-651cbe9b66: POST /api/v1/vsb/{id}/review-gates {stages:['validate']} → stages ['validate']; POST …/repo/ship → HTTP 409 {error:'review gate blocks progress — a human review is pending (Mode 3); ship refused', gate:'validate', clear_by:'POST …/review-gates/validate/decision'}; POST …/review-gates/validate/decision {decision:'approve'} → approved, blocks_progress:false, dcs_hash present; ship then 200. GET …/review-gates lifecycle = the 8 names from vsb.py:62-71; entity.stage is 'commercialise' (never intake/validate/launch). GenesisJourney.tsx:1083: 'A gated stage that is pending or rejected blocks this enterprise's lifecycle movers — ship, evolve, cascades, plan orchestration and the organism's autonomous re-ship/evolve — with a 409 that names the gate'. Spawn path reached only via VSBSpawnStudio.tsx:178 ('/api/v1/vsb/spawn'), its own page.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Name the gates by what they hold ('before ship', 'before evolve', 'before cascade') or run the journey stage-by-stage against the entity's gates; retire or fold /vsb/spawn into Genesis.

### R2.7 · §4.1 — Describe (multimodal intake) — **PARTIAL** · tier 3

- **severity (assessor):** low
- **claim:** §4.1: describe the challenge multimodally — text, voice, image, uploaded data (reports, reviews, data, media, sites, apps, models).
- **observed:** Genesis accepts typed text, browser dictation, and .txt/.md/.json/.pdf files with a text layer. Images, media, sites, apps and models are not accepted at the Describe step; the attach hint says 'research report, brief, dataset' and does not say what is excluded.
- **evidence:** GenesisJourney.tsx:536-542 AttachDocument + DictateButton; AttachDocument.tsx:118 accept = TEXT_DOC_EXT + '.pdf' + text/* + application/json; :76-79 refuses image-only PDFs. Image input exists only in the Cockpit chat (VSBCockpit.tsx:940), after establishment.
- **disclosed to the user:** Not as a gap; the hint reads as the full set.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced from source: the Describe step accepts typed text, dictation and text/PDF documents only; images, media, sites, apps and models named in §4.1 are not accepted. Tier 3 stands. One correction to the disclosure note: the exclusion IS stated at the moment of use (an unsupported file yields an explicit refusal), so the gap is disclosed reactively though not in the hint.
- **refuter's evidence:** GenesisJourney.tsx:536-542 AttachDocument hint 'bring your own data — research report, brief, dataset' + DictateButton. AttachDocument.tsx:118 accept = TEXT_DOC_EXT + PDF + text/* + application/json + application/pdf; :100 `if (!looksText) setErr('Unsupported file type. Attach a text document (…) or a PDF.')`; :76-79 image-only PDFs refused with 'No text layer…'. No image input reachable before establishment (image input only in VSBCockpit.tsx:940 chat).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** State on the attach control what is read (text-layer documents) and that images/media/sites/apps/models are not yet accepted here.

### R2.8 · §4.9 / §13 — Output in any selectable format — **DOC_OVERCLAIM** *(assessed PARTIAL)* · tier 3

- **severity (assessor):** low
- **claim:** §4.9: outputs as Reports, Presentations, Videos, Websites, Platforms, Apps, Products, Services — each 'a living … autonomous enterprise'.
- **observed:** Export works and is honest about provenance: a floor journey saved as a report carries source_served_by and is recorded not assessable; live renders are md/html/slides/txt/json/video-html/svg/png, while pptx/pdf/docx/xlsx/mp4/mp3 are listed as catalogue-only. The vision's 'each deliverable is a living enterprise' is not what a deliverable is (a versioned render); DOC_OVERCLAIM in §4.9/§13 wording rather than a surface defect.
- **evidence:** Executed POST /api/v1/deliverables/produce {content, vsb_id, source_served_by:{native:11}} → ai_provenance.served_by 'verbatim-ingest', quality.not_assessable:true, qms_gate_passed null; GET /api/v1/deliverables/deliv-59c7d67e/export?format=slides → 200 text/html. GET /api/v1/deliverables/output-formats → live 8 formats, catalogue_not_yet_produced [pptx,pdf,docx,xlsx,mp4,mp3] with note 'documented targets, not faked'. GenesisJourney.tsx:232-266.
- **disclosed to the user:** Yes at the surface (format list and provenance); the vision text overclaims.
- **refutation: REFUTED — corrected to DOC_OVERCLAIM.** The surface is verified honest (formats listed with a not-yet-produced catalogue and a note; verbatim ingest carries the source provenance), so the correct verdict is the one the assessor's own text names — DOC_OVERCLAIM of §4.9/§13's 'each deliverable is a living enterprise' — not PARTIAL.
- **refuter's evidence:** Executed GET /api/v1/deliverables/output-formats → live 8 (md, html, slides, txt, json, video-html, svg, png), catalogue_not_yet_produced [pptx, pdf, docx, xlsx, mp4, mp3], note 'live = real in-house renders…'. POST /api/v1/deliverables/produce {brief, content, vsb_id:'vsb-651cbe9b66', source_served_by:{native:11}} → deliv-5e792631 with ai_provenance {served_by:'verbatim-ingest', source_served_by:{native:11}, note:'content supplied verbatim by the caller — not regenerated'} (a request without `brief` is a 422 — brief is required, deliverables.py:161-171). Vision §4.9/§13 text read at docs/WORKSTATION_IDBO_WHOLE_VISION.md:167-170, 430-441; the honest-scope paragraph at :443-452 already narrows §13 to a versioned repo of static site/web app/PWA.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Amend §4.9/§13 to say deliverables are versioned, regenerable renders filed under a living VSB, not enterprises themselves.

### R2.9 · §4.8 / §13 — what an established VSB actually contains, and the honesty machinery — **PARTIAL** *(assessed DELIVERED)* · tier 3

- **severity (assessor):** info
- **claim:** §13: canonical output is a version-controlled VSB repository integrating Website, Web app and Phone app; §15.6 corollary: a floor-served body ships the founder's words plus an honest pending state.
- **observed:** Verified by execution: an established VSB = entity JSON (board of 7 constant directors, economy template, delivery swarm of 4 constant stages, review gates), a plan (3 constant objectives + ops objective), a virtual ledger, a living-roster row, a board pack, and a git-committed repo of 13 files + 3-page website + 4-file web app + 7-file PWA — about 9 stores per entity. Every floor-served field is replaced with 'content pending the owned model …' on the entity, genome, IDENTITY.md, BUSINESS_PLAN.md, OPERATIONS.md, ORGANISATION.md, web pages, webapp/data.json, board pack and plan (all checked); a slug name defers the ship until the founder names it; the journey reports '0/0 assessable · 5 floor-served'; identical candidates replace the ranked cards; gates block with 409 and release on approval; evolve files a CCA proposal (never auto-applied) and re-ships. What is bespoke in the body is the name and the founder's problem statement echoed on every surface; everything else is constant scaffold or pending.
- **evidence:** Executed: journey → vsb-782577eb83 (body_pending all true; initial_ship 5 surfaces, commit ce7faa21); journey without name → vsb-4b269815af name_pending:true, initial_ship deferred 'name pending'; POST …/name → ship 5 surfaces; standalone establish with founder text → vsb-11492e76aa ships that text verbatim on solution.html; POST …/evolve → 1 evidence-based proposal, evolution_pending_cca, repo re_shipped; POST …/board-pack → narrative pending, version tracks content hash. Stores: C:\tmp\w474b\{vsb_entities, business_plans, economy, vsb_board_packs, vsb_repos, genomes, swarm_cascades.json, dcms_registry.json, living roster}.
- **disclosed to the user:** Yes — the Genesis card names the pending fields and the working-name state; the Cockpit badges provenance and pending fields on the Chief's Opening.
- **refutation: REFUTED — corrected to PARTIAL.** The honesty machinery on the FLOOR path is verified (pending markers, deferred ship on a slug name, 0/0 chips, 409 gates, evolve → re-ship), but DELIVERED is too kind for 'what an established VSB actually contains': against §13 the body is a constant scaffold plus the founder's echoed problem, the founder-text birth path escapes the honesty machinery entirely (finding 1: PASS over empty sections), and the same body carries scaffold labels beside real files (finding 5). A region cannot be DELIVERED while two tier-1/2 findings are drawn from the same shipped body; the shortfall is disclosed on the Genesis card and Cockpit, so PARTIAL tier 3.
- **refuter's evidence:** Verified: vsb_entities/{vsb-651cbe9b66, a7d0213698, 782577eb83}.json each board.directors=7, native_swarm.stages=4, review_gates present; stores per entity present under C:/tmp/w474b: vsb_entities, business_plans, economy/<id>_ledger.json, vsb_board_packs/<id>/, vsb_repos/<id>/ (+ manifest/ship/website/webapp/mobile json), living_vsbs.json (11 rows), swarm_cascades.json, dcms_registry.json. Pending replacement counted on vsb-782577eb83: BUSINESS_PLAN.md Concept/Design/Commercialisation 'content pending the owned model…', web/solution.html 1, webapp/data.json 4, mobile/data.json 4, IDENTITY.md 3, OPERATIONS.md 1, ORGANISATION.md 1 occurrences. Gates: 409 then 200 after approval (finding 6 evidence). POST /api/v1/vsb/vsb-651cbe9b66/evolve → generation 2, proposals [], repo_refresh re_shipped commit fec991009203 (an 'evolution' with zero proposals still bumps the generation and re-commits). Journey payload stages_verified '0/0', stages_floor_served 5. Shortfalls in the same body: vsb-651cbe9b66 QUALITY.md 'QMS gate: PASS' over empty sections; README.md:16 '(scaffold)' and webapp/README.md 'not a compiled app' beside app.js. Count nit: on disk web/ has 5 files, webapp/ 6, mobile/ 9 — '3-page/4-file/7-file' are generator counts (SSE file_count), not the shipped tree.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** None for the disclosure machinery; the capability gap (no owned model composing the body in this environment) is the environment.

---

## R3 — §5 + §17.3 + §17.4 — the living organisation, the living business system layers, the three integration modes

**Assessor's region summary:** R3 (§5 living organisation, §17.3 living business system, §17.4 integration modes), HEAD cdd7619f, backend :8083 on the deterministic floor (AI_DISABLE_LOCAL=1). Method: executed every /api/v1/board/*, /api/v1/cca/* submit→review→ratification→implement, /api/v1/business-plan/* (get/generate/roadmap), /api/v1/swarm/{delegate,cascade,agents,runs}, /api/v138/ceo/{chat SSE, vitals, meeting/log, meeting/minutes}, and — after spawning a probe VSB (vsb-2c15bfcde6) — review-gates config/decision and board-pack assembly ×2; read board.py, change_control.py, business_plan.py, vsb.py (pack, gates, spawn seeding), genesis.py, swarm.py, v138/ceo.py, memory_v01.py, native/engine.py, gateway.py, compliance.py, vbs/quality.py, and the six named components plus ChangeControlAgency, GenesisJourney, VSBCockpit, VSBSpawnStudio, lib/api.ts. I could not drive a browser; every UI statement is reasoned from source. THE PICTURE: the chain Chief → Board → AI CEO → C-Suite → CoE → BTO → Build-to-Order executes, persists, and is badged floor by one shared helper on every reached surface; arms-length Change Control with Board ratification is DELIVERED and honest (rule-not-model approval stated, 409/403 gates proven); Mode 3 gates block the pack with a named 409 and the pack is versioned by content with a visible concept note; the CEO chat's terminal SSE frame does name served_by ('native') and the page renders it amber. TWO TIER-1 TRUTH DEFECTS REMAIN: (1) a CEO-chat message mentioning 'meeting'/'debate' makes the floor produce six unanimous 'APPROVE' officer stances (the stance detector substring-matches the prompt's own 'APPROVE, OBJECT or ABSTAIN' instruction) that are persisted, minuted and fed into later CEO grounding — the W404 fabricated-approval defect re-emerging through the floor despite the docstring's claim it was fixed; (2) the floor's '_Acting as: <role>' lead names the PREVIOUS call's persona because gateway recall lines carry prior 'You are the X' prompts and engine._role takes the first match — rendered verbatim in the Swarm, Board and CCA pages (CFO card says 'Acting as: Chief Strategy Officer', the Governance review says 'Acting as: Build-to-Order'). Tier 2: the spawn-path VSB plan shows f-string templates as the 'Chief's Opening' with no provenance (the Genesis path discloses); the CEO chat 'grounded in N directives' chip overstates on a floor answer that ignores both question and grounding; the Board page claims an unqualified digital twin in presence and absence while Mode 2 is admitted NOT BUILT only in the vision (0 twin models, per-VSB Chief titled 'Digital Twin of default'); cascade chips 'compliance: pass' and 'BMS EFFICIENT' carry their screen/simulation basis only in tooltips. Tier 3 (disclosed or unreached): §17.3 Strategic/Action-plan cadence generators are MISSING and the pack still labels ceo_specification/board roster/constant values as those layers (vision §17.3 says so; contents are API-only); avatar-mediated Chief instruction is not wired. No fabricated figures were found on the org surfaces; the economy board-pack, vitals and QMS all state their basis.

### R3.0 · §5 AI CEO → C-Suite (CEO chat 'meeting' path) — /api/v138/ceo/chat + /api/v138/ceo/meeting/* — **STUB** · tier 1

- **severity (assessor):** high
- **claim:** §5: the AI CEO delegates to a specialist C-Suite whose positions are real deliberation; §15.6 honesty over polish — never fabricate. ceo.py's own W404 docstring claims 'each officer now states a REAL position … its own stance is taken from what it said'.
- **observed:** Any CEO-chat message containing 'meeting' or 'debate' runs call_meeting(): six officers (CEvO, CGO, CPEO, CBO, CoS, CEnvO) are prompted on the floor, the floor echoes the instruction sentence ('…end with exactly one word on its own line: APPROVE, OBJECT or ABSTAIN'), and the stance detector `for token in ('APPROVE','OBJECT','ABSTAIN'): if token in text.upper()` picks APPROVE first for every officer. Six unanimous APPROVE stances are persisted in MeetingLog, served as minutes ('**CEvO** - **APPROVE**'), and fed back into every later CEO answer as '## Recent C-Suite debate (real meeting log)'. This is the W404 defect (unanimous invented approval) re-emerging through the floor.
- **evidence:** EXECUTED: POST /api/v138/ceo/chat {"message":"Call a C-Suite meeting on whether to launch the halal marketplace pilot this quarter"} → terminal frame {served_by:'native', grounding.debate_entries:6}. GET /api/v138/ceo/meeting/log → 6 entries, every `stance`='APPROVE', every `argument` begins '_[Workstation native structured engine…]_ … The request concerns: State your position on this agenda in at most two sentences, then end with exactly one word on its own line: APPROVE, OBJECT or ABSTAIN.' GET /api/v138/ceo/meeting/minutes → '# C-Suite meeting minutes … **CEvO** - **APPROVE** …' ×6. READ: agentic_core/api/v138/ceo.py:216-262 (call_meeting; stance loop ~250-254 substring match, no served_by check), agentic_core/ai/ceo/memory_v01.py:83-100 (post_argument/get_recent_debate), ceo.py:~380 (debate injected into grounding). No .tsx reads /meeting/log or /minutes (grep), so the record is API-served, but the CEO chat page (Living Organisation → AI CEO tab, CEOChat.tsx:51) is the reached trigger and its subsequent answers are grounded in the fabricated record.
- **disclosed to the user:** No. The argument text carries the floor marker, but the `stance` field is a bare 'APPROVE' with no served_by; the minutes present it as a decision. The CEO chat grounding chip does not show debate_entries.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. Every officer's floor reply echoes the instruction sentence, so 'APPROVE', 'OBJECT' and 'ABSTAIN' all appear (at consecutive offsets) and the first-token-wins loop in call_meeting stamps APPROVE on all six; the record is then served as governance minutes and fed into every later CEO prompt as a 'real meeting log'. The ceo.py W404 docstring ('its own stance is taken from what it said') is a DOC_OVERCLAIM. Two caveats the assessor understated: (a) no .tsx renders /meeting/log or /minutes, and the floor-served chat answer itself relays none of the stances (my answer contained no APPROVE/MEETING_COMPLETE text), so the untrue record is reached by the API routes and by the CEO grounding, not by a rendered chip; (b) MeetingLog is process memory only (memory_v01.py `self.log: list = []`, no file I/O), so the fabricated record dies on restart. Tier 1 stands on the route-served certification of six positions no officer took.
- **refuter's evidence:** EXECUTED: POST /api/v138/ceo/chat {message:'Call a C-Suite meeting on whether to pause all hiring for the next debate'} → terminal frame {served_by:'native', grounding.debate_entries:18}; the streamed answer contains 0 occurrences of 'APPROVE', 'MEETING_COMPLETE' or 'CEvO'. GET /api/v138/ceo/meeting/log → 18 entries; the newest six: stance 'APPROVE' for CEvO/CGO/CPEO/CBO/CoS/CEnvO, and in each argument text.upper() finds APPROVE at index ~261, OBJECT at ~270, ABSTAIN at ~280 (the echoed instruction '…end with exactly one word on its own line: APPROVE, OBJECT or ABSTAIN.'); last line of each argument is the floor's 'Workstation composes a structured response…', never a stance word. GET /meeting/minutes → '**CEvO** - **APPROVE**: _[Workstation native structured engine…]_'. READ: agentic_core/api/v138/ceo.py:216-262 (call_meeting; stance loop 250-254 substring match, no served_by check), :384-386 (debate injected as '## Recent C-Suite debate (real meeting log)'), :404-410 (any 'meeting'/'debate' in the message triggers it); agentic_core/ai/ceo/memory_v01.py:83-100 (in-memory list; no persistence); grep 'meeting/log|meeting/minutes|debate_entries' in apps/workstation-superapp/src → no hits; CEOChat.tsx:51 is the trigger.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In call_meeting(): when res.served_by == 'native' (or the reply is the floor), record no stance (or stance='no position — floor-served') and do not count the officer as 'deliberated'; otherwise take the stance ONLY from the reply's last non-empty line ∈ {APPROVE,OBJECT,ABSTAIN}. Stamp served_by on each MeetingLog row and render it in export_minutes.

### R3.1 · §5 every tier on the native floor — '_Acting as: <role>._' label (cascade, delegate, Board directive, CCA review) — **STUB** · tier 1

- **severity (assessor):** medium
- **claim:** §5/§6: each tier (Chief, Board, AI CEO, C-Suite officer, CoE, BTO) speaks as itself, in-house, honestly labelled.
- **observed:** The floor's role line names the PREVIOUS call's persona, not the current one. gateway.query_meta (augment=True by default, used by board._q, swarm._q, CCA review) prepends '[native memory recall …]' lines containing prior prompts; engine._role() takes the FIRST 'You are the X' in the augmented text, so it reads the recalled prior prompt's role. W332 neutralised 'User:'/'AI:' inside recall but not 'You are the'. Result: the CFO's card says 'Acting as: Chief Strategy Officer', the CTO's 'Acting as: Chief Financial Officer', the AI CEO directive 'Acting as: Board of Directors', the BTO 'Acting as: Chief Legal Officer', the Chief's Board resolution 'Acting as: Director of Finance & Capital', the Governance review 'Acting as: Build-to-Order'.
- **evidence:** EXECUTED: POST /api/v1/swarm/delegate {"task":"Draft a one-line summary"} → agent_responses.CFO starts '_Acting as: AI CEO._', CTO '_Acting as: Chief Financial Officer._', ceo_synthesis '_Acting as: Chief Technology Officer._' (a clean one-behind chain). POST /api/v1/swarm/cascade {"mission":"Assess launch readiness…"} → level_1_ceo_directive 'Acting as: Board of Directors of this VSB', level_2_csuite.CSO 'Acting as: AI CEO of a VSB', .CFO 'Acting as: Chief Strategy Officer', .CTO 'Acting as: Chief Financial Officer', .COO 'Acting as: Chief Technology Officer', level_4_business_transformation_office 'Acting as: Chief Legal Officer'. POST /api/v1/board/directive {"topic":"Capital allocation…"} → resolution 'Acting as: Director of Finance & Capital on the Workstation IDBO Board' (prompt: 'You are the Chief'), director_inputs.dir_finance.input 'Acting as: Chief of the Board'. POST /api/v1/cca/cca-4622f5a9ff/review → review_result '…_Acting as: Build-to-Order._ … Subject: You are the Chief Governance Officer…'. READ: agentic_core/ai/native/engine.py:90-93 (_role: first regex match), :141-150 (lead = 'Acting as'); agentic_core/ai/gateway.py:92-108 (_augment recall lines), :145 & :168 (augmented text handed to native_engine.generate). RENDERED verbatim: SwarmIntelligence.tsx:298-324 (tier/officer <pre>), BoardOfDirectors.tsx:291,299,322, ChangeControlAgency.tsx:195-200 (review_result <pre>).
- **disclosed to the user:** No. The card heading names the correct officer while the body's italic lead names a different one; nothing says the label is unreliable.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on the cascade, the surface that renders each officer's text verbatim under the officer's own heading: CSO card body says 'Acting as: Chief Financial Officer', CFO 'Chief Strategy Officer', CTO 'Chief Operating Officer', COO 'Chief Technology Officer'. Mechanism confirmed in code: gateway._augment prepends memory-recall lines (prior prompts, truncated to 280 chars) that still contain 'You are the …' (W332 neutralised only 'User:'/'AI:'), and engine._role takes the FIRST 'You are the' match. The evidence is weaker than the assessor stated, though: the defect depends on what token-overlap recall returns at the moment, so it is non-deterministic — /swarm/delegate gave the correct CFO/CTO/AI CEO leads on three runs (including the assessor's own saved del.json), and in my /board/directive run the resolution read 'Acting as: Chief' and dir_finance read its own title. Tier 1 stands for the cascade officer cards, which a real person reads as a mislabelled speaker.
- **refuter's evidence:** EXECUTED: POST /api/v1/swarm/cascade {mission:'Assess launch readiness of a halal e-commerce marketplace'} → run 103057c4af; level_2_csuite.CSO '_Acting as: Chief Financial Officer._', .CFO '_Acting as: Chief Strategy Officer._', .CTO '_Acting as: Chief Operating Officer._', .COO '_Acting as: Chief Technology Officer._', .CLO correct; tiers 0/0b/1/3/4/5 correct in this run. POST /api/v1/swarm/delegate {task:'Draft a one-line summary'} (twice) → CFO 'Chief Financial Officer of a Virtual Sovereign Business', CTO 'Chief Technology Officer…', ceo_synthesis 'AI CEO' — NOT reproduced. POST /api/v1/board/directive {topic:'Capital allocation for the next quarter'} → resolution '_Acting as: Chief._', dir_finance 'Director of Finance & Capital…' — NOT reproduced this run. READ: agentic_core/ai/gateway.py:92-108 (_augment; _neutral replaces only 'User:'/'AI:'), :145-150 (augmented text to native orchestrator); agentic_core/ai/memory.py:105-125 (token-overlap recall, top 3); agentic_core/ai/native/engine.py:90-93 (_role first match), :141-143 (lead); agentic_core/api/swarm.py:391-393 (_q → gateway.query_meta, augment default True), :498 (officer prompt 'You are the {role}'); SwarmIntelligence.tsx:312-320 renders String(cascade.level_2_csuite[role]) under the role heading.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In gateway._augment, neutralise persona tokens inside recall lines ('You are the' → '[recalled role]'), or make engine._role() search only the text after the final 'User:' marker. Alternatively drop the 'Acting as' lead when the prompt was augmented.

### R3.2 · §5 AI CEO chat — /api/v138/ceo/chat SSE terminal frame + CEOChat.tsx grounding chip — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §5: the AI CEO answers from the Board's directives, the living plan and the business plan; the terminal frame names served_by and what the answer was grounded in.
- **observed:** served_by IS named (terminal frame served_by:'native', badge 'structured floor — not model analysis' amber). But on the floor the answer never contains the user's question or any of the offered grounding: the engine's _field() requires 'Label:' while the CEO prompt uses '## Question\n…', so _subject falls back to the longest sentence — the system-role sentence — and every section reads 'Native structured content for "Question", grounded in: You are the AI CEO of this Workstation IDBO enterprise…'. The chip beneath still says 'grounded in 2 directives · 1 objective · scope workstation', which describes what was placed in the prompt, not what the served answer reflects.
- **evidence:** EXECUTED: POST /api/v138/ceo/chat {"message":"What is the state of the organisation today and what should the Board do next?"} → content sections '## Board directives - Validate the structured outputs above…', '## Question Native structured content for "Question", grounded in: You are the AI CEO…', '## Assessment Subject: You are the AI CEO…'; final frame {done:true, served_by:'native', is_external:false, grounding:{scope:'workstation', directives:2 (after the instruct/directive probes), objectives:1, plan_score:0.3, debate_entries:6}}. READ: agentic_core/ai/native/engine.py _field (regex `{label}\s*[:\-]\s*(.+)`), _subject fallback; agentic_core/api/v138/ceo.py:412-420 (prompt uses '## Question'); CEOChat.tsx:285-289 (badge + 'grounded in N directives · N objectives' chip); lib/api.ts:65-72 (provenanceBadge labels native as 'structured floor — not model analysis').
- **disclosed to the user:** Partly: the floor badge is honest and prominent; the 'grounded in …' chip overstates on the floor.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced exactly: on the floor the CEO answer never contains the user's question or any offered grounding because engine._field needs 'Label:' and ceo.py writes '## Question', so _subject falls back to the longest sentence — the system-role sentence. The badge is honest ('structured floor — not model analysis', amber), but the adjacent chip 'grounded in N directives · N objectives · scope …' reports what was placed in the prompt, not what the served text reflects. Tier 2 is right: the shortfall is partly disclosed by the badge beside it.
- **refuter's evidence:** EXECUTED: POST /api/v138/ceo/chat (message about pausing hiring) → streamed content contains 0 occurrences of 'pause all hiring'; contains '## Question\nNative structured content for 'Question', grounded in: You are the AI CEO…'; terminal frame {served_by:'native', is_external:false, grounding:{scope:'workstation', directives:4, objectives:2, plan_score:0.3, debate_entries:18}}. READ: agentic_core/ai/native/engine.py:82-88 (_field regex `label\s*[:\-]`), :96-107 (_subject longest-sentence fallback); agentic_core/api/v138/ceo.py:412-420 (prompt uses '## Question\n'); CEOChat.tsx:285-289 (provenanceBadge + 'grounded in … directive(s) · … objective(s) · scope' chip); lib/api.ts:65-72 (native → 'structured floor — not model analysis').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** On served_by native, label the chip 'context offered (not used by the floor): N directives · N objectives' or omit it; and/or teach engine._field to accept '## Label' headings so the floor at least echoes the question.

### R3.3 · §5 Chief-owned Business Plan opening for a spawned VSB — /api/v1/vsb/spawn → /business-plan?scope=<vsb> — **PARTIAL** · tier 2

- **severity (assessor):** medium
- **claim:** §5 / §15.9: the plan opens with Executive Summary · Concept · Vision — 'the founder's idea framed by the digital-twin Chief', AI-generable in-house, then owner-edited; W471 says who wrote the opening is shown.
- **observed:** The spawn path writes f-string templates into executive_summary / vision / mission / strategy ('<name> is a living VSB IDBO established to solve: <challenge>.', 'A self-running waqf_ltd_hybrid VSB IDBO that commercialises this solution beneficently.', 'Concept → Design → Commercialisation, governed by the Board…') and seeds NO provenance. BusinessPlan.tsx renders them inside the 'Chief's Opening — Executive Summary · Concept · Vision' card and the 'Strategic Layer' with no provenance badge (badge needs provenance.served_by) and no 'pending' note, under the header 'The living plan owned by your Chief (your digital twin)'. The Genesis path (genesis.py) seeds provenance; the spawn path does not.
- **evidence:** EXECUTED: POST /api/v1/vsb/spawn {challenge:'UK Muslim consumers cannot easily find verified halal-certified vendors online', name:'Audit Probe Halal Market'} → vsb-2c15bfcde6; GET /api/v1/business-plan?scope=vsb-2c15bfcde6 → executive_summary 'Audit Probe Halal Market is a living VSB IDBO established to solve: …', vision 'A self-running waqf_ltd_hybrid VSB IDBO that commercialises this solution beneficently.', mission 'Deliver: …', strategy 'Concept → Design → Commercialisation, governed by the Board…', provenance: null, owner_edits: null, objectives 'Validate the concept'/'Deliver the design'/'Launch to market' with kpi '' timeline '' → roadmap phase 'Unscheduled'. READ: agentic_core/api/vsb.py:1560-1578 (template seed, no provenance) vs agentic_core/api/genesis.py:752-769 (seeds provenance.served_by/body_pending); BusinessPlan.tsx:116-137 (card), :122-127 (badge/pending only when provenance present), :159-167 (Strategic Layer); reached from VSBSpawnStudio.tsx:503 'Open plan →' and GenesisJourney.tsx:900.
- **disclosed to the user:** No on the spawn path (no badge, no 'template' or 'pending' note). The Genesis path discloses pending fields.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The spawn path seeds f-string templates into executive_summary/vision/mission/strategy and writes no provenance, while the Genesis path seeds provenance.served_by/body_pending. BusinessPlan.tsx shows the badge and the 'pending the owned model' note only when plan.provenance is present, and the 'No opening recorded yet — nothing here is the Chief's framing' line only when the fields are empty — so the template text sits under 'Chief's Opening' with nothing saying it is a template. Reached from VSBSpawnStudio 'Open plan →' and GenesisJourney. Tier 2 (invisible shortfall) is right; the text itself is a truthful restatement of the founder's input, not a fabricated figure.
- **refuter's evidence:** EXECUTED: GET /api/v1/business-plan?scope=vsb-2c15bfcde6 → executive_summary 'Audit Probe Halal Market is a living VSB IDBO established to solve: UK Muslim consumers cannot easily find verified hala…', vision 'A self-running waqf_ltd_hybrid VSB IDBO that commercialises this solution beneficently.', mission 'Deliver: …', strategy 'Concept → Design → Commercialisation, governed by the Board…', concept '', no 'provenance' or 'owner_edits' key; objectives ('Validate the concept','',''), ('Deliver the design','',''), ('Launch to market','',''). READ: agentic_core/api/vsb.py:1560-1578 (template seed, no provenance) vs agentic_core/api/genesis.py:752-769 (provenance seeded); BusinessPlan.tsx:116-137 (card; badge gated on plan.provenance?.served_by at 122; pending note gated on body_pending at 125-127; 'No opening recorded yet' only when all three empty at 133-135), :32-34 (scope from URL); VSBSpawnStudio.tsx:503 and GenesisJourney.tsx:900 link to /business-plan?scope=<vsb>.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In vsb.py spawn seeding, set plan['provenance'] = {served_by: {'template': 1}, body_pending: ['executive_summary','concept','vision','mission','strategy']} as genesis does, and let BusinessPlan.tsx render a 'template — not the Chief's framing' chip for served_by 'template'.

### R3.4 · §5 / §17.4 Mode 2 — the Chief as the founder's digital twin (Board page, cascade tier 0, per-VSB Chief) — **PARTIAL** · tier 1 *(assessed tier 2)*

- **severity (assessor):** medium
- **claim:** §5: 'Chief of the Board = the Founder's Digital Twin … modelled, simulated and iterated specifically for the founder's instructions and input data … in the founder's presence and absence'; §17.4 Mode 2 expert → digital-twin node.
- **observed:** What serves the Chief is a constant values sentence plus the last five stored instructions (founder_profile) composed by the deterministic floor; /api/v1/twin/models holds 0 models; every spawned VSB's Chief is titled 'Chief of the Board — Digital Twin of default'. The vision's own §17.4 text records Mode 2 as NOT BUILT (P3.4), but the Board page tells the user unqualified: 'Chaired by your Chief, a digital twin of you (Rehan) that represents you faithfully in your presence and absence, and directs the whole organism on your behalf' and 'Instruct your Chief (your Digital Twin)'. The directive it produces is correctly badged floor.
- **evidence:** EXECUTED: GET /api/v1/twin/models → {"models":[],"total":0}; GET /api/v1/board/status?scope=vsb-2c15bfcde6 → represents_owner 'default', chief.title 'Chief of the Board — Digital Twin of default'; POST /api/v1/board/chief/instruct → chief_directive is floor prompt-echo ('Native structured synthesis grounded in the input's salient terms: workstation idbo, precisely effect, owner precisely…'), ai_provenance {native:2}. READ: agentic_core/api/board.py:128-144 (founder_profile = fixed values string + last 5 instructions), :183-194 (board_for_owner title f-string); BoardOfDirectors.tsx:120-124 and :242 (twin claims); the result badge BoardOfDirectors.tsx:274-276 uses provenanceMapBadge (amber floor).
- **disclosed to the user:** Partly: the directive output is badged 'structured floor — not model analysis'; the header's claim that a twin represents the user in absence is not qualified anywhere on the page. Mode 2 is admitted MISSING only in the vision doc.
- **refutation: REFUTED — corrected to PARTIAL.** The facts reproduce, but the tier is too kind. The Board page header asserts, unqualified and persistently, that the Chief is 'a digital twin of you (Rehan) that represents you faithfully in your presence and absence, and directs the whole organism on your behalf', and the instruction card says 'Instruct your Chief (your Digital Twin) … It will represent you precisely'. What exists is a constant values sentence plus the last five stored instructions composed by the deterministic floor, 0 twin models, and per-VSB Chiefs titled 'Digital Twin of default'. The vision's own §17.4 records Mode 2 as NOT BUILT (P3.4). A reached surface telling the user a capability exists that the canon admits does not is a truth defect, not an invisible shortfall; the amber badge sits on the directive output, not on the claim. Corrected: PARTIAL, tier 1.
- **refuter's evidence:** EXECUTED: GET /api/v1/twin/models → {models:[], total:0}. GET /api/v1/board/status?scope=vsb-2c15bfcde6 → represents_owner 'default', chief.title 'Chief of the Board — Digital Twin of default'. GET /api/v1/board/status → chief.title 'Chief of the Board (Owner's Digital Twin)', hierarchy ['Owner','Chief (Owner Digital Twin)',…]. READ: agentic_core/api/board.py:128-144 (founder_profile = fixed values string + last 5 instructions from the board store), :183-194 (board_for_owner f-string title); BoardOfDirectors.tsx:120-124 (header claim), :242-249 (instruction card 'your Digital Twin' / 'represent you precisely'), :274-276 (provenanceMapBadge on the result only); docs/WORKSTATION_IDBO_WHOLE_VISION.md:713-717 (Mode 2 'Digital Twin of default' — not built, P3.4).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** One sentence under the Board header: 'Today the Chief is the standing charter + your last instructions on the owned fabric; no twin model is trained (0 models) — Mode 2 is planned (P3.4).' Title per-VSB Chiefs by the real owner name or 'the founder', never 'default'.

### R3.5 · §5 arms-length Change Control + Board ratification — /api/v1/cca/*, /api/v1/board/ratifications, /api/v1/board/{status,charter} — **DELIVERED**

- **severity (assessor):** low
- **claim:** W464 rulings: a HIGH change approved by a review waits for Board ratification on the Owner's direction; nothing implements it until then; decisions written to the UEG; lower tiers cannot instruct the Board.
- **observed:** The gate works end to end and says what decided it. A HIGH code_change reviewed on the floor is 'approved' by the organism-health threshold rule (composite_health 0.90 ≥ 0.5) with the model contributing nothing — stated verbatim in review_result; it then waits for the Board; implement refuses 409; a ratification without on_owner_direction refuses 403; twin pre-validation says 'no twin model — health gate only'. The Board page renders the queue with an Owner-direction checkbox that gates the Ratify/Refuse buttons; the CCA page labels 'approved by review — awaiting Board ratification'. Board status/charter list the 8-role roster, hierarchy and the arms-length clause.
- **evidence:** EXECUTED: POST /api/v1/cca/submit {title:'Audit probe: rename a helper', change_type:'code_change', impact_tier:'high'} → cca-4622f5a9ff, HIGH, submitted. POST /api/v1/cca/cca-4622f5a9ff/review {} → {decision:'approved', decision_source:'health_threshold_rule', review_result:'DECIDED BY RULE, NOT BY THE MODEL: no [DECISION: …] marker…', awaiting_board_ratification:true, ueg_logged:true}. GET /api/v1/board/ratifications → pending[0].cca_id cca-4622f5a9ff, twin_prevalidation {verdict:'pass', source:'health_gate_default', source_label:'no twin model — health gate only'}. POST /api/v1/cca/cca-4622f5a9ff/implement → 409 'waits for the Board to ratify it on the Owner's direction'. POST /api/v1/board/ratifications/cca-4622f5a9ff {decision:'ratify'} → 403 'resend with on_owner_direction: true'. GET /api/v1/board/status → pending_ratifications 1, 7 directors + chief; GET /api/v1/board/charter → arms_length_agency + ratification text. READ: board.py:278-302, change_control.py:352-366 & 1239-1300; BoardOfDirectors.tsx:154-220; ChangeControlAgency.tsx:233-238, 278-282.
- **disclosed to the user:** Yes — the rule-not-model basis, the awaiting state, and the absent twin model are all on the surface.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced end to end: the HIGH change is approved by the health-threshold rule with that basis written into review_result, implement refuses 409 pending the Board, ratification without on_owner_direction refuses 403, the queue exposes decision_source and the honest 'no twin model — health gate only' pre-validation, and both pages render the state and gate the buttons on the Owner-direction checkbox. DELIVERED, tier 3 stands.
- **refuter's evidence:** EXECUTED: GET /api/v1/board/ratifications → pending 2 (cca-2bdf322b90, cca-4622f5a9ff), each HIGH, decision_source 'health_threshold_rule', twin_prevalidation {verdict:'pass', source:'health_gate_default', source_label:'no twin model — health gate only'}. POST /api/v1/cca/cca-4622f5a9ff/implement → 409 'approved by a review (the organism-health threshold rule), so it waits for the Board to ratify it on the Owner's direction…'. POST /api/v1/board/ratifications/cca-4622f5a9ff {decision:'ratify'} → 403 'resend with on_owner_direction: true'. GET /api/v1/cca/cca-4622f5a9ff → status approved, awaiting_board_ratification true, review_result 'DECIDED BY RULE, NOT BY THE MODEL: … composite_health 0.90 >= 0.5 → approved'. READ: BoardOfDirectors.tsx:150-220 (queue, 'approved by: the organism-health threshold rule (not the model)', §17.5 pre-validation line, checkbox gating Ratify/Refuse); ChangeControlAgency.tsx:195-200, 233-238, 278-282 ('approved by review — awaiting Board ratification').
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None required for truth. Capability note: a HIGH change 'approved' purely by a health number is a weak review; consider holding HIGH changes as 'no review possible on the floor' instead of rule-approving them.

### R3.6 · §17.4 Mode 3 review gates + §17.3 Board Pack (versioned, refused while gated / over an empty concept) — /api/v1/vsb/{id}/review-gates*, /board-pack, /board-packs — **DELIVERED**

- **severity (assessor):** low
- **claim:** §17.4 Mode 3: per-VSB review gates; lifecycle movers refuse 409 while a gated stage is pending. §17.3: on-demand Board Pack assembled fresh, DCS-registered, versioned by what it carries, refused over an empty concept (P1.14).
- **observed:** Gates: configurable, a pending gate blocks the board pack with a 409 that names the gate and how to clear it; the human decision is DCS-hashed and unblocks. Pack: floor-served narrative is filed as 'narrative pending the owned model…' (not the marker), concept_source 'challenge' with a visible concept_note, QMS null (not assessable) with basis, version 1 → re-assembly 'unchanged: true, unchanged_since', history counts assemblies vs versions. Refusal over NO concept AND no challenge is coded (409) but could not be executed because spawn requires a challenge. Remaining §17.3 shortfall (already stated in the vision): layers.strategic = the pending ceo_specification string, layers.action_plan = the board roster, constitutional.values is one constant string and constitutional.vision is the challenge verbatim; Strategic/Action-plan cadence generators do not exist (no quarterly/weekly refresh anywhere; heartbeat.py only auto_evolve/economy/align). Genesis renders only the four layer NAMES + narrative, so the layer contents are API-only.
- **evidence:** EXECUTED on vsb-2c15bfcde6: GET review-gates → stages [], 8 lifecycle stages, statuses all not_gated. POST review-gates {stages:['design']} → design gated. POST board-pack → 409 {error:'review gate blocks progress — a human review is pending (Mode 3); board pack refused', gate:'design', clear_by:'POST …/review-gates/design/decision'}. POST review-gates/design/decision {decision:'approve'} → status approved, blocks_progress false, dcs_hash. POST board-pack → 200 narrative 'narrative pending the owned model…', concept_source 'challenge', concept_note 'no concept recorded yet…', version 1, unchanged false, dcs_registered true, quality.qms_gate_passed null, compliance 'review', layers.constitutional.values 'Integrity · Compassion · Excellence · Halal/Sharia · Beneficence · Stewardship', layers.strategic.ceo 'content pending the owned model…'. Second POST → version 1, unchanged true, unchanged_since 2026-09-19T04:30:52Z. GET board-packs → total 2, versions 1. READ: vsb.py:1063-1073 (409 no-concept), :1123-1200 (pack), GenesisJourney.tsx:1045-1069 (layer-name chip, provenance/QMS/compliance chips, concept_note).
- **disclosed to the user:** Yes for the narrative, concept and QMS state; the layer contents (constant values, challenge-as-vision) are not rendered, so their mislabelling is not seen — and the vision §17.3 already records the cadence layers as MISSING (P3.3).
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, including a fresh gate: gating 'build' made the board pack refuse 409 naming the gate and the clearing route; approving it (DCS-hashed) unblocked. The pack is versioned by content (v1, unchanged since first assembly across three filings), floor narrative filed as 'narrative pending the owned model', QMS null with an explicit basis, concept_note visible, and Genesis renders only layer names + narrative so the constant-values/challenge-as-vision layers are API-only — which the vision §17.3 already records as MISSING (P3.3). One correction to the assessor's field names: quality/compliance are null at the pack top level; the QA lives under quality_assurance.quality (overall compliance 'review', not green). Verdict and tier stand.
- **refuter's evidence:** EXECUTED on vsb-2c15bfcde6: POST review-gates {stages:['design','build']} → build gated; POST board-pack → 409 {error:'review gate blocks progress — a human review is pending (Mode 3); board pack refused', gate:'build', clear_by:'POST …/review-gates/build/decision …'}; POST review-gates/build/decision {decision:'approve'} → status approved, blocks_progress false, dcs_hash; POST board-pack → 200 version 1, unchanged true, unchanged_since 2026-09-19T04:30:52Z, concept_source 'challenge', concept_note 'no concept recorded yet…', narrative 'narrative pending the owned model…', quality_assurance.quality {qms_gate_passed:null, qms_basis:'not assessable — floor-served…', delivery_coverage 0.0, stub_found true}, compliance.overall 'review', layers.constitutional.vision = the challenge verbatim, layers.strategic.ceo 'content pending the owned model…', layers.action_plan.board = roster; GET board-packs → total 3, versions 1. READ: vsb.py:1063-1073 (_refuse_empty_blueprint 409); GenesisJourney.tsx:1045-1069 (layer-name chip, version chip, qmsChip, complianceCls, concept_note, narrative); heartbeat grep: no quarterly/weekly cadence; docs/WORKSTATION_IDBO_WHOLE_VISION.md:704-709 (cadence layers MISSING, P3.3).
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Rename layers.strategic/action_plan keys to what they hold ('ceo_specification', 'board_roster') or mark them 'placeholder until P3.3'; derive constitutional.vision from the plan's vision field rather than the challenge.

### R3.7 · §5 org cascade delivery — /api/v1/swarm/cascade + SwarmIntelligence.tsx chips — **PARTIAL** · tier 2

- **severity (assessor):** low
- **claim:** §5 Chief → Board → AI CEO → C-Suite → CoE → BTO → Build-to-Order, appraised tier by tier, QMS-gated, §11-screened, DCS-controlled; §15.6 label simulation.
- **observed:** The full chain runs and persists with honest provenance (native×22 → amber floor badge), QMS 'not assessable' with basis, a per-criterion bar (2 measured · 0 attested · 14 not measured), DCMS hashes and a real fabric requisition trace. Two chips overstate on the visible surface: 'compliance: pass' (green) while the uk_legal row is 'review' with coverage 'none' — _overall excludes coverage-none rows, and the 'keyword screen, not a certification' reasons live only in the tooltip; and 'BMS EFFICIENT · $7e-06/insight' (roi 69444) is computed from a simulated $0.5-per-insight constant, with the caveat only in the tooltip (the EMS chip says '(sim)' inline; the BMS chip does not).
- **evidence:** EXECUTED: POST /api/v1/swarm/cascade {mission:'Assess launch readiness of a halal e-commerce marketplace'} → run_id 7d17db8c31, ai_provenance {native:22}, quality.qms_gate_passed null, qms_basis 'not assessable — floor-served…', bar_measured.summary '2 measured · 0 attested · 14 not measured', compliance.overall 'pass' with verdicts[1] {framework:'uk_legal', status:'review', coverage:'none'}, management_systems.bms {status:'EFFICIENT', roi:69444.444, roi_basis:'(insights x $0.5 simulated value) / energy cost', caveat:'…$/Wh is the catalogue's simulated constant'}, governance {status:'allowed', arms_length:true}, document_control 4 SHA3 hashes, persistence.org_cascade_runs true. READ: agentic_core/api/compliance.py:233-247 (_overall skips coverage 'none'); SwarmIntelligence.tsx:255-260 (QMS via qmsChip), :266-271 (compliance chip, reasons in title only), :287-296 (BMS chip without '(sim)', EMS with '(sim)'); lib/api.ts:114-126 (qmsChip three states).
- **disclosed to the user:** Mostly: floor badge, QMS not-assessable and the bar are explicit; the compliance 'pass' and BMS 'EFFICIENT' chips disclose their basis only on hover.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. The run is honestly badged (native×22 amber, QMS not assessable with basis, bar 2 measured · 0 attested · 14 not measured), but two visible chips overstate: 'compliance: pass' (green) while uk_legal is 'review' with coverage 'none' — _overall deliberately excludes coverage-none rows and the per-framework detail is only in the title tooltip; and 'BMS EFFICIENT · $3e-06/insight' derived from a simulated $0.5/insight constant, with the caveat only in the tooltip (the EMS chip carries '(sim)' inline; the BMS chip does not). Minor: document_control was null in my run rather than four hashes. Tier 2 stands — disclosure exists but only on hover.
- **refuter's evidence:** EXECUTED: POST /api/v1/swarm/cascade → run 103057c4af; ai_provenance {native:22, any_external:false}; quality.qms_gate_passed null, qms_basis 'not assessable — floor-served…'; bar_measured.summary '2 measured · 0 attested · 14 not measured'; compliance.overall 'pass' with verdicts sharia_halal pass/vocabulary, uk_legal review/none, regulatory pass/screen, ehs pass/screen, ethical pass/engine; management_systems.bms {status:'EFFICIENT', roi:172711.572, cost_per_insight_usd:3e-06, roi_basis:'(insights x $0.5 simulated value) / energy cost', caveat:'…$/Wh is the catalogue's simulated constant'}; governance {status:'allowed', arms_length:true}; document_control null. READ: agentic_core/api/compliance.py:233-247 (_overall skips coverage 'none'); SwarmIntelligence.tsx:266-271 (compliance chip, frameworks in title only), :287-296 (BMS chip caveat in title only; EMS '(sim)' inline); lib/api.ts:82-84 (complianceCls green on 'pass').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Render 'compliance: pass (screen)' and append '(sim)' to the BMS chip label as the EMS chip already does; or colour the compliance chip amber when any framework row is 'review' regardless of coverage.

### R3.8 · §5 Chief instruct → AI CEO delegation → living plan + living Roadmap — /api/v1/board/chief/instruct, /api/v1/business-plan/{roadmap,generate} — **PARTIAL** *(assessed DELIVERED)* · tier 3

- **severity (assessor):** low
- **claim:** §5: the Chief's direction lands as objectives on the living plan; the Roadmap is a time-phased view (phases · progress · current phase · next milestone) integrated into the plan and the VSB Cockpit; the opening is AI-generable then owner-edited.
- **observed:** On the floor the delegation cannot parse TITLE|KPI|TIMELINE|OWNER lines, so the instruction itself becomes ONE objective (source 'chief_instruct_fallback', kpi '(KPI to be set by the Board)', timeline 'next review'); the roadmap then reports current_phase 'next review' and that objective as next_milestone. Plan generate on the floor writes nothing and says so (reason + body_pending), and the page shows 'pending the owned model: …' and 'No opening recorded yet'. The objective's 'source' is not rendered, but its KPI text discloses that nothing was planned.
- **evidence:** EXECUTED: POST /api/v1/board/chief/instruct {instruction:'Prioritise a halal-compliant marketplace pilot in Q4 with a 3-month timeline'} → directive_id dir-0c0a3896, objectives_added 1, ai_provenance {native:2}, governance {status:'allowed'}; GET /api/v1/business-plan → objectives[0] {title:<instruction>, kpi:'(KPI to be set by the Board)', timeline:'next review', source:'chief_instruct_fallback', directive_id:'dir-0c0a3896'}; GET /api/v1/business-plan/roadmap → phases [{timeline:'next review', count:1}], current_phase 'next review', next_milestone {phase:'next review', title:<instruction>}. POST /api/v1/business-plan/generate → written [], reason 'structured floor — not model analysis; nothing was written…', provenance.body_pending [executive_summary, concept, vision, mission, strategy]. READ: board.py:375-393 (fallback objective), business_plan.py:396-470; BoardOfDirectors.tsx:274-284 (floor badge + '1 objective → living plan'), BusinessPlan.tsx:115-135 (genNote, pending list, 'No opening recorded yet'), :180-199 (roadmap), VSBCockpit.tsx:565-586 (roadmap).
- **disclosed to the user:** Yes.
- **refutation: REFUTED — corrected to PARTIAL.** The execution reproduces, but DELIVERED is too kind. The vision's delegation is a timelined action plan; on the floor the instruction itself becomes one objective with a placeholder KPI and the literal timeline 'next review', and the roadmap then presents 'next review' as the current phase and that objective as the next milestone. That is an honest fallback (the KPI text and generate's reason say nothing was planned; the page shows 'pending the owned model' and 'No opening recorded yet'), so the shortfall is disclosed — PARTIAL, tier 3 — not 'works as the vision says'. The objective's 'source' field is not rendered anywhere.
- **refuter's evidence:** EXECUTED: GET /api/v1/business-plan → objectives include {title:'Prioritise a halal-compliant marketplace pilot in Q4 with a 3-month timeline', kpi:'(KPI to be set by the Board)', timeline:'next review', source:'chief_instruct_fallback', directive_id:'dir-0c0a3896'} and a second fallback objective (dir-adb2030b) of the same shape; provenance.body_pending [executive_summary, concept, vision, mission, strategy], executive_summary ''. GET /api/v1/business-plan/roadmap → phases [('next review', 2)], current_phase 'next review', next_milestone {phase:'next review', title:<the instruction>}. POST /api/v1/business-plan/generate {scope:'workstation'} → written [], reason 'structured floor — not model analysis; nothing was written to the plan…'. READ: agentic_core/api/board.py:375-393 (fallback: instruction | '(KPI to be set by the Board)' | 'next review' | AI CEO); BusinessPlan.tsx:115 (genNote), :125-127 (pending list), :133-135 ('No opening recorded yet'), :180-199 (roadmap renders ph.timeline and current_phase verbatim); VSBCockpit.tsx:565-586 (same roadmap).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Optional: render the objective's `source` ('placed by the Chief's fallback — no CEO action plan on the floor') beside the ⌘ directive chip.

### R3.9 · §5 Chief instructed 'through multimodal communication or by navigation/guidance through any platform area via the enterprise-aware avatar' — **PARTIAL** · tier 3

- **severity (assessor):** low
- **claim:** §5: the Chief is instructed multimodally or by navigating/guiding through any platform area via the enterprise-aware avatar.
- **observed:** Chief instruction reaches the backend only from two textareas (Board page and VSB Cockpit 'Chief & Board' tab). Avatar components exist (components/avatar/*, FourthColumn) and dictation/TTS helpers exist on Genesis and the Cockpit, but no avatar or navigation path calls /api/v1/board/chief/instruct. Not verified in a browser; reasoned from source.
- **evidence:** READ: grep for '/api/v1/board/chief/instruct' in apps/workstation-superapp/src → only BoardOfDirectors.tsx:105 and VSBCockpit.tsx:169; components/avatar/{AvatarFace,AvatarWidget,ConversationPanel,VoiceVisualizer}.tsx exist; VSBCockpit.tsx:85 browser STT/TTS; DictateButton.tsx. No route ties the avatar to the Chief.
- **disclosed to the user:** Not applicable — nothing on the surface claims avatar-mediated Chief instruction; the gap is not reached.
- **refutation: SURVIVED** (reproduced by the refuter). Confirmed from source (no browser): the Chief is instructed only from two textareas; the avatar components contain no reference to the Chief or any /board/ route, so no multimodal or navigation path reaches /api/v1/board/chief/instruct. Nothing on a surface claims otherwise, so the gap is unreached and disclosed-by-absence — tier 3 stands.
- **refuter's evidence:** READ: grep 'board/chief/instruct' in apps/workstation-superapp/src (excluding tests) → BoardOfDirectors.tsx:105 and VSBCockpit.tsx:169 only; ls components/avatar → AvatarFace.tsx, AvatarWidget.tsx, ConversationPanel.tsx, VoiceVisualizer.tsx; grep -l 'chief|/board/' across components/avatar and FourthColumn → no files.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** None for truth; capability item for a later phase (route the avatar ConversationPanel's intents to chief/instruct with the same provenance chips).

---

## R4 — §6 + §7 + §17.2 — the native AI mandate, the reconfigurable resource fabric, the seven biomimetic layers

**Assessor's region summary:** R4 (§6/§7/§17.2) at HEAD cdd7619f, measured against the live :8083 backend and the superapp source. The native-AI mandate's honesty is genuinely DELIVERED: the gateway routes through the owned orchestrator first, external providers are key-AND-flag gated, every completion/swarm/tree/ensemble returned served_by 'native' / is_external false, /native-ai/status says the floor is active and why (basis row, 'history not a live probe', floor_note 'NOT an LLM'), floor serves are recorded, and the provenanceBadge helper labels floor output 'structured floor — not model analysis' across the reached renderers. The reconfigurable fabric works end-to-end (list → reconfigure params → simulate → save → run → real endpoints hit; swarm define/run persisted), and the organism APIs carry bases (self-healing health null with health_basis, composite terms marked SIMULATED/defaulted, Agent Hub 'records only'). Tier-1 truth defects that REMAIN on reached surfaces: (1) the Command Center's Avatar/Predictive/Spatio-Temporal channels still fabricate readings ('Latency: 18ms', 'Resonance drop predicted at 14:00Z', 'CROSS-PLANETARY SYNC ACTIVE') — siblings were retired in W415/W460, these were not; (2) green certification built on gates that could not assess — the Native AI tree paints emerald 'proceed / consensus reached / worst-case 0.85' when QMS is 'not assessable' and the utility is a constant of the code path, and the Resource Fabric paints emerald 'commit-ready' when the gate verdict is None. Tier-2: the /organism overview shows the blended 88% without the measured-only figure; the unlinked Introspection page relabels immune health as 'System Health' under a fabricated 'Apotheosis v139.0' caption; the §17.2 'nine cognitive engines' are a constant-payload stub (unreached) and, on the reached path, floor prompts under engine-name headings ('AIGateway' in engines_used) — a DOC_OVERCLAIM against the W446 'never a prompt approximation' rule; Forge lacks a provenance chip and mislabels the Petri stage persona; Sovereign Evolution renders a defaulted 'proceed' verdict and an uncaptioned 'Realisation 89%'. Tier-3: the seven layers are not presented as seven (Cardiovascular/Respiratory/Endocrine/Musculoskeletal never named on Anatomy), reflex arcs 0 with no registrar, immune-reconfigure has no caller. I could not drive a browser; UI claims are reasoned from source with file:line, route claims from execution.

### R4.0 · §9/§17.2 Command Center channels (packages/ui/src/CommandCenter.tsx, reached via Shell.tsx:265 → FourthColumn.tsx:62/153 'channels' tab) — **STUB** · tier 1

- **claim:** §15.6 honesty over polish — never fabricate; §17.2 the organism's readings are measured (nervous/cardiovascular/endocrine signals).
- **observed:** Three channels still render invented readings. Avatar: 'Sovereign Avatar Active — WebRTC stream synchronized … Latency: 18ms' (CommandCenter.tsx ~L343-352, a string constant; no WebRTC exists) and 'Calibrate Voice' is a 1.2 s setTimeout that always ends 'Calibrated' (L322-329). Predictive: 'Time-Series Forecast' bars are twelve hardcoded CSS heights, followed by 'Resonance drop predicted at 14:00Z.' and 'Energy surplus detected in L2 CL1 nodes.' (L368-392). Spatio-Temporal (components/organism/SpatioTemporal.tsx L46-52): 'Mars Relative: T + 842s' and 'Sovereign Consensus Status: CROSS-PLANETARY SYNC ACTIVE' from a local setInterval clock. The same file already retired the Ethical, Holo, OrganismVitals and NeuralLink fabrications (W415/W460 comments) but left these three.
- **evidence:** Read packages/ui/src/CommandCenter.tsx (channels array L48-60; avatar/predictive contents L335-395), SpatioTemporal.tsx L1-56; reachability read at Shell.tsx:265 and FourthColumn.tsx:62,117,153. No fetch, no backend route feeds any of these values.
- **disclosed to the user:** No — presented as live ('Live' pulse dot, 'Multi-Modal Fabric v3.0').
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced exactly. The three channels render invented readings with no fetch behind them, and the surface is reached (Shell.tsx:265 → FourthColumn.tsx:153 renders <CommandCenter tiled /> on the 'channels' dock tab). The strings ship in the bundle the live backend serves. Tier 1 stands: 'WebRTC stream synchronized … Latency: 18ms', a forecast bar chart of hardcoded heights with 'Resonance drop predicted at 14:00Z', and 'CROSS-PLANETARY SYNC ACTIVE / Mars Relative: T + 842s' are fabricated figures presented under a pulsing 'Live' dot.
- **refuter's evidence:** packages/ui/src/CommandCenter.tsx L48-60 channels array; L322-329 handleCalibrateVoice = setTimeout(1200) → 'Calibrated'; L343-352 avatar text constants ('WebRTC stream synchronized', 'Latency: 18ms'); L403-419 twelve hardcoded 'h-[..%]' bars + 'Resonance drop predicted at 14:00Z' + 'Energy surplus detected in L2 CL1 nodes'; L243-244 emerald pulse + 'Live'. apps/workstation-superapp/src/components/organism/SpatioTemporal.tsx L46-52 'Mars Relative: T + 842s' / 'CROSS-PLANETARY SYNC ACTIVE' driven only by a local setInterval (L8-12). Reachability: Shell.tsx:7,265; FourthColumn.tsx:4,62,153. Served bundle: curl http://127.0.0.1:8083/ → assets/index-BLlQlh2x.js, and grep of apps/workstation-superapp/dist/assets/index-BLlQlh2x.js matches 'WebRTC stream synchronized' and 'CROSS-PLANETARY SYNC ACTIVE'. No route in agentic_core feeds any of these values.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Do to avatar/predictive/spatio what W415/W460 did to the sibling channels: replace the constants with 'not measured — nothing streams here' copy (or retire the three tiles), and make 'Calibrate Voice' say nothing was calibrated.

### R4.1 · §6 Native AI workflow tree — decision + consensus on floor-served runs (agentic_core/ai/native/orchestrator.py:650-706; NativeAI.tsx:127-139) — **PARTIAL** · tier 1

- **claim:** §6 cascades are 'verified, tested and validated'; §15.6 never certify what was not assessed.
- **observed:** POST /api/v1/native-ai/tree {goal:'Plan a halal bakery launch'} returned governance.qms_passed=null with qms_basis 'not assessable — every node floor-served; the length proxy cannot fail' and qms_coverage_proxy=1.0 — yet decision={recommendation:'proceed', consistency:1.0, worst_case_utility:0.85} and consensus={reached:true, choice:'proceed', proceed_fraction:1.0, votes:{qms:'abstain', validation:'proceed', minimax:'proceed', immune:'proceed'}}. In _util (L661-671) qms_ok treats None as passed, the non-assessable coverage proxy adds +0.05, and the base 0.92 makes 'proceed' a constant of the code path regardless of goal content (0.92+0.05−0.30·1.0·0.4 = 0.85 exactly). NativeAI.tsx L130 paints the consensus chip emerald when choice==='proceed' and L137-138 shows 'proceed · consistency 100% · worst-case 0.85 vs hypoxia · oxidative_burst…'.
- **evidence:** Executed POST /api/v1/native-ai/tree (response fields governance/decision/consensus/validation quoted above); read orchestrator.py L650-706 and NativeAI.tsx L112-139.
- **disclosed to the user:** Partly — the QMS chip beside it is grey 'not assessable' (qmsChip), but the proceed/consensus chips are green and nothing says the recommendation is independent of the plan's content.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced and strengthened. I ran the tree with the assessor's goal and with an absurd one ('Launch a payday-loan casino with borrowed money and no plan'); both returned decision={recommendation:'proceed', consistency:1.0, worst_case_utility:0.85} and consensus={reached:true, choice:'proceed', proceed_fraction:1.0, votes:{qms:'abstain',…}}. The recommendation is a constant of the floor path: _util treats qms_passed None as passed (no −0.15), adds +0.05 for the not-assessable coverage proxy of 1.0, and the maximin over the four stressors gives 0.92+0.05−0.30·1.0·0.4 = 0.85 for every goal. NativeAI.tsx paints the consensus chip emerald on choice==='proceed' and shows 'proceed · consistency 100% · worst-case 0.85' with nothing saying the verdict is independent of the plan. That is certifying what was not assessed on a reached surface — tier 1 holds.
- **refuter's evidence:** POST /api/v1/native-ai/tree {goal:'Plan a halal bakery launch'} and {goal:'Launch a payday-loan casino with borrowed money and no plan'} → identical decision/consensus blocks (fields quoted above; stressors ['hypoxia','oxidative_burst','high_load','thermal_stress']). agentic_core/ai/native/orchestrator.py L656 `qms_ok = … is not False` (W449 comment), L657 cov default, L661-671 _util base 0.92 / +(coverage−0.5)·0.10 / sens 0.30·smag 1.0·0.4; L689-695 voters with qms 'abstain'. apps/workstation-superapp/src/pages/developers/NativeAI.tsx L128-131 emerald consensus chip, L135-139 minimax chip; only the qmsChip at L112 is grey.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** When qms_passed is None, return decision=null/consensus 'not assessable' (or vote 'abstain' for validation and minimax too) instead of feeding the un-assessable proxy into the utility; render the chips grey with the basis.

### R4.2 · §7 Resource Fabric — 'commit-ready' on a not-assessable gate (agentic_core/api/resource_fabric.py:1495-1506, 931, 1102-1126; ResourceFabric.tsx:473-474) — **PARTIAL** · tier 1

- **claim:** §7 'the platform modelling and simulating the configuration before commit'; §15.5 verified · tested · validated before delivery.
- **observed:** POST /resources/compose/simulate {resource_ids:[bdp,cognitive_cascade]} → simulation.quality.served_by='template', not_assessable=true, and commit_ready computed as `qms_gate_passed is not False` (i.e. None ⇒ ready). POST /resources/compositions/comp-15fe55d4/run → commit_ready:true, quality_warning:null while both trace steps were served_by 'native' (floor). ResourceFabric.tsx L473-474 paints an emerald 'commit-ready' chip; the docstring itself says a not-assessable verdict 'neither blocks nor certifies', but the chip certifies.
- **evidence:** Executed compose/simulate, compose (id comp-15fe55d4) and compositions/{cid}/run (run_id cr-23de20cf, real_resource_runs hit /api/v1/petri/culture and /api/v1/intelligence/solve); read resource_fabric.py L1490-1507 and ResourceFabric.tsx L468-480.
- **disclosed to the user:** Partly — the adjacent 'QMS gate: —' chip is grey with a 'not assessable' tooltip; the green 'commit-ready' contradicts it.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. With a supported usage area the simulation returns commit_ready:true while simulation.quality.not_assessable:true, qms_gate_passed:null, served_by:'template'; the run of the saved composition returns commit_ready:true, quality_warning:null while its own quality_assurance says 'not assessable — floor-served… no instrument here can say whether this content is good'. ResourceFabric.tsx renders an emerald 'commit-ready' chip directly under the heading 'Modelled & simulated before commit'. The docstring's 'neither blocks nor certifies' is exactly what the green chip does not honour; the grey 'QMS gate: —' beside it does not undo a green certification. Tier 1 stands (borderline with tier 2 because the adjacent chip is honest, but the chip that carries the verdict certifies).
- **refuter's evidence:** POST /api/v1/resources/compose/simulate {name:'r4 probe', resource_ids:['bdp','cognitive_cascade'], usage_area:'synthesis'} → commit_ready true, simulation.quality {served_by:'template', not_assessable:true, qms_gate_passed:null}; with usage_area 'general' it was correctly false (usage-area incompatibility). POST /api/v1/resources/compositions/comp-15fe55d4/run {objective:'halal bakery Leeds'} → run_id cr-a9d920df, commit_ready true, quality_warning null, quality_assurance.quality {served_by:'native', not_assessable:true, qms_gate_passed:null, qms_basis:'not assessable — floor-served…'}. agentic_core/api/resource_fabric.py L1490-1507 (_simulate_configuration, `qms_gate_passed is not False`), L931 (`commit_ready = bool(comp.get('commit_ready', True))`), L1102-1126 (quality_warning None when commit_ready). apps/workstation-superapp/src/pages/synthesis/ResourceFabric.tsx L472-480 chip; L609-610 warning only when quality_warning set.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Make commit_ready tri-state (true/false/null) and render null as a grey 'gate could not assess — commit at your judgement' chip; keep quality_warning non-null on floor-only runs.

### R4.3 · §17.2 Nervous layer — the 'nine cognitive engines' (agentic_core/api/cognitive.py; agentic_core/cognitive/cascade_v16.py, inkashaf_engine.py; fabric resource 'cognitive_cascade' → /api/v1/intelligence/solve) — **DOC_OVERCLAIM** · tier 2

- **claim:** §17.2 'Nervous — the nine cognitive engines … `agentic_core/api/cognitive.py` … (sense → think → decide)'; §7 W446 rule 'never a prompt approximation of a real engine … every composed resource runs its REAL engine'.
- **observed:** POST /api/v1/cognitive/cascade {problem:'halal bakery Leeds'} returned a constant payload: patterns {status:'SUCCESS', insight:'revealed'}, understanding 'grasped', plan 'computed', alignment 0.99, mjm {result:'optimised', compliance:1.0}, engines_run:9, status 'fully_integrated' — inkashaf_engine.py L10-13 returns that literal for any input. No frontend calls /api/v1/cognitive/* (grep: zero consumers), so this stub is API_ONLY. The path a user DOES reach (fabric resource 'cognitive_cascade' → /api/v1/intelligence/solve, run above) produces floor keyword-lists under engine-name headings and reports engines_used [...,'MJM','AIGateway'] — gateway prompts, not engines; the fabric catalogue even describes it as '6 cognitive engines' (GET /api/v1/resources) versus the vision's nine.
- **evidence:** Executed POST /api/v1/cognitive/cascade and the composition run (real_resource_runs[1].engines_used, output headings '## INKASHAF'); read cognitive.py L22-29,64-100, cascade_v16.py L25-45, inkashaf_engine.py L10-20; grep for 'cognitive/cascade' in apps/workstation-superapp/src → none.
- **disclosed to the user:** On the reached path yes-ish: the output carries the floor banner and ResourceFabric chips the trace amber 'structured floor'; nothing says the 'engines' are prompt headings. The constant-payload route is unreached.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. /api/v1/cognitive/cascade returns a constant payload for any input (two different problems differed only in 'problem' and 'elapsed_seconds'); InkashafEngine.unveil_patterns returns the literal {'status':'SUCCESS','insight':'revealed'}; engines_run is hardcoded 6/9. No frontend file calls /api/v1/cognitive/*, so that stub is API_ONLY. The reached path is the fabric resource 'cognitive_cascade' → /api/v1/intelligence/solve, whose engines_used appends 'MJM','AIGateway' by construction (intelligence.py L545) and whose output is floor text under engine-name headings; the catalogue entry is named 'Nine Cognitive Engines + MJM' but describes '6 cognitive engines' in the same record. The vision §17.2 L667-669 names cognitive.py as the nine engines that 'sense → think → decide'. DOC_OVERCLAIM tier 2 is the right call: the floor is disclosed on the reached path, that the 'engines' are prompt headings is not.
- **refuter's evidence:** POST /api/v1/cognitive/cascade {problem:'halal bakery Leeds'} and {problem:'zzz'} → cascade {patterns:{status:'SUCCESS',insight:'revealed'}, understanding:'grasped', plan:'computed', alignment:0.99, status:'fully_integrated'}, mjm {result:'optimised', compliance:1.0}, engines_run 9 — identical bar problem/elapsed. agentic_core/cognitive/inkashaf_engine.py L10-13; agentic_core/api/cognitive.py L64-100 (engines_run = 6 if not include_mjm else 9). grep 'cognitive/cascade|/api/v1/cognitive' across apps/workstation-superapp/src and packages/ui/src → no matches. GET /api/v1/resources → cognitive_cascade {name:'Nine Cognitive Engines + MJM', description:'6 cognitive engines (Inkashaf/Samajh/Soch/Aqal/Hoshiyari/Iman) + MJM meta-judgement.', endpoint:'/api/v1/intelligence/solve'}. Composition run real_resource_runs[1].engines_used ['INKASHAF','SAMAJH','SOCH','AQAL','HOSHIYARI','IMAN','MJM','AIGateway'], output headings '## INKASHAF'. agentic_core/api/intelligence.py L545.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Archive or label agentic_core/cognitive/* + /api/v1/cognitive/* as stubs (P1 principle 10), correct §17.2 to say the nervous 'engines' are prompt roles over the orchestrator, and have engines_used stop listing engine names the floor merely headed.

### R4.4 · §8 Organism overview — blended health shown as the health (OrganismDashboard.tsx:242-266, default tab of /organism from Sidebar 'Organism') — **PARTIAL** · tier 2

- **claim:** §8 'profitability, satisfaction, alignment, compliance are continuously monitored, evaluated'; §15.6 label simulation.
- **observed:** GET /api/v1/organism/status → composite_health 0.875 with composite_health_terms: immune 0.9 measured; self_healing 1.0 'defaulted to 1.0, not a measurement'; metabolic 0.574 'ATPSimulator on a constant metabolic_load — simulated'; composite_health_measured_only 0.9; health_summary 'Organism nominal — … health 88%'. The overview tab renders only the blended 88% and that summary (L262-266); the measured-only figure, the SIMULATED chips and health_basis are rendered on the Anatomy tab alone (OrganismAnatomy.tsx L228-241).
- **evidence:** Executed GET /api/v1/organism/status and /organism/health-summary; read OrganismDashboard.tsx L242-268 (no composite_health_terms/measured_only usage — grep) and OrganismAnatomy.tsx L228-241.
- **disclosed to the user:** Not on the tab a user lands on; yes one tab over.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced structurally; the numbers have moved since the assessor's run but the class is intact. composite_health blends immune (weight 0.4, measured) with self_healing (0.4, 'defaulted to 1.0, not a measurement') and metabolic (0.2, 'ATPSimulator on a constant metabolic_load — simulated'), i.e. 60% of the weight is not measured. OrganismDashboard (the default 'overview' tab of the Organism hub reached from the sidebar) renders only composite_health and health_summary; it never reads composite_health_terms, composite_health_measured_only or health_basis (grep: zero uses). Those are rendered only on OrganismAnatomy. Tier 2 stands: an invisible shortfall on the tab a user lands on, disclosed one tab over.
- **refuter's evidence:** GET /api/v1/organism/status → composite_health 0.96, composite_health_measured_only 0.9, composite_health_terms {immune_health {0.4, 0.9, measured:true}, self_healing_health {0.4, 1.0, measured:false, basis:'no circuits tracked yet — defaulted to 1.0, not a measurement'}, metabolic {0.2, 1.0, measured:false, basis:'ATPSimulator on a constant metabolic_load — simulated…'}}, health_summary 'Organism nominal — ACTIVE_REST cycle, health 96%…'; GET /api/v1/organism/health-summary later read 1.0/1.0 with the same unmeasured terms. apps/workstation-superapp/src/pages/organism/OrganismDashboard.tsx L242-266 (composite_health + health_summary only; grep for composite_health_terms/measured_only/health_basis → none); OrganismHub.tsx L22-33 default tab 'overview'; App.tsx L16-17, Sidebar 'Organism' L93. OrganismAnatomy.tsx L228-241 renders measured-only, 'blended (20% simulated)', health_basis and SIMULATED chips.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Show composite_health_measured_only on the banner with '(blended 88% includes 60% defaulted/simulated)' from the terms the backend already sends.

### R4.5 · §8 Cognitive Introspection page (apps/workstation-superapp/src/pages/cognitive/Introspection.tsx; routed App.tsx:246 '/cognitive-introspection', linked from nowhere) — **STUB** · tier 2

- **claim:** §17.2 cardiovascular = dynamic resource allocation; readings measured; §15.6.
- **observed:** 'System Health 90.00%' is immune.health relabelled (L33 `vitals = { system_health: bio.immune.health }`); the caption 'Apotheosis Architecture v139.0 Active' (L95) names nothing that exists; 'Live Biometrics → Cardiovascular (Flow)' (L105) is app_mvp.py L423 `resource_flow = 100 − psutil cpu` with no label that it is CPU idle. Vision §17.2 already concedes the biometric is 'CPU-idle relabelled' (P2.7) but this page presents it as a live cardiovascular reading. Tier 2 rather than 1 only because no sidebar/dashboard link reaches the route (grep for 'cognitive-introspection' outside App.tsx: none).
- **evidence:** Read Introspection.tsx L25-33, 86-110; app_mvp.py L407-471 (biometrics_status); grep across src for links.
- **disclosed to the user:** No.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. 'System Health' is immune.health relabelled (L57), captioned 'Apotheosis Architecture v139.0 Active' (L95) which names nothing in the codebase; 'Live Biometrics → Cardiovascular (Flow)' (L105) is app_mvp.py's resource_flow = 100 − psutil.cpu_percent with no CPU-idle label. The route exists at App.tsx:246 under a comment '(wired, reachable by URL)' and no sidebar/dashboard/link references 'cognitive-introspection' anywhere else in src or packages/ui, so tier 2 (reachable by URL, not linked) is the right tier — the page pretends, but a user is not steered to it.
- **refuter's evidence:** apps/workstation-superapp/src/pages/cognitive/Introspection.tsx L28 axios.get('/api/v1/biometrics/status'), L57 `const vitals = { system_health: bio.immune.health }`, L94-95 '{(vitals.system_health*100).toFixed(2)}%' + 'Apotheosis Architecture v139.0 Active', L103-106 'Live Biometrics' / 'Cardiovascular (Flow)' = bio.cardiovascular.resource_flow. agentic_core/app_mvp.py L421 psutil.cpu_percent, L427 resource_flow = max(0, 100 − cpu), L471 cardiovascular.resource_flow. GET /api/v1/biometrics/status → cardiovascular.resource_flow 61.0, immune.health 1.0. grep 'cognitive-introspection' across apps/workstation-superapp/src and packages/ui/src → only App.tsx:246.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Relabel 'System Health' → 'Immune health', drop the version caption, caption Cardiovascular as 'CPU idle % (proxy)' — or retire the route (principle 10).

### R4.6 · §6 Native AI mandate — in-house-first posture and floor disclosure (agentic_core/ai/gateway.py:136-166, ai/native/orchestrator.py:84-101, api/native_ai.py:25-90; NativeAI.tsx:545-558; lib/api.ts:65-102) — **DELIVERED**

- **claim:** §6 own swarm·models·orchestration, external providers optional accelerants never dependencies; every floor-served surface discloses it.
- **observed:** GET /native-ai/status: posture 'in-house-first', external_allowed false, selection_order ['native'], floor_active true with floor_active_basis (first 'no successful completion recorded yet; predicted', after one completion 'most recent SUCCESSFUL recorded completion was served by native (one row, at …) — this is history, not a live probe'), mode_measured, floor_note 'NOT an LLM'. gateway.complete_with_provenance routes through the native orchestrator first; anthropic/openai require a key AND AI_ALLOW_EXTERNAL. complete/swarm/tree/ensemble all returned served_by 'native', is_external false; floor serves are recorded (throttled to one row/60 s, documented L84-91) so status cannot resurrect a stale real-model claim. selfcheck 13/13 modules live; model-health rule text explains its own blind spot (probation retry). NativeAI.tsx paints an amber 'Deterministic floor active' card with the basis (data-testid native-status-basis). provenanceBadge labels the floor 'structured floor — not model analysis' in 17 renderers; the three files that bypass it still name the server (NativeAgentPanel 'served by native' + notice L75 'answers come from the deterministic native floor'; QEPStudio floor_served chips and translation refusal; ServiceContracts 'served by …'). Minor: the ensemble card reads 'Ensemble · 1 owned models in parallel → consensus' when only the floor ran and synthesis is null (NativeAI.tsx L699).
- **evidence:** Executed GET status (twice), models, resources, selfcheck, homeostasis, lifecycle, capabilities, operations/model-health; POST complete, swarm, tree, ensemble; read the files cited.
- **disclosed to the user:** Yes.
- **refutation: SURVIVED** (reproduced by the refuter). Verified. Status reports posture in-house-first, external_allowed false, selection_order ['native'], floor_active true with a basis that names its own limit ('this is history, not a live probe'), floor_note 'NOT an LLM'; selfcheck 13/13 modules live; POST complete served_by 'native', is_external false; gateway.complete_with_provenance goes to the native orchestrator first and falls back to native_engine.generate. Disclosure is present on the reached surfaces: NativeAI.tsx 'Deterministic floor active' with data-testid native-status-basis; provenanceBadge ('structured floor — not model analysis') used in 17 files; NativeAgentPanel notice 'answers come from the deterministic native floor'; QEPStudio 'served by {lesson.served_by}' warn chip on floor_served; ServiceContracts 'served by …'. The ensemble-caption nit at L699 is real ('{ensRes.members.length} owned models') but minor.
- **refuter's evidence:** GET /api/v1/native-ai/status (fields quoted: posture, external_allowed, selection_order, floor_active, floor_active_basis 'most recent SUCCESSFUL recorded completion was served by native (one row, at 2026-09-19T04:42:23Z) — this is history, not a live probe…', floor_note, resources[anthropic/openai].note 'only with a key AND AI_ALLOW_EXTERNAL=true'). GET /api/v1/native-ai/selfcheck → total 13, live 13, all_live true. POST /api/v1/native-ai/complete {prompt:'Plan a halal bakery launch'} → served_by 'native', is_external false. agentic_core/ai/gateway.py L136-168 (native orchestrator first, native_engine fallback). apps/workstation-superapp/src/pages/developers/NativeAI.tsx L549, L558 (native-status-basis), L699. src/lib/api.ts L69 provenanceBadge floor label; grep -l provenanceBadge → 17 files. NativeAgentPanel.tsx L75, L202; QEPStudio.tsx L125, L295; ServiceContracts.tsx L300.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None required; optionally have the ensemble card say 'no owned model installed — floor only' when models_run == ['native'].

### R4.7 · §7 Reconfigurable fabric — compose · simulate · run · swarm designer · Forge (agentic_core/api/resource_fabric.py, forge.py; ResourceFabric.tsx; ForgePipeline.tsx) — **PARTIAL** · tier 2

- **claim:** §7 users select, reconfigure, combine, re-run resources with the platform modelling/simulating before commit; every composed resource runs its REAL engine; §6 bespoke per solution/founder.
- **observed:** DELIVERED parts: GET /resources lists engines with reconfigurable_params; simulate → save (comp-15fe55d4) → run applied run_params to both resources and invoked the real endpoints (/api/v1/petri/culture viable:true, /api/v1/intelligence/solve); swarm/define persisted swarm-69f7c9ce and swarm/run traced each stage's served_by; the cascade designer lives on Native AI and ResourceFabric chips every trace through provenanceMapBadge. Shortfalls: (a) POST /forge/run → the Petri Dish stage output opens '_Acting as: Centre of Excellence integrating a Forge pipeline._' — the CoE-integration role string (forge.py L177) is reused as the persona for a Petri stage, so the deliverable mis-states who produced it; (b) ForgePipeline.tsx's RunResult interface (L8) omits ai_provenance and renders no provenance chip although the response carries ai_provenance {served_by:{native:5}} — only the in-text banner discloses the floor; (c) swarm/run reports grounded_in_live_vsb:false, i.e. 'bespoke to each founder' exists only when a VSB is attached.
- **evidence:** Executed GET /resources, /resources/swarm, /forge/resources, /bto/components; POST compose/simulate, compose, compositions/{cid}/run, swarm/define, swarm/run, forge/run (run_id forge-9335015d, pipeline petri_dish→laboratory→factory); read forge.py L117-180, ForgePipeline.tsx L8,80-92, ResourceFabric.tsx L2,473-489,609-697.
- **disclosed to the user:** Mostly — floor banner in every output; the Forge page lacks the chip and the persona line is wrong.
- **refutation: SURVIVED** (reproduced by the refuter). Verdict and tier stand on shortfalls (b) and (c), but shortfall (a) is NOT reproducible and should be struck. My POST /forge/run produced a Petri Dish stage opening '_Acting as: Digital Petri Dish._' (laboratory 'Digital Laboratory', factory 'Digital Factory', CEO 'AI CEO of a VSB'); the floor's _role() takes the FIRST 'You are the|As a …' match in the prompt, and every stage prompt begins 'As a Digital <stage>' (forge.py L44-86), so the CoE string from the integration prompt (L177) cannot lead a stage output — the assessor most likely read the integrated deliverable, where 'Centre of Excellence' is the correct persona. (b) confirmed: ForgePipeline.tsx's RunResult omits ai_provenance and the file has no provenance/served_by rendering, although the response carries ai_provenance {served_by:{native:5}}; only the in-text banner discloses the floor. (c) confirmed: swarm/run reports grounded_in_live_vsb:false. Delivered parts (catalogue with reconfigurable_params, simulate → compose → run invoking real endpoints, swarm define/run with per-stage served_by) verified.
- **refuter's evidence:** POST /api/v1/forge/run {objective:'halal bakery Leeds'} → pipeline petri_dish→laboratory→factory, ai_provenance {posture:'in-house-first', served_by:{native:5}, any_external:false}; stage_outputs[0].output begins '_[Workstation native structured engine …]_ / _Acting as: Digital Petri Dish._'. agentic_core/ai/native/engine.py L90-93 (_role regex, first match), L146-150 lead line; agentic_core/api/forge.py L44-86 stage prompts start 'As a Digital …', L172-180 CoE integration prompt. apps/workstation-superapp/src/pages/synthesis/ForgePipeline.tsx L8 RunResult (no ai_provenance); grep provenance|served_by|Badge in that file → none. POST /api/v1/resources/swarm/run {swarm_id:'swarm-69f7c9ce', objective:'halal bakery Leeds'} → grounded_in_live_vsb false, any_external false, trace [('researcher','native'),('planner','native')]. Composition run cr-a9d920df real_resource_runs hit /api/v1/petri/culture and /api/v1/intelligence/solve (see index 2).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Pass the correct role per Forge stage; add ai_provenance to ForgePipeline's RunResult and render provenanceMapBadge.

### R4.8 · §17.2 The seven biomimetic layers — what is real vs named (agentic_core/organism/*, api/change_control.py, OrganismAnatomy.tsx, OrganismDashboard.tsx:330, AgentHubPanel.tsx) — **PARTIAL** · tier 3

- **claim:** §17.2 Genome · Nervous · Immune · Cardiovascular · Respiratory · Musculoskeletal · Endocrine, with homeostasis loops and reflex/auto-triggered responses.
- **observed:** Real and honest: Genome (encode returned encoded:false, all ten traits 'defaulted', fitness_provenance 'default-unencoded', encoding_note explains the floor cannot declare traits); Nervous bus (131 signals/60 s, buffer scope stated) but reflex_arcs_registered 0 and no register/add_reflex caller anywhere (grep), shown only as '0 reflex arcs' (OrganismDashboard.tsx:330); Immune sensing real, but the defence route POST /cca/immune-reconfigure has no frontend caller (grep: only playbook strings); Self-healing carries health_basis 'no circuit has carried a call yet — nothing measured, health unknown' with overall_health null — DELIVERED honesty; Cardiovascular has no module — homeostasis genuinely caps max_parallel (3 → 1 'protected') and travels with every run, while the named biometric is CPU idle; Respiratory: Agent Hub says 'Records only — no executor is subscribed' (AgentHubPanel.tsx:134,214); Musculoskeletal: facilities run real endpoints; Endocrine: biobus imported by 10+ modules (real bus); the PID regulator §17.2 mentions exists only in biomimicry/cycles/* and is imported by no organism module. The Anatomy tab never names Cardiovascular/Respiratory/Endocrine/Musculoskeletal (grep across pages: only BiometricStatus dots, AgentHubPanel, Introspection) — the seven-layer anatomy is not presented as seven layers.
- **evidence:** Executed GET /organism/status, /systems, /self-healing/status, /nervous/status, /genome, POST /genome/encode, GET /heartbeat/status, /vbs/backbone/health (latency 'SIMULATED transport', failure_rate null with basis); greps cited.
- **disclosed to the user:** Largely yes at the API and Anatomy/Agent-Hub surfaces; the 'seven layers' framing and the un-fired reflex/defence capabilities are simply absent rather than misrepresented.
- **refutation: SURVIVED** (reproduced by the refuter). Verified on every point. Nervous bus is real (signal counts, buffer scope stated) but reflex_arcs_registered is 0 and NervousSystem.register_reflex (nervous.py L75) has no caller anywhere in agentic_core; the immune defence route /cca/immune-reconfigure has no frontend caller; self-healing honestly reports overall_health null with basis; genome encode returns encoded:false with all ten traits 'defaulted' and fitness_provenance 'default-unencoded'; Agent Hub states 'Records only — no executor is subscribed'; the PID regulator exists only under biomimicry/ (cycles/*, geospheric/regulator.py) and no organism module imports agentic_core.biomimicry.cycles; the Anatomy page's systems section is 'immune · nervous · self-healing' plus genome — Cardiovascular/Respiratory/Endocrine/Musculoskeletal are never named there. The vision §17.2 realisation map itself concedes most of this, so tier 3 (disclosed/not-reached capability gap) is right.
- **refuter's evidence:** GET /api/v1/organism/nervous/status → reflex_arcs_registered 0, signals_last_60s 22, total_signals {reflex:176, cognitive:692, motor:647, sensory:25}, scope 'in-memory, this server process since start'. grep register_reflex|add_reflex in agentic_core → only the def at organism/nervous.py:75. grep 'immune-reconfigure' in apps/workstation-superapp/src + packages/ui/src → none. GET /api/v1/organism/self-healing/status → overall_health null, health_basis 'no circuit has carried a call yet — nothing measured, health unknown'. POST /api/v1/organism/genome/encode {entity_name:'r4probe', domain:'food'} → encoded false, trait_provenance.defaulted = all 10 traits, fitness_provenance 'default-unencoded (no FITNESS line parsed)'. AgentHubPanel.tsx L134, L214. grep PID in agentic_core/organism → none; PID only in agentic_core/biomimicry/cycles/*.py and geospheric/regulator.py; grep 'from agentic_core.biomimicry.cycles' in agentic_core/organism/*.py → none. OrganismAnatomy.tsx L266 comment 'Systems: immune · nervous · self-healing'; grep cardiovascular|respiratory|endocrine|musculoskeletal in that file → none. docs/WORKSTATION_IDBO_WHOLE_VISION.md L680-694 (realisation map concedes reflex 0, defence route uncalled, PID unimported).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add a seven-layer panel on Anatomy that maps each layer to its live status incl. 'reflex arcs: 0 registered — auto-response cannot fire' and 'immune defence: no caller'.

### R4.9 · §8 Sovereign Evolution cycle + heartbeat 'Realisation' (agentic_core/api/sovereign_evolution.py:37,125; SovereignEvolution.tsx:173-195; organism/heartbeat.py:294-297; HeartbeatMonitor.tsx:99) — **PARTIAL** · tier 2

- **claim:** §8 the organism delivers its own transformation through AI CEO → C-Suite verdicts → CoE/BTO roadmap; readings measured.
- **observed:** POST /sovereign-evolution/cycle → ceo_directives=[{title:'Routine organism health review', verdict:'proceed', rationale:'No parseable CEO directives; defaulting to a maintenance sweep.'}] and bto_roadmap a floor keyword narrative. The API calls gateway.query (no provenance) and the page renders `d.verdict ?? 'proceed'` (L180) and the roadmap text (L195) with no served_by chip — the C-Suite 'verdict' is a default, disclosed only in the small rationale line; the floor is disclosed only by the in-text banner. Heartbeat: 'Realisation 89%' (tone='good', L99) is transformation._realise (L142-155) — the fraction of pillar evidence checks (route existence / store counts) met — with no caption saying what it measures; beats also carry health 1.0 (immune only, heartbeat.py L184-187) beside the organism's 0.875 blended figure.
- **evidence:** Executed POST /sovereign-evolution/cycle (cycle_id 7971b90c7d), GET /sovereign-evolution/status, GET /heartbeat/status (last_realisation 0.894, recent[0].health 1.0); read the files cited.
- **disclosed to the user:** Partly (rationale text, floor banner); not for what 'Realisation' measures.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced, with two corrections to the observed text. The cycle's ceo_directives is the maintenance default (rationale 'No parseable CEO directives; defaulting to a maintenance sweep.', verdict 'proceed'); bto_roadmap is floor text ('_Acting as: Centres of Excellence._'); the response has no served_by/provenance field because the API uses gateway.query (L125) and the page renders `d.verdict ?? 'proceed'` and the roadmap text with no provenance chip. Heartbeat: 'Realisation' is transformation._realise — the fraction of pillar evidence checks (route existence / store counts) met — shown tone='good', and it now reads 100% (last_realisation 1.0) on the same day this audit finds tier-1 defects. Correction 1: HeartbeatMonitor.tsx L71 does say the beat is 'ticking vision-realisation', so a caption exists — but nothing says what that number measures, so the invisible-shortfall tier holds. Correction 2: beat health is immune-only (heartbeat.py L184-187) — confirmed, recent[0].health 1.0. Tier 2 stands; I considered upgrading the 100% Realisation to tier 1 but it is a defined internal proxy with a (thin) label rather than a fabricated figure.
- **refuter's evidence:** POST /api/v1/sovereign-evolution/cycle {} → keys [cycle_id, created_at, duration_ms, introspection, ceo_directives, bto_roadmap, curated_by, items_proceeding, change_control_submissions] (no served_by/provenance); ceo_directives [{id:'dir-8bfcf0e5', function:'maintenance', owner:'COO', title:'Routine organism health review', rationale:'No parseable CEO directives; defaulting to a maintenance sweep.', verdict:'proceed'}]; bto_roadmap begins '_[Workstation native structured engine …]_ / _Acting as: Centres of Excellence._ / ## CoE Quality Gate'. agentic_core/api/sovereign_evolution.py L125 gateway.query, L210 default rationale. apps/workstation-superapp/src/pages/…/SovereignEvolution.tsx L180 `{d.verdict ?? 'proceed'}`, L195 roadmap text; grep served_by|provenance in file → none. GET /api/v1/heartbeat/status → last_realisation 1.0, recent[0] {beat:123, realisation:1.0, health:1.0, actions:[pulse, homeostasis, genome_scan, transformation_tick]}. agentic_core/organism/heartbeat.py L184-187 (health = immune.status().health), L294-297 (_realise().overall_realisation); agentic_core/api/transformation.py L143-155 (_realise: checks of routes/evidence counts → frac met). HeartbeatMonitor.tsx L71 caption 'ticking vision-realisation', L99 Metric 'Realisation' tone='good', L179 per-beat emerald %.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Return served_by from the cycle and chip it via provenanceBadge; render a defaulted directive as 'default sweep — no directive parsed' instead of a verdict; caption Realisation as 'vision-evidence checks met (routes/stores), not delivery'.

---

## R5 — §1–§3, §3A, §9, §14, §15, §17.1 — the offerings, the avatar/UX, democratisation, the founding principles, the 4×6×4 grid

**Assessor's region summary:** R5 (§1–§3A, §9, §14, §15, §17.1) at HEAD cdd7619f, assessed against the live backend on :8083 (AUTH off, AI_DISABLE_LOCAL=1 → every model call floor-served) and the SuperApp source; no browser was driven, so what 'a user sees' is reasoned from the components named. ONE Tier-1 truth defect remains on a reached surface: the marketplace (/marketplace) counts and labels 'enterprise-file-hub' as one of '5 live products' with an open link to /text-index, a path no <Route> serves — the user lands on the SPA's not-found element; the fix is a one-line ROUTE_OVERRIDES change plus a route-existence guard. Three Tier-2 invisible shortfalls, all in the avatar/floor class: (1) with an image attached and no vision model, the honest 'image received but not analysed' sentence the vision promises is built into the prompt, dropped by the floor, and never rendered although image_understood:false is returned; (2) the avatar's floor answer addresses its own persona line rather than the user's message, and on one call its 'Key factors' were terms recalled from an unrelated Law request via gateway memory recall — labelled only by the amber floor badge; (3) the Law Document Analyser, Application Studio generate and (to a lesser, better-disclosed degree) the Tafsir tool emit 'risks'/'key factors' drawn from their prompt templates, not the user's document, disclosed by the badge and disclaimer but not in the body. Verified DELIVERED: the Care clinical scores (NEWS2 6/medium and MUST 3/high computed correctly from the published tables, incomplete inputs refused as a lower bound, score rendered first); the compliance-frameworks card (each engine states its mechanism and limit); the employment job search (illustrative, zero results under the floor with the exact reason); the 26-tool single registry with every hub endpoint live and the front door counting from it; the QEP status/surah honesty; voice gating (browser-native in-house, external 503 honest); the explicit user profile; and the W473 rewrite of both docs/compliance mandate pages into honest inventories. Disclosed capability gaps (Tier 3): the Products axis of the 4×6×4 grid has no code and configs/realms.yaml is orphaned drift (both recorded in §17.1 with the P3.0 ruling); 'all languages' is five partial UI dictionaries with an honest English-fallback notice; only Employment exposes a /services route. Net: Phase P1 'truth first' holds except for the marketplace's phantom live route and the avatar's undisclosed non-analysis of attached images.

### R5.0 · §3A offering 1 / §17.1 — marketplace catalogue (/marketplace, LivingMarketplace.tsx) — **STUB** · tier 1

- **claim:** The marketplace counts only what is served: 'Only an entry marked live opens to a served surface on Workstation's own native AI fabric' (LivingMarketplace.tsx:178-179); catalogue status 'live' = 'a served surface in the SuperApp (route set)' (agentic_core/catalog/api.py:28-30).
- **observed:** GET /api/v1/catalog/products returns counts {registered:20, live:5, source:9, legacy:6}; the five 'live' entries are capital_fund→/capital, digital_reactor→/reactor, enterprise-file-hub→/text-index, OctoVeritasEngine→/constitution, qep-sdk→/qep-religion. Four of those routes exist in App.tsx (lines 195, 203, 227, 183). /text-index exists NOWHERE in apps/workstation-superapp/src (grep over App.tsx, Sidebar.tsx and all pages: zero hits); the SPA renders its path="*" not-found element (App.tsx:251) for it. The marketplace header prints 'Sovereign Economy · 5 live products' (LivingMarketplace.tsx:175) and renders an open link for any product with live && route (:306), so a user is told enterprise-file-hub is a served product and is sent to a blank not-found page.
- **evidence:** Executed GET http://127.0.0.1:8083/api/v1/catalog/products → item {slug:'enterprise-file-hub', status:'live', live:true, route:'/text-index'}; curl http://127.0.0.1:8083/text-index → 200 (SPA catch-all serving index.html, not a page); ROUTE_OVERRIDES hard-codes the route at agentic_core/catalog/api.py:14-20; App.tsx has no <Route path="/text-index">.
- **disclosed to the user:** No — the surface certifies 'live' and counts it in '5 live products'.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced exactly. The catalogue certifies enterprise-file-hub as 'live' with route /text-index; the SPA has no such route and renders its 'Page Not Found' catch-all; the marketplace counts it in '5 live products' and renders an Open button for it. A reached surface tells the user a product is served when nothing is.
- **refuter's evidence:** Executed GET http://127.0.0.1:8083/api/v1/catalog/products → counts {registered:20, live:5, source:9, legacy:6}; live items: capital_fund→/capital, digital_reactor→/reactor, enterprise-file-hub→/text-index, OctoVeritasEngine→/constitution, qep-sdk→/qep-religion. curl /text-index → HTTP 200 (SPA index.html). grep -rn 'text-index' over apps/workstation-superapp/src → zero hits; App.tsx lines 159-246 list every <Route> (capital 195, reactor 203, constitution 227, qep-religion 183; no /text-index); App.tsx:251-256 renders 'Page Not Found — Navigate using the sidebar'. ROUTE_OVERRIDES hard-codes the route at agentic_core/catalog/api.py:14-20; _status() (api.py:35-38) marks any entry with a route as 'live'. LivingMarketplace.tsx:175 prints '{catalogProducts.filter(p => p.live).length} live products' and :306-313 renders 'Open' for product.live && product.route.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Drop 'enterprise-file-hub' from ROUTE_OVERRIDES (status falls to 'source' by _status at catalog/api.py:35-38, count becomes 4 live) or route a real page; add a guard test that every ROUTE_OVERRIDES value matches a <Route path> in App.tsx (the W470 test evidently checks counts, not route existence).

### R5.1 · §9 — multimodal avatar, image understanding (components/avatar/*, hooks/useAvatarSession.ts, agentic_core/avatars/api.py) — **PARTIAL** · tier 2

- **claim:** §9 W53/E6-E7: image understanding runs on an owned vision model when provisioned, an external accelerant if opted in, 'and otherwise an honest "image received but not analysed" — never a fabricated description'.
- **observed:** With no vision model (this environment), the backend composes the honest note only INTO THE PROMPT (api.py:338-346, 'Image analysis: (Image attached and received, but no in-house vision model … so its contents were not analysed.)'). The deterministic floor discards it: the returned response text contains no mention of the image at all (it is the generic 'Understanding: The request concerns: You are the Workstation Sovereign Mesh avatar…' scaffold). The response does carry image_understood:false / image_served_by:null, but the hook stores only served_by, is_external and suggested_areas (useAvatarSession.ts:233-238) and ConversationPanel.tsx:91 renders just the user's thumbnail. So a user who attaches an image sees their picture, an amber 'structured floor — not model analysis' badge, and an answer that never says the image was not looked at. Nothing is fabricated (no description is invented), which keeps this out of tier 1.
- **evidence:** Executed POST /api/v1/avatar/chat {message:'What is in this image?', context:'general', image_base64:<1px PNG>} → {image_understood:false, image_served_by:null, image_is_external:false, served_by:'native'}; response text checked: 'not analysed' absent. Read agentic_core/avatars/api.py:294-346, hooks/useAvatarSession.ts:222-246, components/avatar/ConversationPanel.tsx:88-100.
- **disclosed to the user:** No at the conversation surface (the badge says the TEXT is floor-served; nothing says the IMAGE was not analysed).
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. With no vision model the honest 'not analysed' note is composed only into the prompt, and the deterministic floor's output never carries it; the response's image_understood:false / image_served_by:null are dropped by the avatar hook, so the ConversationPanel shows the thumbnail, an amber floor badge for the TEXT, and an answer that never says the image was not looked at. Nothing is fabricated → tier 2 stands. Additional evidence: the VSB Cockpit chat is worse on promise — its attach label reads 'analysed by the in-house vision model' and it renders a 'vision ·' badge only when image_served_by is non-null, so with null nothing about the image appears either.
- **refuter's evidence:** Executed POST /api/v1/avatar/chat {message:'What is in this image?', context:'general', image_base64:<1px PNG>} → {image_understood:false, image_served_by:null, image_is_external:false, served_by:'native', language:null}; response text = the generic '## Understanding — The request concerns: You are the Workstation Sovereign Mesh avatar…' scaffold; 'not analysed' absent, the word 'image' absent. Read agentic_core/avatars/api.py:294-346 (image_note built into prompt only), hooks/useAvatarSession.ts:233-238 (stores servedBy, isExternal, suggestedAreas only), components/avatar/ConversationPanel.tsx:91 (renders m.imageDataUrl thumbnail), grep 'image_understood' over src → only VSBCockpit.tsx:29,241 (stored) with render at :918-919 gated on m.image_served_by; VSBCockpit.tsx:937 and :967 promise 'analysed by the in-house vision model'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** In the hook, when resp.data.image_understood === false and an image was sent, prepend/append a fixed line to the assistant message ('Image received but not analysed — no vision model is provisioned') — the field is already returned; or have the API append image_note to response_text when served_by == 'native'.

### R5.2 · §9 — enterprise-aware avatar, guidance/navigation (avatar chat under the floor + native memory recall) — **PARTIAL** · tier 2

- **claim:** §9: multimodal interaction with the AI CEO and the Chief, guidance/navigation through any platform area; the avatar 'answers IN its context' (enterprise-aware).
- **observed:** (a) Navigation is real: every chat returns suggested_areas keyed on the message ('export'→/deliverables, 'compliance'→/governance-hub, 'establish'→/genesis) and ConversationPanel.tsx:97-100 renders them as 'Take me there' chips. (b) VSB grounding is real: chat with vsb_id 'vsb-782577eb83' returned grounded_in set and the floor's key factors were the VSB's own mission terms ('beekeepers yorkshire', 'lose hives'). (c) But under the floor the answer body never addresses the user's message: '## Understanding — The request concerns: You are the Workstation Sovereign Mesh avatar — a helpful, concise assistant…' — the floor takes the system prompt's first line as the request. (d) On the FIRST avatar call the '## Key factors' list contained 'refusing overtime / employer dismiss / dismiss employee' — the salient terms of an unrelated /api/v1/law/research call I had made minutes earlier, pulled in by gateway._augment's memory recall (gateway.py:93-108; owner_id None in single-user mode → the platform namespace) and presented as key factors of the avatar question. Not cross-tenant here (AUTH off = one user), but the answer misattributes another request's terms without any recall label reaching the text. Disclosed only by the amber 'structured floor — not model analysis' badge.
- **evidence:** Executed POST /api/v1/avatar/chat ×4 (general with/without image, context 'vsb' with vsb_id, 'How do I export my lesson plan as a PDF?'); responses quoted above; read agentic_core/avatars/api.py:279-385, agentic_core/ai/gateway.py:93-108 and 128-146, components/avatar/ConversationPanel.tsx:88-105.
- **disclosed to the user:** Partly — the amber badge (provenanceBadge('native') at lib/api.ts:69-70) labels the text as floor, not model analysis; the recalled-terms misattribution is not labelled anywhere the user sees.
- **refutation: SURVIVED** (reproduced by the refuter). Core reproduced: navigation chips are real and message-keyed; under the floor the answer body never addresses the user's message — the 'Understanding' section quotes the system prompt's first line as the request. One sub-claim NOT reproduced: I could not make the memory-recall misattribution happen — after a law/research call with distinctive terms (zebra/quokka/pension trustee), an avatar chat on the same terms returned only the system-prompt key factors, no recalled terms, no recall label. The assessor's observation may have depended on prior memory state; treat that sentence as unverified, not as a shown defect. Tier 2 stands on the verified part (badge discloses floor, but nothing discloses that the 'request concerns' line is not the user's request).
- **refuter's evidence:** Executed POST /api/v1/avatar/chat {message:'How do I export my lesson plan as a PDF?'} → suggested_areas [{route:'/domains', because:'matched: lesson'}, {route:'/deliverables', because:'matched: export'}]; response '## Understanding — The request concerns: You are the Workstation Sovereign Mesh avatar — a helpful, concise assistant…' with key factors 'workstation sovereign / concise assistant / …'. Then POST /api/v1/law/research {question:'Can a zebra pension trustee be personally liable for quokka investment losses?', area_of_law:'trusts'} → 200 (key factors 'trustee personally / quokka investment / pension trustee'); then POST /api/v1/avatar/chat {message:'zebra quokka pension trustee, where do I start?'} → text contains neither 'zebra' nor 'quokka' nor 'recall'. Read agentic_core/ai/gateway.py:93-108 (_augment labels recall '[native memory recall …]'), avatars/api.py:358 (query_meta default augment=True), ConversationPanel.tsx:93-105 (badge + 'Take me there' chips), lib/api.ts:65-72 (amber 'structured floor — not model analysis').
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Pass augment=False for the avatar's floor path (as W332 did for generation callers) or make the floor render the recall block's own '[native memory recall …]' label; and have the floor's 'Understanding' take the 'User:' line rather than the persona line for avatar:* agents.

### R5.3 · §3A offering 1 — Law Document Analyser, Application Studio, Qur'anic Tafsir under the floor (LawHub.tsx, ApplicationStudio.tsx, ReligionHub.tsx) — **PARTIAL** · tier 2

- **claim:** §3A: 'immediate, usable, in-house-AI-mediated working capabilities (research · analyse · generate …)'; LawHub copy: 'Workstation's own AI runs a structured England & Wales analysis (key clauses, risks, missing provisions, recommendations)'.
- **observed:** For tools whose prompt places the output template BEFORE the user's input, the floor's salient-term extraction keys on the template, not the document. POST /api/v1/law/analyse with a 3-sentence contract (Acme Ltd, £500/day, 7 days' notice) returned '## Legal Risks — Exposure on england wales · Exposure on provisions recommendations · Exposure on solicitor specialising'; not one word of the user's document appears in the analysis. POST /api/v1/career/generate (Application Studio) → 'Key factors: markdown format, ats-optimised, education certifications' (template words). POST /api/v1/religion/quran-tafsir → the study frame's bullets are 'scholarly interpretations / verses cross-references' (its own template), though here the authentic Arabic is real (alquran.cloud) and the response carries floor_note + sections_withheld ['Transliteration','Translation'] + a disclaimer, which DomainTool renders above the text (DomainTool.tsx:58-64, 113-121). By contrast employment/cv and law/research did extract the user's terms. Disclosure at the surface: LawHub.tsx:124 renders the amber badge, :127 the disclaimer; every response's header line reads '_[Workstation native structured engine — owned, no external dependency]_' and the risk frame says 'surface, don't invent'. A reader who does not read the badge sees 'Legal Risks' with items that look like findings about their contract.
- **evidence:** Executed POST /api/v1/law/analyse, /api/v1/career/generate {output_types:['cv']}, /api/v1/religion/quran-tafsir {surah:1, ayah_start:1}, /api/v1/employment/cv, /api/v1/law/research, /api/v1/science/hypothesis, /api/v1/education/lesson-plan — all served_by 'native', qms_gate_passed null with the honest 'not assessable — floor-served' basis; read LawHub.tsx:20-30 and 120-128, components/DomainTool.tsx:58-64, 113-121, 239-250, lib/api.ts:65-72.
- **disclosed to the user:** Yes at badge/disclaimer level (amber 'structured floor — not model analysis', QMS '—' not-assessable chip); the tafsir additionally states in-text what is withheld. Not disclosed: that the 'risks' listed are template vocabulary rather than anything read from the document.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. Law analyse and career/generate key on their own template vocabulary rather than the user's document; the Executive Summary even opens 'Subject: Structure your analysis with these sections: ## Executive Summary ## Key Clauses…' — the template leaked into the summary. Tafsir does state what is withheld (floor_note + sections_withheld rendered in ReligionHub). The amber badge and disclaimer are rendered on LawHub, so the shortfall is disclosed at badge level but not that the listed 'risks' are template words; tier 2 stands.
- **refuter's evidence:** Executed POST /api/v1/law/analyse {document_text:'This Consultancy Agreement is between Acme Ltd and Jane Doe. Acme pays £500 per day. Either party may terminate on 7 days notice.', document_type:'contract'} → analysis '## Legal Risks — Exposure on england wales · Exposure on provisions recommendations · Exposure on solicitor specialising · Exposure on comprehensive analysis'; no 'Acme', '£500' or '7 days' anywhere; served_by 'native', qms_gate_passed null with the 'not assessable — floor-served' basis. POST /api/v1/career/generate {output_types:['cv'], profile:{summary:'Data analyst with 5 years SQL experience at Tesco'}} → key factors 'markdown format / professional ats-optimised / education certifications'. POST /api/v1/religion/quran-tafsir {surah:1, ayah_start:1} → arabic_text sourced from alquran.cloud, sections_withheld ['Transliteration','Translation'], floor_note present, disclaimer present. Contrast: law/research (above) did key on the user's terms. Read LawHub.tsx:20-30 and 120-128 (provenanceBadge + disclaimer rendered), ReligionHub.tsx:153-155 (floor_note + 'Withheld on the floor'), DomainTool.tsx:239-250, 274, lib/api.ts:65-72.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** For floor-served law/analyse and career/generate, emit an explicit line in the body ('The floor did not read your document; the items below are a structural frame') as the tafsir already does via floor_note, or order the prompt so the user's text precedes the template so the frame at least reflects the user's own terms.

### R5.4 · §3A / §15 principle 6 — Care domain clinical scores (CareHub.tsx risk tab, agentic_core/care/scoring.py) — **DELIVERED**

- **claim:** Never fabricate; a validated tool is scored 'from its published table (NEWS2 · MUST · Waterlow · NICE CG161 factor count)' and shown as a decision aid.
- **observed:** NEWS2 for RR 22 / SpO2 94 (scale 1) / air / SBP 105 / HR 98 / Alert / 38.2°C → components {2,1,0,1,1,0,1}, total 6, band 'medium', the RCP 'key threshold for urgent response' text — arithmetic matches the 2017 table. MUST for BMI 17.5 / 8% loss / no acute → {2,1,0} = 3 'high', matches BAPEN. With observations missing the scorer refuses a total ('no total — missing …', complete:false, a lower-bound note) rather than guessing; scale-2 SpO2 ≥93% without an air/oxygen flag is refused explicitly (scoring.py:121-123). CareHub.tsx:73-90 renders the computed score FIRST with table, basis, components, the 'Incomplete — the total is a lower bound' warning, and the floor's narrative below it as interpretation.
- **evidence:** Executed POST /api/v1/care/risk-assess {tool:'news2', patient_data:{…}} and {tool:'must', …} and one deliberately mis-keyed call; GET /api/v1/care/tools → 8 tools with standards; read agentic_core/care/scoring.py:60-140, agentic_core/api/care.py:98-135, CareHub.tsx:66-104.
- **disclosed to the user:** Yes — 'computed in-house from the published table — a decision aid, not a diagnosis' on every score.
- **refutation: SURVIVED** (reproduced by the refuter). Verified by execution; arithmetic matches the published tables and the scorer refuses a total when observations are missing. The CareHub renders the computed score first with basis, components and the incomplete warning.
- **refuter's evidence:** Executed POST /api/v1/care/risk-assess {tool:'news2', patient_data:{respiratory_rate:22, spo2:94, spo2_scale:1, on_oxygen:false, systolic_bp:105, heart_rate:98, consciousness:'Alert', temperature:38.2}} → components RR 2, SpO2 1, air 0, SBP 1, pulse 1, alert 0, temp 1, total 6, band 'medium', complete:true, basis 'computed in-house from the published table — a decision aid, not a diagnosis' (RCP NEWS2 2017: RR 21-24=2, SpO2 94-95=1, SBP 101-110=1, HR 91-110=1, T 38.1-39.0=1, total 5-6 = medium). {tool:'must', patient_data:{bmi:17.5, weight_loss_percent:8, acute_disease:false}} → {2,1,0} total 3 band 'high' (BAPEN). {tool:'news2', patient_data:{respiratory_rate:22}} → total 2, band null, complete:false, missing [spo2, oxygen, systolic_bp, pulse, consciousness, temperature], note 'INCOMPLETE: the total is a lower bound'. Read CareHub.tsx:66-100 (score block rendered first, 'Incomplete — missing …' warning, table · basis line).
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None needed.

### R5.5 · §15 principle 6 — spot-checks: compliance frameworks card, employment job search, mandate pages — **DELIVERED**

- **claim:** Never fabricate; label simulation. The compliance card says what each framework checks; job search never presents synthesised listings as real adverts; docs/compliance/ mandate pages claim only what the repo can show.
- **observed:** GET /api/v1/compliance/frameworks returns six frameworks each with an 'engine' string that states the mechanism and its limit ('keyword screen … not legal advice', 'not a certification', 'content is not checked here'); ComplianceChecker.tsx:48-52 renders that string on each card and :40-41 says 'It flags; it does not certify'. POST /api/v1/career/job-search under the floor → results [], total 0, illustrative:true, basis 'AI-synthesised example listings — not a live job board'; ApplicationStudio.tsx:391-440 shows 'not a live job board', 'no sources searched', and the exact floor message 'the deterministic native floor served this search and cannot compose listings'; every listing badge is 'illustrative · no live URL'. docs/compliance/MANDATES.md and MANDATES_FINAL.md (rewritten W473) mark each former VERIFIED/ENFORCED row against its real status (NOT PRESENT / UNWIRED / RETIRED) with file paths; no UI route reads them (grep: zero consumers in src).
- **evidence:** Executed GET /api/v1/compliance/frameworks, POST /api/v1/career/job-search {query:'data analyst'}; read pages/governance/ComplianceChecker.tsx:5-52, components/employment/ApplicationStudio.tsx:200-245 and 391-460, docs/compliance/MANDATES.md:1-80, MANDATES_FINAL.md:1-30.
- **disclosed to the user:** Yes on all three.
- **refutation: SURVIVED** (reproduced by the refuter). Verified by execution and source read: each compliance framework's engine string states its mechanism and limit and is rendered on the card; job search under the floor returns zero listings marked illustrative with the exact floor message shown; the mandate docs are rewritten with real statuses and nothing in the UI consumes them.
- **refuter's evidence:** Executed GET /api/v1/compliance/frameworks → 6 frameworks; engine strings e.g. sharia_halal '… not a certification', uk_legal '… not legal advice', constitutional '… content is not checked here'. ComplianceChecker.tsx:38-41 'It flags; it does not certify', :48-52 renders f.engine per card. POST /api/v1/career/job-search {query:'data analyst'} → results [], total 0, illustrative:true, sources_used [], basis 'AI-synthesised example listings — not a live job board…'. ApplicationStudio.tsx:397 'not a live job board', :418 'no sources searched', :427 'the deterministic native floor served this search and cannot compose listings', :447 'illustrative · no live URL'. docs/compliance/MANDATES.md has 12 NOT PRESENT/UNWIRED/RETIRED marks, MANDATES_FINAL.md 4; grep 'MANDATES' over src → zero consumers.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None needed.

### R5.6 · §3A offering 1 / §17.1 — the six Domains' tool sets (toolRegistry.ts, the six *Hub.tsx pages, /api/v1/{religion,science,education,law,employment,care}/*) — **PARTIAL** · tier 3

- **claim:** Each Domain offers real in-house-AI-mediated tools of comparable scope; the front door counts what is mounted.
- **observed:** Wiring is DELIVERED: the one registry holds 26 tools (Religion 5 incl. the QEP flagship, Science 3, Education 4, Law 3, Care 4, Employment 7); DomainsHub.tsx:27,49 and AIToolsCatalogue.tsx:17 derive the count from TOOL_TOTAL; every hub endpoint (religion/fatwa-research, quran-tafsir, halal-review, hadith-study; science/synthesise, experiment-design, literature; education/lesson-plan, curriculum, assessment, feedback; law/analyse, generate, research; care/care-plan, risk-assess, safeguarding, handover; employment/cv, cover-letter, application, interview-prep, career-path, salary-negotiation; career/generate) is present in openapi.json (441 paths) and answered when executed with its schema. Reference lists are real data (religion/schools 4 madhabs, science/methodologies, law/templates, education/frameworks, care/tools 8). Shortfalls: only Employment has a /services route (the other five 404 on /services — the task's assumed route, not a UI-reached one); under AI_DISABLE_LOCAL every generative tool is the deterministic scaffold, disclosed by the amber badge + QMS '—'; the hubs' pre-submit copy ('Workstation's own AI synthesises a structured, methodology-grounded evidence report') promises model output the floor cannot give, and the reader learns that only after submitting. Science's hypothesis route and Religion's interfaith route are not in the registry (API_ONLY, harmless). QEP: /api/v1/qep/status states honestly 'NO recitation assessment', text from alquran.cloud, and /qep/surah/1 returned the sourced Arabic.
- **evidence:** Executed the 6 /services routes (5×404, employment 6 services), one or two tools per domain (listed above), GET /api/v1/qep/status, /qep/surah/1, /religion/schools, /science/methodologies, /law/templates, /education/frameworks, /care/tools; read lib/toolRegistry.ts (68 lines), each hub's endpoint= lines, pages/domains/DomainsHub.tsx:6-49, pages/AIToolsCatalogue.tsx:5-17.
- **disclosed to the user:** Yes after a run (badge, QMS chip, floor_note where present); not before a run (hub copy speaks of model synthesis unconditionally).
- **refutation: SURVIVED** (reproduced by the refuter). Verified: one registry with 26 tools (Religion 5 incl. QEP flagship, Science 3, Education 4, Law 3, Care 4, Employment 7), both counting surfaces derive from TOOL_TOTAL, openapi lists 441 paths, only Employment has /services (not a UI-reached route). The hubs' pre-submit copy ('Workstation's own AI designs a rigorous study / synthesises a structured, methodology-grounded evidence report') promises model output the floor cannot give; disclosed only after a run by the amber badge and QMS chip. Tier 3 stands because the shortfall is disclosed on every result.
- **refuter's evidence:** Read lib/toolRegistry.ts:20-59 (entries counted by hand: 5+3+4+3+4+7 = 26), :61 TOOL_TOTAL; DomainsHub.tsx:6,27 and AIToolsCatalogue.tsx:5,17 import TOOL_TOTAL. Executed GET /{religion,science,education,law,care}/services → 404 each, /employment/services → 200; GET /openapi.json → 441 paths; POST law/analyse, career/generate, religion/quran-tafsir, law/research, career/job-search, care/risk-assess all answered with served_by 'native'. GET /api/v1/qep/status → tajweed 'NO recitation assessment', quran_text 'alquran.cloud … never AI-generated'. ScienceHub.tsx:54,69,82 pre-submit copy quoted above; DomainTool.tsx:239-250 renders provenanceBadge + qmsChip after a run.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Have DomainTool read /api/v1/avatar/status-style posture once and show a one-line 'served by the structured floor today — no owned model is provisioned' notice above the form when native-only, so the promise and the badge agree before the user types.

### R5.7 · §17.1 — the 4 Realms × 6 Domains × 4 Products grid (agentic_core/taxonomy.py, lib/taxonomy.ts, configs/realms.yaml, Settings.tsx) — **DOC_OVERCLAIM** · tier 3

- **claim:** 'The product grid — 4 Realms × 6 Domains × 4 Products (96 combinations), one lifecycle pattern.'
- **observed:** Two axes are real and exposed: REALMS and DOMAINS are the single source in taxonomy.py:11-12 (mirrored in lib/taxonomy.ts:5), Settings offers 'Default realm' and 'Default domain' selects (Settings.tsx:82-92), and realm_directive() (taxonomy.py:36-70) genuinely changes the register of generation. The Products axis has no code realisation: grep for PRODUCTS in taxonomy.py and lib/taxonomy.ts → nothing; Reactor/Incubator/Factory/Laboratory exist only as fabric facilities (resource_fabric.py:114-160) and developer pages, never as a picker. No surface shows a 96-cell grid. configs/realms.yaml still lists six domain-shaped 'realms' (bio, legal, climate, materials, religion, education) and is read by nothing in agentic_core/ or apps/ (grep: only docs mention it). The vision's own §17.1 paragraph already records this gap with an Owner ruling (P3.0), so it is a disclosed capability gap, not a surface lie.
- **evidence:** Read agentic_core/taxonomy.py:1-120, apps/workstation-superapp/src/lib/taxonomy.ts:1-6, configs/realms.yaml (13 lines), pages/Settings.tsx:70-100; grep -rn PRODUCTS / realms.yaml over agentic_core, apps, configs (code hits: none).
- **disclosed to the user:** Not applicable to end users (no surface claims the grid); disclosed in the canon itself.
- **refutation: SURVIVED** (reproduced by the refuter). Verified: Realms and Domains are real single-source axes with a Settings picker; no PRODUCTS constant exists in either taxonomy module; configs/realms.yaml lists six domain-shaped 'realms' and is read by no code; the vision's own §17.1 already records the gap with an Owner ruling (P3.0), so it is a disclosed canon overclaim, not a surface lie.
- **refuter's evidence:** Read agentic_core/taxonomy.py:11-12 (REALMS, DOMAINS), lib/taxonomy.ts:5-6; grep -n PRODUCT over both → zero hits. Settings.tsx:82-92 'Default realm' / 'Default domain' selects over REALMS/DOMAINS. configs/realms.yaml (bio, legal, climate, materials, religion, education); grep 'realms.yaml' over agentic_core and apps (py/ts/tsx) → zero hits. docs/WORKSTATION_IDBO_WHOLE_VISION.md:641-654: 'the Products AXIS has no code realisation — no PRODUCTS constant … OWNER RULING (delivery-plan P3.0)'.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Either add PRODUCTS to taxonomy.py and a picker feeding Genesis, or amend §17.1 to 'design intent' per the P3.0 ruling; delete the orphaned configs/realms.yaml (already scheduled as P2.5).

### R5.8 · §9 / §14 — accessible to all: all languages, adaptive UI, explicit personalisation (lib/i18n.tsx, Settings.tsx, AdaptiveUIProvider.tsx, /api/v1/user/profile) — **PARTIAL** · tier 3

- **claim:** 'Accessible to all — all languages; adaptive and responsive, dynamically and intelligently personalised to each user's instructions, requests, history, preferences, needs and abilities.'
- **observed:** i18n is a dependency-free keyed layer with five dictionaries (en, ar, fr, es, ur — i18n.tsx:18-346) covering chrome strings only, with English fallback and RTL for ar/ur/fa/he; Settings offers 8 languages and, for one without a dictionary, prints in amber 'The interface is not translated into this language yet — it stays in English' (Settings.tsx:119-120); dictation follows prefs.language (useAvatarSession.ts:257-261). Adaptive UI: fontScale/guidedMode/tone are real stored choices applied by AdaptiveUIProvider.tsx:23-44. Explicit profile: GET /api/v1/user/profile returns the five empty fields with applied_to 'generation prompts on this platform (never shared with other users)' and is_recall:false; Settings shows 'Nothing saved — no profile is added to your prompts' (Settings.tsx:202). The avatar's `language` field is honest: null when the floor could not honour it (api.py:381). 'All languages' and 'intelligently personalised … history' are canon overclaims; the surface itself claims only what it does.
- **evidence:** Executed GET /api/v1/user/profile; POST /api/v1/avatar/chat with language pref → language:null; read lib/i18n.tsx:1-60 and 347-368, pages/Settings.tsx:56-222, components/AdaptiveUIProvider.tsx:1-44.
- **disclosed to the user:** Yes — partial translation and floor language limits are stated on the surface.
- **refutation: SURVIVED** (reproduced by the refuter). Verified: five chrome dictionaries with English fallback and RTL for ar/ur/fa/he; Settings prints an amber 'not translated into this language yet — it stays in English' note for a language without a dictionary; adaptive UI is three real stored choices; the profile endpoint reports an empty profile honestly and the avatar reports language:null when the floor could not honour it. 'All languages' / 'intelligently personalised … history' remain canon overclaims that the surface itself does not repeat.
- **refuter's evidence:** Read lib/i18n.tsx:14-16 (RTL set), :326 DICTS = {en, ar, fr, es, ur}, :347-350 coverageFor; Settings.tsx:115-121 (translated-strings count vs amber not-translated note), :202 'Nothing saved — no profile is added to your prompts'; AdaptiveUIProvider.tsx:21-26 (fontScale/guidedMode/tone from prefs). Executed GET /api/v1/user/profile → five empty fields, applied_to 'generation prompts on this platform (never shared with other users)', is_recall:false; every POST /api/v1/avatar/chat above → language:null; avatars/api.py:379-381 sets language only when honoured.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Amend §9's 'all languages' to 'five partial UI dictionaries + English fallback; model-served answers in the requested language only when a model serves' — a canon correction, not a code change.

### R5.9 · §9 — avatar voice and status (/api/v1/avatar/status, /speak, /transcribe; AvatarWidget.tsx, useAvatarSession.ts) — **DELIVERED**

- **claim:** Voice runs in the browser (Web Speech, feature-detected — no fake microphone); external speech is an opt-in accelerant, honestly labelled.
- **observed:** POST /api/v1/avatar/speak → 503 'External voice output is unavailable: requires a configured OPENAI_API_KEY AND AI_ALLOW_EXTERNAL=true … The browser's speechSynthesis is the in-house path — nothing was sent externally'; /transcribe requires a file upload (422 without one). The client uses window.speechSynthesis (hook :166-175) and SpeechRecognition feature-detected (hook :257-261) with the user's language. AvatarWidget renders a real recording/transcribing state, no fake mic. One minor honesty wrinkle: GET /api/v1/avatar/status reports ollama_online:true and openai_key_present:true while AI_DISABLE_LOCAL=1 means neither serves a call; the hook collapses this to 'online' (:126-129) and the key_note says 'presence of the env var only', so nothing false reaches the UI, but an Owner reading the JSON could infer Ollama is serving when the floor is.
- **evidence:** Executed GET /api/v1/avatar/status, POST /api/v1/avatar/session, /speak {text:'hello'}, /transcribe {}; read agentic_core/avatars/api.py:398-420, 460-495, hooks/useAvatarSession.ts:120-130, 160-180, 250-265, components/avatar/AvatarWidget.tsx.
- **disclosed to the user:** Yes.
- **refutation: SURVIVED** (reproduced by the refuter). Verified: external speech is refused with an honest message naming the in-house browser path; transcribe requires a real file; the hook uses window.speechSynthesis and feature-detected SpeechRecognition. The assessor's 'wrinkle' is overstated: ollama_online is a live GET /api/tags probe of the daemon (a true fact about reachability), key fields are labelled 'presence of the env var only', and the hook's 'online' collapse reflects the native floor which does always answer — nothing false reaches the UI.
- **refuter's evidence:** Executed GET /api/v1/avatar/status → {online:true, posture:'in-house-first', native:true, ollama_online:true, openai_key_present:true, anthropic_key_present:false, key_note:'presence of the env var only — validity is only known at call time'}; POST /api/v1/avatar/speak {text:'hello'} → 503 'External voice output is unavailable: requires a configured OPENAI_API_KEY AND AI_ALLOW_EXTERNAL=true … The browser's speechSynthesis is the in-house path — nothing was sent externally'; POST /api/v1/avatar/transcribe (no file) → 422 body.file required. Read avatars/api.py:405-420 (ollama probe via /api/tags; env-presence comment), hooks/useAvatarSession.ts:127-129, 166-175 (speechSynthesis), 257 (SpeechRecognition || webkitSpeechRecognition).
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** Add a `local_disabled: bool` (from AI_DISABLE_LOCAL) beside ollama_online in /avatar/status so the status cannot be read as 'Ollama is serving'.

---

## R6 — §8 + §12 + §17.5 — the biomimetic living organism, the economic organism, the ten architecture invariants

**Assessor's region summary:** R6 (§8 organism · §12 economy · §17.5 invariants), assessed against the LIVE backend at :8083 (DATA_DIR C:/tmp/w474b) by executing routes and reading handlers/pages; the UI was reasoned from source, not driven. The organism does run itself: the heartbeat beat 1→4 unattended (200 s cadence in MAINTENANCE_REST), homeostasis adjusts, the SE office cycles with its floor content plainly marked, immune/nervous/self-healing report honestly with 'unknown/not measured' where nothing was measured. The economy core is the strongest surface in the region: the approved 6-stage waterfall computes exactly, virtual-WST is disclaimed everywhere, owner payouts and real rails are gated, a 5M-WST cycle is held for Change Control, the books balance, transfers conserve funds, and an unreadable ledger is refused ('no balances shown rather than empty books') on every reader and writer. Two Tier-1 truth defects remain on reached surfaces: (1) the Transformation page and Heartbeat tab certify '100% overall realised' with every pillar 'realised', where the evidence is that routers are mounted — the figure went 89%→100% because this audit ran one evolution cycle and one economy cycle; (2) the CFO statements capitalise declared costs into the reserve_fund asset and exclude them from expenses (revenue 1000, costs 200 → 'net profit 400', 'reserve_fund 400', 'balanced'). Tier-2 invisible shortfalls: a receiver's in-transit WST appears on none of its surfaces; the default Organism tab shows a blended 88% health of which 60% is defaulted/simulated (disclosed only on Anatomy); the living roster says the organism 'autonomously tends' VSBs while Self-run is OFF by default; economy actions report GaaS 'allowed' from a prohibited-word list with no genome loaded. §17.5: six invariants hold in code (append-only DCS verified, single mount, bus lock, arms-length, twin fallback, read-time plan); user isolation is absent on the organism perimeter, torch optionality fails by execution, and no KPI gate exists — all three already stated in the canon's own status text, so the 'absolute' headline is the overclaim, not a surface. Scratch outputs: C:/tmp/w474_R6; the audit's malformed probe ledger was removed and the waterfall override was posted back to template values (source remains 'owner_override').

### R6.0 · §8 self-running organism / §16-style self-introspection surface — 'Vision Realisation' (Transformation page + Heartbeat tab) — **STUB** · tier 1

- **claim:** §8/§15.6: the organism continuously monitors and evaluates itself; honesty over polish — never fabricate, label simulation.
- **observed:** GET /api/v1/transformation/realisation returns overall_realisation 1.0 with all 11 pillars 'realised' (end_to_end, generate_vsb, vsb_org, chief_twin, resource_fabric, self_running, synthesis_lab, governance, biomimetic, economy, living_alignment — every evidence check met). The evidence is route-mount presence: _PILLARS in agentic_core/api/transformation.py use `_has(r, "/api/v1/sovereign-evolution")`, `_has(r, "/api/v1/heartbeat")`, `_has(r, "/api/v1/gaas")`, `d.get("organism_health") is not None` ('Organism systems healthy'), `d.get("economy_ledgers",0) > 0`, `bool(d.get("last_evolution"))`; _realise() at transformation.py:142-156 averages the fraction met. The figure moved 0.894 → 0.939 → 1.0 across heartbeat beats 1-4 (GET /api/v1/heartbeat/status recent[]) purely because this audit ran one POST /sovereign-evolution/cycle and one POST /economy/cycle. The same canon (§17.5) records GaaS gating partial, KPI gate not held, torch optionality not held, Mode-2 twin not built — yet the page certifies 100%.
- **evidence:** apps/workstation-superapp/src/pages/TransformationDashboard.tsx:122 (`overall = Math.round(pic.realisation.overall_realisation*100)`), :159-160 renders `{overall}% overall realised` in 5xl emerald, :232 heading 'Vision Pillars (computed from live evidence)', :244-249 shows each evidence label with a green check (labels read 'Self-evolution live', 'Organism systems healthy', 'gaas.v5 gate live'). HeartbeatMonitor.tsx: `<Metric label="Realisation" value=… tone="good">` and per-beat green % in Recent Beats; header copy 'ticking vision-realisation'. Heartbeat status last_realisation now 1.0. Route /transformation is mounted in App.tsx:216.
- **disclosed to the user:** No. The page says 'computed from the live organism' and 'computed from live evidence'; nowhere does it say the evidence is 'a router is mounted'. The Heartbeat tab shows a bare 'Realisation 100%'.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced in full. GET /api/v1/transformation/realisation at 07:52Z returns overall_realisation 1.0 with all 11 pillars 'realised'; every check in _PILLARS (agentic_core/api/transformation.py:73-118) is either `_has(routes, prefix)` (a router-mount test) or a count/non-None test (`d.get('organism_health') is not None` is labelled 'Organism systems healthy'; `bool(d.get('last_evolution'))` = a roadmap file has a created_at). _realise() (:142-156) averages met/total. TransformationDashboard.tsx:122 rounds it, :159-160 renders it as '100% overall realised' in 5xl emerald, :232 heads the list 'Vision Pillars (computed from live evidence)', :110-112 says 'computed from the live organism'. HeartbeatMonitor.tsx:99 renders `Realisation 100%` tone='good' and :179 a green % per beat; heartbeat/status recent[] shows 1.0 on beats 126-131. The route is mounted (App.tsx:216). No text on either surface says the evidence is route presence; the check label 'Organism systems healthy' is untrue as a description of what was tested (immune.status().health is not None). This certifies what was not assessed — tier 1 stands.
- **refuter's evidence:** curl GET /api/v1/transformation/realisation → overall_realisation 1.0, 11×'realised', evidence_counts {vsbs 11, economy_ledgers 7, organism_health 1.0, last_evolution '2026-09-19T04:38:08Z'}; transformation.py:34-45 (_routes/_has), :73-118 (_PILLARS), :142-156 (_realise); TransformationDashboard.tsx:110-112,122,159-160,232,244-249; HeartbeatMonitor.tsx:71,99,179; heartbeat/status last_realisation 1.0.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Rename the figure to what it measures ('API surface coverage: 11/11 pillar routers mounted') or derive realisation from the delivery plan's measured item states (/api/v1/plan/state) — and drop the 'Organism systems healthy' check, which cannot fail. Remove the % from the Heartbeat tab until it means realisation.

### R6.1 · §12 economic organism — CFO statements (P&L, balance sheet) after a cycle with costs — **PARTIAL** · tier 1

- **claim:** §12: double-entry books with period close and CFO statements; Stage 0 legal/operating reserves off the top (costs + 20% of revenue).
- **observed:** POST /api/v1/economy/cycle {revenue:1000, costs:200} → homeostasis_reserves 400 (=200 costs + 200 prudential), distributable 600 split 120/180/120/90/90 (waterfall arithmetic correct). But the 200 declared costs are posted Dr reserve_fund / Cr cash (metabolism.py:191-192 `reserves = costs + revenue*effective_reserve` → `ledger.record("reserves", …)`; ledger.py:281 maps 'reserves' to ("reserve_fund","cash"); ledger.py:266 classes reserve_fund as an ASSET). POST /economy/close-period then reports net_profit_wst 400.0, balance_sheet.assets.reserve_fund 400.0, equity.retained_earnings 400.0, 'balanced': true, prepared_by 'CFO agent (AI C-Suite) — computed from the double-entry postings'. The books balance internally, but spent costs are carried as a 400 WST reserve asset and excluded from expenses: with 1000 revenue, 200 costs, 600 distributed, the honest net is 200 and the reserve is 200.
- **evidence:** Executed: cycle.json / close.json in C:/tmp/w474_R6 (close: {"net_profit_wst":400.0,"balance_sheet":{"assets":{"reserve_fund":400.0}…}}); ledger file C:/tmp/w474b/economy/workstation-idbo_ledger.json postings[1] {debit:'reserve_fund', credit:'cash', amount:400, memo:'homeostasis (reserves + costs)'}. UI: VSBEconomy.tsx:235 'Books closed — net profit {…} WST → retained earnings {…} WST', :595 net profit, :600-601 assets total + 'balanced'.
- **disclosed to the user:** No. The cycle memo says 'reserves + costs' but the P&L, balance sheet, close message and board pack present the combined figure as profit/asset with no note that costs are inside it.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced on a fresh entity. POST /economy/cycle {vsb_id:'vsb-r6ref-books', revenue:1000, costs:200} → homeostasis_reserves 400, distributable 600 (120/180/120/90/90). metabolism.py:191-192 posts `reserves = costs + revenue*effective_reserve` via ledger.record('reserves'); ledger.py:279 maps it to Dr reserve_fund / Cr cash and :266 classes reserve_fund as an ASSET; the CHART (:264-275) has no cost/expense account at all, so declared costs are never expensed in any period. POST /economy/close-period → close.net_profit_wst 400.0, P&L expenses 600 (distributions only), balance_sheet.assets.reserve_fund 400.0, equity.current_period_net_profit 400.0, balanced true, retained_earnings 400.0, prepared_by 'CFO agent … computed from the double-entry postings'. VSBEconomy.tsx:235 renders 'Books closed — net profit 400 WST → retained earnings 400 WST' and :595-601 renders the P&L net and 'balanced' on the board pack; only the cycle card (:485-489) labels the figure 'Costs + reserves'. A person who entered 200 of costs is told their profit is 400 and their reserve asset 400 — vision §12 (WHOLE_VISION.md:421) does define Stage 0 as 'reserves off the top (costs + 20%)', so the posting is per design, but the statements present the cost provision as profit and asset with no note. Tier 1 stands.
- **refuter's evidence:** C:/tmp/w474_R6ref/cycle.json (homeostasis_reserves 400.0, distributable_profit 600.0); C:/tmp/w474_R6ref/close.json (close.net_profit_wst 400.0; bs.assets.reserve_fund 400.0; pl.expenses = five distribution_* accounts + transfer_out 0); metabolism.py:191-192; ledger.py:264-283, :431-466 (statements), :478-502 (close_period); VSBEconomy.tsx:235, :485-489, :595-601.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Post costs as Dr operating_costs (expense) / Cr cash and only revenue×reserve_rate to reserve_fund; or, if the Stage-0 lumping is deliberate, label the account 'reserves_and_costs' and subtract costs in the P&L.

### R6.2 · §12 inter-VSB transfer — the receiver's books — **PARTIAL** · tier 2

- **claim:** §12: virtual funds conserved between VSBs; every VSB self-manages its finances with honest books.
- **observed:** POST /api/v1/economy/transfer {from:'workstation-idbo', to:'vsb-007b4aa76c', amount:50} → sender reserve_fund 400→350, transfer_out expense 50, receiver_pending_wst 50, receiver_leg_closed true, settlement 'the receiver's next metabolic cycle consumes this as intake revenue'. The 50 WST now lives only in C:/tmp/w474b/economy_pending_transfers.json. Every receiver-side surface shows nothing: GET /economy/ledger/vsb-007b4aa76c (entry_count 0, reserve_fund_wst 0.0, no pending field), GET /economy/status?vsb_id=vsb-007b4aa76c (ledger balances all 0.0), GET /economy/board-pack?vsb_id=… (assets {}), GET /economy/living-vsbs roster entry (no pending field), GET /economy/transfers/open?from_vsb=vsb-007b4aa76c (open []). Platform-wide the WST is out of the sender's books and not yet in the receiver's — a real VSB owner looking at their own entity sees 0 WST with no sign 50 is in transit. Transfer refusal into an unregistered receiver ('no transfers into the void') is correct.
- **evidence:** transfer.json; agentic_core/economy/transfers.py:24 `_PENDING_STORE = data_path("economy_pending_transfers.json")`, :222-263 queue + response; agentic_core/api/economy.py ledger/status/board-pack handlers read only the ledger (grep 'pending' in economy.py hits only :581 transfer settle path). UI: EconomyOperations.tsx:153-200 TransferPanel lists only the SENDER's open legs.
- **disclosed to the user:** Only to the sender, in the transfer response's 'settlement' sentence. The receiver's ledger, status, board pack and roster say nothing.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. POST /economy/transfer {from:'vsb-r6ref-books', to:'vsb-260c3cef57', amount:50} → sender_reserve_fund_after 350.0, receiver_pending_wst 50.0, receiver_leg_closed true, settlement 'the receiver's next metabolic cycle consumes this as intake revenue'. Every receiver-side read then shows nothing: GET /economy/ledger/vsb-260c3cef57 → entry_count 0, reserve_fund_wst 0.0, no pending key; GET /economy/status?vsb_id=… → all balances 0.0, the serialised body contains no 'pending'; GET /economy/board-pack?vsb_id=… → total_revenue 0.0, body contains neither 'pending' nor 'transit'; living-vsbs roster row for the receiver carries no pending field. grep 'pending' in api/economy.py hits only the transfer settle path (:581) and unrelated notes; living_vsbs.py's pending references (:352-382) are revenue events, not inter-VSB receipts. The 50 WST exists only in C:/tmp/w474b/economy_pending_transfers.json. Sender told, receiver not — tier 2 stands.
- **refuter's evidence:** C:/tmp/w474_R6ref/transfer.json; receiver ledger/status/board-pack/roster responses quoted above (all 0.0, no pending field); transfers.py:24 _PENDING_STORE, :222-263; economy.py grep 'pending' → :581,:1047,:1320,:1456 only; living_vsbs.py:150-170 (roster rows, no pending).
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add `pending_intake_wst` (from the pending store, strict-read) to the ledger statement, economy/status and the living roster entry, and render it on VSBEconomy as 'in transit, enters the next cycle'.

### R6.3 · §8 organism composite health — default Organism tab — **PARTIAL** · tier 2

- **claim:** §8: health is continuously monitored, evaluated and improved (a measurement).
- **observed:** GET /api/v1/organism/status → composite_health 0.877 with composite_health_terms disclosing that 60% of the weight is not measured: self_healing_health weight 0.4 'no circuits tracked yet — defaulted to 1.0, not a measurement', metabolic weight 0.2 'ATPSimulator on a constant metabolic_load — simulated, not a measurement of this platform'; composite_health_measured_only 1.0. The API is honest. The default Organism tab renders only the blended number: OrganismDashboard.tsx:262 `{Math.round(status.composite_health*100)}%`, :265 HealthBar, :266 health_summary ('Organism nominal — … health 88%'); no reference to composite_health_terms/measured (grep 'measured|basis' in that file: 0 hits). The Anatomy tab (OrganismAnatomy.tsx:228-241) does show 'measured vs blended' with warn chips.
- **evidence:** organism/status and organism/health-summary responses; OrganismDashboard.tsx:250-266; OrganismAnatomy.tsx:228-241.
- **disclosed to the user:** Only on the Anatomy tab, not on the default Organism tab where the 88% headline is.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. GET /organism/status now → composite_health 0.96 with composite_health_terms disclosing self_healing_health (weight 0.4) 'no circuits tracked yet — defaulted to 1.0, not a measurement' and metabolic (0.2) 'ATPSimulator on a constant metabolic_load — simulated', composite_health_measured_only 0.9. OrganismHub.tsx:14-16 makes 'Organism' (OrganismDashboard) the first tab; OrganismDashboard.tsx:262 renders `Math.round(status.composite_health*100)` ('96%'), :265 HealthBar, :266 health_summary 'Organism nominal — … health 96%'; grep 'measured|basis|blended' in that file: 0 hits. OrganismAnatomy.tsx:228-231 renders 'Composite health — measured vs blended' with the measured-only figure. The blended headline on the default tab is undisclosed there — tier 2 stands.
- **refuter's evidence:** organism/status response (composite_health 0.96, measured_only 0.9, terms with measured:false + basis); OrganismDashboard.tsx:250-266; OrganismAnatomy.tsx:13-15, :228-231; OrganismHub.tsx:14-16.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** On OrganismDashboard render composite_health_measured_only as the headline (or the blended figure with the health_basis sentence under it).

### R6.4 · §12/§4 living roster — 'enterprises the organism autonomously tends' — **PARTIAL** · tier 2

- **claim:** §8/§12: once established a VSB is forever run by the organism (self-running economy on the circadian heartbeat).
- **observed:** GET /api/v1/economy/living-vsbs note: 'Established VSB enterprises the organism autonomously tends (paced virtual economy cycles on the circadian heartbeat)'. In fact operate_one() runs only `if self.auto_economy:` (heartbeat.py:239-246), auto_economy is false by default and persisted (heartbeat status auto_economy:false); my spawned VSB vsb-007b4aa76c shows operating_cycles 0, last_operated null after 4 beats. The Heartbeat tab does disclose 'All five are OFF by default' and offers the Self-run toggle, but the economy page renders the roster note as written (VSBEconomy.tsx:700 `{living.note}`; :644 comment 'continually operated on the heartbeat').
- **evidence:** heartbeat.py:239-246; heartbeat/status after 4 beats (last_vsb_operated null); living-vsbs response; VSBEconomy.tsx:644,700; HeartbeatMonitor.tsx AUTONOMY block.
- **disclosed to the user:** Partly — on a different tab (Heartbeat) the flags read OFF; the roster itself says 'autonomously tends' unconditionally.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. heartbeat.py:239-246 runs operate_one() only `if self.auto_economy:`; GET /heartbeat/status after 131 beats → auto_economy false, last_vsb_operated null; the living roster rows for the two 'Refuter Probe Halal Market' entities show operating_cycles 0 / last_operated null, and the R2 entities show operating_cycles 1 with last_operated == registered_at (a birth cycle only). GET /economy/living-vsbs note (living_vsbs.py:169) and the economy.py:326 docstring say 'the organism autonomously tends … on the circadian heartbeat' unconditionally; VSBEconomy.tsx:648 heads the card 'Living Enterprises · autonomously tended (§4)' and :700 renders the note verbatim. HeartbeatMonitor.tsx:110 does say 'All five are OFF by default'. The rows' visible last_operated null partly contradicts the caption, so tier 2 (invisible shortfall on that page, disclosed elsewhere) is the right tier.
- **refuter's evidence:** heartbeat.py:239-246; heartbeat/status {auto_economy:false, last_vsb_operated:null, beats:131}; living-vsbs response rows; living_vsbs.py:169; economy.py:326; VSBEconomy.tsx:644,648,700; HeartbeatMonitor.tsx:110.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Have living-vsbs include `auto_economy` and phrase the note conditionally ('tended on the heartbeat when Self-run is ON — currently OFF'); render that on the roster.

### R6.5 · §17.5 invariant 2 — 'mandatory GaaS gate on every output' as it presents on economy actions — **PARTIAL** · tier 3 *(assessed tier 2)*

- **claim:** §17.5: mandatory GaaS gate on every output; §11 ethics architecturally binding.
- **observed:** POST /economy/transfer returns governance {status:'allowed', checkpoint:'CHK-1789792086321'} and the economy docstrings say 'gaas.v5-gated'. The gate that produced 'allowed' is ConstitutionalPolicyGate.validate (gaas/v5/policy_gate.py:35-53): a substring check of the action intent against _PROHIBITED_INTENTS plus a requires_human flag, then validate_output; uci_v16_omega.py:130 returns status='allowed' when neither trips. No genome/legal model is loaded in this environment — the VSB spawn stream says so honestly ('Constitutional gate NOT evaluated — no genome/legal model is configured (GAAS_GENOME_PATH + GAAS_LEGAL_PATH); nothing was checked', vsb.py:53,1667), but the economy path reports 'allowed' with a checkpoint id as though a constitutional evaluation ran. §17.5 itself records that most outputs pass only a three-regex keyword filter (P2.6).
- **evidence:** transfer.json governance block; policy_gate.py:31-53; uci_v16_omega.py:96-130; economy.py:631-679; vsb.py:1667.
- **disclosed to the user:** Disclosed in the vision canon (§17.5) and on the VSB spawn stream; NOT on the economy responses/page, which say gated/allowed.
- **refutation: REFUTED — corrected to PARTIAL.** The evidence does not support tier 2. The gaas.v5 gate genuinely ran on my transfer: economy.py:631-635 wraps the action in UnifiedConstitutionalInterceptorV16Omega.intercept, which executes ConstitutionalPolicyGate.validate (policy_gate.py:35-53: Article 11.1 prohibited-intent check + Article 7.3 human-approval flag), the action, validate_output, and then writes a UEG checkpoint (uci_v16_omega.py:123-130) — the response's governance {status:'allowed', checkpoint:'CHK-1789804474899'} is a true description of what happened. The assessor's comparison to vsb.py:1666-1667 is a different component: GaaSValidatorV4 (vsb.py:47-56) needs GAAS_GENOME_PATH/GAAS_LEGAL_PATH YAML; gaas.v5's UCI never takes a genome, so 'no genome loaded' is not a hidden condition of the economy gate. On the reached surfaces the copy claims nothing more than what ran: EconomyOperations.tsx:325-345 renders 'Transfer posted', the settlement sentence and only the bypass/retry statuses; VSBEconomy.tsx:489 renders 'governance: allowed'. The shallowness of the gate (a keyword/flag gate, not a constitutional model) is a capability-depth gap that the canon discloses (§17.5 status, WHOLE_VISION.md ~:729-733) — tier 3, PARTIAL.
- **refuter's evidence:** transfer.json governance block; economy.py:631-635; policy_gate.py:31-53; uci_v16_omega.py:96-130; vsb.py:47-56 and :1655-1670 (a separate validator); EconomyOperations.tsx:325-345; VSBEconomy.tsx:489; WHOLE_VISION.md §17.5 status paragraph.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Carry `evaluated: false / basis: 'prohibited-intent word list; no genome/legal model loaded'` in the governance block (as vsb.py already does) and show it beside 'allowed'.

### R6.6 · §17.5 invariants 1, 6 and 10 — user isolation on the organism perimeter, torch optionality, KPI gate — **DOC_OVERCLAIM** · tier 3

- **claim:** §17.5: the ten invariants are 'absolute'.
- **observed:** Torch optionality NOT held — executed: importing agentic_core.app_mvp with `torch` blocked fails with ImportError through governance/uci_interceptor.py:11 → mjm/recursive_meta_learner.py:4 → mjm/hd_omni_learner.py:1 (`import torch`, unguarded). User isolation NOT held on the organism control perimeter — 0 `Depends(` in api/heartbeat.py, api/organism_status.py, api/sovereign_evolution.py, api/living_plan.py (economy.py does carry get_current_user + _require_economy_access on every handler). KPI gate before customer delivery — no code at all (grep 'kpi_gate|KPI gate|kpi-gate' across agentic_core: 0). All three are recorded as not held / partial / Owner-ruling in §17.5's own status paragraph, so the canon is honest about them; the 'absolute' headline is the overclaim.
- **evidence:** Import probe output; grep counts; vision §17.5 status text (lines ~726-737).
- **disclosed to the user:** In the canon doc, yes; no user surface claims these invariants, so nobody is misled at a surface.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced all three. Torch: importing agentic_core.app_mvp with a meta_path finder that blocks `torch` fails with ImportError through app_mvp.py:93 → avatars/__init__.py:1 → avatars/core/recirculation_orchestrator.py:24 → governance/uci_interceptor.py:11 → mjm/recursive_meta_learner.py:4 → mjm/hd_omni_learner.py:1 (`import torch`, unguarded). Isolation: `Depends(` count is 0 in api/heartbeat.py, api/organism_status.py, api/sovereign_evolution.py, api/living_plan.py (economy.py: 27); app_mvp.py adds only CORSMiddleware (:21,:36), no auth middleware. KPI gate: grep 'kpi_gate|KPI gate|kpi-gate' across agentic_core → 0. §17.5 headlines the ten as 'absolute' yet its own status paragraph records isolation partial, torch not held, KPI gate not held (Owner ruling). No user surface asserts these invariants — tier 3 DOC_OVERCLAIM stands.
- **refuter's evidence:** Import probe output ('FAIL ImportError torch blocked by probe', traceback chain listed); grep -c Depends( results 0/0/0/0/27; grep kpi → 0; app_mvp.py:21,36; WHOLE_VISION.md §17.5 heading + status paragraph.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Guard the torch imports (try/except + capability flag) in mjm/hd_omni_learner.py and recursive_meta_learner.py; add get_current_user to the four perimeter routers; keep KPI gate as an open Owner ruling in the plan.

### R6.7 · §8 self-healing / immune organs — what they actually observe — **PARTIAL** · tier 3

- **claim:** §8: the organism defends and heals itself (immune ↔ nervous ↔ metabolic loops).
- **observed:** GET /organism/self-healing/status: overall_health null, 'no circuit has carried a call yet — nothing measured, health unknown', tracked_endpoints 0 (honest). But the only code that ever feeds a circuit is the EXTERNAL AI gateway (ai/gateway.py: 14 record_success/failure calls for claude/openai/ollama) plus one call in ai/native/orchestrator.py — under the §6 native mandate no platform route is circuit-protected, so the self-healing organ can never heal anything the platform itself serves. Immune inputs are AI failures, timeouts and compliance fails (gateway.py:203-248, vbs/quality.py:296, heartbeat.py:89, management_systems.py:329); an SE cycle registered 1 'compliance_fail' from this audit's cycle, health 1.0→0.9 — that part is live. Heartbeat 2c (heartbeat.py:206-222) probes open circuits each beat — correct but idle. Homeostasis POST returned a real scheduler adjustment ('defer_non_urgent … MAINTENANCE_REST'). Survival instinct exists in code (heartbeat.py:181 self-regulation; metabolism.py:156-161 raises reserves when ATP < 0.3) but was not exercised (ATP 0.38-0.46).
- **evidence:** Route responses; grep of self_healer.record_* callers (gateway.py 14, orchestrator.py 1, biobus.py 1); OrganismAnatomy.tsx:27 renders health_basis.
- **disclosed to the user:** Yes — 'health unknown / no circuit has carried a call' is rendered on the Anatomy tab; the SCOPE (external providers only) is not stated.
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced. GET /organism/self-healing/status → overall_health null, health_basis 'no circuit has carried a call yet — nothing measured, health unknown', tracked_endpoints 0, scope 'in-memory, this server process since start' (a process scope, not an endpoint scope). Callers of self_healer.record_success/record_failure: ai/gateway.py 14, ai/native/orchestrator.py 1 (`model:{name}` — the native floor), organism/biobus.py 1 (a wrapper with no other caller found). No platform route feeds a circuit, so under AI_DISABLE_LOCAL=1 the organ measures nothing this server serves. OrganismAnatomy.tsx:27 types and renders health_basis; :271 renders the process scope chip; nothing states that only AI-provider calls are circuit-protected. Disclosed-as-unknown capability gap — tier 3 stands.
- **refuter's evidence:** self-healing/status response quoted; grep of record_success/record_failure callers (gateway.py 14, native/orchestrator.py:72, biobus.py:117-120); OrganismAnatomy.tsx:27,:271.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Add `scope: 'external AI provider calls only'` to self-healing status, or wrap the native serving resource and the economy/genesis handlers in circuits so the organ measures the platform.

### R6.8 · §8 heartbeat cadence and waterfall reset copy — small surface inaccuracies — **PARTIAL** · tier 3

- **claim:** §8 circadian operation; §12 Owner-adjustable, template-bounded waterfall.
- **observed:** (a) HeartbeatMonitor.tsx renders `{s.beats} beats · every ~{s.interval_seconds}s` = 'every ~60s', but run() sleeps interval/intensity (heartbeat.py:465-467, _INTENSITY MAINTENANCE_REST 0.3) = 200 s; observed beats at 04:22:51Z, 04:26:11Z. The organism does run itself (beats 1→4 with no intervention). (b) POST /economy/waterfall with proportions summing to 1.7 (owner 0.9) was accepted and silently normalised to owner 0.5294 with note 'Owner-set proportions persisted'; posting the template defaults afterwards leaves source 'owner_override' although the note promises 'Reset by posting the template defaults'. 'Template-bounded' in practice means only 'a non-distributing form forces owner=0' (economy.py:243-245).
- **evidence:** HeartbeatMonitor.tsx status line; heartbeat.py:456-467; waterfall POST responses (owner 0.5294 accepted; reset → source owner_override).
- **disclosed to the user:** Partly (intensity 30% is shown next to the cadence; the normalised values are echoed back).
- **refutation: SURVIVED** (reproduced by the refuter). Reproduced both. (a) HeartbeatMonitor.tsx:86 renders `{s.beats} beats · every ~{s.interval_seconds}s` = 'every ~60s'; heartbeat.py:465-467 sleeps interval_seconds / _INTENSITY[phase] and _INTENSITY (:115) ACTIVE_REST = 0.7 → ~86 s; observed beats 07:44:23, 07:45:48, 07:47:14, 07:48:40, 07:50:06, 07:51:32 (85-86 s apart) in ACTIVE_REST. Intensity 70% is shown beside it (:98). (b) POST /economy/waterfall with proportions summing 1.7 (owner 0.9) → 200, owner normalised to 0.5294, note 'Owner-set proportions persisted'; posting the template defaults afterwards → source still 'owner_override' and GET /economy/waterfall shows source 'owner_override' with waterfall == template_default, although the note promises 'Reset by posting the template defaults'. Both small, partly disclosed — tier 3 stands.
- **refuter's evidence:** HeartbeatMonitor.tsx:86,98; heartbeat.py:115,465-467; heartbeat/status recent[] timestamps; waterfall POST responses (owner 0.5294 / then 0.2 with source owner_override) and GET /economy/waterfall?vsb_id=vsb-r6ref-books.
- **smallest honest fix (assessor's proposal — a lead, not a decision):** Show effective cadence (`interval/intensity`) on the page; say 'normalised from your input' when the sum ≠ 1; flip source back to entity_template when the posted values equal the template.

### R6.9 · §12 economic organism core + §17.5 invariants 3,4,5,7,8,9 — verified working — **DELIVERED**

- **claim:** §12: 6-stage waterfall, virtual WST until real rails, owner payments gated, material distributions held for Change Control, double-entry with close and CFO statements, stores that refuse rather than replace; §17.5: append-only DCS, arms-length, twin pre-validation, single router mount, signal-bus atomicity, plan ≤5-min staleness.
- **observed:** Waterfall: cycle 1000/200 → reserves 400 then 120/180/120/90/90 (=600) exactly the approved 20/30/20/15/15; every economy response and the VSBEconomy page carry 'Virtual/simulated WST — not real money'. Owner payments: payout 500 refused ('Insufficient balance: 120.0 WST available'), payout 20 recorded with real_money_moved false, rails DISABLED. Materiality: cycle revenue 5,000,000 → cycle null, governance held_for_change_control cca-8df6865bb1, impact_tier CRITICAL, threshold 250,000 WST. Books: trial balance 1000/1000 balanced, close-period rolls to retained earnings and next period opens clean; transfer conserved (sender 400→350 + transfer_out 50; pending 50). Strict reads: a malformed C:/tmp/w474b/economy/vsb-r6-test_ledger.json made GET ledger → 'could not be read whole (JSONDecodeError…). No balances are shown rather than empty books; nothing was changed', economy/status ledger.available false, board-pack refused, cycle refused ('No cycle ran and nothing was posted'), transfers/open ledger_unreadable true, reconcile ledgers_unreadable 1 (file removed after the probe); ledger repair on a whole ledger: 'reads whole — nothing to repair'. Roster shows compliance standing honestly (verdict null → never_screened true; 'review' for the other VSB). Charity: curated weights provenance and donation_100pct_verified 'not_checked' stated. Sovereign Evolution cycle runs; the floor-served roadmap is marked '_[Workstation native structured engine — owned…]_' and the CEO directive says 'No parseable CEO directives; defaulting to a maintenance sweep' — both rendered verbatim (SovereignEvolution.tsx:173,195). Invariants: UEG chain GET /ueg/verify chain_valid true, tail_anchored true (sha3_512) and /gaas/ueg/verify valid; all 79 include_router calls live in app_mvp.py (cross_platform.py's mention is a docstring); biobus threading.Lock (biobus.py:88) + bounded 200-signal buffer; sovereign_evolution.py writes nothing to board/genome (arms-length); twin pre-validation with an honest 'no twin model — organism health gate only' fallback (change_control.py:55-57,747); /plan/state reconciled_at is stamped at read time.
- **evidence:** All under C:/tmp/w474_R6 (cycle.json, ledger1/2.json, close.json, transfer.json, open.json, se.json, spawn.json) and the route outputs quoted above.
- **disclosed to the user:** Yes — virtual-WST, gated-rails, held-for-CCA and unreadable-store states are all surfaced in the API and on the Economy page (VSBEconomy.tsx alerts at :266,:311,:386,:568).
- **refutation: SURVIVED** (reproduced by the refuter). Verified by my own execution: cycle 1000/200 → reserves 400 then 120/180/120/90/90 (=600) exactly 20/30/20/15/15, every response carrying 'Virtual/simulated WST'; owner payout 5000 refused ('Insufficient balance: 120.0 WST available'), payout 10 recorded with real_money_moved false and real_money_rails 'DISABLED'; cycle revenue 5,000,000 → cycle null, held_for_change_control cca-4699c63bdb, impact_tier CRITICAL, threshold 250,000; close-period rolls 400 to retained earnings with balanced true; transfer conserved (350 + pending 50); a malformed C:/tmp/w474b/economy/vsb-r6ref-bad_ledger.json made GET ledger and POST cycle refuse with 'could not be read whole … nothing was changed / No cycle ran and nothing was posted' (file removed after the probe); GET /ueg/verify chain_valid true, tail_anchored true, sha3_512; 79 include_router calls all in app_mvp.py (cross_platform.py:6 is a docstring); torch/isolation/KPI correctly excluded from this DELIVERED set. The verdict is right and the tier-3 label is only nominal for a delivered item.
- **refuter's evidence:** cycle.json, close.json, transfer.json under C:/tmp/w474_R6ref; payout responses; materiality-hold response (cca-4699c63bdb); strict-read probe responses; /ueg/verify response; grep -c include_router app_mvp.py = 79.
- **residual lead (assessor's note on a DELIVERED entry — a lead, not a defect):** None needed for these.

---

*Regenerated by W474 from the audit workflow's journal. Every entry above is an observation against
the booted HEAD named in the header — routes executed, handlers and components read, stores counted —
not a claim read from another document. No browser was driven: statements about what a user SEES
(a chip's colour, a tab's default, a rendered badge) are reasoned from the component source, and the
assessors and refuters say so where it matters.*
