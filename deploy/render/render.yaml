services:
  - type: web
    name: workstation-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn agentic_core.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
