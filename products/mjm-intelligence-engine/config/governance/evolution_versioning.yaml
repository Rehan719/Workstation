versioning:
  strategy: "semantic_versioning"
  format: "MAJOR.MINOR.PATCH"

evolution:
  proposal_threshold: 0.85
  governance_required: true

migration_paths:
  - from: "1.0.0"
    to: "1.1.0"
    steps:
      - "Initialize pattern compatibility matrix"
      - "Enable reproduction features"
